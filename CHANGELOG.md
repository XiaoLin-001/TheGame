# 變更紀錄

本專案的所有重要變更記錄於此。每批工作一節，倒序排列。

---

## [0.30.1] — 2026-08-10 — 按鈕終於讀了美術 token（B2.9）

缺陷審視批。**沒有新玩法、沒有新系統**，三筆是把已經寫在文件上、但程式碼沒有兌現的東西補上。

### ① 全遊戲 41 顆按鈕從來沒有讀過美術 token（RG-161）

```
grep -c "Button.new()"                → 41
grep "add_theme_stylebox_override" 在按鈕上 → 0
```

每一顆鈕都是 Godot 的預設灰色方塊。而 `20_ART_DIRECTION.md` §1.1 的
`bg.raised #162c3d` 那一列寫的就是「浮起元件（**按鈕**、卡片）」，
§1.4 的 `radius.ui = 2` 寫的是「UI 面板與**按鈕**（僅此一種）」。
**兩條 token 訂了三個月，沒有一顆鈕讀過它們。**

為什麼漏這麼久：每一批的 UI 一致性檢查問的是「有沒有新的硬編碼色值」（`Grep Color(`），
而**沒有套色值的東西不會出現在那個 grep 裡**。清單抓得到「用錯顏色」，抓不到「根本沒上色」。

修法是 `UiKit.theme()` 加一組 Button 樣式，**設在主題上而不是逐顆設**——
主題在每個畫面的根 Control 設一次、子節點自動繼承，所以那是唯一蓋得完 41 顆的地方。
五態各有自己的樣子，因為它們各自回答一個不同的問題：

| 態 | 它在說 |
|---|---|
| normal | 這是可以按的（`bg.raised` ＋ `border.subtle`） |
| hover | 游標在我身上（底色偏 `order.dim`、邊框 `border.strong`） |
| pressed | 我按到了（底色沉到 `bg.panel`、邊框 `order.cyan`） |
| disabled | 這顆現在不能按（**底色退回面板色**，看起來是凹下去的，不是一顆比較暗的凸起） |
| focus | 鍵盤走到哪了（`stroke.emphasis` 的 3px，P3「操作不得只靠 hover」的另一半） |

### ② 提示文字的 x 是寫死的 520（RG-162）

套上內距之後底欄七顆鈕加起來變寬，提示文字整段被壓在「說明」那顆鈕上面。
**寫死的座標記的是「當時」那七顆鈕有多寬**（也違反手機預留條款 P1）。

§7.1 A-1 的結論一字未改地適用：「驗收條件要改成『鈕的矩形與底欄矩形不相交』，
不是『按得到』」——**被壓在字底下的鈕照樣按得到**，所以既有的每一條斷言都是綠的。

現在提示的 x 跟著底欄的實際右緣走，並補上兩條斷言。
順手把按鈕內距從 12 降到 8：**按鈕的內距是一個全域尺寸，在最擠的那個版面上才看得出代價。**

### ③ 新寫的那條斷言自己也是綠的（RG-163）

「底欄與提示不得相交」第一版把提示的矩形寫成 **1px 高**（y 666–667），
而底欄從 y 668 起——兩個矩形**永遠**不相交。是把提示故意推到底欄正中間去測它才發現。
RG-158 的同一課，這次連新寫的斷言都中招。兩個方向都跑過：正常 PASS、故意疊上去 FAIL。

### 順手清掉兩筆掛著的美術缺陷

**A-2 的後半（轉角）**：§1.6 原文是「抖動 ＋ **轉角改圓弧或磨掉尖角**」，
B2.4.3 只做了抖動，每一個轉彎仍然是硬 90°。現在外凸的格點吐**兩個**點而不是一個，
沿兩條邊各退 0.3 格；凹角不動（那是兩段帶子交會的內側，磨它會開一個洞）。
⚠ 第一版寫死 7px，fit 倍率下只改了 30 個像素——**看不見的東西等於沒做**，改成比例之後翻倍。

**C-2 擊殺碎片看不見**：**數量不動**（反模式清單擋著「200 顆粒子」），
動的是單片的尺寸（4–5 → 6.5–8px）、初速（行程 22 → 34px）、
以及生命前 30% 用亮階起亮再落回本色——**明度差在小尺寸下活得下來**。

### 沒有動的

| 項目 | 為什麼不動 |
|---|---|
| C-3 色彩層次校準 | §0.1 明寫「需要真人長時間遊玩才判得出來，agent 判不了」 |
| P3-2 碎浪／霜礁 兩個放射狀形狀 | 「換位置不是解決，如果實玩會混就再改」——要實玩才判得了 |
| 無盡地圖死區 45% | 三條路寫在 `10_GDD.md` §7.10，**待使用者決策** |
| RG-8 渲染 23 FPS | 不是能順手改的東西（渲染重寫），屬 G3 的效能判準，排 M3 |
| R-15／R-16 | 已登記待觸發，觸發條件都在 M3 |

---

## [0.30.0] — 2026-08-10 — M2 驗收：G2 通過，而勾稽自己找到兩個缺口（B2.8）

M2 Alpha 收尾。**沒有新玩法**——這一批交的是證據，以及證據找出來的東西。

### G2 四項全過

| 項目 | 證據 |
|---|---|
| GDD §3 系統清單逐項對照 | 12 個系統的實作／測試／畫面鉤子逐列填表（`50_QA_PLAN.md` §6） |
| 存檔：新開局／存／讀／舊版遷移 | `save_test` **33 → 83 項**（磁碟往返這次真的跑了） |
| §2 設計承諾斷言全部生效 | 補上兩個缺口，見下 |
| 零阻斷、零嚴重 bug | 本批修掉 RG-159；其餘見登記簿 |

### 勾稽找到的兩個缺口

**① 全案最重要的安全規則沒有任何斷言。** 鐵律 1「有測試鉤子時絕不寫真存檔」
——保護的是使用者的進度——一直只是 `Hooks._ready()` 裡的一行 `print`。
現在它是 `TL_CLICKTEST/title` 的一格，而那支自檢每一次都跑在鉤子底下。
把斷言反過來寫證明它紅得起來。

**② 紅線 R1／R2 沒有測試。** 理由是「變現排 M4，還沒有東西可以違反它們」。
**等到有東西違反再寫，就是等到玩家先看到它。** 現在：
- **R2**（不賣次數）掃存檔 schema 的每一個欄位名比對禁字（stamina／lives／
  retries／plays_today…）。**禁字清單而不是允許清單**——後者每加一個功能就要改一次。
- **R1**（失敗只花時間）走真正的局末結算路徑（0 星、0 波），斷言星、無盡紀錄、
  材料、等級、研究數據**一項都不減**。

### 兩個真缺陷

**RG-159 — 型別錯掉的存檔會讓整個局外層噴 14 條錯誤。** `_fill_defaults()` 只補
**缺的鍵**，不修**型別錯的鍵**；而每一個讀取端都寫著 `d.get("campaign", {}) as Dictionary`，
那個 cast 對一個整數只會得到 null。一個 `"campaign": 5` 的檔（舊版寫的、寫到一半的、
手改壞的）於是讓成就、名冊、材料餘額、難度層解鎖全部中招。

修法是遷移**之前**多一步 `_repair_containers()`，只修「容器 vs 非容器」不合的鍵。
**不是嚴格比對型別**——JSON 沒有整數，`"data": 12` 讀回來是 `12.0`，嚴格比對會把
玩家的研究數據當成壞資料清掉（測試有反向對照）。修一處，14 條錯誤歸零。

**RG-160 — 一條永遠「待補」的測試等於沒有這條測試。** 磁碟往返（原子寫入 → 讀回
→ 改名）的規則是「偵測到既有存檔就整段跳過」——立意對，結果是它**在任何玩過遊戲的
機器上都沒被執行過**，也就是我的機器與使用者的機器。它從 B0.1 起一直印著「待補 —」，
每一批都被讀過、每一批都被當成背景。

現在 `SaveService.path` 是實例欄位（預設仍是玩家那一個），測試寫**自己的檔名**：
真實存檔一個位元組都沒被碰到，而原子寫入三段真的跑得到。順手補上「半寫壞的檔要
退回預設值，不是半份進度」。

### 內容矩陣：M2 欄有 6 列沒填滿，而那不是 G2 的判準

| | 角色 | 生產節點 | 敵人 | 戰役 | 科技 | 無盡佈局 | 難度層 | 成就 | 訂單 | 廠等 |
|---|---|---|---|---|---|---|---|---|---|---|
| M2 目標 | 16 | 11 | 12 | 15 | 30 | 3 | 3 | 20 | 8 | 10 |
| 實際 | **8** | **5** | **3** | **5** | **12** | **1** | 3 ✔ | 20 ✔ | 9 ✔ | **8** |

內容矩陣是 **G3** 的判準（G2 問系統、G3 問內容），而填內容的批次 B3.1–B3.6
**直接跳到 M3 的數字**——§5 的 M2 欄是一個沒有任何批次交付的中間目標。
記下來是為了不讓它靜靜地變成「以為填過了」。

**「做了但到不了的內容」是 0 項**：每一種可蓋節點、每一隻敵人、每一隻角色、
每一條成就的計量都有路走到（`m2_test` 逐項斷言）。

### 新增

- `tests/m2_test.gd`（365 項）：內容矩陣、可到達性、玩過一輪的存檔冪等、紅線 R1／R2
- 測試總數 **19 支 1961 項**

---

## [0.29.0] — 2026-08-10 — 難度層：買到的是「打得動」，不是「碾得平」（B2.6）

無盡模式前 3 層難度（`10_GDD.md` §3.11、§7.16）。M2 只剩驗收。

### 三條規則，每層只多疊一條

| 層 | 名稱 | 敵人血量 | 敵人破壞建築 | 塔交戰耗能 |
|---|---|---|---|---|
| 0 | 標準 | ×1.00 | ×1.00 | ×1.00 |
| 1 | 漲潮 | **×1.25** | ×1.00 | ×1.00 |
| 2 | 暴潮 | ×1.50 | **×1.50** | ×1.00 |
| 3 | 深潮 | ×1.80 | ×1.50 | **×1.20** |

血量是校準軸（每層都動），另外兩條各自在第 2、第 3 層才登場。三條打的是三件
不同的事——塔夠不夠、線撐不撐得住、電夠不夠。一層同時多三條的話，玩家唯一的
心得會是「這層難」，而難度層的全部價值是說得出**難在哪**。

規則卡逐條列在難度層選擇畫面上；局內頂欄也寫著現在是第幾層。

### ★ 第 3 層的 ×1.80 就是等級軸滿級

憲法 B2 ②：「等級軸的增幅必須被難度層同步吃掉」。等級軸滿級是傷害 ×1.80，
所以第 3 層的敵人血量就是 ×1.80——**不是一個調出來好看的數字，是那一軸的鏡像**。
`difficulty_test` 逐位元比對這兩個數，任一邊被調動另一邊當場變紅。

實測（第 5 關參考解，同一份佈局跑三次）：

| 配置 | 結果 | 核心 | 建築失血 |
|---|---|---|---|
| 第 0 層／零進度 | 通關 | 956/1000 | 44 |
| 第 3 層／等級軸滿級 | 通關 | 877/1000 | 123 |
| 第 3 層／零進度 | **核心已毀**（第 1940 tick） | −1/1000 | 1001 |

**沒有等級軸過不了第 3 層，有了也只是過得驚險。** 那正是 B2 ② 想要的形狀。

### 三個測試抓到的真缺陷

| # | 缺陷 |
|---|---|
| **RG-158** | **對照組自己不會動。** 原本只有兩腳，而那兩腳的差距在「等級軸根本沒生效」時一樣是綠的——量到的只會是難度層。補上第三腳才看見上面那張表真正的結論 |
| **RG-157** | `save_test` 把最新存檔版本**寫死成 2**。sv3 一加它就紅了，但紅的是斷言不是程式碼：那兩條要問的是「遷移鏈跑得完」 |
| **RG-156** | 第 0 層的規則列塞了一句「沒有額外規則」，讓「第 N 層有 N 條規則」這條斷言在第 0 層失效——而它是「每層只多疊一條」的唯一守衛 |

### 解鎖與紀錄都是推導的

- 第 1 層要**戰役全通**；之後每層要在上一層撐過 **10 波**。階梯跳不過去。
- 「解鎖到第幾層」沒有存檔欄位（sv2 砍 `campaign.cleared` 的同一條）。
- 個人最佳**逐層各一筆**（sv3）：同一個「12 波」在標準層與深潮層不是同一件事。
  舊存檔那一筆搬進第 0 層——它是在還沒有難度層的世界裡打出來的。

### 憲法 B3 沒有改一個字就守住了它

難度層是 `Loadout` 的第三軸。它進了 `Loadout.KEYS`，所以 B2.7.1 那道**單一**閘
（`Daily.meta_for()`）自動涵蓋它，`progress_test` 逐鍵列舉的斷言不必動。
**這就是那一批重構買到的東西。**

### 新增

- `data/Difficulty.gd`（表 ＋ 讀它的函式）、`scripts/screens/Tiers.gd`（無盡入口）
- `tests/difficulty_test.gd`（65 項）、`TL_PANEL=tiers`
- 主選單「無盡」改成先開難度層選擇；`TL_PANEL=endless` 仍直達第 0 層的局內畫面

---

## [0.28.3] — 2026-08-09 — 文案稽核：介面說事實，玩家自己下結論（B2.7.3）

掃過全案 **505 條玩家看得到的字串**。零玩法變更。

### 找到的問題只有一種形狀

**我的程式碼註解語氣漏進了 UI。** 註解是寫給自己的（為什麼這樣做），
UI 是寫給玩家的（這是什麼、我現在能做什麼）。

| # | 問題 | 前 | 後 |
|---|---|---|---|
| ① | **破折號說教**（尾巴把前半句換句話說一次） | 27 條 | **8 條**，全部是「錯誤＋補救步驟」 |
| ② | **替玩家下結論** | 圖鑑 8 條 | 全部改寫 |
| ③ | **替設計辩護** | 3 處 | 删掉 |
| ④ | **開發代號寫給玩家看** | 2 條 | 删掉（見下） |

### ④ 是真的缺陷，不是風格問題（RG-155）

兩條字串把批次編號印給玩家看，**而且兩條都已經過期**：

- 「升級材料 N（塔的等級・**B2.7 開放兑換**）」—— B2.7 上一批就上了
- 「藍圖槽 +1。（**藍圖庫本體排 M2**，先把槽位存起來。）」—— 藍圖庫 B2.3 就做完了

**批次編號寫進字串，就是一個沒有任何東西看著的到期日。**

### 兩條保留（不是所有破折號都要死）

- **錯誤訊息的補救步驟**：`合金不夠——合金要蓋熔爐、而且熔爐要接到核心才入帳`。
  這裡的破折號在接「怎麼辦」，不是「所以你懂了嗎」。
- **「標籤：值」與教學步驟列**是版面結構，不是說教。

### 規則寫下來了

`20_ART_DIRECTION.md` **§3.5 文案聲音**（新）是 UI 文字的唯一權威：四條禁令、
兩條保留、以及「強調一律用「」」（`**` 要在**資料表的來源**就不出現，
不能靠畫面層 `.replace()` 收尾——那是保險，不是規則）。

### 驗證

17 支測試全綠、9 個 clicktest 在原始碼樹與 exe 上各 exit 0、
三張重寫過的畫面實拍確認。稽核腳本可重跑（掃字串 ＋ 比對四條禁令）。

---

## [0.28.2] — 2026-08-09 — UiKit 收掉畫面層的重複，並修掉一個活的版面缺陷（B2.7.2）

`/codebase-design` 全案審視的第二批結論。**零玩法變更、淨減行數。**

### ① `UiKit.click()` —— 五份逐字相同的合成點擊收成一支

`Main` / `Campaign` / `Roster` / `Tech` / `TycoonOrders` 各寫了一份，而且掛在
**三個不同的名字**底下（`_press` ×3、`_click` ×1、直接內嵌 ×1）。
`Battle` 那份留著——它要 `button_mask` 與滑鼠移動事件（拖曳），是真的不一樣。

★ **去重當場暴露一個回歸（RG-153）**：寫在畫面裡時 `await get_tree()` 問的是
畫面（活著），搬進 `UiKit` 之後問的是**那顆鈕**——而這一顆很可能就是觸發
`_build()` 的那一顆，送完事件它已經被釋放。**十七支 headless 測試全綠，
四個 clicktest 直接拒絕執行**。修法是先把 `tree` 拿起來再送事件。

### ② `UiKit.back_row()` —— 一個**當時還活著**的缺陷

八顆返回鈕裡七顆包了 `HBox`、**一顆沒包**：`DailyScreen` 的「返回標題」
被拉成 735px 寬（截圖證實）。同一個 bug 的第二次（B2.5 的 `TycoonLines`）。

**要記得的規矩會被忘記**，所以做成一支函式。同批 `UiKit.screen()` 收掉六份
margin 樣板——行數不是重點，重點是 **24 是 `20_ART_DIRECTION.md` §1.3 的間距階**，
六份各自寫死等於日後要改就要改六個地方。

### 審視裡**不改**的兩條，各自登記了觸發條件

| # | 事 | 為什麼不現在做 |
|---|---|---|
| **R-15** | `Battle.gd` 4290 行 | 它的 interface 只有 5 個欄位——**它是 deep module，不是 shallow**，拆成十個畫面反而把它變淺。只抽鏡頭，而且**等 M3 動到地圖尺度那一批**（`_world()` 是座標權威） |
| **R-16** | `SaveService` 15 支公開函式裡 10 支是經濟規則 | 拆出 `Ledger.gd` **是搬家不是減量**（刪除測試上複雜度總量不變）。觸發：第 11 支經濟函式 |

另外補寫了 `data/` 與 `scripts/meta/` 的分界規則（CLAUDE.md）——現況是對的，
缺的只是那句話；搬檔案只會製造 `.uid` 變更換零行為改變。

### 驗證

17 支測試全綠、9 個 clicktest 在原始碼樹與 exe 上各 exit 0、
每日挑戰截圖重拍確認返回鈕已經是內容寬。

---

## [0.28.1] — 2026-08-09 — loadout seam：憲法 B3 從「靠列舉」改成結構性的（B2.7.1）

只改形狀，**零玩法變更**。起因是一次 `/codebase-design` 審視。

### 問題：一軸一個參數、一軸一道閘

```
setup(map, unlocked, tech)          B1.3
setup(map, unlocked, tech, levels)  B2.7        ← 參數隨軸數成長
Daily.tech_for() / Daily.levels_for() / Daily.difficulty_for()…   ← B2.6 要再加一支
```

而那幾支 `_for()` 合起來就是憲法 B3（統一配置榜與玩家的任何進度與課金
完全無關）在程式碼裡的全部實作。**漏掉一道不會有任何東西報錯**，
而漏掉的後果是一條可以課金加速的軸洩進「對 P2W 的唯一制衡」那張榜。

同一個形狀本案已經踩過兩次：RG-142（以 type 為鍵的表）、
RG-149（「全部在畫面內」的斷言只列舉了鈕）。**第三次踩到的會是憲法。**

### 修法：`scripts/sim/Loadout.gd`（新，純函式，60 行）

| | 之前 | 之後 |
|---|---|---|
| `SessionState.setup()` | 4 個位置參數，B2.6 會是 5 個 | **3 個，不再成長** |
| B3 閘 | `tech_for` ＋ `levels_for` ＋… | **`Daily.meta_for()` 一道** |
| 斷言 | 一軸一條手寫 | **逐鍵列舉 `Loadout.KEYS`** |

B2.6 加難度層、M4 加任何一根付費軸，只要它進了 `Loadout.KEYS`，
那條斷言自動涵蓋它；忘記處理就會自己變紅。**忘記把一軸加進 `KEYS`
的下場是它根本進不了局（功能沒生效，吵的），不是洩進統一榜（安靜的）**
——兩種漏法之中選安全的那一種。

### 驗證

- 17 支測試全綠（`progress_test` 131 → 135）、9 個 clicktest 全綠
- **兩個方向都驗過**：把 `meta_for()` 改成只擋 `tech`（模擬「忘記加一道閘」），
  `daily_test` 1 條、`progress_test` 4 條當場變紅
- `daily_test._saves()` 的「滿進度」存檔補上等級軸——不補的話那條
  `state_hash()` 斷言只驗得到科技軸，而等級軸才是可以課金的那一軸

---

## [0.28.0] — 2026-08-08 — 進度整合：等級軸、成就、公司等級（B2.7）

B2.5 交出來的升級材料，到這一批才有花的地方。規格寫在 `10_GDD.md` §7.15。

### 等級軸（§1 B2 ②）

兩軸各 10 級、**每級 +8% 加法**、滿級 **×1.80**：塔那一軸乘傷害，生產那一軸
乘 `ore_out`／`power_out`／`alloy_out`。**折進 `Tech.mods()` 那一個字典**，
所以五個既有的消費端一行都不必改——每一個「要記得也乘上等級」的呼叫端，
都是一個會靜靜失效的地方。

成本 `12 × n`，單軸 660、兩軸 **1320 材料**。

### 升級材料的三條路（§4.1 的落地）

| 來源 | 數量 |
|---|---|
| **塔防局末結算**（新） | **撐過的波數**，戰役與無盡都算 |
| 潮汐公司訂單 | `tier`（B2.5 已有） |
| **成就**（新） | 一次性，不碰 tycoon 拿得到 860 |

★ 局末**只數波數，不加星數加成**：加了的話「重刷第 1 關（3 波 3 星）」會比
「打完第 5 關（5 波）」划算——**一條讓最短的局付最多錢的曲線是退化的**。
`progress_test` 有一條單調性斷言看著它。

### 成就 20 條：**推導的，不是領取的**

每一條都是「存檔上某個量 ≥ 門檻」，所以沒有 `claimed` 清單、沒有事件監聽 →
**不可能重複發放，也不可能漏發**（B2.4 以前存的檔照樣算得出來）。畫面上
**一顆領取鈕都沒有**，而那是它最重要的性質。同 §7.13 的券、§7.14 的產線位佔用。

### 材料餘額也是推導的

`SaveService.components() = 三條路的累計 − 兩軸現在等級反推的花費`。
**沒有餘額欄位**，所以「等級」與「材料」不可能互相矛盾。

### 三個測試抓到的真缺陷

**① 成就對著已經在數的進度再發一次券**（`roster_test` 變紅）。
`Roster.earned()` 已經把戰役星數與無盡波數換成券了，成就在「五關全通」「滿星」
「無盡五十波」上又各發一張 → **§7.13 那條刻意的對齊（戰役滿星 3 券 ＝ 剛好抽乾
招募池，也就是 B6 的證明）變成 5 券**。改成只發給「別處沒在數」的三條，
並寫成**通則斷言**（發券的成就不得用 `earned()` 已在數的 metric），不是釘住那四個 id。

**② 主選單的版本號被切掉，而 B2.5 加的位置斷言照樣是綠的**——它只走
`_menu_buttons`，**沒有被列舉的東西不會被守住**（RG-139 的第三次）。現在問的是
容器的每一個子節點；把間隔調回 20 驗證它真的會紅（`切到 @Button@30@690`）。

**③ 局外成長畫面的說明段落切在畫面右緣**（`UiKit.label()` 預設不換行）。

### 統一配置榜吃不到等級軸（憲法 B3）

等級軸是**可以課金加速的那一軸**，所以它比科技軸更該被堵住。
`Daily.levels_for()` 是 `Daily.tech_for()` 的同一道閘；`progress_test` 拿滿級
滿科技與零進度兩份存檔在統一榜上開局，斷言 `mods` 一模一樣。

### 公司等級（共享進度條）

**推導的彙總，沒有欄位、不發獎勵**——掛上獎勵它就變成第四套經濟。
塔防那幾項與 tycoon 那一項都在式子裡，所以「兩層都推得動」是式子的性質。

### 版面

主選單沒有變成十顆鈕：**等級軸併進科技樹畫面**（兩者回答同一個問題：我的永久
強化買到哪了，畫面改名「局外成長」），而「成就」那一顆換掉了**「測試圖　淺灘」**
——它是開發鷹架，不該出現在玩家的主選單上（`TL_PANEL=battle`／`sandbox` 仍到得了）。

### 驗證

- 17 支測試全綠（新增 `progress_test` 131 條）
- 9 個 clicktest 全綠（新增 `TL_PANEL=achievements`）
- **`levels` 是新增的存檔鍵，沒有 bump `SAVE_VERSION`**（加鍵不是結構改動）；
  舊存檔讀進來自動長出零級零材料，有斷言看著

---

## [0.27.0] — 2026-08-08 — 潮汐公司（B2.5）

M2 的最後一個系統。**這一批的每一個決定都在回答同一個問題：怎麼讓它不要長成
第二個遊戲**（`00_CONCEPT.md` §三 約束 2、風險 R-2）。

### 三處與 §3.8 原文不同，理由都寫進 `10_GDD.md` §7.14

| §3.8 原文 | 實作 | 為什麼 |
|---|---|---|
| 排產線「**複用流量網路引擎**」 | **產線位指派** | 空間式編輯器＝第二個 `Battle` 畫面，而且**重複塔防的核心動詞**。§3.8 自己說「刻意保持單薄」、留存角色是「一兩分鐘就走」——五分鐘的佈局編輯器直接違反那句話 |
| 訂單有「**交期**」 | **沒有交期** | 約束 1 明文「沒有失敗狀態、沒有時間壓力」。交期就是時間壓力，錯過就是失敗狀態。**約束是上位的** |
| 「倉儲上限可升級」 | **一個產線位一次做一張，做完就停** | 不另開一個數字：**產線位本身就是倉儲上限**。離線再久的上限就是「每個產線位各完成一張」（RG-10） |

### 一個看起來無害的乘數推翻了收斂

§4.2 的兩條公式都沒問題（成本 `500 × 1.35^L` 指數、報酬 `120 × t^1.25` 線性偏下），
直覺上必然收斂。出問題的是**我自己補的第三個參數：產線位**。

它是收入的**乘數**，所以決定收入曲線的階數。第一版寫「每兩級加一個」，
於是 `slots ≈ L/2`、收入 ≈ `L^1.25`，而在 M2 的八級範圍內**指數還來不及贏過它**：

| | 存到下一次擴廠要多久 |
|---|---|
| 每兩級加一個 | 廠等 1 **338 秒** → 廠等 7 **314 秒**（❌ 越擴越快） |
| 每三級加一個 | 廠等 1 **338 秒** → 廠等 7 **419 秒**（✅ 收斂） |

**教訓（RG-146）**：驗收斂不能驗公式長什麼樣，要驗**玩家感覺得到的量**
——「我還要存多久」。斷言的第一版比的是「成本倍率 vs 報酬倍率」，
那兩個根本不可比（一個每級、一個每張訂單，中間差了產線位與工作量）。

### 截圖抓到的兩個東西，斷言一個都不會說

- **`Label` 不解析 Markdown**，`**做完就停**` 原樣印出兩排星號。**這是第四次**
  （B0.7.1／B0.7.3／B1.1 各一次）。同批把檢查寫成一行 grep，並掃過全部畫面
  （只有 `Tech.gd` 有，而它已經 `.replace("**", "")`）。
- 「返回訂單板」被 `VBox` 拉成整個畫面寬。

> 兩個都是 **`TL_CLICKTEST` 全綠的狀態下發生的**——它驗的是鈕按不按得到。
> 產線編輯沒有自己的 `TL_PANEL`（兩個畫面、一條路），所以它差一點變成
> 「從來沒有人看過的那張圖」，也就是 RG-142 的成因。借 `TL_LEVEL=2` 拍它。

### 落地內容

- `scripts/meta/TycoonSim.gd`：**純函式、零 RNG、不讀時鐘**（「現在幾點」由呼叫端傳）
- 兩個畫面：訂單板（收成／接單／擴廠）＋ 產線編輯（指派）
- 存檔加 `tycoon` 鍵，**沒有 bump `SAVE_VERSION`**（加鍵不是結構改動）
- 招募券的**第三條路**：第 4 階起每張訂單 1 張。是**加法不是替代**——
  `roster_test` 仍然斷言「純戰役滿星就畢業得了」（憲法 B6）
- `tests/tycoon_test.gd`（64 條）：離線上限、時鐘倒退不獎不罰、沒有失敗狀態、
  **UI 深度恰好兩個畫面**（數檔案——人會忘記查，grep 不會）、
  **以「從沒開過公司」的存檔跑完戰役五關參考解**

---

## [0.26.6] — 2026-08-06 — 徽章說得出餓的是電還是礦（B2.4.8）

遊玩測試 P3／P2 首跑回收的四條，全做。**其中一條做完發現是我自己讀錯。**

### P2-1 ★★★★ 徽章分家：缺礦砂 vs 缺電

三態徽章的 `缺料` 把兩種餓畫成同一個橙色倒三角，於是「**地圖上唯一指出瓶頸的
元素**」說得出誰在挨餓、說不出**餓的是什麼**。而本作的核心命題就是峰值電力，
「我現在缺的是電還是礦」是玩家每分鐘都要答的問題——把它留給頂欄數字，
正好是 R-3 要對策掉的東西。

三態改四態：`STARVED`（缺礦砂，**橙色倒三角**）／`STARVED_POWER`（缺電，
**琥珀閃電**）。**形狀與顏色同時不同**（§1.6 說換顏色不夠要換形狀，這裡兩個都換），
而琥珀是配色紀律 2「琥珀專屬於能量」在幫忙。

同一張第 5 關 tick 900 的裸眼圖，現在讀得出「**碎浪缺電、熔爐缺礦**」；
修之前那幾個徽章一模一樣。

> `flow_test`（斷電的熔爐）與 `hud_test`（沒礦砂的發電機）**互為對照組**。
> 單獨一條可以靠「永遠回傳同一個值」矇混過去。
> 凡是問「有沒有在餓」一律走 `SessionState.is_starved()`——寫死 `== STARVED`
> 的地方在分家的當下就會安靜地漏掉一半。

### P3-1 ★★★ 錨與發電機是同一個形狀

都是實心方塊，只差 6px 與顏色——違反 §1.6「不靠顏色分辨」。而它們分屬
**生產側與防線側**，正是「這 40 礦砂餵產線還是餵防線」那個核心決定的兩端。

改**錨**不改發電機（琥珀實心方和能量的關係更強，波及也更大）：
錨 → **上寬下窄的實心梯形**（打進地裡的樁）。

### P3-2 ★ 回收者與碎浪都是「方框裡包東西」

改的是**碎浪**：「實心方＋外框」→ **四角爆散星**。這樣不只解掉碰撞，還讓它
**把自己的機制講出來**——碎浪是全遊戲唯一一隻範圍傷害的塔。

> ⚠ **換來一個新鄰居要盯**：碎浪（四角爆散星，實心）與霜礁（六芒星，細線）都是
> 合金銀的放射狀形狀。fit 合照上「實心塊 vs 細線」分得開，但這是**換位置不是解決**。

### P2-2 ❌ 誤判，撤回——而且是同一個錯法的第三次

初稿宣稱「階段色調讀不到」並提議刪掉它。取樣像素之後：

| 階段 | 空地 RGB |
|---|---|
| 準備期（`order.cyan` 0.03） | `(19, 37, 52)` 偏藍 |
| 波次期（`warn.orange` 0.10） | `(40, 42, 47)` 偏暖灰 |

**R 差 21。色調一直在工作。** 錯誤來源是**前後分開看兩張截圖**（相隔數分鐘），
而人眼對絕對色的記憶極差。

登記 **RG-144**。這是同一族的第三次——RG-141（A-3）錯在證據是縮圖、
RG-143（B-3）錯在用 grep 推論、這一條錯在沒有並排也沒有取樣。共同形狀是
「**用了一個比實際結論弱的觀察，然後照結論開了處方**」，而三次的處方
都會動到已經正確的東西。

**由此定一條**：遊玩測試的產出一律要**再驗一次才進 backlog**。首跑四條裡有一條是誤判。

---

## [0.26.5] — 2026-08-06 — 說明預設不開、滾輪只在地圖上縮放（B2.4.7）

兩條使用者實玩回報。

### 說明面板預設不開

原本空地圖時自動彈出，理由是「這局還沒開始，正是需要它的時刻」。但它整片是
文字、蓋在地圖上，而且**每一局開頭都會再來一次**——第二次之後它就只是一道要
關掉的門。提示列（`_hint`）本來就在講下一步，說明鈕也一直在底欄。
**要看的人按得到，不要看的人不必關掉它。**

順手刪掉 `_help_auto` 那一整套「自動開的那份會自己收起來」的狀態機——
預設不開之後它沒有存在的理由了。

### 滾輪：捲到邊界的事件會掉進地圖的縮放

「選單滑到最上面，繼續滑會導致畫面放大。」

原因是 `ScrollContainer` **只在捲得動的時候**吃掉滾輪事件；捲到頂還往上滾，
事件就落回 `Battle._gui_input`，而那裡不分位置一律縮放地圖。

修法是**限制範圍**：滾輪只在地圖框架（`FRAME`）內縮放。這順帶擋掉頂欄與底欄
上的滾輪——那本來也不該縮放地圖。

> ⚠ **不要改成「讓捲動容器在邊界也吞掉事件」**：那會壞掉「捲到底之後想捲外層」
> 這個標準行為。**限制範圍才是對的層級。**

斷言 `TL_CLICKTEST/view` 加兩條（欄上不縮放 ＋ 圖上照樣縮放）。
**只驗前者的話，把整段滾輪邏輯刪掉也會全綠。**
紅燈證明跑過：拿掉守衛 → `欄上不縮放=false`，證明合成事件真的落到 `_gui_input`，
那條斷言不是空的。

---

## [0.26.4] — 2026-08-06 — 頂欄三階、底欄分群、字級階回歸（B2.4.4）

美術稽核 §7.2 的 B 級四條一次做完。**這一批最值得記的不是做了什麼，是量出來的東西
推翻了自己的稽核。**

### B-3：用 grep 推出來的缺陷，連「症狀存在」都還沒證明

稽核初稿靠 `grep tabular|FontVariation|monospace` 零命中，推論「**頂欄每秒橫向抖
十次**」，並開出「用 `FontVariation` 開 OpenType `tnum`」的處方。

B2.4.4 先寫量測（`_topbar_selftest()`）才動手：

| 情況 | 修之前 | 意義 |
|---|---|---|
| 同位數換數字（1111 → 8888） | **不位移** ✅ | 微軟正黑體的數字**本來就是等寬的** |
| 跨位數（1111 → 99） | 位移 ❌ | 這才是真的缺陷 |

所以：**「每秒抖十次」是假的**（礦砂逐 tick 的變動絕大多數不跨位數），而原本的
處方**修的是不存在的那一半，對真正的那一半無效**（`tnum` 只保證每個數字等寬，
不保證總寬相同）。有效的只有 §3 自己寫的備案：**固定寬度 ＋ 右對齊**。

登記為 **RG-143**。它和 A-3（RG-141）是同一族但更危險：A-3 錯在證據（縮圖），
這一條錯在**推論**——`grep` 證明的是「沒有用某個 API」，不是「有那個問題」。

### B-2：三階字級排不下 11 個數字，所以真正的結論是「數字太多」

第一版排完**超出畫面 166px**，`bar_clear` 當場紅。而 §2 反模式清單自己就寫著
「每個畫面最多 4 個『主要數字』」。七欄縮成五欄，**搬家不是刪除**：

| 原本在頂欄 | 去了哪裡 | 理由 |
|---|---|---|
| 儲槽存量 | 能量面板 | 它是「儲槽充能／放電」那兩列的分母；每顆儲槽身上還有琥珀弧 |
| 交戰座數 | 能量那一格的註腳 | 它**就是**能量需求的解釋 |
| 快進倍率 | 底欄「快進 4×」鈕自己身上 | 它原本因在底欄、果在頂欄，而那顆鈕**看不出開沒開** |
| **擊殺數** | ❌ **移出局內 HUD** | 計分不是決策輸入；局末結算裡有，回收者的貢獻在能量面板有專屬一列 |

留下的三階：**主（22）＝能量／波次／核心**（「我現在會不會輸」）、
**次（16）＝礦砂／合金**（「我買不買得起」）、**附屬（13）＝註腳**。
顏色分群維持不變——**層級交給字級，分群交給顏色**。

### B-5：稽核自己開的處方違反了配色紀律

原方向是「快進／提前召喚帶 `energy.amber` 描邊」。但 §1.1 配色紀律 2 說
**琥珀專屬於能量**，而這兩顆跟耗能無關。

改成：**只有「提前召喚」單獨用 `warn.orange` 文字色**——紀律 1 說橙／品紅專屬於
「敵人與危險狀態」，而這顆鈕做的事**就是把敵潮叫過來**。同一條紀律，而且是更強的
編碼（全底欄唯一不可逆的那顆，顏色說得出來）。快進可逆，不上色。
三群以間距分（群間 24、群內 8），不加線。

### 字級階：一條沒有人跑過的檢查等於不存在

§1.2 的字級階（11/13/16/22/32/48）**從來沒有被真的執行過**——全專案 13 處階外
（14／15／17／18／28／34／40），而 §6.3 的檢查清單裡就有這一條。全部歸位，
並把檢查寫成一行 grep 放進 §1.2，因為**能寫成 grep 的檢查就該寫成 grep**。

---

## [0.26.3] — 2026-08-06 — 三隻招募塔在地圖上是隱形的（B2.4.6）

使用者回饋：「**長哨沒有模型顯示**」。

### 1290 條斷言，沒有一條看畫面

B2.4 新增的三隻招募塔，**資料表、戰鬥、耗電、存檔、招募、音效全部正確**，
只有一件事沒做：`_draw_nodes()` 的 `match` 沒有它們的 case。

**GDScript 的 `match` 沒對到就靜靜地什麼都不做**，所以症狀不是報錯，是
**那三座塔在地圖上完全隱形**——蓋得下去、會開火、會吃電、會被啃，就是看不見。
當批十五支測試全綠，而使用者開起來五秒就看到了。

同一個錯法在 `_codex_lines()` 的字典（`.get(type, "")`）上原封不動又發生一次：
圖鑑那一行是空白的。**兩處都是「以 type 為鍵、查不到就等於什麼都不做」的表。**

### 修法：先做迴圈，再修缺陷

1. `match` 補一條 `_:` default，把漏網的型別記進 `_no_glyph`——**這不是防禦性
   程式碼，是一支感測器**。
2. `TL_CLICKTEST/glyph`：把 13 種節點全部塞進 `s.nodes` 逼 `_draw()` 跑過每一個
   分支，再看 `_no_glyph` 有沒有東西；圖鑑說明一起驗。**先跑一次確認它是紅的**
   （點名 `ballast,frostreef,longcall`），修完才轉綠。
3. 三隻各補一個輪廓，**不靠顏色分辨**（§1.6）：
   - **長哨**＝細桅杆＋頂端橫桿（瞭望塔，射程 12），秩序青
   - **霜礁**＝六芒星（發散，與潮鳴同族但認得出），合金銀
   - **定潮**＝頂桿＋倒三角（吊著的配重，與稜鏡的正三角互為鏡像），合金銀
4. 補三段圖鑑說明。

> 長哨的第一版橫桿在中間，截圖上讀起來是一個**十字**不是一座塔——分辨得出來，
> 但講錯了話。移到頂端才成立。**這一步只有讀圖才抓得到**，斷言永遠會說 PASS。

### 順手掃過其餘每一張以 type 為鍵的表

`DEFAULT_PRIORITY`／`PRIORITY_ROWS`＋`PRIORITY_GROUP`／每塔音效／`Daily.UNIFORM_BUILD`
／戰役解鎖／`Roster.RECRUIT_POOL` — **無其他破口**。憲法 B3（統一配置榜不含招募池）
與 B6 完好。音效那張表是**從 `BUILDABLE` 推導的**，結構上不會腐爛——那是其他表
該學的寫法。

### `50_QA_PLAN.md` §8：遊玩測試腳本

自動化擋得住「算錯」，擋不住「沒畫」「看不懂」「不好玩」「進不去」。新增十場
遊玩測試（P0–P10），每一場**一個問題、一條停止規則、事先寫好的缺陷判準**——
判準要在開始玩之前寫下來，否則玩完只會得到「感覺還行」。

同時寫明**什麼不該進遊玩測試**：凡是已有自動化閘門的一律不重複驗。
**§8.3 那張表變長，代表自動化沒跟上。**

---

## [0.26.2] — 2026-08-06 — 侵蝕暈改回貼齊格線（B2.4.5）

使用者實玩回饋：「**路線可以做抖動，但是侵蝕暈不要做抖動**」。

### 一個元素答兩題，不代表兩題該用同一個語彙

第一版把路徑帶與侵蝕暈**兩層都做成抖動邊緣**——理由是 §0 的視覺論題
（潮＝混沌＝不對齊、抖動），而暈是潮的一部分。**這個推理在暈這一層是錯的。**

兩層要答的問題根本不同：

- **路徑帶講氣氛**：這條帶子不是我的，它屬於混沌側 → 抖動是對的。
- **侵蝕暈講規則**：我的建築要退開幾格才不會被啃 → **一條抖動的輪廓線量不出格數。**

而格子正是玩家放建築的單位。§0 說「任何新增視覺元素都必須回答：它屬於秩序還是
混沌」——暈的答案是**秩序**，因為它是一把給玩家用的尺，不是潮本身。
**把一條規則畫成混沌側的樣子，等於用美術把它的可用性拿掉。**

改動只有一處：暈走新的 `_cell_quad()`（貼齊格線），抖動留給 `_band_quad()`。
交界仍然不可能有縫——暈是整片畫的（暈 ∪ 路徑），路徑帶抖出去的 4px 永遠落在
暈的內部。振幅維持 ±4px（使用者同一則回饋拍板）。

### 順帶量了 exe 大小這件事

使用者問「有辦法把 exe 做小一點嗎，大部分的 exe 都只有幾 MB」。**量過之後結論是
現有的 105.9 MB 裡有 104.1 MB 不是我們的**：

| | 大小 |
|---|---|
| Godot 4.7 `windows_release_x86_64` 匯出範本 | **104.1 MB** |
| 我們全部的內容（19 支 wav ＋ 匯入產物 ＋ 全部 .gd） | **約 1.8 MB** |

**引擎佔 98%。** 動素材、動 `export_filter`、動 `script_export_mode` 都省不到
有意義的量（`script_export_mode=2` 本來就已經是最小的那一檔）。真正的槓桿只有
**自編匯出範本**（關掉 3D／物理／未用模組），而這台機器上 `scons` 與 `cl.exe`
都沒有。壓縮成 zip 是 **38 MB**（免費、零風險）。

> ⚠ **不要用 UPX。** Godot 的匯出把 pck 內嵌在 PE 裡，而 UPX 過的 exe 幾乎必然
> 吃到防毒誤判——一個玩家下載後被 Windows Defender 攔下來的成本，遠大於省下的 60 MB。

---

## [0.26.1] — 2026-08-06 — code review 的收穫

推送前跑了一輪雙軸 code review（standards ／ spec）。抓到的東西比我預期多，
而且**最重的兩條都是我自己驗不到的那一類**。

### 我在註解裡寫了一個沒有量過的效能數字

`_danger_cells()` 的註解寫著「路徑 ~95 格 × 8 個鄰居 ＝ 760 次查表，比快取一份
還要便宜」。那個數字**只算了這支函式自己**：下游的 `_band_quad → _interior_vertex`
每個角落又呼叫一次 `_is_danger()`，而那支再跑一遍 3×3 掃描。實際約 **45,000 次
查表／幀**，低估了 50 倍——而它就在 `_draw()` 的內圈裡。

改法讓程式碼**同時變短又變快**：`_danger_cells()` 回傳**集合**而不是陣列，
查表變 O(1)，`_is_danger()` 整支刪掉（那個 3×3 迴圈本來就寫了兩遍）。
每幀降到約 6,500 次。順手把四個 `Vector2i` 字面量陣列提成常數——它們原本在
內圈裡每格重建四次。

> 這正是 `benchmarks-need-their-own-assertions` 那條記憶：**註解裡的效能宣稱
> 如果沒有量過，它就只是一個願望。**

### 侵蝕暈與橋的規則互相矛盾（結論是修措辭，不是修畫面）

暈宣稱自己是「walk-by 傷害半徑」。但 `Tide.immune_indices()` 讓**橋與引道上的
導管免疫**，而引道格正好落在暈裡——畫面等於把遊戲唯一認可的安全動線塗成致命。

查清楚之後結論是**不動畫面**：`BattleController.BLAST_CELLS` 對**節點**沒有任何
免疫，所以暈對節點說的是實話；免疫只發生在**導管**上。所以修的是措辭——暈講的是
**建築**，導管那半邊由**橋的圖形**講（§1.6：「架高」是那條規則的唯一解釋）。

**特別記下「不要在橋兩側把暈挖一個缺口」**：那會讓一個蓋在缺口裡的中繼看起來
安全，而它會被啃。

### 暈與帶子的交界會露縫

暈原本只畫外面那一環。環的**內**緣對暈而言是內部格點（不抖），而路徑帶的**外**緣
是抖的——同一條邊界兩層各畫各的，中間最多露出 4px 的背景。改成**整片畫**
（暈 ∪ 路徑），路徑帶直接疊在上面，交界結構上不可能有縫。

### 其餘

- **核心 42 → 48px**：§7.1 A-4 自己訂的是「≥1.5 格」，42 是 1.3 格——**勾了一個
  低於自己規格的數字**。同時補上 §1.6 一直沒做的「受擊時整體閃 `warn.orange`」。
- **`Roster.gd` 十處階外字級與間距**（14/15/18/19/20/34…）。§1.2／§1.3 訂了階，
  §6.3 的檢查清單上也有這一條，而它**從來沒有人真的跑過**——這一批自己新寫的
  檔案就違反了十次。已全部拉回階上；舊畫面同樣的問題排 B2.4.4。
- **自檢重新賦值了存檔容器**（`["recruited"] = []`）→ 改 `.clear()`。CLAUDE.md
  明文：狀態原地變更，絕不重新賦值容器。
- **自檢把建造欄捲到底之後沒有捲回去**，而 `TL_CLICKTEST + TL_SHOT` 會一起下 →
  截圖會拍到捲到底的欄。順手改用 `get_v_scroll_bar().max_value` 取代魔術數字。
- **建造欄分段假設了「生產在前」**：那份清單是各關手寫的，目前碰巧如此。改成
  自己排序——有一關把塔寫在採集器前面時，那顆採集器會安靜地歸到「防線」。
- 橋的落影 0.7 → 0.55：§1.6 要的是「橋下留**可見的**路徑帶陰影」，陰影要看得見
  但帶子也要還在。讓它讀成「架高」的是**偏移**不是深度。
- **A-2 誠實化**：§7.1 原文是「抖動 ＋ **轉角改圓弧或磨掉尖角**」，而轉角仍然是
  硬 90°。勾選改成半勾，等使用者對振幅拍板時一起定案。

15 支 1290 項全過；八條 clicktest 全 PASS（原始碼樹與 exe 各一輪）。

---

## [0.26.0] — 2026-08-06 — B2.4.2／B2.4.3 美術稽核與地圖底層

對 v0.25.1 的實機 build 逐張截圖稽核（`20_ART_DIRECTION.md` §7），然後修掉
A 級全部。**稽核最重要的結論是：問題大多不是「文件缺了設計」，而是「實作沒有
做到文件已經寫下的設計」**——所以這一批多半是還債，不是新方向。

### A-1　建造欄溢位，「藍圖」鈕整顆掉出畫面（功能缺陷）

B2.4 把可建造類型從 10 加到 13，建造欄的垂直串長出畫面：「加粗」「拆除」和底欄
疊在一起，「藍圖」不在畫面上。**而這正是名冊系統的目標狀態**——全收集的玩家打
無盡就是 13 種；戰役第 5 關只有 10 種，所以永遠不會現形。

**`TL_CLICKTEST` 全程沒紅過**，因為 `_press()` 是往 `global_position + size*0.5`
送合成事件的——**一顆被蓋住、甚至被推出畫面的鈕照樣按得到**（RG-139）。

判準改成幾何的，而且分兩種東西問（捲出去的清單項目本來就在視窗外，那是合法的）：

| 對象 | 條件 |
|---|---|
| 三顆模式鈕、捲動容器本身（不會捲） | 永遠不得越過 `BAR_Y` |
| 最後一顆建造鈕 | **捲到底之後**要整顆在捲動視窗內 |

紅得起來：把捲動區撐到 700 → `last_btn=false`。

清單裝進捲動容器，模式鈕釘在它下面。**不選「壓矮一點」或「排兩欄」**：44px 是
手機移植的硬底線（P2），兩欄在 112px 欄寬裡塞不下「回收者」三個字，而真正的
理由是**這條清單還要再長一倍**（M3 是 24 隻角色 ＋ 5 種生產節點 ＝ 29 顆鈕），
任何「剛好排得下」的方案都只是把同一個 bug 往後推一批。

順手補上**生產／防線分段**：優先權面板為了同一件事特地做了雙欄，而建造欄是玩家
做同一個決定的地方（這 40 礦砂餵產線還是餵防線）。

### A-2　路徑帶終於長得像混沌側

§0 是全文件的統御命題（秩序＝直線／對齊網格；混沌＝曲線／不對齊／抖動），
§1.6 因此要求路徑帶「不對齊網格的抖動邊緣」——而它一直是一個**完美的軸對齊
矩形**：畫面上最大的一塊混沌側元素，是整張圖裡最幾何的東西。

`Shapes.band_jitter()`，振幅約 ±4px。三個實作決定：

1. **抖動掛在「格點」上不是「格子」上**——相鄰兩格共用同一個角落，偏移量必須是
   那個格點座標的函式，兩邊才算得出同一個值。掛在格子上會讓帶子裂成一排錯開的方塊。
2. **零 RNG**，純粹是座標的函式 → `TL_SHOT` 同參數拍得出同一張圖。
3. **只有外緣抖**，內部格點不動，否則帶子內部會出現裂縫。

★ 這是刻意克制的第一版，等使用者看過再定案。

### C-1　侵蝕暈 ＝ walk-by 的傷害半徑（使用者拍板）

路徑外一格的 `tide.deep` 0.13 填色 ＋ **最外圈一條 0.5 的輪廓線**。它同時是
`10_GDD.md` §3.5 的「每 tick 傷害相鄰 1 格內的建築」——氣氛與規則同一個元素，
和來襲箭羽是同一條理由。

★ **只有填色是不夠的**：淡淡一塊顏色說得出「這附近危險」，說不出「危險到哪裡
為止」，而後者才是玩家要下的決定（塔要退開幾格）。`TL_NAKED` 下一眼看得出哪幾座
建築在圈內。

### A-4　核心終於看起來像核心

30 → **42px**（1.3 格）＋ 3px `order.bright` 描邊 ＋ 內方塊走 `dur.ambient × 2` 的
極慢呼吸（心跳，不是警示）。原本它和採集器的 22px 圓沒有量級差，而**這是全遊戲
唯一一個「歸零就結束」的物件**。形狀仍是正方，和儲槽的同心圓／交戰環分得開。

### B-1　跨越點終於像一座橋而不是一道門框

§1.6 的三個元素**本來就都在程式碼裡**（稽核初稿說「只做到第一件」，那是錯的）。
問題是強度與語彙：

- 落影內縮 3px、alpha 0.55 → 改成**滿格 ＋ 往側邊偏 4px**。**有偏移才讀得出高度**，
  一圈等距的暗邊只是描邊。
- 引道是**直線延長 6px**，不是 §1.6 寫的 45°。改成真的 45°，橋頭往外張開——
  而 45° 是秩序側的既有語彙（導管只走 90°/45°），用它橋才接得回玩家自己的線。

### ❌ 撤回一條誤判（A-3）

稽核初稿依據兩張縮到 1280 寬的截圖，宣稱路徑帶轉角的 alpha 疊了兩次、「看得到
接縫」，並排進了修復批次。**查證後完全不成立**：`Maps.path_of()` 每格只 append
一次、`_draw_path()` 一格一個不重疊的 `Rect2`、4× 近照下轉角與直線段同一個值。
我讀到的深色是 `_draw_incoming()` 的品紅箭羽被縮圖糊成一片。

★ 記成 RG-141：**縮圖上讀到的瑕疵，在變成工作項目之前必須先拍一張近照。**
它與 RG-106 是同一條規則的兩面——設計不能只在近照上驗，缺陷不能只在縮圖上判。

### 回歸

15 支 1290 項全過；八條 clicktest 全 PASS；`TL_NAKED` 檢定通過（暈的輪廓、核心、
橋在沒有任何數字的畫面上都說得出話）。

---

## [0.25.1] — 2026-08-06 — B2.4.1 招募入口看不見（實玩回饋）

使用者的原話：**「我沒有看到抽卡畫面」**。

不是沒做，是入口做得看不出來。招募鈕被接在「返回」後面共用一排——於是這個畫面
**唯一會改變存檔的動作，看起來像導覽列的一部分**；零進度時它還是灰的，更像一行
狀態文字。

招募**沒有第二個畫面是刻意的**（§3.9 不使用賭博語彙：沒有十連演出、沒有轉場）。
但「不做演出」不等於「入口可以看不見」——它只表示這個動作當場結算，不表示它不重要。

改成一塊有框的區域，和「返回」分開，三個狀態各自說完整的一句話：

| 狀態 | 鈕 | 旁邊那句話 |
|---|---|---|
| 有券 | **招募（1 張券）**（字大一級） | 還剩 3 隻未收集・聲望券 3。抽到的必定是你還沒有的那幾隻。 |
| 沒券 | 招募（灰） | 還剩 3 隻未收集・聲望券 0——還差 5 顆星或 10 波 |
| 畢業 | 已收集完畢（灰） | 招募池的 3 隻全在你手上了，聲望券不會再被消耗。 |

自檢的斷言跟著改成看**那句話**而不是鈕的 `disabled`——灰掉的鈕只說「不行」，
湊不出下一步。

`TL_SHOT` 配 `TL_CLICKTEST` 時，畫面現在會留在「有券可招募」那一格（放在全部斷言
跑完之後，一項都沒被跳過）。零進度拍出來是灰的、抽完拍出來是「已收集完畢」，
而要看的正是中間那個狀態——它在沒有進度的存檔上到不了。

---

## [0.25.0] — 2026-08-06 — B2.4 名冊與招募

M2 的排程項。角色圖鑑、確定性解鎖途徑、招募（不重複、有畢業、無賭博語彙）（§3.9）。
同時還清 B2.2 記在 §7.11 的那筆債：**每日統一配置榜的「固定角色組」從
「空陣列＝全部」釘死成一份明列的十種**。

### 擁有清單是推導的，不是存的

這一批最容易做錯的事是「在存檔裡開一個 `owned` 陣列」。那會讓兩個事實講同一件事：
`campaign.stars` 已經知道你開到第幾關，而每一關的建造欄（§7.9）已經知道那一關給
你哪幾顆鈕。**存第二份就會漂**——漂掉的那天玩家看到「第 3 關通了但潮鳴還鎖著」，
而兩個欄位各自都「對」。這是 sv2 砍掉 `campaign.cleared` 的同一條理由。

所以 `data/Roster.gd` 沒有解鎖對照表：

| 問題 | 答案來自 |
|---|---|
| 我有哪些角色？ | `CampaignData.open_count(stars)` ＋ `roster.recruited` |
| 我有幾張聲望券？ | 賺到的（戰役每 5 星、無盡每 10 波）− 已招募數 |
| 這一隻怎麼拿？ | 掃一次 `L1..L5_BUILD` 看它第一次出現在哪一關 |

**存檔只多了一個 `roster.recruited` 字串陣列**（沒有 bump `SAVE_VERSION`，加鍵不是
結構改動）。

「這一關能不能點」的規則也順手收成一份：`Campaign.open_count()` 現在同時服務
關卡選擇畫面與名冊——各判一次的話，日後改成「要 2 星才開下一關」只會有一邊跟著改。

### B6 是一條斷言，不是一句承諾

§3.9 說「稀有角色全部都能純靠遊玩取得」。券的數字是照著這句話定的：
**戰役滿星 15 顆 ÷ 5 ＝ 3 張 ＝ 剛好抽乾招募池**。

`roster_test._free_to_play_can_graduate()` 拿一份**只推戰役、從未碰過無盡、
一毛錢都沒花**的存檔，把名冊收到全滿。

### 憲法 B3：招募不進統一配置榜

`Daily.UNIFORM_BUILD` 是一份明列的十種（五種生產節點 ＋ 五隻確定性角色）。
「空陣列＝全部」在名冊落地的當下就會出事——那等於把抽到才有的三隻發給榜上
每一個人，而且**每加一隻新角色都會悄悄改變這張榜**，昨天的成績跟今天的不再可比。

紅得起來：把三隻加進 `UNIFORM_BUILD` → 五條斷言同時倒。

### 三隻招募角色（§7.13）

全部**只用既有的欄位**，`sim/Combat.gd` 一行都沒改——招募不該是新機制的載體，
那與「所有東西都能純靠遊玩取得」直接衝突。三隻都不進戰役，所以 B1.2 校準過的
五關難度不受影響（`campaign_test` 現在斷言這件事）。

| 角色 | 每瓦 dps | 它買的是 |
|---|---|---|
| **長哨** 180 礦砂・8 電・射程 12 | 2.28 | **覆蓋範圍**。無盡大圖上一座蓋掉一個象限 |
| **霜礁** 200 礦砂＋40 合金・16 電・減速 65% | — | **隘口**。潮鳴的極端版：更兇、範圍剩一半、電費 1.8 倍 |
| **定潮** 240 礦砂＋100 合金・3 電 | **8.0** | **電費折扣**。取捨從峰值電力挪到經濟，不是取消取捨——100 合金要一整條熔爐產線 |

### 優先權面板：一列＝一個「角色的角色」

§3.1 鎖死「5–8 條滑桿恆在同一位置」（R-1）。一隻角色一條滑桿的話，M3 的 24 隻
就是 **28 條滑桿**——那個面板已經不是一個手勢，是一份表單。

所以新角色**併進既有的那一列**（`NodeDefs.PRIORITY_GROUP`）：長哨・定潮 → 錨
（單體物理），霜礁 → 潮鳴（光環）。只影響 UI 與預設值，`FlowNetwork` 仍然逐 type
讀 `priorities[type]`。

### ⚠ 一個玩法變更

**無盡與每日自由配置榜現在吃名冊**（之前是全建造欄）。一份零進度的存檔進無盡
只有錨。「名冊 → 下局玩法組合改變」（§1 服務循環圖）要成立，招募到的東西就得有
地方用得到。**戰役不吃名冊**——那是難度階梯，交集它會讓「通關第 4 關卻蓋不出
碎浪」變成可能。

### 兩個測試自己抓到的東西

| 缺陷 | 怎麼發現的 |
|---|---|
| `campaign_test` 的「第 5 關全解鎖」寫死成 `BUILDABLE.size()` | 加了三隻之後當場變紅。**壞掉的是斷言不是功能**——改成「全部確定性節點」，並補一條「招募池不在任何一關的建造欄」 |
| `TL_CLICKTEST` 的合金閘門寫死 `_build_type == "breaker"` | 表尾不再是碎浪。改成問 `_buildable()` 的最後一個是誰，**並另外斷言它真的要合金**——少了後半句，表尾哪天換成不用合金的節點，這條會安靜地變成空操作 |

新音源兩支（`fire_longcall`／`fire_ballast`，程序生成）又踩到 B2.1f 記過的坑：
新 wav 的 `.import` 預設 QOA 失真壓縮，`audio_test` 的 16-bit 與削頂四條當場抓到。

### 回歸

15 支 **1290** 項全過；**八**條 clicktest 全 PASS。

---

## [0.24.0] — 2026-08-05 — B2.3 藍圖庫

M2 的排程項。框選一組節點與導管存成藍圖，下一局一鍵展開（§3.7）。

### 全有全無

這一批最該怕的事不是「展開失敗」，是**展開了一半**：花掉資源換到一個接不起來的
殘骸，而拆掉只退 75%——玩家沒有辦法補救。

所以 `BuildController.blueprint_place()` 先整份驗過，一格不合就一格都不放：

| 情況 | 結果 |
|---|---|
| 礦砂差 25 | `✕ 礦砂差 25`，**一格沒放、一塊錢沒扣** |
| 有格子被佔住 | `✕ 有 2 格蓋不下（(21,9)…）`，同上 |
| 都過 | 展開，扣掉估價時算出來的那個數字 |

紅燈證明（拿掉檢查閘門）：錢扣了 75、只放了 2 格，七條斷言同時倒。

**缺口是具體的**：「礦砂差 25」而不是「資源不足」，「(21,9)」而不是「失敗」。
估價與實際扣款另有斷言鎖住是同一個數字——不然玩家照著提示湊到錢，還是蓋不起來。

**落點預覽逐格上色**（綠＝放得下、橘＝這一格擋住了）。整份一個顏色只能說「不行」，
指不出是哪一格，而那正是玩家的下一步要知道的事。

### 座標存成 `[dx, dy]`，不是 `Vector2i`

藍圖要進 `user://save.json`，而 `Vector2i` 過不了 JSON（`stringify` 會把它變成
`"(1, 2)"` 這種讀不回來的字串）。存成 JSON 原生型別 → **存檔往返不需要任何
序列化程式碼**，少一層轉換就少一個「存進去和讀出來不一樣」的機會。
回歸測試直接走一趟真的 `JSON.stringify` / `parse_string`。

### 框選的兩條收錄規則

- **核心不收**——它蓋不出來，收進去只會讓每次展開都固定失敗一格。
- **導管要兩端都在框內才收**——只有一端在框內的，展開時另一端接不到任何節點。
  那不是藍圖的一部分，是它和外界的接線，而接線是玩家在新地圖上要自己決定的事。

節點與導管**排序後才存**：同一份佈局不論當初蓋的順序如何，框出來逐欄相同。

### 槽數吃玩家的科技，不吃 `s.mods`

免費 2 槽 ＋ 後勤科技各 +1（上限 4）。**用 `s.mods` 是錯的**——藍圖庫是跨局資產，
而每日挑戰的統一配置榜會把 `s.mods` 壓成中性值（憲法 B3），那樣玩家打統一配置榜時
槽會憑空少兩個。

### 三個自己踩到的坑

- **測試佈局蓋在靜水沙盤的核心格與界外**，`place()` 靜靜失敗 → 藍圖少一個節點 →
  十幾條斷言全紅，而紅的樣子看起來像是藍圖模組壞了。`_built()` 現在斷言自己的
  前置條件（量測台自己也要驗）。
- **「數量對得上」在數量為 1 時是恆真的**。clicktest 比對「藍圖節點數 ＝ 框到的格數」，
  框到 1 個時它必然成立——既測不到導管也測不到多格落點。**截圖上只看到一格預覽
  才發現**，補了「框到的格數 ≥ 2 且導管非空」。
- **藍圖抽屜第一版放 (128,150)，被圖鑑整個蓋住**（文字從後面透出來）。畫面上已經有
  三張可同時開啟的浮層，位置是擠出來的不是挑出來的；移到 (560,400) 並補一條
  「不得和另外兩張相疊」的斷言。

另外又撞了一次同名變數（`nodes_before`），`Battle.gd` 整支載入失敗、視窗掛住。
同一支自檢函式裡第二次（0.22.0 是 `plays_before`）。

回歸：14 支 **1177** 項全過；七條 clicktest 全 PASS。

---

## [0.23.0] — 2026-08-04 — B2.2 每日挑戰雙榜

M2 的排程項。**這一批不新增任何玩法**：每日挑戰＝無盡模式跑在一個固定的日種子上，
跑兩次、兩種起始配置。地圖、波次、模擬、計分全部沿用 §7.10 那一套，一行都沒改
（§3.10 明文：「不得為此拆成兩套邏輯」）。

### 兩榜的差異只有一個參數

```gdscript
s.setup(MapGen.generate(日種子), [], Daily.tech_for(board, 玩家科技))
#                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
#                       統一配置榜拿到空陣列 ＝ 完全沒有科技
```

`SessionState.setup()` 的第三個參數是**所有局外成長進入這一局的唯一入口**，
堵住它就堵住全部。這就是憲法 B3（統一配置榜是對 P2W 的唯一制衡）的全部實作。

### ★ 一個差點成立的假綠燈

DoD 寫的是「統一配置榜的起始 `SessionState` 在三組不同進度／課金的存檔下完全相同」，
最自然的寫法是比 `state_hash()`。**但 `state_hash()` 不涵蓋 `mods`**——它是科技的
查表結果、不是權威狀態，`30_TECH_DESIGN.md` §2.4 刻意把它排除在外。

而 `mods` 正是科技生效的地方，也正是 B3 要擋的東西。

紅燈證明（把統一榜改成吃玩家科技）的結果：

| 斷言 | 結果 |
|---|---|
| `state_hash()` 相同 | ✅ **全綠**（驗不到） |
| `mods` 是中性值 | ❌ 兩條同時倒 |

所以兩層都斷言。單靠雜湊驗 B3 會是一句假話。

### 其他

- **種子是 `YYYYMMDD` 本身，不做雜湊混合**。除錯與回報時看得懂，而 `Rng` 底下是
  PCG32、相鄰種子本來就長出完全不同的序列——`daily_test` 直接斷言「連續 14 天的圖
  互不重複」。⚠ 不得改用 `String.hash()`：那是引擎實作細節，換一版 Godot 就可能
  換一組數字，而「同一天全球同一張圖」是跨版本的承諾。
- **跨日處理在 `SaveService.apply_daily()`，不在畫面層**：畫面有兩個入口（兩張榜）
  而換日只該發生一次。**今天的兩榜一起歸零**——只歸零玩的那一榜的話，昨天的另一榜
  會混進今天，而它是在另一張圖上打出來的。
- **離線註記是規格的一部分**（§3.10）：畫面上寫「兩張榜都是你自己的本機紀錄，
  還沒有對手」。`TL_CLICKTEST/daily` 的 `honest` 斷言守著它。不要在文案中宣稱一個
  尚未存在的競技場。
- 存檔**沒有 bump `SAVE_VERSION`**——加鍵不是結構改動。斷言舊存檔讀進來長出
  `daily` 這一格且不動原有資料。

### 修掉一條位置相依的斷言

主選單插進「每日挑戰」之後索引整個往後挪一格，`TL_CLICKTEST/title` 於是去按了
科技樹以外的東西（`tech=false`）。**選單順序是會動的**，而那條測試要驗的是
「這顆鈕通到那個畫面」，不是「它排第三」。改成依按鈕文字找鈕。

### 已知未完

「固定角色組」目前是「全部可建造」——名冊系統還沒有（B2.4）。
**B2.4 落地時要回來把它釘死成一份明列的清單**，否則日後每加一隻角色
都會悄悄改變統一配置榜。

回歸：13 支 **1125** 項全過；**七**條 clicktest 全 PASS。

---

## [0.22.1] — 2026-08-04 — B2.1f.1 砍掉死碼（`ponytail-review`）

對 B2.1e／B2.1f 兩批的 diff 做一次過度設計稽核。**六處，淨 −25 行，模擬逐位元不變。**

| 位置 | 砍什麼 |
|---|---|
| `FlowNetwork.prepare()` | **入邊 CSR（`in_start`／`in_list`）整組刪除**。它是為了「吸收需求時扣掉所有指著我的邊」而建的，但那個作法在 B2.1e 開發途中就被祖先鏈走訪取代了——建了、存進拓樸、在 `solve()` 裡解包，**從頭到尾沒有人讀它**。22 行 |
| `FlowNetwork.solve()` | `is_silo` 陣列。儲槽在第一趟就能收進 `silo_ids`，第二趟改成只掃儲槽而不是全部節點 |
| `FlowNetwork.solve()` | `ready` 旗標。`down_a.is_empty()` 講的是同一件事 |
| `FlowNetwork.solve()` | 一句指向 `_absorb()` 的註解——那支函式在開發途中被內聯掉了，從來沒有存在過 |
| `AudioBus.music()` | `want` 的四項布林式與 `if track == "menu"` 分支。要播的那一首算一次就好；`_bgm_file()` 的 `%` 格式是留給已經不存在的 BGM 分層的 |
| `BattleController` | 破壞迴圈裡的 `[-1, 0, 1]` 陣列字面量——**每隻敵人每 tick 配四個新陣列**。提成 `BLAST_CELLS` 常數 |
| repo | `assetgen/__pycache__/*.pyc` 被 commit 進來了（紅燈證明時 `import gen_audio` 的副產物）。`git rm --cached` ＋ `.gitignore` 補 Python 段 |

**副作用：效能又快了一點。** 少建一份 CSR、少掃一趟節點：

| | B2.1f | B2.1f.1 |
|---|---|---|
| 無盡 18×11（219 節點） | 8.1 ms | **7.8 ms**（三次複量 7.7／7.7／7.8） |
| 無盡 31×19（610 節點） | 21.0 ms | **20.7 ms** |
| 規模哨兵 | 3.5 ms | **3.4 ms** |

★ **219 節點因此進到 8 ms 上限之內**——`30_TECH_DESIGN.md` §5.3 的「門檻約 215 節點」
與 R-17 風險條同步更新。

驗證：`TL_SIM=5600` 除了 `version` 欄位之外與 B2.1e 基準**逐字相同**；
12 支 1082 項全過；六條 clicktest 全 PASS。

---

## [0.22.0] — 2026-08-04 — B2.1f 戰鬥期沒有音樂

使用者實玩：「我不喜歡戰鬥音樂有一個敲擊聲，意義不明且會與其他東西誤會。
應該是要開始的時候有一個戰鬥開始的音效，後面就不要有音樂。」

### 那顆敲擊聲為什麼會被誤會

不是音色難聽的問題，是**通道**的問題。它是 `bgm_perc()` 的「敲玻璃」動機
（494Hz ＋ 兩個非整數比泛音，只在第 2 與第 5 小節各響兩下）：

| | 音色 | 頻率 |
|---|---|---|
| 音樂的敲擊 | 非諧和金屬 | 494 / 1363 / 2668 Hz |
| `sfx_build_place` 節點放置 | 非諧和金屬 | 880 / 1319 / 1976 Hz |
| `sfx_build_wire` 的「卡住」 | 非諧和金屬 | 1180 / 1770 Hz |

同一個音色家族、同一個音域，而且**它是整首音樂唯一的前景瞬態**（其他都是持續音）。
耳朵把瞬態一律歸類成「剛剛發生了一件事」，然後去找那件事——找不到，
因為它不對應任何事件。這款遊戲的音訊是**診斷通道**（核心受擊是全場最醒目的音、
缺電有警報、生產嗡鳴的音量跟著流量走），所以那顆敲擊等於在訓練假警報。

### 改成什麼

| 階段 | 聽得到什麼 |
|---|---|
| 準備期 | `tl_bgm_battle_base`（drone ＋ 稀疏機械節拍），照舊 |
| 開打的那一刻 | **號令響一次**，音樂同時開始淡出（1.2 秒） |
| 戰鬥期 | **沒有音樂**。生產嗡鳴（音量跟著流量）＋ 開火／受擊／警示 |
| 回到準備期 | 音樂淡回來 |

反差本身就是訊號（音樂停＝開打了），而戰鬥期要讀的資訊全在 SFX 上。

**號令（`tl_sfx_wave_start`）的三個設計條件**，每一條都是為了不再被誤會：

- **起音慢**：實測 411 ms 才到滿音量（打擊類全在 10 ms 內）。這是「不是一記敲擊」
  在資料上唯一量得到的樣子。
- **音域壓在 900 Hz 以下**（低通 2000）：讓開建造類音效的 880–2700 Hz。
- **音高往上走**：A2 → E3 → A3 的堆疊五度，每 0.35 秒進一個。全庫沒有第二個
  會往上走的音，也沒有第二個超過一秒還在漲的音。`warn_power` 是同音域，
  但它是 30 Hz 抖振的兩短聲——一個顫、一個開。

`bgm_perc` 與 `tl_bgm_battle_perc.wav` **刪除**。`audio_test` 斷言那個檔案不存在：
留著一個沒人播的 wav 會一路被打包、被改音色、再被誤以為還在用
（潮鳴的開火音就這樣活過一整批）。

### 回歸斷言

- `audio_test`：號令到滿音量 ≥100 ms、打擊類 ≤12 ms、號令比核心受擊慢三倍以上。
  **紅得起來**：把被刪掉的敲擊聲拿來當號令 → 5 ms，兩條同時倒。
- `TL_CLICKTEST/battle`：`wave_hush`（進波次後音樂真的降到全靜）、
  `wave_sting`（開打號令真的響了一次）。**紅得起來**：拔掉兩行呼叫 → 兩條同時倒。

### 兩個踩到的坑

- **新增 wav 的 `.import` 預設是 QOA 失真壓縮**（Godot 4.4 起 `compress/mode=2`）
  → 格式變 `FORMAT_QOA`、峰值被推到削頂 32768。`audio_test` 的 16-bit 與削頂
  兩條當場抓到。改成 `compress/mode=0`。
- **跟實時掛鉤的東西不能用「等 N 幀」去等。** 淡出斷言原本等 120 幀，
  而那個視窗沒有垂直同步——120 幀可能只有 0.2 秒，淡出要 1.2 秒。改成等牆鐘時間。
  同一段還踩到「這個局面的 `spawn_queue` 是空的 → 下一個 tick 就被打回準備期」，
  phase 要每幀釘住才等得到淡出跑完。

---

## [0.21.0] — 2026-08-03 — B2.1e R-17 解算器

無盡大圖上流量解算不隨規模擴張的問題（R-17，B1.7 起登記）。

### ⓪ 先修量測台——原本的數字低估 50 倍

`perf_test` 的無盡規模曲線佈局只放中繼／發電機／錨，**一顆採集器都沒有**。
中繼與錨不產礦，發電機的能量產出又要乘上自己的礦砂滿足率，於是全網供給恆為 0，
`FlowNetwork` 的傳播迴圈**一次都沒有跑過**（實測 `pops=0`）。

那條曲線量到的是建索引的成本，卻被當成解算成本寫進 `30_TECH_DESIGN.md` §5.2。

| 219 節點／373 導管 | 記在文件上 | 真值 |
|---|---|---|
| 單 tick | 27 ms | **1,449 ms** |

量測台改成在礦點上蓋採集器，並補三條斷言「這張網真的有供給」。
「效能沒問題」和「什麼都沒跑」在輸出上長得一模一樣，所以量測台自己也要有驗收條件。

### ① 可送達需求改用生成森林上的重根法

「順著這條線下去還有多少需求」原本是**對每一次推送**重跑一趟 DFS
（`O(推送次數 × (V+E))`）。改成在生成森林上兩趟掃完：後序求子樹（`down`）、
前序把根重掛到每個節點（`up`），`O(V+E)`。

森林是**純拓樸**的（不看殘餘容量），所以三張資源網、三次迭代共用同一棵。

兩者算的都是同一個近似量——**有環圖上不存在精確解**，這正是 §2.5「已知近似」
一直在講的事。

**兩個必要的補丁，各自都有回歸斷言：**

- **非樹邊給「一跳」的量**（只看對面那一格自己的需求）。給 0 的話，玩家
  「兩條線一起餵同一個大消費者」的佈局會有一條完全不載流——等於偷偷禁掉迴路。
  `flow_test` 那一條當場紅（稜鏡滿足率 0.5）。
- **節點吸收掉需求時，沿祖先鏈 `O(深度)` 把森林上看得到它的量一起扣掉。**
  舊法每次推送重算，所以 `demand_left` 永遠是活的；新法一個迭代只算一次，
  中繼拿的是上一刻的快照：**發電機已經吃飽了，上游還以為它餓著**，
  於是把三分之二的餘量原路推回去。淺灘示範佈局當場掉 55% 產出
  （`delivered_total` 4412.8 → 2008.4），而 12 支測試全綠。

補的回歸斷言 `flow_test._satisfied_nodes_stop_attracting()` **第一版是假綠燈**：
採集器擺在 id 1（＝生成森林的根），餘量往樹下走，根本不經過那個吃飽的節點，
拿掉修法照樣全綠。改成「採集器在離核心最遠端、中間隔兩個中繼、吃飽的那個
優先權高於核心」才紅得起來（核心滿足率 1.0 → 0.43、2.28 單位彈回去）。

### ② 三筆逐位元相同的改寫

| 改動 | 為什麼 |
|---|---|
| `BattleController._advance_and_damage` 改格索引 | `in_blast` 是 Chebyshev ≤ 1 ＝ 剛好九格，「誰挨打」可以查表。舊寫法 `O(敵人 × (節點 + 導管 × 每條的格數))`，而且每一對（敵人, 導管）都重算一次 `immune_indices()`。219 節點時 17.2 ms，佔整個 tick 六成 |
| `BattleController._edges` 不再呼叫 `SessionState.node_at()` | 那是線性掃描，每條導管查兩次 ＝ `O(導管 × 節點)`。610 節點／1107 導管時光這一支 25.8 ms，**比整個解算器還貴** |
| 拓樸一個 tick 只建一次（`FlowNetwork.prepare()`） | 三張資源網的圖逐位元相同，只有供需不同。舊寫法把 CSR、孿生邊、生成森林都算了三遍 |

三筆的 `TL_SIM=5600` 輸出與改前**逐欄完全相同**。

### 效能

| 情境 | 節點／導管 | 前 | 後 | 倍率 |
|---|---|---|---|---|
| 戰役第 5 關（M1 最重） | 25／25 | 6.13 ms | **1.03 ms** | 6.0× |
| 規模哨兵 | 65／210 | 508 ms | **3.5 ms** | 145× |
| 無盡 18×11（live） | 219／373 | 1,449 ms | **8.1 ms** | 179× |
| 無盡 31×19（live） | 610／1107 | — | **21.0 ms** | — |
| **RG-8 壓力情境** | 547／2045／200 敵人 | 30,200 ms | **31.0 ms** | 974× |

**戰役五關全部進入 3 ms 目標內**（B1.7 時第 4、5 關是黃燈）。

### 校準：不需要重調

生產計畫從 B1.7 起就寫著「改這個會動到分配比例 → 五關校準全部重做」。
比例確實動了（`delivered_total` 4412.8 → 4448.8、淺灘通關晚 45 個 tick），
但**五關的參考解全部照舊通關**，`campaign_test` 416 項全過，星等門檻一項未動。

### 修掉一條快照式斷言

`hud_test` 的「儲槽已充飽」寫在**通關那一格**上——那一刻塔正在放電打最後幾隻。
通關晚了 45 個 tick，同一個局面就變紅了（儲槽第 299 tick 就充飽過，
末值 283.2 是放電中的快照）。改在充飽的那一格驗，中繼徽章兩個局面都驗。

### 已知未完

- 8 ms 上限在**約 215 節點**被追上；610 節點 21 ms（佔 `TICK = 100 ms` 的兩成，
  功能上不是阻斷）。剩下的成本三分之二在解算器本身。
- **RG-8 的另一半——渲染 23 FPS——這一批完全沒碰**，RG-8 仍不算通過。

---

## [0.20.1] — 2026-08-03 — B2.1d.1 礦點安全距離、小地圖不常駐

### ① 礦點的安全距離量錯了種類

使用者實玩：「這種生在道路的旁邊會直接不能用」——採集器蓋下去當場被啃。

根因是**兩個距離用了不同的度量**：

| | 度量 | 值 |
|---|---|---|
| `Tide.in_blast()`（誰會挨打） | **Chebyshev**（八方相鄰） | ≤ 1 |
| `MapGen.OFF_MIN`（礦點離路徑多遠） | **四方 BFS** | ≥ 2 |

**斜對角貼著路徑轉角**的格子：四方距離 2（通過檢查）、Chebyshev 距離 1
（每 tick 挨打）。實測 300 張圖有 **167 顆**這種礦——約每兩張圖一顆。

距離場改成八方 BFS，`endless_test` 也改用 Chebyshev **獨立**量一次
（刻意用「對全路徑取 min」而不是抄生成器的 BFS）。紅得起來：改回四方 → 167 顆。

### ② 小地圖不常駐

使用者：「小地圖不能常駐存在，不然有些地方會點不到。」

**縮放或平移時出現，視野靜止 3 秒後淡出**（最後 0.5 秒淡）。
**隱藏時不攔截點擊**——這才是重點，不是視覺乾淨。

三條斷言：`mini_on`（動過之後在）／`mini_gone`（隱藏後點得到底下那一格，
用哨兵字串驗）／`mini_back`（再動一下它回來）。紅得起來：拿掉隱藏時的
攔截判斷 → `mini_gone=false`。

⚠ `TL_SHOT` 下不倒數，否則同一組參數在不同機器上會拍出兩種圖。

---

## [0.20.0] — 2026-08-03 — B2.1d 小地圖手感、平均礦點、屏障擋格、戰鬥曲

實玩回饋一次五條，**五條都做了**。另外在做第四條時抓到一個**已經上線一批**的
渲染缺陷（見下方 ★★）。

### ① 小地圖推到底真的貼邊

視野框推到底時離可見的外框還有 5px。用數字驗過**夾限本身是對的**
（`flush=true`：視野的世界矩形對齊地圖四角），成因是外框畫在 `r.grow(5.0)`
上、比地圖範圍大一圈。改成**底板往外長、外框畫在 `r`**——框就是地圖的邊界。

### ② 小地圖按住拖曳，視野平滑跟隨

`_pan_goal` 成為平移的**目標**，`_pan` 每幀以 `1-exp(-k·dt)` 收斂（與幀率無關）。
按住小地圖不放時視野持續跟著游標；放開就停。

**中鍵直接拖地圖不走這條**——直接拖曳加上緩動會變成「黏手」，手指到哪畫面就到哪。
`reduce_motion` 開啟時一律直接到位。

### ③ 礦點平均鋪滿整張圖

**這一條改了三次才對。** 三次都是實玩回饋，三次的失敗都很有代表性：

| 版本 | 作法 | 回報 | 為什麼失敗 |
|---|---|---|---|
| B2.1b | 全圖均勻**隨機** | 「顯然是隨機分布」 | 均勻隨機**必然結塊** |
| B2.1c ① | **隨機**錨點長礦脈 | 「一坨擠在一起」 | 隨機從點搬到錨點，結塊照舊 |
| B2.1c ② | 沿**路徑索引**分層 | 「只生在道路旁邊」 | 沿路線均勻了，但 `OFF_MAX=7` 把礦鎖在路邊 |
| **B2.1d** | 沿**地圖面積**分層 | — | 一區塊一顆，「均勻」是構造出來的 |

- **礦點數 ＝ 區塊數**（網格取自一張表：5×4／7×3／6×4／8×3／5×5）。由 `n` 反推
  區塊數會多出一兩塊，多的被跳過 → 那一區就是空的（實測 181 個大區空著）。
- **`OFF_MAX` 移除**，`OFF_MIN=2` 留著（walk-by 打不到）。
- **礦脈（共線成群）移除**——那是我從手作圖歸納的規則，不是使用者要的。
- 「平均」的規格寫成常數：整張圖切 `4×3` 大區，**每一區都要有礦**，生成器
  放置後補洞保證，`endless_test` 獨立再驗。
- **死區面積 45% 一併解掉**：離路徑遠的地方不再是空的，而是「安全但要拉長線」的取捨。

### ④ 屏障擋格

能量傷害被屏障吃掉時，敵人身上閃一個**六邊形護盾泡**＋白閃，擋越多泡越實。
純渲染（`shields` 不進 `state_hash()`）。屏障在此之前**完全沒有視覺通道**——
玩家只看得出熾泳「快」，看不出自己的能量傷害被砍掉四成。

第一版畫成朝 +X 的一段弧，在 32px 的格子上和敵人輪廓分不開，而且「朝右」對
一隻往下走的敵人沒有意義。改成封閉六邊形之後與方向無關，
而六邊形正是本作既有的「硬」語彙（甲殼就是六邊形）。

### ★★ 抓到一個已上線一批的缺陷：四座塔的開火**從來沒有畫出來過**

`s.shots.append(rec)` 在 B1.6.3 被誤縮排進 `if splash_at.x >= 0:` 裡面，
於是**只有濺射（碎浪）留得下開火記錄**——稜鏡光束、實體彈、回收珠三種形態
整整一批都是啞的。而**所有測試都是綠的**：`shots` 是純渲染、不進
`state_hash()`，在此之前沒有任何斷言看過它。

B1.6.3 正是我請使用者評估「開火特效」的那一批。回歸斷言補在 `hud_test`：
非濺射塔開火後 `shots` 不得為空（拿掉修正 → 兩條當場紅）。

### ⑤ 戰鬥曲重寫

第一版三個毛病：每一拍都一顆 kick（24 拍 24 顆，一波三分鐘敲兩百多次）、
1320/1328Hz 的 8Hz 拍音（那個頻段長時間聽會刺耳）、和聲完全不動。

新版：節奏**切分**（1、2.5、4）、低音**有進行**（Am–Am–F–F–G–G）、
威脅層改用**低頻小二度叢集**＋低通噪音呼吸、「敲玻璃」的金屬動機只在
第 2 與第 5 小節出現（稀少才有份量）。

⚠ 過程中 `audio_test` 抓到 loop 接點爆音（跳幅 1602／鄰近平均 271）——
低音的包絡最低點寫成 0.35 而不是 0，每小節是「跳」進來的。

---

## [0.19.1] — 2026-08-03 — B2.1c 礦脈分層（修正）

**實玩回饋第 8 次**，原話：「會有一坨擠在一起，然後只在某些地方有，其他地方都是空的」。
0.19.0 把「均勻撒點」換成「**隨機**挑錨點再長脈」——**隨機挑錨點一樣會結塊**。

| | 0.19.0（隨機錨點） | 0.19.1（分層） |
|---|---|---|
| 沿路徑最大空檔 | 16.8 格（最差 **43**） | **5.8**（最差 19） |
| 半徑 4 內最多幾顆 | 2.4（最差 5） | 3.2（最差 6） |
| 路徑切 6 段，完全沒礦的段 | 3% | **0%** |

### 均勻要靠分層，不能靠隨機

`_crossings()` 早就是這樣寫的（切成 n+1 段、各段中央附近挑一格），橋看起來
散得開就是因為這個。礦脈沿用同一招，**段的單位是路徑索引**，所以礦脈天生沿著
路線鋪開。三件事一起做才夠：

1. **沿路徑索引分層**（取代隨機錨點）
2. **段內掃描不跳過**——抽到的那格兩側塞不下時（轉角、貼邊、已被上一條脈佔走），
   舊版直接放棄整段 → 路線上開一個十幾格的洞
3. **空間隔離**——沿路徑索引分層**不等於**空間上散得開：轉角處相隔十幾個索引的
   兩段在地圖上可能只差 5 格，兩條脈就疊成一坨

另加**兩岸輪流**（連續幾條脈長在同一岸，另一岸整片空著）、脈長 2–4 → **2–3**。

注意「最擠」從 2.4 升到 3.2 是**預期的**：一條脈本來就是二三顆靠在一起。

### 生成不變量 9 → 11 條

新增「沿路徑最大空檔 ≤24 格」與「半徑 4 內 ≤8 顆」。兩條都證明會紅：
拿掉分層與空間隔離 → 空檔 52、最擠 13。

### ⚠ 量到一個未解問題：死區面積 45%

路徑是由角到角的**單調階梯**，對角兩個角落永遠離路徑很遠——實測離路徑 >10 格的
格子佔 **45%**。**這不是這批造成的**（B2.1b 的折返路徑是 42%），根因是路徑長度與
地圖面積的比例。**「死列 0%」的指標看不到這件事**——對角階梯碰得到每一列，
卻碰不到角落。三條路（縮圖／加長路徑／定位成安全後場）寫進 `10_GDD.md` §7.10，
**待使用者決策**。

### GDScript 地雷

`var a: Array[int] = [1,-1] if c else [-1,1]` 的三元式產出 untyped `Array`，
**過得了編譯、執行期才炸**——炸在生成器裡的樣子是「礦點靜靜地變成 0 個」。
已寫進 `CLAUDE.md` 的地雷清單。

---

## [0.19.0] — 2026-08-03 — B2.1c 掃掠與礦脈

**兩個缺陷都來自實玩回饋**，使用者的原話：「東西都集中在路線上半區，下半區很空」
「採集器的分布顯然是隨機分布，缺乏合理規劃」。300 個種子的實測證實兩句都成立，
而且比看起來嚴重——最差的種子路徑只跨 8 列（全高 40），50% 的礦點離路徑 8 格以上。

| | B2.1b | B2.1c |
|---|---|---|
| 路徑縱向跨幅（全高 40） | 19.3 列（最差 **8**） | **33.1** 列（最差 28） |
| 死列（離路徑 >6 列） | **26%** | **0%** |
| 礦點離路徑 >8 格 | **50%** | **0%** |
| 孤點（最近的另一顆礦 >4 格） | **72%** | 12% |
| 路徑長度（＝一波的秒數） | 95.9（73–119） | **95.1（90–98）** |

### ① 單調掃掠：跨越全高的代價只付一次

舊版每段橫走獨立抽一列，走出來的是一條**在窄帶裡折返**的路徑：花掉 34 格的
直走，只換到 19.3 列的跨幅——折返的部分是白花的。改成**單調掃掠**（由上而下
或由下而上，不折返）之後，直走總長恆等於 `y[末] − y[首]`，**跨幅拿滿而路徑
長度不變**。列的來源改為分層抽樣（等寬帶一帶一列，頭尾兩帶只取外側半邊）。

**為什麼不能用「把路徑拉長」來填滿地圖**：`Tide.advance()` 是
`progress += speed * TICK`，**路徑格數就是一波的秒數**。拉長路徑等於直接把
每一波變慢——那是拿一個缺陷換另一個。

**附帶收穫**：路徑長度的離散度從 73–119 收成 90–98，**一波的秒數不再憑種子浮動**。

### ② 礦點成脈，不是撒點

「規劃」長什麼樣，看手作圖的註解就知道：「北岸三顆，共用一座橋」——**共線、
共用一條幹線、合起來是一個決定**。舊版是二十幾顆各自獨立、且有一半在死區裡
的瑣碎決定。脈把這件事寫成規則：`2–4` 顆沿單一軸、間距 2，離路徑 `2–7` 格
（下限是 walk-by 打不到，上限是「值不值得拉一條導管穿過空地」），
兩條脈的錨點至少隔 5 格。

### 生成不變量 5 → 9 條

新增 4 條，**全部來自實玩回饋而不是設計時想得到的**：跨幅 ≥18 列、死列 ≤6 列、
礦點離路徑 2–7 格、孤點 ≤20%。格式正確、可通關、且完全不好玩的圖，
生成器一天擲得出幾千張。

### 沒有動任何難度參數

路徑長度、礦點總數、起始礦砂、跨越點數、波次公式全部不變——動的只有**同樣的
東西擺在哪裡**。`campaign_test` 416 項逐項未變（戰役完全不碰生成器）。

---

## [0.18.0] — 2026-08-02 — B2.1b 大圖與導引

### 地圖放大過一屏：34×18 → **64×40**

框架 1152×604 在**可讀性地板**（24px／格）下看得到 48×25 格
→ 64×40 看得到 **75%×62% ＝ 面積的 46%**。

**縮放下限從「就是 fit」改成 `max(fit, 地板)`**：一屏可見的圖（全部戰役關卡）
fit 本來就在地板之上 → 行為完全不變；大過一屏的圖則**看全圖的地方是小地圖，
不是主畫面**。縮到看得見全圖但讀不出瓶頸，等於把 R-3 的驗收轉包給玩家（RG-59）。

### ★ 第一版做的是 56×32，實看之後改掉

那個尺寸看得到 86%×79%，**小地圖上的視野框幾乎和外框重疊**——「我在哪」讀不
出來，兩個導引通道的價值一起被稀釋。**一個只是技術上大過一屏的地圖，不值得
為它做導引。**

**高的比寬的多加**：路徑長度 ≈ 寬度 ＋ 各段直走（跨距有上限），所以**加高幾乎
不影響一波的時間，加寬是一比一地加**。路徑實測 min 73／p50 95／max 119 格。

### ★ 跟著面積重算四個參數

| 參數 | 34×18 | 64×40 |
|---|---|---|
| 礦點 | 6–8 | **20–26** |
| 跨越點 | 2–3 | **4–6** |
| 起始礦砂 | 900 | **1900** |
| 不必過橋的礦點下限 | 3 | **7** |

**放大地圖而不動它們，等於悄悄把難度調高**（密度掉 4 倍、每條產線的導管長度
變 1.9 倍而起始礦砂沒動）。§7.7 說難度只能用玩家看得見的參數表達——**那也包括
「不要用改尺寸偷改」**。

### 兩個導引通道，缺一不可

| 通道 | 答的問題 | 為什麼不能只留一個 |
|---|---|---|
| **小地圖** | 全局長什麼樣？我在哪？ | 箭頭只說「那個方向有事」，說不出離多遠、有幾個、產線整體結構 |
| **邊緣箭頭**（上限 3） | 現在有東西壞了，在畫面外哪一邊？ | 小地圖是常駐靜態資訊，玩家在忙時不會盯著它 |

兩者都可點跳轉——這也是大圖平移唯一的**純左鍵**動線（中鍵在觸控上不存在，R-16）。

**「問題」不新發明判定**：就是既有的 `node_state`（缺料／滿溢）與 `_threat`
（正在被啃），和三態徽章／四角括號同一份資料。

### ★★ R-17 量到了 —— B2.1c 由條件批轉為**必做批**

B1.7 把判定推遲到「大圖才是真的會撞到它的地方」。大圖做出來了，**它撞到了**。
佈局刻意是玩家真的會蓋出來的形狀（分支度約 4），不是 RG-8 壓力情境的 7.5：

| 節點／導管 | ms/tick | 判定 |
|---|---|---|
| 54／80 | **5.8** | ✅ 在 8ms 上限內 |
| 180／298 | **22.5** | ❌ 超上限 2.8 倍 |
| 565／1021 | **92.5** | ❌ 吃掉 92% 的 tick |

**門檻約在 100 個節點。** 34×18 上很難蓋到（第 5 關參考解是 25 個），
**64×40 上輕易做得到**——這正是放大地圖買到的東西，也正是它的代價。

### 新增 `TL_CLICKTEST/endless`（11 條斷言）

驗 DoD 的前提：**問題在畫面外時，畫面上真的有一個指得到又點得到的東西。**

三個過程中修掉的自己的問題：

1. **第一版蓋不到那個節點**——目標在畫面外，合成點擊落在框架外會被輸入層擋掉。
   那個行為是**對的**（玩家也點不到看不見的格子），所以測試改成「先移過去蓋、
   再移回來」。
2. **`mini_safe` 是假斷言**——第一版看「節點數沒變」，而那一格剛好不能蓋，
   攔不攔截都是綠的。改成哨兵字串（`_act()` 一定會覆寫 `_message`）才紅得起來。
3. **視野框大小肉眼判不準**——小地圖只有 224px 寬，視野框和外框差幾個像素。
   改成數字斷言 `view=75%×63%`。

**證明會紅**：拿掉 `_guide_click()` 攔截 → `jump`／`mini_pan`／`mini_safe` 三條同時倒。

### 驗證

- 12 支 1056 項全過；**六**條 clicktest 全 PASS
- `TL_SIM=5600 TL_SEED=42` 逐欄與 B1.8／B1.9／B2.1a／B1.6.3 相同 ＝ 對既有局面零影響
- `campaign_test` 416 項未動（戰役全部一屏可見，縮放下限的改動對它們是空操作）

### 已知限制

- **導引只在無盡出現**（戰役一屏可見，多一個浮層只是多一塊遮擋）
- 小地圖**不畫敵人**：玩家不能改變敵人路線，畫進去只會讓它每一幀都在閃
- **一波的時間變長約 1.9 倍**（路徑 52 → 95 格），這是換建造空間的代價
- **R-17 未修**，B2.1c 是下一批

---

## [0.17.1] — 2026-08-02 — B1.6.3 敵人與開火的差異化

> 批次編號跟的是**美術打磨那條線**（B1.6／B1.6.1／B1.6.2），版本號跟的是**出貨順序**，
> 所以是 `0.17.1` 不是 `0.10.3`。使用者回報起的批。

**使用者回報兩件**：「所有敵人都長一個樣，根本看不出差異」「塔的攻擊可以有更多
不同的攻擊特效嗎」。**兩件查證都屬實**：

- 三種敵人畫的是**同一個 9 邊形、同一個 `tide.magenta`**，只有半徑不同（9／12／8）
- 五座塔的開火畫的是**同一條 `order.bright` 2px 直線**；碎浪的濺射半徑玩家完全看不到

### 統御規則：視覺由**既有數值**推導，不新增美術資料

每個視覺特徵都掛在 `Enemies.DEFS`／`NodeDefs.DEFS` 已經存在的機制欄位上
（`armor`／`speed`／`dmg_type`／`pierce`／`splash`／`reclaim`），不新增 `shape`／`fx`
之類的欄位。**所以做不出一隻「有護甲但看起來不硬」的敵人**，M3 擴到 18 種敵人、
24 隻角色時也自動正確。

### 敵人：三種明度結構

| 條件 | 畫法 | 命中 |
|---|---|---|
| 預設 | 9 邊柔性水滴、wobble ±15% | 漂蟲 |
| `armor > 0` | 6 邊硬稜角、wobble ±7% ＋ `tide.deep` 同形鎖邊 | 甲殼 |
| `speed > 1.2` | 垂直行進方向壓扁 ×0.55 ＋ `tide.bright` 亮核心 | 熾泳 |

新增 palette token `tide.bright` `#ff9aab`（與 `order.bright` 對稱）。

### ★ 這張表被實看 A/B 否決兩次才定案

留在文件裡（§1.7），因為它是「特效不能只在腦裡設計」的直接證據：

1. **拖尾被否決**——第 5 波間距是 **0.6 格**，殘影落在後面那一隻身上，
   一列 11 隻分得開的敵人糊成一條連續香腸。**把「看不出差異」修成了「看不出有幾隻」。**
2. **沿行進方向拉長也被否決**——砍掉拖尾改拉長本體，A/B 顯示問題一模一樣。
3. **輪廓在 fit 倍率不夠用**——敵人只有 16–24px，六邊形勉強讀得到、壓扁幾乎讀不到。
   **明度差活得下來**，這才是 `tide.bright` 存在的理由。

→ 寫成規則：**敵人的差異化只能用「不增加行進軸 footprint」的手段。**

### 塔的開火：四種形態

| 條件 | 畫法 | 命中 |
|---|---|---|
| 預設 `physical` | 短促**實心彈丸**沿線飛行（不是整條線亮起） | 錨 |
| `pierce` | **整條貫穿光束**，寬度 3→1 衰減 | 稜鏡 |
| `splash > 0` | 彈丸 ＋ 命中點**擴張環**，半徑＝資料裡的 `splash` 格 | 碎浪 |
| `reclaim > 0` | 細彈丸 ＋ `energy.amber` 珠**回流到塔** | 回收者 |

**物理 vs 能量用形狀分，不用顏色分**：琥珀專屬於能量這個**資源**（§1.1），
拿它畫「能量傷害」會污染全案最重要的資訊通道。回收者那顆琥珀珠是唯一例外，
而它是對的——那顆珠真的是能量，正在流回電網。

**濺射環只掛在一發上**：碎浪是「一發打中 N 隻」，模擬層為記傷害會產生 N 筆命中
記錄，照著畫就是 N 個同心圓——既吵又把機制講錯（實看抓到）。

### ★ 我自己造成的一次全關卡失敗

改 `BattleController` 時用 python 做行替換，**整塊多縮排一層**，於是開火邏輯掛到
`if count <= 0: continue` 底下變成死碼。GDScript 不報錯（那是合法的不可達區塊），
但**五關全部通不了、擊殺 0**。`campaign_test` 當場抓到（9/416 失敗）。
教訓：這種結構性編輯要用精確字串比對的編輯工具，不要用行號手術。

### 驗證

- 12 支 1053 項全過；五條 clicktest 全 PASS
- `TL_SIM=5600 TL_SEED=42` 逐欄與 B1.8／B1.9／B2.1a 相同
  （`won`／1000／41／4412.8／1017.0／186.0／0.8）＝ 純渲染改動
- 實看：fit 倍率 A/B（改前／改後）＋ 300% 近照三種同框 ＋ `TL_FOCUS` 開火近照
- **驗收陷阱記進文件**：開火特效只活 3 tick，`TL_SHOT` 凍結在剛發射那一格時
  濺射環半徑是 0——看起來就像沒做（RG-100 的同一類）

### 已知限制

- `barrier`（能量抗性）**刻意不給視覺通道**——三個結構已經解掉「看不出差異」，
  第四個訊息疊在 16px 上會變成資訊過載。精確數字在圖鑑
- 潮鳴仍只有 B1.8 的場環（它不開火）
- 第 5 波最密時仍然會互相碰到——那是 0.6 格間距的固有結果，改前改後都一樣

---

## [0.17.0] — 2026-08-02 — B2.1a 無盡模式核心

**M1 收尾**：R-14（「陌生人可無指導玩 10 分鐘」沒有執行者）由使用者拍板
——**不找真人，他自己再玩一輪代理驗收**。風險條 **降級留存、不標 🟢**：
他是設計者，那一輪能證明「沒有阻斷級問題」，證不了「陌生人看不看得懂」。
M1 閘門依此結案，進 M2。

**B2.1 拆成 a／b／c**（原本一批塞 4 個功能＋解算器重寫，違反「一批＝一個
可出貨單位」）：a 程序生成一屏圖＋無限波次＋個人最佳、b 大圖與小地圖導引、
**c 是條件批**——R-17 只在 b 實測撞到才做（`FlowNetwork.gd:302` 那條
`ponytail:` 註解自己就寫「量到瓶頸再換」，先改要連五關校準一起重做）。

### 生成器輸出的是**和手作地圖一模一樣的一份資料**

`sim/MapGen.gd` 回傳的 dict 與 `data/Maps.gd` 同構，所以渲染、建造、命中判定、
`Maps.path_of()`／`to_sets()` **零改動**。無盡的建造欄也不必另列一份會過期的
清單——`SessionState.setup()` 早就寫著「空 `unlocked` ＝ 全部」。

### 五條生成不變量（`endless_test.gd` 掃 300 個種子）

前四條是格式，第五條才是「這張圖玩得動」：

1. 同種子逐格相同
2. 路徑不自交 —— **結構保證**：轉折的 x 嚴格遞增，兩段橫走碰不到彼此
3. 跨越點落在路徑的**直線段**上
4. 礦點不在路徑格、不壓核心
5. ★ **至少 3 個礦點不必過橋**——構造出來的（先把核心那一側的配額塞滿，
   剩下的才自由撒），不是撒完再檢查

第 5 條是這一批的重點：手作圖有設計者的眼睛把關「礦砂別全在對岸」，
生成器每天會擲出幾千張沒有人看過的圖，這條斷言是唯一能替代那雙眼睛的東西。

### ★ 兩個 ASCII 讀圖才看得出來的缺陷

截圖看不出來（浮層遮著、格子太小），把三張圖印成 ASCII 才現形：

- **轉角上的橋是廢的**——導管從那裡「過去」會變成沿著路徑走，不是跨過去。
  一張圖只有 2–3 座橋，浪費一座就是三分之一的空間預算。
  **拿掉修正重跑：300 張圖裡有 108 座廢橋**（36% 的圖至少浪費一座）。
- **路徑會貼到 y=1**——遊戲自己的說明面板寫著「塔與導管退開路徑 2 格就安全」，
  貼邊的橫走讓那一側做不到。改成 `EDGE = 2`。

兩條都改成**構造性**的（從合格的候選裡直接抽），不是擲了再檢查重擲——
重擲迴圈的執行次數會跟著種子跑，那種東西在確定性測試裡很難看。

### ★ 截圖抓到的假綠燈

第一版 clicktest 印 `endless=true` 而**遊戲打開的是淺灘**。

根因：`Main._enter()` 先 `add_child()` 才 `setup.call()`，而 `_ready()` 在
`add_child` 當下就跑完 `_setup_session()` 了。`Battle.gd` 的 `level` 註解從
B1.2 就寫著「由呼叫端在 `add_child()` **之前**指派」，這條契約一直是反的——
沒爆是因為戰役走 `Campaign.gd` 自己指派、而 `_campaign_level` 用 `call_deferred`
（延到 idle，那時已經在樹上了）。**無盡是第一個真的踩到的。**

斷言也一起改：**不是看 `endless_seed` 有沒有值**（那個欄位在畫面已經用錯地圖
之後才被設上一樣是 true），而是看局面裡的地圖真的帶著 `endless` 旗標。

### 無限波次與個人最佳

- 沿用 §7.6 的兩條公式（`1.11^(w-1)` 血量、`4 + ⌊w/3⌋` 隻數），**出場間隔是
  固定 1.0 秒**：壓力已經由血量與隻數表達完了，再給間隔第三條曲線，玩家就
  分不出「這波難是因為血厚還是因為密」。
- 敵種池與戰役同一條登場順序：`w≤2` 漂蟲｜`w 3–5` ＋甲殼｜`w≥6` ＋熾泳。
- 個人最佳**兩欄各自取最大值**（波次、產能積分）。綁成一組的話，一局
  「波次 +1、積分砍半」會洗掉玩家真正的最佳產線紀錄。
- **沒有 bump `SAVE_VERSION`**：B1.9 刪掉空殼鍵時就寫下了理由——`_fill_defaults()`
  在讀檔時補缺鍵，加一個新鍵不是結構改動。斷言直接證明舊存檔讀進來長出 0/0
  且不動到原有資料。

### 驗證

- 12 支測試 1053 項全過（新增 `endless_test` 64 項）
- 五條 clicktest 全 PASS，含新的 `endless=true(gen=true)`
- **證明會紅**：拿掉轉角排除 → `endless_test` 1/64 失敗（得到 108，預期 0）
- `TL_SIM=5600 TL_SEED=42` 逐欄與 B1.8／B1.9 相同（`won`／1000／41／4412.8／
  1017.0／186.0／0.8）；`campaign_test` 416 項逐位元未動 ＝ 對既有局面零影響

### 已知限制

- **只有 1 種佈局**（M2 目標 3 種、M3 目標 5 種）
- **地圖仍是一屏可見**，小地圖與邊緣指引箭頭在 B2.1b。R-17 尚未量到，
  因為還沒有大過一屏的圖
- 無盡沒有難度層、沒有排行榜（B2.6／B2.2）

---

## [0.16.0] — 2026-08-02 — B1.9 消重複

`ponytail-audit` 掃出的十七條，第一條在 B1.8 順手做掉，這一批做掉其餘十五條
（**跳過 Battle.gd 搬家**：淨行數 0、churn 最大、clicktest 要吃節點私有成員）。

### ★ 先講一個我報錯的數字

我上一輪報「淨 −155 行」。**實際是 −15 行**（去註解去空行，4461 → 4444），
錯了一個數量級。原因：稽核把被刪的行連同註解算成節省，但沒算 replacement
也要寫註解，也沒算重構會逼出新的測試涵蓋。

**行數從來不是重點，「改一個值要改幾個地方」才是**：

| 項目 | 之前 | 之後 |
|---|---|---|
| `_enter_*` 進畫面 | 4 支 | 1 支 `_enter(script, setup)` |
| `_clear()` | 3 份 ＋ 5 處內嵌 | 1 支 `UiKit.clear()` |
| ESC 返回處理器 | 3 份 | 1 支 `UiKit.esc_returns()` |
| ESC 自檢區塊 | 2 份 ×14 行 | 1 支 `UiKit.esc_reaches()` |
| `const TICK := 0.1` | 4 份 | 1 份 `scripts/sim/Clock.gd` |
| 存檔空殼鍵 | 8 個 | 0 |
| 死函式／死欄位 | 8 個 | 0 |

### 消掉的重複

- **`Main._enter()`**：四支 `_enter_battle` / `_enter_campaign` / `_enter_tech` /
  `_enter_settings` 收成一支。順手修掉一個不一致——舊的四支只 `queue_free()`
  不 `remove_child()`，而 `_back_to_title()` 兩個都做。
- **`KNOWN_PANELS` 刪除**：它是畫面名單的第二份拷貝，唯一工作是印警告。
  現在名單只有 `PANEL_SCREENS` 一份——**能不能路由，就是認不認得**。
- **`sim/Clock.gd`**：`TICK` 的唯一來源。四個檔案原本各寫一份，每份還各配一段
  解釋自己為什麼要再宣告一次的註解。抄四份的代價不是行數，是「有一天有人只改了
  其中三份」——而這個值改了，每一份存檔、重播、榜單都會對不上。

### 刪掉沒有呼叫端的東西

`Motion.linear()`、`Motion.ease_in_out_sine()`、`Tech.all_ids()`、
`SessionState.conduit_at()`、`Battle._middle_drag()`、`FlowNetwork` 回傳的
`silo_supply_total`、`Settings.RESOLUTIONS` 別名、`Enemies.endless_*`。

`§4.2` 訂的三條緩動曲線一條都沒少：`linear` **就是「不套緩動」本身**，
`ease-in-out-sine` 已經實作在 `pulse()` 裡（`sin(TAU × …)` 就是那條曲線）。
刪的是重複入口，不是 token。

### 存檔 schema 砍掉八個空殼鍵

`company_level` `roster` `endless` `daily` `tycoon` `levels` `entitlements`
`blueprints` —— 沒有任何讀取端，卻讓 schema 看起來比實作大四倍。
**提前宣告買不到相容性**：`_fill_defaults()` 本來就在讀檔時補缺鍵。

反過來它有真的代價：每個空殼鍵都會寫進玩家的存檔檔案，而看到 `"tycoon": {...}`
的人會以為那個系統已經在跑了。

新增一條斷言證明這樣砍是安全的：**用舊版玩過、存檔裡已經有 `tycoon` 的玩家，
讀進來不會掉東西**——`normalize()` 只補不刪。

### ★ 重構自己逼出一個漏洞

主選單五顆鈕改走 `_enter.bind(Screen)` 之後，`Callable.bind()` 把參數綁在
**尾端**，綁錯就是一顆按了沒反應的鈕。而三條既有 clicktest 全走 `TL_PANEL`、
**完全繞過主選單**——那條路是零覆蓋。

新增第五條 clicktest（`TL_PANEL=title`），驗一圈完整往返：
戰役（bind 帶 setup）→ ESC 回標題 → 科技樹（bind 不帶 setup）。
**並且證明它抓得到**：故意把「科技樹」綁成設定畫面 → `tech=false`、exit=1。

順帶修掉一個同類的舊坑：`Campaign` 與 `Tech` 的自檢沒有 panel 守衛，
從標題畫面掛起來時會跑到自己的 `get_tree().quit()`，把別人的自檢從中間打斷
（`Settings` 早就踩過並修過同一個坑）。

### 回歸

- 11 支 989 項全過；**五條** clicktest 全 PASS（新增 title）
- `TL_SIM=5600` 逐欄與 B1.7／B1.8 相同（`won`／1000／41／51.6／4412.8／1017.0／186.0）
  —— `TICK` 動了確定性地基，這一欄是它的逐位元證明
- `UiKit.esc_returns()` 也證明會紅：拿掉 `on_exit.call()` → tech／settings 的
  `esc_back=false`、exit=1

---

## [0.15.0] — 2026-08-02 — B1.8 潮鳴的場

起因是使用者回報「我不喜歡潮鳴的聲音」。我連續兩次回報「潮鳴不開火，所以那個音
從來沒播過」——**兩次都只是讀程式碼推的，沒有跑過任何東西**。這一批從建一個
會紅的迴圈開始。

### 診斷

探針跑「淺灘」示範佈局 5600 tick，照抄 `screens/Battle.gd::_audio_tick()` 的
選擇邏輯（抄它而不是重寫近似版，否則綠燈證明的是我自己寫的判定）。

| 音效 | 次數 | 交戰 tick | 滿足率 |
|---|---:|---:|---:|
| `fire_reclaimer` | **171** | 967 | 1.00 |
| `fire_prism` | 41 | 1006 | 0.84 |
| `enemy_hit` | 31 | — | — |
| `fire_anchor` | 13 | 135 | 0.71 |
| `warn_power` | 7 | — | — |
| **`fire_knell`** | **0** | **156** | **0.98** |

**錨是對照組**：135 個交戰 tick → 13 次開火音。潮鳴拿到 156 個交戰 tick、電是滿的、
每秒付 9 能量 → 產出零。紅燈的原因是 `rof = 0`，不是沒敵人也不是沒電。
`git log -p --all` 查全部歷史：潮鳴的 `rof` 只有一行 `+`，就是 `0.0`——
**沒有任何一個 build 播過 `fire_knell`**。

使用者實際聽到的多半是**回收者**：171 次，全遊戲最密集的音，第二名的四倍。

### ★ 真正的缺陷比回報的大

潮鳴每秒吃 9 能量，而畫面上一個回饋都沒有——沒有開火線、沒有音效、沒有光環視覺。
等於把「塔在交戰時每秒吃電」這條全案的心臟藏起來。

**新增（先改文件再改程式碼）**：`10_GDD.md` §7.4 加硬性要求、
`20_ART_DIRECTION.md` §1.6 加畫法。

- **場環**：交戰時在**實際射程**（5 格＝160px）畫一圈青色脈動細環。與交戰環的
  15px 差一個數量級——「同一個位置上的兩個訊息，換顏色不夠，要換形狀」（§4.3b）。
- **被抓住的敵人描一圈青色閉合折線**：減速 −40%／破甲 −25% 作用在**敵人**身上，
  標在那裡因果才讀得出來；標在塔上只說得出「它開著」。與血量弧一個是線一個是弧。
- 透明度跟著能量滿足率走——電不夠時控場真的比較弱，圈跟著淡。

### 刪掉一個播不到的資產

`sfx_fire_knell` 產生器（21 行）與 `tl_sfx_fire_knell.wav`（11.5 KB）一併刪除。

它活著的唯一原因是 `audio_test._paths()` 的篩選條件是 `tower`，不是
`tower and rof > 0`——**測試要求一個永遠播不到的 wav 存在**，於是那個檔案一路
過關、一路被打包，還在 B1.5.1 被「改良」過一次音色。改的是一個沒有人聽得到的音。

> **RG-98：斷言「可達性」，不要斷言「存在性」。** 一個只檢查資產在不在的測試，
> 永遠抓不到「這個資產從來沒被播過」。

`audio_test` 133 → 126 項。

### 回歸

- `TL_CLICKTEST` 新增 `knell_field`：斷言繪圖層每幀真的算了 `_auras`、潮鳴真的在交戰。
  **把 `_auras` 打掉重跑 → `knell_field=false`、`over=false`、exit=1** —— 紅得起來才算數。
- 11 支 996 項全過；四條 clicktest 全 PASS。
- `TL_SIM=5600 TL_SEED=42` 逐欄與 B1.7 相同（`won`／1000／41／51.6／4412.8／1017.0／186.0）
  ＝ 這一批是純渲染改動。

### 修

- GDScript 地雷第 n 次：`for` 迭代變數**共享同一個作用域**，內外同名是 parse error
  不是遮蔽（`_draw_enemies` 加了敵人索引 `i` 之後撞上多邊形的 `i`）。
- `_threat_cells()` 改吃傳進來的敵人格：`_draw()` 裡原本算兩次 `enemy_cells`。

---

## [0.14.0] — 2026-08-01 — B1.7 M1 驗收

### ★ 效能：量到了一個真的問題

新增 `tests/perf_test.gd` 與 `TL_STRESS=1`（渲染，模擬凍結）。數字全部記進
`30_TECH_DESIGN.md` §5.1，**包含不通過的那一項**。

| 情境 | 模擬單 tick | 判定 |
|---|---|---|
| 戰役第 1／2／3 關 | 0.79／1.9／1.9 ms | ✅ 目標（3ms）內 |
| 戰役第 4／5 關 | 4.7／6.2 ms | 🟡 超出目標，在上限（8ms）內 |
| **RG-8 壓力情境**（547 節點／2045 導管／200 敵人） | **30,200 ms**（渲染 23 FPS） | ❌ **不通過** |

**根因**：傳播的拜訪次數是 `O(迭代 × (V+E)²)`——`FlowNetwork._reach` 對每一次推送都重跑
一次「下游還有多少需求」的 DFS。**節點數不是主因，邊數才是**：同樣 17 個節點，
密集網（42 條導管）比戰役關卡慢一個數量級。

**這一批做的**：把每次拜訪都要重算的常數（邊的 `to`／`cap`、節點的優先權）提到迴圈外，
換成 `PackedInt32Array`／`PackedFloat64Array`。第 5 關 9.2 → 6.2 ms（−32%）、
壓力情境 55 → 30 秒（−46%）。

**驗收條件不是「比較快」，是「結果逐位元相同」**：`campaign_test` 416 項全過，
`TL_SIM=5600 TL_SEED=42` 的每一欄（`delivered_total` 4412.8、`reclaimed_power` 1017.0、
`salvage_ore` 186.0…）都與最佳化前完全相同。快取式最佳化一旦改變了結果，
就變成一次沒有驗收條件的改版。⚠ 用 `PackedFloat64Array` 不是 `Float32`——後者會降精度。

**這一批不做的**：真正壓下 RG-8 要換掉 `_reach` 的估計方式，**那會動到分配比例** →
五關的難度校準全部要重做。登記為風險 **R-17**，綁在 **B2.1**（程序生成大圖才是
真的會撞到這條線的地方）。

### ★ 死因歸因：M1 驗收句子的那一半

M1 的驗收句子是「陌生人可無指導玩 10 分鐘並**說得出自己為什麼輸**」。
局末面板原本只有成績（撐過幾波、產能積分）——那是**結果**不是**原因**。

現在多一行：**第幾波開始有敵人走到核心**，以及那是**缺電**（戰鬥時間有幾成供不應求）
還是**火力不足**。只給一句：三條並列的診斷等於沒有診斷。

全部從畫面層累計，`scripts/sim/` 一個欄位都沒動。

### RG-56：塔的擺位幾何（B1.2 就登記、一直只被間接守著）

`campaign_test._tower_geometry`：每一關至少一座**會開火的**塔搆得到核心
（敵人只在核心駐足，搆不到＝任何一次漏怪都必輸）、每一座稜鏡都與某段路徑共線且在射程內
（貫穿武器擺在沒有共線路徑的地方 ＝ 130 礦砂的裝飾品）。射程判定直接呼叫 `Combat`
的同一支函式——測試裡另寫一份幾何，總有一天會和真的規則分岔而兩邊都「對」。

### 音色（使用者回報）

- **潮鳴**：0.42 秒的諧和鐘（587＋880Hz，完全五度）→ 敲一塊悶掉的金屬板
  （基音 165Hz、泛音改成非整數倍 2.76／5.40、0.26 秒）。五度是協和音程，
  聽起來像獎勵音不像武器；0.42 秒在連射時會疊成嗡的一片。
  ⚠ **潮鳴目前 `rof = 0`（不開火），所以這個音實際上從來沒有播過**——見下方「已知問題」。
- **拉導管**：0.28 秒的上升掃頻 → 「推進去 → 卡住」兩段（0.20 秒）。
  掃頻在這套音色裡像雷射，而拉管子是機械動作；它又是全遊戲按最多次的音之一。

### 補洞

`TL_LEVEL`／`TL_FOCUS` 從 B1.2／B1.6 起就沒進 `Env.HOOKS`——**單獨使用時會寫進玩家的
真實存檔**（鐵律 1）。實務上都跟 `TL_PANEL` 一起用所以沒出事，但「實務上不會單獨用」
不是保護。三個一起補齊（含新的 `TL_STRESS`）。

### 已知問題

- **潮鳴不開火也不發聲**：它靠 `slow` 與 `armor_break` 的光環工作，`rof = 0`，
  所以既沒有開火線也沒有音效。玩家很難確認它有沒有在工作。登記待處理。
- **RG-8 不通過**（見上，R-17／B2.1）。
- **「陌生人可無指導玩 10 分鐘」未通過**：需要真人，R-14 待老闆拍板。

---

## [0.13.1] — 2026-08-01 — B1.5.1 局末卡住（使用者回報）＋ 兩個音色

### ★ 局末：塔停在開火那一幀、音效一直響（使用者回報）

**根因是一個差值偵測器在「差值不會再變」的地方仍然每幀被問。**

開火音的判定是「這條開火線的 `ttl` 還是滿的 → 它是這一 tick 才生出來的」。
但 `BattleController.step()` 在 `won`／`lost` **直接 return**，`ttl` 從此不再遞減——
於是最後那一 tick 的開火線永遠停在「剛剛才開火」的狀態：畫面上每座塔卡在開火的那一幀，
音效每秒重播 60 次。

**同一個根因在正常遊玩時也一直在犯**：畫面 60fps、tick 10Hz，同一發子彈會被連續
六幀看到「`ttl` 還是滿的」，所以**每一發開火音本來就播了六次**。

修法一次修在源頭：`_audio_tick()` 的一次性音效判定改成**只在 `tick_count` 真的
前進了才執行**。用差值偵測的東西，就要確定自己是在變化的那一刻被呼叫的。

另外兩件跟著收乾淨：
- 局末面板出現時清掉凍住的開火線與碎片（純渲染、不進 `state_hash()`）。
- 局末停掉生產的低頻循環——它是「東西正在跑」的聲音，而東西已經不跑了。

**回歸測試證明過會失敗**：把守衛拿掉，`TL_CLICKTEST` 的 `over_silent` 當場變 false。
（第一版的測試是**空的**——假的開火線起點放了一個中繼，而 `fire_relay` 根本沒有這個檔案，
`play()` 直接 return，於是不管修沒修都會過。改成先蓋一座真的錨。）

### 音色（使用者回報）

- **錨的開火音太沉重** → 高一個八度、長度砍半（0.18s → 0.10s），收尾帶一點乾的木質敲擊。
  錨是射得最快的那一座塔，一秒好幾發的東西不能用重擊的音色。
- **主選單曲不夠帶感** → 加入八分音符的低音撥弦（A–A–C–E，反拍輕一半——那個強弱差
  就是「行進」的感覺）與每小節一個很輕的低頻脈衝。**沒有加鼓**：它是選單曲，
  把打擊層搬過來等於每次回主選單都被催一次。

---

## [0.13.0] — 2026-08-01 — B1.5 音訊 ＋ B1.4.1 局內選單

### 局內選單與 ESC（使用者指定）

ESC 開關局內選單：**繼續／重來（本局歸零）／設定／退出（放棄本局）**。
左上角同時有一顆「選單」鈕——ESC 是快捷，不是唯一的路（手機移植預留條款 P3：
操作不得只靠 hover／右鍵／鍵盤）。全遊戲其餘畫面的 ESC 一律是「返回上一層」。

- **★ 選單不暫停。** `10_GDD.md` B5「不可暫停」是鎖定的設計，一顆可以隨時停下來
  慢慢想的鍵正是它要擋掉的東西。面板上直接寫「時間仍在走」——**沒說出口的規則會被當成 bug**。
- 選單開著時有半透明遮罩擋住地圖：點下去不該蓋出一個節點來（自檢有量）。
- 設定是**同一個設定畫面**疊上來，不做一份「局內版設定」——兩份遲早有一份漏掉新選項。
- ESC 的歸屬（局內 vs 關卡選擇，兩邊都接了 ESC）不靠「Godot 應該是由深到淺傳遞」的推論，
  `campaign` 自檢實際量了一次。

### 音訊（B1.5）

**音源是程序生成的**（`assetgen/gen_audio.py`，純 Python 標準庫）——沿用 §6.1
「零外部素材」的同一條理由：授權乾淨、體積可控（3 首 BGM ＋ 15 支 SFX 共 2.4MB）、
改音色是改一支腳本再跑一次。**那支腳本就是音源的原始碼，不要去修 wav。**

- **BGM 分層無縫切層**：`tl_bgm_battle_base`（準備期：低頻 drone ＋ 稀疏機械節拍）與
  `tl_bgm_battle_perc`（戰鬥期：打擊層 ＋ 失諧高頻）**同長度、同 BPM、同調性**，
  兩層從第一個取樣就一起跑，切換時只淡入淡出音量（1.2 秒）——
  等到要打了才 `play()` 那一層，接進去的會是曲子的中間。
- 主選單曲 `tl_bgm_menu`（極簡、留白多，同一個調性中心）。
- **15 支 SFX**（DoD 寫 12）：建造／導管／損毀、生產低頻循環、**五座塔各一個開火音**、
  敵人受擊、能量不足、核心受擊（全場最醒目）、UI 點擊／取消／解鎖。
- **音效一律從畫面層推導**（比對上一幀的 `kills`／節點數／核心血量／開火線），
  `scripts/sim/` 維持零副作用——在模擬層塞一行 `play()` 就等於讓每日挑戰的重播會出聲。
- 核心受擊與能量不足**節流**（0.8 秒／4 秒）：核心每 tick 都在掉血，照實播就是
  每秒十次爆炸；響個不停的警報等於沒有警報。
- 點擊音接在 `UiKit.touchable()` 這個唯一的隘口——逐一在各畫面連 `pressed` 的那條路，
  漏掉的那幾顆鈕會安靜到沒有人發現。

### 新測試：`audio_test`（133 項）

問的是「音源本身有沒有滿足設計條件」，那幾件事在耳朵上是「怪怪的」，在資料上可以直接量：

- 每一個程式碼會去要的檔案都存在，**開火音的清單從 `NodeDefs` 推導**
  → 日後多一座塔沒配音會在這裡失敗，不是上線才發現。
- 三首 BGM 幀數完全相同（差一個取樣，跑一分鐘就差 60 個，節拍會慢慢錯開）。
- **★ 循環接點不爆音**，而且門檻**自己校準**：拿接點兩側 128 個取樣的平均步幅當基準。
  第一版拿固定門檻比，把 330Hz 鋪底音自己正常的取樣步幅誤判成爆音。
- 格式統一 16-bit 單聲道 22.05kHz、沒有削頂、沒有靜音檔。

### 修掉的三個真缺陷（都是測試抓的，不是看出來的）

1. **Godot 的 WAV 匯入器預設用 QOA 有損壓縮**（`compress/mode=2`）。壓過的資料連
   「這個檔案有幾個取樣」都問不到，循環接點更無從談起。18 個 `.import` 一律改成 PCM。
2. **`tl_sfx_prod_flow` 的循環接點真的會啪一聲**：低通濾波器有狀態，從 0 起跑的暫態
   和訊號末端的穩態接不起來。會循環的素材改用「跑兩遍取第二遍」的週期性穩態。
3. **`AudioStreamWAV.data` 是屬性，每讀一次複製一整份 PackedByteArray**（BGM 是 700KB）。
   寫在取樣迴圈裡就是 5 萬次迴圈複製 35GB，測試看起來像當掉了。

---

## [0.12.0] — 2026-07-31 — B1.4 外框完整化

### 主選單

戰役（附進度 `N／15 ★`）／科技樹（附現有研究數據）／設定／測試圖／離開遊戲。
**戰役排第一顆**：它是這款遊戲，其餘是它的周邊，測試圖是給開發用的。
進度寫在鈕上是刻意的——**回來的理由要在按下去之前就成立**，不是進了關卡選擇才發現還差幾顆星。

### 設定畫面

- **音量三軌**（主／音樂／音效）。`AudioBus` 從「只有一條 Master ＋ 靜音旗標」長成三條真的匯流排，
  音量走 `linear_to_db`（線性套上去的話滑桿前 20% 什麼都聽不出來、後面又爆掉）。
  ⚠ **音樂與音效目前沒有東西可播**（音源排 B1.5），畫面上直說了——不寫的話玩家會以為滑桿壞了。
- **減少動態效果**：勾了 `Motion.reduce` 當場為真。
- **全螢幕 / 視窗大小**（1280×720 / 1600×900 / 1920×1080，全 16:9）。
- **改了當場生效、當場存檔**，沒有「套用」鈕。滑桿拉到一半才知道結果的音量沒有意義。

**設定的套用集中在 `GameState.apply_settings()` 一支**（開機一次、改一次動一次）。
散在各畫面的 `_ready()` 裡讀設定的話，新加一個畫面就多一個會忘記讀的地方——
而忘記的那個畫面看起來完全正常。`Battle` 原本自己讀 `reduce_motion` 的那一行因此刪掉。

**★ 不做鍵位設定。** B1.4 的 DoD 列了它，但這款遊戲一顆鍵都沒有綁（局內說明就寫著「沒有鍵盤」），
做一個空的鍵位表只是把還不存在的東西畫出來給人看。設定畫面上直接寫明。

### 存檔 sv1 → sv2（本專案第一個真的遷移分支）

`campaign.cleared` 移除，改由 `campaign.stars` 推導（`cleared` ⟺ `stars[id] >= 1`）。
**同一個事實存兩份遲早會漂移**——漂掉的那一天，玩家會看到「這一關通關了但下一關還鎖著」，
而兩個欄位各自都「對」。

遷移機制本身也一起長出來：一次跳一版（不寫「從任何版本直達最新」的捷徑——
捷徑要在每加一版時重寫全部組合，而漏掉的那一格是玩家的存檔）、沒有 `sv` 的檔當成 1。
`save_test` 覆蓋四件事：典型舊檔升級、**只在 `cleared` 裡但沒有星數的關補成 1 星**
（遷移不能假設舊資料自洽）、沒有 `sv` 的檔、以及**冪等**
（遷移最常見的事故就是「開遊戲兩次跑了兩次遷移」）。

### 截圖當場抓到的兩個版面缺陷

- **最後一行說明掉到 720 以外**（RG-75 的同一條）。間距 14 × 13 列就是這個結果；
  改成 8 並加上 `fits` 斷言。
- **`CheckBox` 在這套配色裡幾乎看不見**（深色勾選框畫在深色底上）。
  改用 toggle `Button`——全遊戲其他開關本來就是這個語彙，順便不必替一個元件單獨調主題。

### 驗證

9 支測試 848 項全過（save 23 → 33）；四條 `TL_CLICKTEST` 路徑全 PASS
（新增 `settings`：音量寫進去、匯流排真的存在、**靜音時 `apply()` 是空操作**、
`Motion.reduce` 當場生效、視窗真的變大再變回來、沒有寫檔、整頁留在畫面內）；
五關參考解逐格相同；平衡基準 `won／1000／41／51.6` 未動。

---

## [0.11.1] — 2026-07-31 — B1.3.1 局內 UI 三個小改（使用者指定）

### ① 建造鈕上只寫名字

價牌在提示列的第一行，滑鼠停在鈕上時圖鑑浮層也會寫一次。同一個數字印三遍只是把鈕撐長。

### ② 「連線」與「移動」兩顆模式鈕拿掉

- **連線**：B1.6.2 的拖曳已經完全取代它。留一顆只能點兩次的模式鈕，等於在教一個比較慢的做法。
  `Mode.CONNECT` 與 `_connect_from` 整組刪除。
- **移動**：改成**按住滑鼠中鍵拖**。平移是一個持續性的動作，做成模式代表玩家每次看完別的地方都要記得切回來，
  而忘記切回來的代價是「點地圖沒反應」。

模式從五個變三個（建造／加粗／拆除）。

⚠ **觸控沒有中鍵**（P3 條款）。影響範圍有限——fit 倍率下平移恆被夾成 0，只有玩家自己放大之後才需要平移——
但移植批要補一個雙指拖曳手勢，已登記為風險 **R-16**。

### ★ 順帶逼出一個縮放的實質缺陷

寫中鍵平移的自檢時 `mid_pan` 一直是 false。查下去發現不是平移壞了，是**縮放把畫面甩到左上角**：
`_zoom_by()` 的 anchor 修正錯了兩層——`_map_origin()` 讀的是**已經更新過**的 `_zoom`（量到新原點不是舊的），
而且 `_map_origin()` 本來就把地圖置中、那一半位移不該再由 pan 補一次。

結果是**按一次「＋」畫面就甩到角落，pan 當場撞上夾限、再也拖不動**。
而 B1.2.2 當時的斷言全是綠的——「倍率有變大」與「pan 夾得住」都成立，**錯的是「往哪邊放大」**。

改成照定義寫（新原點 = anchor − k × (anchor − 舊原點)），並補兩條斷言：
`centered`（以框架正中放大時畫面中心不跑）與 `anchored`（以滑鼠位置放大時游標底下還是同一格）。

### 驗證

9 支測試 838 項全過；`TL_CLICKTEST` 新增 `mid_pan`、`centered`、`anchored` 三項，三條路徑全 PASS。
自檢裡原本靠「連線」鈕走的兩段（45° 拉線、一格斜線加粗）改走 `_drag()`——
**那本來就是玩家現在唯一的路徑**。

---

## [0.11.0] — 2026-07-31 — B1.3 科技樹

局外成長的**科技軸**（`10_GDD.md` §3.6、§7.8）。研究數據從 B1.2 就開始入帳，本批給它一個花掉的地方。

### 12 節點，三支路線

| 支線 | 串 | 效果 |
|---|---|---|
| 產能 | 導管擴容 I–III | 所有導管的**基礎** cap +2/級（10 → 16，與局內加粗疊加，滿配 34） |
| 產能 | 採集精煉 I–II | 每座採集器 +1 礦砂/秒（6 → 8） |
| 防務 | 能量效率 I–III | 塔的**交戰**耗能 ×0.92/級（連乘，−22.1%） |
| 防務 | 校準 I–II | 塔傷害 ×1.06/級（連乘，+12.4%） |
| 後勤 | 藍圖槽 I–II | 藍圖槽 +1/級（藍圖庫本體排 M2，先把槽位存起來） |

效果一律走 `Tech.mods()` 回傳的那一個字典，開局解算一次存進 `SessionState.mods`。
散開來讓各處自己去數 `unlocked` 裡有幾個「導管擴容」的話，每加一個節點就要記得改四個地方——
而漏掉的那個地方不會報錯，它只會讓玩家買的東西沒有生效。

### ★ 驗收的重點是「買了真的有用」，不是「`mods()` 回傳對的數字」

一個買了沒生效的科技在畫面上完全正常。所以 `tech_test` 的四條效果檢查**各自跑模擬取差**：

- 導管擴容三級 → 幹線瓶頸移開（兩座採集器產 12，原本被 cap 10 卡掉 2，現在全數入帳）
- 採集精煉二級 → 兩條獨立線 12 → 16 礦砂/秒（用兩條線是為了避開 cap，否則加成會被管子吃掉而測試會誤判成「沒生效」）
- 能量效率三級 → 交戰需求 ×0.7787（**必須先讓敵人進到射程內**才量得到；待機耗能是 0，量不到差異就代表接錯地方了）
- 校準二級 → 60 tick 打掉的血 ×1.1236

還有一條反向的：**沒買任何科技的一局，數字必須和 B1.3 之前逐項相同。** `mods` 的中性預設只要有一個寫錯，
五關參考解與平衡基準線會整組漂掉而沒人立刻發現。

### B2 上限（+35%）

`tests/tech_test.gd` 鎖住 `0.1236 + 0.2213 = 0.3449 ≤ 0.35`。量法在 §7.8 定案：
**相加不相乘**（兩者作用在不同的東西上，相乘沒有意義，而相加是比較嚴的那個算法），產能支不計入
（導管基礎 cap 10 → 16 是 §7.2 已明文的設計，它是經濟數值不是戰鬥數值）。

### 文件修正：§7.8 的成本公式複述不出它自己那張表

原本寫「`50 × n^1.6`，n 為該支已解鎖數＋1」。依它算，「採集精煉 I」是產能支的第 4 個節點、要 460 而不是表上的 50；
就算改讀成「同串的階數」，`50 × 2^1.6 = 151.57`（要進位才得 152）而 `50 × 3^1.6 = 289.98`（要捨去才得 289）——
**沒有任何一種取整方式同時得到 152 與 289**。§7 的數值表是數值權威，所以實作直接列表（`TIER_COST`），並修正那句話。

### 順手抓到的畫面缺陷：最後一張卡被推到畫面外

最長那一支是 5 張卡，加上頁首就超過 720。第一版沒有捲動容器，於是第 5 張卡直接在畫面外，
而畫面上看起來只是「這一欄比較短」（RG-47／RG-60 的同一個教訓）。改成 `ScrollContainer`——
**M3 這棵樹要長到 45 個節點**，靠「把卡片再壓矮一點」是撐不過去的——並加上 `last_reachable` 斷言：
捲到底之後最後一顆鈕必須整顆在畫面內。

### 其他

- 建造預覽同步吃科技：交戰耗能、採集器產出、加粗上限的文字都改讀 `s.mods`（玩家在最重要的那條資訊上讀到舊值＝預覽在說謊）。
- 「已經是最高級（cap 28）」的文案拿掉寫死的 28——導管擴容會把它推上去。
- 入口兩個：標題畫面（鈕上直接顯示現有研究數據）與關卡選擇畫面（數據是在那裡賺到的）。
- 新鉤子面板 `TL_PANEL=tech`。

### 驗證

9 支測試 **838 項全過**（新增 `tech_test` 56 項）；五關參考解 tick 數／核心／擊殺／產能積分逐格相同；
平衡基準 `won／1000／41／51.6` 未動；`TL_CLICKTEST` 三條路徑（battle／campaign／tech）全 PASS。

---

## [0.10.2] — 2026-07-31 — B1.6.2 操作手感：拖曳拉線

使用者指定的方向是「先處理遊玩的感受與操作」。畫面那半在 B1.6／B1.6.1 交了，這一節是**操作**。

### 拖曳拉線：把最頻繁的動作上那 10 次無決策點擊拿掉

真正的建造節奏是**蓋一個 → 接一條 → 蓋一個 → 接一條**——第 1 關的參考解就是 6 蓋 6 接交替。
舊路徑每接一條線都要往返左欄切一次模式，光第 1 關就是 **10 次模式切換，而其中沒有任何一次是決策**。

- **在建造／連線模式下，從既有節點按住左鍵拖到另一個節點放開＝拉一條導管。** 不必先切「連線」。
- 作法是**按在節點上時把該次點擊延到放開才判定**：放開在別的節點上＝拉線，放開在原地＝該模式原本的動作。
  於是「連線」模式的**兩次點擊完全沒變**（觸控與精確指定還要用它），舊的自檢也一條都不用改。
- 拖曳起點用**獨立的 `_drag_from`**，不共用 `_connect_from`：共用的話「連線模式已選好起點 A、玩家又從 B 開始拖」會把 A 悄悄蓋掉。
- 建造模式點在既有節點上時，訊息從「這一格已經有東西了」改成教這個手勢——那句舊話對玩家沒有下一步。
- 提示列、`_next_step()` 的三條指引、局內「說明」面板全部改口徑（**手勢不寫進提示列就等於不存在**）。

**GDD §3.2 本來就寫著「點起始節點 → 拖到目標節點」**——這一節是把程式碼補回設計文件，不是改設計。

### 圖鑑浮層蓋掉能量收支（本批截圖當場抓到）

`Label` 不設寬度就會長到最長那一行那麼寬（熔爐那一行 850px），於是圖鑑從 x=124 一路蓋過 x=870 的能量面板。
兩顆開關各自獨立 → 玩家可以同時開。改成限寬 640 ＋ 自動換行，並把「不重疊」升級成斷言（比照優先權面板）。

### 驗證

- `TL_CLICKTEST` 新增 `drag_wire`：**全程停在建造模式**，合成「按下 → 移動 → 放開」三顆事件拉出一條導管。
  `_click_at()` 那種原地按放驗不到「放開在別的格子」，所以另寫 `_drag()`。
- 8 支測試 782 項全過；五關參考解全部重跑通關；平衡基準 `won／1000／41／51.6` 逐項未動。

---

## [0.10.1] — 2026-07-30 — B1.6.1 導管與流動珠的可讀性

使用者實看後的回報：「管道跟能量粒子的表現還是不夠好，有破圖，有重疊，有看不清楚」。**三個症狀在近照上都找到了，而且各有明確根因。**

（新增鉤子 `TL_FOCUS` 之後才拍得出這些近照——特效與線寬是十幾個像素的東西，全圖截圖上判不出好壞。）

### 破圖：珠子掛在管子外面

lane 偏移是 `w × 0.45 + 1.0`，而它**沒有夾在管子裡**：8px 寬的線上偏移 4.6 ＋ 珠半徑 2.6 ＋ 光暈 1.2 ＝ 8.4，遠超過半寬 4——三排珠子兩側都掛在管外，看起來像從管子裡漏出來。

夾住偏移不是辦法（真的夾進去之後三排會擠成一團更難讀）。改成**共用中線、沿長度錯開相位**：白、琥珀、紫依序前進。位置不再承載資訊——那本來也不是它該講的事，顏色照樣分得出資源。

### 重疊：兩條共線導管可以疊在同一排像素上

畫面上是一條線、實際是兩條，兩排珠子交錯成一團；`conduit_near()` 在它們之間也只能任選一條（加粗會加到你沒指的那一條）。

新規則：**共用 2 格以上不准，共用 1 格照樣可以**（交叉／同節點分岔本來就該允許）。刻意**不是**「不准穿過節點」——從節點旁邊經過而不接它是一個真的戰術選擇；被禁掉的只有「看起來是一條線、其實是兩條」。

這條規則一上線就抓出四處既有的重疊：沙盤的兩條餵熔爐的線、`combat_test`／`hud_test` 的共用夾具、**以及我自己在 B1.2 寫的第 4／第 5 關參考解**。全部改掉了。

### 看不清楚

- **珠子的描邊改成螢幕固定粗細**（`1 / zoom`）。原本是固定 +1.2 地圖 px：在 r=1.4 的細線上光暈佔了直徑的 46%，讀起來是「管子上的洞」；放大到 300% 又變成一圈粗黑框。
- **加粗刻度改成明確伸出管緣**，並貼近起點端。原本固定 ±5px 而滿載的管子本身就有 8px 寬——刻度只比它寬一點點、又是近白青畫在青管上，讀起來是「管子這裡破了一個洞」。第一版改完貼太近，被節點自己的圖形蓋住看不見（截圖當場抓到），再往外挪到 17px。

### ★ 一條在修的過程中被逼出來的關卡設計規則

`sim/FlowNetwork` 的節點語意是**先吃滿自己的需求才轉發**。所以一座塔如果同時是幹線上的接點，它會**餓死它下游的一切**——第 5 關的參考解一度把稜鏡當接點，結果核心旁邊那座錨滿足率 0.00，而頂欄顯示供給 60（問題從來不在發電量上）。

要讓大消費者拿到兩條進線，就得由**兩個中繼**（不吃電）從幹線分岔進去。第 5 關的核心因此上移一格（`(31,15)` → `(31,14)`），把 y=16 讓出來給這個菱形；起始礦砂 1000 → 1240。

**過程中的一個教訓值得記**：我寫了一支探針想診斷第 5 關為什麼輸，它給的結論（防線全餓死）是錯的——那支探針忽略了參考解的 `wait` 分段，跑的是一個和真實局面完全不同的東西。**診斷工具本身要先對得上它要診斷的那條路徑。**

驗證：8 支測試 **782 項全過**（build 74 → 79 新增重疊規則、campaign 407 → 406）。五關參考解全部重跑通關，第 5 關現在是 `核心 1000/1000、擊殺 72、產能積分 0.85`。平衡基準逐項未動。

## [0.10.0] — 2026-07-30 — B1.6 美術打磨與打擊感（提前）

使用者重排優先序：「先不處理門檻，我想先處理遊玩的感受與操作」→「首先我想把遊戲的特效與畫面先提升」。B1.6 因此提前於 B1.3–B1.5。

範圍不是我發明的——`20_ART_DIRECTION.md` 把它定死了，我做的是**把已經寫下來但從來沒實作的部分實作出來**。

### `scripts/render/Motion.gd`：一個從第一批就列在專案結構裡、卻從來不存在的檔案

`CLAUDE.md` 的目錄樹列著它、§4.1 定義了時長階，但它一直沒有被寫出來。於是每個動效都是散在畫面層的字面量（`sin(tick * 0.4)`、`sin(tick * 0.25)`、`ttl = 3`），四個問題跟著來：

- 時長階形同不存在，沒有人在對 `0.12 / 0.24 / 0.50 / 2.0`
- **`settings.reduce_motion` 是死設定**：存檔裡有、**沒有任何人讀**
- 同一種脈動在兩個地方用不同係數，看起來像兩件不同的事
- 「所有動效都可跳過」（§4.4）沒有任何一個掛勾點

現在時長階、三條緩動、tick 驅動的脈動、效果進度、確定性碎片方向全部在一個地方，而 `reduce_motion` 真的關得掉東西。

### 打擊感：擊殺與建築被毀

**之前敵人與建築都是「直接消失」**——你的產線斷了，產能掉了，而畫面上沒有任何一格告訴你剛剛發生了什麼。

規格直接抄 §161 反模式清單那一行：**「爆炸不用 200 顆粒子。用 3–5 個幾何碎片＋一次縮放閃光」**。碎片形狀服從 §0 核心命題，這不是裝飾選擇：

| | 形狀 | 色 | 動態 |
|---|---|---|---|
| `chaos` 敵人消散 | **不規則三角**碎片 | 品紅 | 往外飛並**自轉** |
| `order` 建築破裂 | **正方**碎片 | 青 | 往外飛但**不自轉**（秩序連壞掉都是方的） |

碎片方向零 RNG（由來源 id 決定），所以重播與每日挑戰看到的是同一場爆炸。碎片是**純渲染**，不進 `state_hash()`——`hud_test` 直接拿「開與不開碎片，局狀態雜湊完全相同」把這件事鎖住。

### 兩項 §4.3「一定要動」清單裡漏掉的

- **正在被啃的建築**：敵人 walk-by 每 tick 傷害相鄰 1 格，但那一刻在畫面上完全沒有表現（血條會慢慢變短，而你不知道「現在」有東西在被吃）。改由渲染層從 `Tide.in_blast` 推導，**不新增任何狀態**。
- **波次倒數最後 10 秒**：階段那一格轉橙並脈動。

**★ 這裡我自己踩了一次同樣的坑**：受擊指示第一版做成橙色圓環，截圖當場抓到——它和「交戰中」的琥珀圓環在同一顆 12px 的節點上肉眼分不出來。這是這個專案第二次（B0.6 的「血條不是圓環」是第一次）。改成**四角括號**：**同一個位置上的兩個訊息，換顏色不夠，要換形狀。**

### 新鉤子 `TL_FOCUS="x,y,zoom"`

把鏡頭對到某一格並放大。理由很實際：**特效是 0.2 秒、十幾個像素的東西**，在 fit 倍率的全圖截圖上根本判不出好壞——沒有它，我會一直宣稱「碎片爆做好了」而沒有真的看過它。

驗證：8 支測試 **778 項全過**（hud 45 → 64，新增動效 token 與「碎片是純渲染」）。截圖逐張讀過：`b16_chaos.png`（不規則三角＋品紅閃光）、`b16_order.png`（軸對齊方塊＋青色擴張環，同一格看得到它帶走的那條供應線 → `▲0.0/秒`）、`b16_threat2.png`（核心的四角括號與塔的交戰環分得開）。平衡基準逐項未動。

## [0.9.2] — 2026-07-29 — B1.2.2 地圖框架與縮放

使用者要求：「玩家看到的地圖的大小是不變的，但是支援讓玩家自由縮放，同時關卡進去的一開始就應該是縮放在剛好可以填滿整個地圖框架的大小」。

- **地圖框架恆定**（`1152×604 px`）。進關卡時地圖自動縮放到剛好填滿它——第 1 關（20×12）不再只佔左上角一小塊，五關的視覺尺度從此一致。
- **自由縮放**：下限＝ fit（比它更小只是把地圖縮進框架裡的一小塊，正是這一批要修掉的東西）、上限＝ fit×3。滾輪或頂欄右上角 `−／＋` 皆可，**「全景」一鍵回 fit**（鈕上同時顯示當前倍率）。滾輪以滑鼠位置為錨點，不會把你正在看的東西推出畫面。
- **平移**：新增「移動」模式，按著左鍵拖。**用模式鈕而不是右鍵或中鍵**——手機移植預留條款 P3「操作不得只靠 hover／右鍵／鍵盤」，而拖曳在觸控上就是拖曳。平移夾在「地圖永遠蓋滿框架」之內。
- 縮放與平移**只是視野，不影響模擬**：它們不進 `state_hash()`。

實作上只動一個 `draw_set_transform`——9 支繪圖函式全部改用**格為單位的地圖座標**，完全不知道縮放存在。`_draw()` 裡沒有任何 `draw_string`（HUD 全是 Control 子節點），所以文字不會跟著縮。放大後溢出框架的部分用四塊邊條遮掉（`Control.clip_contents` 夾的是整個滿版節點，對「只夾住框架」沒有用）。

**★ 尺度的代價，寫成一條可測的線**：框架固定之後地圖尺寸不再受畫面限制，但一格的像素大小 ＝ `32 × fit`，圖越大格越小。而線寬（2–8px）、三態徽章那套視覺編碼是**在 32px 的格子上校準與驗收的**。所以定 **可讀性地板 24px**（`Shapes.MIN_READABLE_CELL`），`campaign_test` 逐關檢查。現行五關是 `50.3 / 40.3 / 35.5 / 33.6 / 33.6 px`；40×40 的圖會掉到 15.1px，**要做它得先拿那張圖重跑 `TL_NAKED` 讀圖**——進場那一眼不可讀，不能用「玩家可以自己放大」當藉口。

順帶修掉一個自己造出來的溢出：加上「移動」之後第 5 關的 10 顆建造鈕 ＋ 4 顆動作鈕會撞到底欄，動作鈕改**兩欄兩列**。

**★ 另一個順手抓到的隔離缺陷**：測試鉤子原本只擋存檔的**寫入**，讀取照舊。於是使用者實際玩過之後，`TL_CLICKTEST` 對「第 2 關是鎖著的」這種斷言就在他的機器上無故變紅，而 `TL_SHOT` 「同參數在任何機器上拍出同一張圖」那句保證在關卡選擇畫面上根本是假的（已通關幾關就長不一樣）。改成**有任何 `TL_*` 時連讀都不讀真存檔**——鉤子在兩個方向上都該是空操作。

驗證：8 支測試 **760 項全過**（campaign 396 → 407，新增框架 fit 幾何與可讀性地板）。`TL_CLICKTEST` 新增一整組 `view`：進場即 fit、fit 填滿一軸且兩軸不溢出、頂欄不撞縮放鈕、放大／全景／下限、平移夾得住，以及 **`hit_after_zoom`——放大兩級並把目標平移到框架正中之後，點下去蓋出來的還是同一格**（縮放最容易靜靜壞掉的地方，就是繪圖與滑鼠各算一次變換）。

## [0.9.1] — 2026-07-29 — B1.2.1 45° 的線點不到

使用者實玩後的回報：「45 度的線似乎沒辦法加粗」。**他是對的，而且比他說的更廣。**

命中判定原本比對的是「點到的格，是不是這條線的**中間格**」（頭尾兩個節點格排除在外）。於是：

- **兩個對角相鄰的節點之間那條線，一個中間格都沒有** → 加粗與拆除都永遠點不到。而 45° 線在實戰佈局裡多半就是這種一格長的——B1.2 那五關的稜鏡菱形，四條邊全部是。
- 就算有中間格，45° 的線只從那些格的**正中央**穿過去，滑鼠往垂直方向偏半格就掉進一個不屬於這條線的格；水平線則整排格子都算命中。**同一條線，畫得斜一點就變難點**——那不是規則，是實作漏出來的。
- 那段程式碼的註解本來就寫著「命中判定放寬到半格」。**它從來沒有被實作過。**

改成**距離判定**（`SessionState.conduit_near()`）：點到線段的垂直距離在半格內就算命中，多條線擠在同一個節點上時靠哪條近選哪條。畫面層改傳**格為單位的浮點座標**而不是格號——格解析度分不出「你指的是這個節點的哪一條線」。

順帶：一格長的**水平／垂直**線以前也點不到，同一個根因，一起修掉。

驗證：`build_test` 67 → **74 項**（一格長的 45° 點得到且加粗得起來、同長度的水平線一樣好點、偏一格多不誤觸、同節點的兩條線分得開）。`TL_CLICKTEST` 加一項 `diag_upgrade`——**走完整輸入路徑**：切「加粗」→ 點兩個節點之間那個像素點 → 級數要真的變。平衡基準逐項未動。

## [0.9.0] — 2026-07-29 — B1.2 戰役框架：五關、關卡選擇、星等、獎勵

### 五關手作地圖（全部一屏可見）

| 關 | 新解鎖 | 要教的那件事 | 尺寸 | 橋 | 波 | 準備期 |
|---|---|---|---|---|---|---|
| 1 潮口 | 採集器・發電機・中繼・錨 | 礦砂 → 能量 → 塔 | 20×12 | **0** | 3 | 60s |
| 2 雙灣 | ＋儲槽・稜鏡 | 峰值電力：一條導管餵不飽一座稜鏡 | 26×15 | 1 | 4 | 60s |
| 3 窄橋 | ＋潮鳴・回收者 | 一座橋就是一條幹線 | 28×17 | 1 | 5 | 45s |
| 4 熔渣灣 | ＋熔爐 | 第三資源：合金把幹線加粗到 22 | 30×18 | 2 | 5 | 45s |
| 5 潮汐之喉 | ＋碎浪 | 全解鎖綜合考 | 34×18 | 3 | 5 | 45s |

**難度階梯就是建造欄的解鎖順序**——第 1 關給四顆鈕，不是十顆。十顆對一個還不知道「電是流率」的人來說不是自由，是雜訊。鎖做在規則層（`sim/Build.can_place` 的 `locked` 原因碼）而不是只在畫面上，否則藍圖展開與重播都會繞過它。

### 星等與獎勵

- ★ 通關／★★ 核心無損／★★★ 產能積分達標。三條**累進**：核心破了封頂 1 顆——「產能爆表但核心快沒了」拿三星等於把第一課教反。
- 局末逐條列出三顆星拿到沒有、**沒拿到的那顆差多少**。
- 研究數據 ＝ `基礎獎勵 × 星數`，以歷史最高星數結算、**只補發差額**。回頭補星拿得到，重刷同星數拿不到第二次。滿星全通 855 點。
- 失敗不扣星、不抹通關紀錄（紅線 R1）。

### ★ 修：兩條線一起餵一個大消費者，只送得到四分之一

第 2 關的正解是「菱形」——發電機分兩條 cap 10 的線並聯餵一座吃 20 的稜鏡。做出來才發現**它根本不成立**：一條實體導管的兩個方向各自有一份完整的 cap，於是中繼會把一半的量沿著來路推回去（它「看到」經由另一條分支還有需求），三次迭代彈完只送到 25%。

修法是 `FlowNetwork._residual()`：**管子是一根，容量就該是一份**——兩個方向的流量共用同一份 cap。`flow_test` 直接鎖住那個數字（44 → 46 項）。淺灘的平衡基準逐項未動（示範佈局是樹狀，沒有並聯）。

### 兩條在校準時才現形的關卡設計規則（已寫進 GDD §7.9）

1. **每一關至少要有一座塔搆得到核心**——敵人只在核心駐足，射程搆不到核心的防線一次漏怪就是必輸。
2. **稜鏡必須和某一段路徑同排或同列**——它的貫穿是沿著一條軸線，不是「射程內全打」。擺歪的稜鏡看起來和擺對的一模一樣。

### 其他

- `Maps.path_of()` 改吃地圖自己宣告的 `waypoints`，刪掉寫死 shoal 的分支（淺灘展開後逐格相同，determinism 基準未動）。
- 提示列與說明抽屜**不再提這一關沒解鎖的東西**：第 1 關不會再叫玩家去點不存在的「熔爐」，橋的座數改讀地圖（零座也是一句有用的話）。
- 新鉤子 `TL_LEVEL=1..5`，配 `TL_PANEL=campaign` 直接進某一關；戰役關卡的截圖與 `campaign_test` 跑的是同一份參考解。

**驗證**：8 支測試 **749 項全過**（flow 46／build 67／combat 95／tide 49／save 31／determinism 20／hud 45／**campaign 396**）。新的 `campaign_test` 把五關的參考解各自實跑到底，斷言 `won`、無建造失敗、第 1–2 關核心無損——**「這一關過得了」不再是一句宣稱**。輸入層 `TL_CLICKTEST` 兩條路徑（局內、關卡選擇）皆 PASS。平衡基準 `TL_SIM=5600 TL_SEED=42` 逐項未動：`won`／1000／41／51.6。

## [0.8.1] — 2026-07-29 — B1.1.1 線寬改講流量

使用者實看後的回報：「為什麼有些管道大有些管道小？而且有時候升級的管道卻比沒升級的還小」「應該要是越大的流量管道越粗」。

**他是對的，而且指出的是一個真的沒設計好的地方。**

- **線寬的分母從「這條線自己的 cap」改成「全遊戲的最大 cap」**。原本畫的是飽和度，所以加粗一條線會讓它**變細**（分母變大）——而那顆按鈕就叫「加粗」。現在粗細＝絕對流量，在全圖上可以互相比較：同樣粗就是同樣流量，不管那條線升到幾級。
- 順帶修掉的是**兩個視覺通道在講同一件事**：線寬與顏色原本都由 `flow / cap` 驅動，重複了一次飽和度，而「這條線實際搬了多少」沒有任何通道在講。
- **顏色改成連續的**（原本是「滿了才亮」的兩段跳）。飽和度現在只剩顏色這一個通道，兩段跳會讓九成滿和一成滿長得一模一樣，瓶頸在真的塞爆之前完全看不出來。用 `sat²` 而不是線性，保住「第一台發電機讓那條線當場亮起來」那個 B0.3 的教學瞬間。

驗證：`determinism_test` 15 → **20 項**，直接釘死使用者回報的那件事——「同樣的流量，升不升級都一樣粗」「流量越大線越粗」「一成滿與九成滿不同色」。`TL_NAKED` 截圖複驗：沙盤上滿載的那條 45° 電線現在明顯又粗又亮，2 合金/秒的線最細——**粗細第一次真的在講流量**。

## [0.8.0] — 2026-07-29 — B1.1 第三資源：合金、熔爐、碎浪

★ **G1 閘門通過**（使用者實玩後：「我覺得遊戲不錯，可以繼續做」）→ 進入 **M1 垂直切片**。

### 合金網（第三種資源）

- **熔爐 Smelter**：80 礦砂，吃 8 礦砂/秒 ＋ 10 能量/秒，產 2 合金/秒。**待機也吃電**——這是它與塔最大的不同，蓋下去那一刻起你的塔就少了 10 能量/秒。
- **解算順序固定為礦砂 → 能量 → 合金**。熔爐的產出乘上「兩個滿足率中較低的那一個」（不是相乘：50% 電 ＋ 50% 礦不該變成 25% 產出）。
- **合金與礦砂同一條入帳規則**：要經導管送達核心才算。熔爐因此是全圖唯一的「三路匯流」擺位問題——礦、電、核心三條線都要規劃。

### 合金的兩個去處（＝核心取捨在第三種資源上再演一次）

- **導管加粗 2/3 級要合金**：1 級 20 礦砂（不變，B0.3 的教學動作不被鎖掉）／2 級 40 礦砂 ＋ 20 合金／3 級 60 礦砂 ＋ 50 合金。
- **碎浪 Breaker**（新角色）：140 礦砂 ＋ 60 合金，交戰 12 能量/秒，濺射 2.5 格。**單體 dps 13.2 比錨的 21.6 還低**，要同時打到 5 隻才追平「同樣 12 能量的三座錨」——濺射是對敵人排隊方式的賭注，不是免費傷害。物理傷害 → 打不動甲殼，剋制表沒被壓平。

### HUD

- 頂欄新增「合金 N ▲x/秒」，緊貼礦砂（兩個都是造價貨幣）。**擠掉的是「節點 N　導管 M」**——沒有任何決定依賴那個計數。
- **合金流動珠用紫 `alloy.violet`，不是鋼銀**：鋼銀的明度和導管的青幾乎一樣，珠子會融進線裡，重演 B0.6「看不到貨物流動」。三種資源靠色相分開：近白／琥珀／紫。
- 能量收支面板新增「熔爐」一列。**B1.1 之前的算式會把熔爐那 10 能量/秒記到儲槽充能頭上**——那一列會開始說謊。
- 優先權面板 7 → **9 列，改雙欄**：左生產（發電機／熔爐／核心／儲槽）、右防線（五隻塔）。**那條分界就是這個面板在問的問題。** 順帶修掉它和能量面板疊了 66px 的舊位置。
- 建造欄 8 → 10 顆鈕，間距收緊到 2px（44px 觸控高度不能縮，手機移植前提）。

### 驗證

- 測試 346 → **380 項**：`flow_test` ＋11（合金端到端：入帳、缺電降速、送不掉滿溢、兩張網互不干擾）、`combat_test` ＋4（濺射與「不強過錨」）、`build_test` ＋5（2/3 級的合金收支）。
- **`TL_PANEL=sandbox`**（新）：沙盤「靜水」——沒有敵人，只有三種資源同時在跑。**合金流動珠只有這張圖拍得到**（淺灘的示範佈局沒有熔爐），`flow_test` 用的是同一張圖同一份佈局。
- `TL_CLICKTEST` 6 → **9 項**：建造欄長到 10 顆之後「最後一顆鈕還點不點得到」、碎浪被擋下的理由是「合金不夠」而不是「鈕沒被按到」、優先權面板整張留在畫面內且不蓋能量面板。
- `TL_SIM=5600` 對過 B0.7 基準：`won` / 核心 1000 / 擊殺 41 / 研究數據 51.6，**逐項相同**——合金沒有擾動既有平衡。

### 修

- 角色簡介裡的 `**任何**` 會原樣印出星號（`Label` 不解析 Markdown，這是第三處，一併掃掉）。

---

## [0.7.4] — 2026-07-28 — B0.7.4 能量面板定住、餵不飽的點名

使用者回報兩件事。

### ①「面板會因為一些小改動一直跳動，導致很難看清楚」

我讓沒有數值的列自己隱藏（當時的理由是「只列這一局真的存在的項目」），而 `PanelContainer` 會跟著內容縮放——於是**每過一個 tick 版面就位移一次**。儲槽從放電轉成充能、回收者緩衝歸零，整張表就上下彈。

**一張一直在動的表讀不了**，這條比「不列無關項目」重要得多。改成**固定尺寸、固定列序**：列不隱藏，沒在作用的留在原位變暗。**位置固定本身就是可讀性**——玩家記得「淨值在第八行」，那一行就不該跑。

### ②「希望有什麼能量不足的也可以顯示在上面」

淨值只說「差多少」，不說「誰在挨餓」——而玩家能採取的動作（拉優先權、加粗那條線、多蓋一台發電機）取決於後者。面板新增點名，最多列三座、其餘收成「…還有 N 座」：

```
淨值　-7/秒　有東西在挨餓
　餵不飽：稜鏡(33, 4) 84%
```

名單直接取三態徽章的 `STARVED` 判定，**與地圖上那個橙色倒三角是同一份資料**——面板上的名字和地圖上的圖示不會各說各話。

### 順帶：浮層背景

預設的 `PanelContainer` 太透明，導管會從字後面穿過去，暗色的次要文字直接讀不到。新增 `UiKit.panel(opacity)`：近乎不透明的底 ＋ 一圈細邊 ＋ `radius.ui`，並**一律 `mouse_filter = IGNORE`**（RG-39 從此由建構子保證，不靠各處自己記得設）。角色簡介維持 0.88 半透明——它刻意要讓人看見底下是什麼。

`TL_CLICKTEST` 現在尊重明寫的 `TL_DEMO_TICKS`：有示範佈局在場時只開浮層、略過建造斷言，用來拍「波次進行中的能量收支」這種**只有互動才到得了**的畫面。

測試 312 項不變（這批全是呈現層）。

---

## [0.7.3] — 2026-07-28 — B0.7.3 45° 導引、能量收支、角色簡介

使用者實玩後提的三點，逐條回應。

### ① 「沒有辦法 45 度放置管道」

**規則其實是通的**——`TL_CLICKTEST` 補上一段用真實輸入拉 45° 導管的檢查，PASS。真正的問題有兩個：

- **說明面板把點擊整片吞掉了。** `PanelContainer` 預設 `mouse_filter = STOP`，而它預設就開在玩家要放第一個節點的那片區域上——**教學把遊戲擋住了**。所有可收合浮層一律改成 `IGNORE`；自動開的那一份也真的會在玩家放下第一個節點時自己收起來（B0.7.1 寫了這個意圖，但只在 `_ready` 判斷一次，等於沒實作）。
- **正斜角難用肉眼找。** 45° 要 `|dx|` 恰好等於 `|dy|`，在 36×19 的網格上不試不會知道。現在選好起點就**在地圖上畫出八條方向導引線**，預覽線依合法性染綠／橙，提示列同步說出差在哪：「✕ 不是直線：橫 4 直 3。要走 45° 兩邊得一樣長；否則先放一個中繼轉彎。」

> **一條規則如果只能靠試錯才知道自己有沒有踩中，它在體感上就等於壞掉。**「不是 45°」對玩家沒有動作可做，「橫 4 直 3」有。

### ② 能量收支面板（底欄「能量」）

頂欄的能量條講的是**結果**（30/37），它不說「這 37 是誰吃的」。新面板逐條攤開並即時跟著跑：

```
＋ 發電機 ×2      +40
＋ 儲槽放電       +10
─────────────
− 塔（交戰 2 座）  37   ※ 待機不耗電
− 儲槽充能        10（受自己那條線的 cap 限速）
淨值 +3/秒  餵得飽
加電：多蓋發電機（每台吃 4 礦砂/秒）
省電：塔蓋少一點，或用「優先權」決定誰先餓
```

**只列這一局真的存在的項目**——一張永遠有八行的表沒有資訊量；有幾行會動、什麼時候動，本身就是資訊。它同時是答案與教材：蓋一台發電機，`+` 那欄當場多 20，**因果在同一個畫面上發生**。

### ③ 角色簡介（底欄「圖鑑」）

滑鼠停在左欄任一建造鈕上，半透明浮層貼著那顆鈕浮出來，移開就收。**先講它在取捨裡的位置，再列數值**——一串沒有脈絡的數字幫不了正在決定「這一分鐘要蓋什麼」的人。內容一律從 `data/NodeDefs.gd` 現算，不另寫文案表（兩份會漂移，而漂移的那份剛好是玩家讀到的那份）。

### 順帶

- 底欄擴到六顆 toggle（快進／提前召喚／優先權／說明／能量／圖鑑），提示列右移讓位。
- 優先權抽屜下移，與能量面板可同時開著不重疊。
- **`TL_CLICKTEST` 與 `TL_SHOT` 併用時不退出**，把驅動完的畫面交給截圖鉤子——浮層要按鈕才會開、簡介要 hover 才會浮，**有些狀態只有互動才到得了**，沒有這條就永遠拍不到它們。上面三項的截圖都是這樣拍的。
- `TL_CLICKTEST` 由 2 項檢查擴到 6 項（放置／路徑格拒絕＋原因／45° 節點／45° 導管／能量面板有內容／簡介有內容）。

測試 312 項不變（這批全是呈現層與輸入層）。

---

## [0.7.2] — 2026-07-28 — B0.7.2 地圖點不動（一個活了五批的缺陷）

**使用者回報：「我點左鍵都沒有用，無法放置，也無法建造。」** 是真的——**從 B0.3 到 B0.7.1，整張地圖從來沒有一次點得動。**

### 兩層原因

1. **`Battle` 的 `size` 一直是 `(0, 0)`。** 建立時用的是 `set_anchors_preset(PRESET_FULL_RECT)`，那個函式**只設錨點、不動 offset**。而 `_draw()` 用的是絕對座標、`CanvasItem` 的繪圖不受 Control 尺寸裁切——於是**畫面 100% 正常，滑鼠命中區卻是空的**。這是「看起來對」與「真的對」差最遠的一種缺陷。修法：`set_anchors_and_offsets_preset()`。
2. **父節點 `Main` 是滿版的 `Control`，預設 `mouse_filter = STOP`**，它在 GUI 那一層就把滑鼠事件吃掉並標記為已處理，所以 `_unhandled_input` 也永遠不會被呼叫。修法：本畫面改用 `_gui_input`。

兩層都要修才會通——只修任一層，地圖依然點不動。

### 為什麼拖了五批沒被發現

**整個輸入層是零覆蓋，而所有既有的驗證手段在結構上都繞過它**：

- `TL_SIM` 不開視窗；
- `TL_PANEL` 的示範佈局是**用程式呼叫 `BuildController`**，不經過輸入層；
- 312 項斷言全部直接呼叫 `BuildController` / `BattleController`；
- 而「絕不在沒有測試鉤子的情況下開遊戲視窗」這條紀律，讓我也沒有手動點過。

**可稽核的教訓（`50_QA_PLAN.md` RG-38）**：**一條路徑如果所有自動化驗證都繞過它，它就等於沒有被測過**——不管其他測試有幾百項。截圖證明「畫得對」，`TL_SIM` 證明「算得對」，**兩者都不證明「按得動」**。凡是只有真人會走的路徑（輸入、焦點、拖曳），都要有一個走完整管線的檢查。

### 新鉤子 `TL_CLICKTEST=1`

開局內畫面，用**合成滑鼠事件真的點地圖**（走 `Input.parse_input_event()` 完整管線，不是直接呼叫 `_act()`——那樣測不到 `mouse_filter` 與命中區這兩層，也就測不到這次的缺陷），驗證兩件事後印 PASS/FAIL 並退出：點礦點會蓋出節點、點路徑格會被擋下**且說明原因**。

⚠ **這是唯一一個不能 `--headless` 的檢查**：dummy display server 不做 GUI 滑鼠命中測試，`_gui_input` 根本不會被呼叫（我第一次寫這支自檢時就是在 headless 下拿到 FAIL，差點以為修法錯了）。

```bash
TL_CLICKTEST=1 TL_MUTE=1 <godot> --path godot --rendering-driver opengl3
```

測試 312 項不變（那 312 項全部繞過輸入層——這正是重點）。

---

## [0.7.1] — 2026-07-28 — B0.7.1 操作說明

**使用者拿到 B0.7 的 build，第一句話是「你沒教我要怎麼操作，怎麼玩」。**

我在 B0.7 把 `50_QA_PLAN.md` §4.4 的第一條（「新玩家開局後 15 秒內知道要做什麼嗎」）打了勾。那個勾是錯的，而**失效的方式值得記下來**：我逐條檢查的是「資訊在不在」，那一條問的其實是「玩家會不會操作」。提示列當時寫的是「先在礦點上蓋一台採集器」——資訊在、目標明確，**但畫面上從頭到尾沒有任何一個地方說過「蓋」＝左欄選一種、再左鍵點地圖**。對已經知道怎麼玩的人那句話是完整的；對新玩家它少了唯一重要的那一半。

修法三件：

1. **提示列改成兩行。** 上行永遠是「下一步」，下行才是當下這個動作的細節。B0.7 讓放置預覽**取代**下一步，於是玩家把滑鼠移到地圖上——正是最需要指引的那一刻——指引就消失了。
2. **每一句提示都講出「按哪裡、點哪裡」。** 「左欄點『採集器』→ 左鍵點地圖上任一個青色空心圓」，不是「先蓋一台採集器」。只講目標不講動作的提示，對會玩的人是提醒，對新玩家是零。
3. **新增「操作說明」抽屜**（底欄）：全部操作、三條建造規則、walk-by 的安全距離、塔的耗能模型。**空地圖時預設開啟，放下第一個節點之後就不再擋路**——不用存檔旗標（那要跨局狀態），用「這一局蓋了東西沒有」判斷就夠了。收合後地圖 100% 可見，展開時也不蓋到路徑與跨越點（§6.2 硬性要求 5）。

**順帶修掉一個顯示缺陷**：幾句提示裡混進了 Markdown 的 `**強調**`，而 `Label` 不解析 Markdown，畫面上直接印出星號。

`50_QA_PLAN.md` §4.4 補上三條可稽核的教訓，往後每批照用——其中第二條是：**自檢要問「這句話對一個沒讀過程式碼的人夠不夠」，不是問「這個資訊有沒有被顯示」**；我讀得懂畫面是因為我知道輸入怎麼接的，而那正是這條驗收要排除的東西（和 §4.5 用 `TL_NAKED` 遮數字是同一個道理）。

測試 312 項不變（這批全是呈現層）。

---

## [0.7.0] — 2026-07-28 — B0.7 M0 打磨與 G1

M0 最後一批。**交付可玩 build，樂趣判定交給使用者**（`50_QA_PLAN.md` §0.1：agent 只驗程式碼與可讀性）。

### 確定性：完整局狀態雜湊

`SessionState.state_hash()` 把權威狀態攤成標準字串再 `sha256_text()`。`determinism_test` 由 8 項補到 **15 項**，`pending` 清空。

三件靠它才測得到的事：

1. **同一組 `(seed, ops)` 跑 1200 tick → 同一個雜湊。** 這是每日挑戰雙榜（B2.2）的地基——統一配置榜是本作對 P2W 的唯一制衡，它的公平性完全踩在這條性質上。沒有這個測試，「同種子同地圖」只是一句宣稱。
2. **`700+500` tick ＝ `1200` tick。** 跨 tick 的累加器（射速、儲槽充能、回收者緩衝）不得偷藏幀率相依的東西。
3. **反向對照**：少蓋一條導管、差一個 tick，都必須得到**不同**的雜湊。沒有這條，前兩條可以被一個常數雜湊全部騙過。

紀律：遍歷依 id 排序（順序不是狀態的一部分）；不雜湊 `rates`（推導值放進來只會讓同一個缺陷被記兩次）；用 `sha256_text()` 不自己寫雜湊（順帶繞開「hash 常數超過 int64 會在剖析期溢位」那顆地雷）。

### 首次體驗（`50_QA_PLAN.md` §4.4）

這是 G1 的前提：使用者要實玩 20 分鐘判斷核心循環好不好玩，**如果開局 15 秒不知道要幹嘛，G1 量到的就不是「好不好玩」而是「我看不懂」**。

- **常駐的「下一步」提示**：一句讀當前狀態算出來的話，順序＝這張圖真正的依賴鏈（礦→入帳→電→塔→接電→召喚）。**不是教學系統**——沒有進度旗標、沒有步驟機、不持久化，玩家拆光重蓋它就跟著退回去。做成教學系統的話它會變成第二套要維護的狀態，而且擋在玩家和遊戲之間。
- **來襲方向箭羽**（`20_ART_DIRECTION.md` §1.6）：一條帶子只說「這裡是路」，**沒說潮從哪邊來**——而那是新玩家的第一個問題，也決定所有擺位。順著箭羽走就找得到核心，一個元素答兩題。
- **工具鈕有選中狀態**：11 個建造／模式鈕共用一個 `ButtonGroup`，任一時刻恰好一個按下——那正好就是這個 UI 的真相。用 Godot 內建的 toggle 群組，不自己維護一套高亮。之前完全沒有選中指示，玩家只能從提示列的文字推自己在哪個模式。
- **優先權鈕改成 toggle**：抽屜開關本來就是一個開關，按下狀態才**真的**等於「抽屜開著」。之前它拿到焦點時的外框看起來就像被選中。

### 修掉的兩個缺陷

1. **提示列會說謊。** 它只在滑鼠移動時更新，於是「礦砂開始入帳了」「波次開打了」這類轉折要等玩家碰一下滑鼠才反映——手不動的那幾秒，提示列講的是舊狀態。改成每幀重算。（這個 bug 是在讀 B0.7 的截圖時當場抓到的：畫面上明明有兩台採集器，提示列還在叫玩家去蓋採集器。）
2. **`GameState.BATCH` 連兩批沒同步**，標題畫面掛著 `B0.5` 而版本已經是 `0.6.1`。**直接刪掉這個常數**——批次編號本來就編在 `VERSION` 裡（`0.<批次序號>.<修補>`），兩個必須手動保持一致的常數遲早會不一致。批次名稱在 `CHANGELOG.md` 與 git log。

### 交付

`build/TideAndLine.exe`（Windows x86_64，內嵌 pck，**109 MB**）。匯出的 build 以 `TL_SIM=900` 對過原始碼 build：`kills 2`／`salvage 6.0`／`silo 292.1`／稜鏡 `缺料`，逐項相同。

> **exe 體積超出目標**：`30_TECH_DESIGN.md` §5 的目標是 < 80 MB（可接受 < 150 MB）。目前沒有任何美術素材，這 109 MB 全是 Godot 執行檔模板；要降只能改用自建的精簡模板，排 **B1.7**，不在早期批次花這個時間。

**測試**：八支合計 **312 項**（flow 33／build 62／combat 77／tide 49／save 31／determinism 15／hud 45）。

---

## [0.6.1] — 2026-07-28 — B0.6.1 流動珠

**使用者實看 B0.6 的畫面後回報**：「貨物跟管道的畫面對比有些不清楚，看不到貨物流動的感覺。」

診斷正確且指到了一個真的缺口：線寬與顏色說的是「這條線**有多滿**」，**沒有任何元素在說「東西正在動」**。一張靜態的粗線和一張靜態的細線都不會動，玩家看到的是一張接線圖，不是一條產線。

**流動珠**（`20_ART_DIRECTION.md` §1.4a）：導管上跑一列小珠子。

- **礦砂近白、能量琥珀**。同一條幹線同時走兩種資源時，看得出**礦砂往核心走、能量往塔走**——這在示範佈局的主幹線上就是兩列反向的珠子。
- **每顆珠子先畫一圈 `bg.deep` 底再畫亮芯**。導管本身就是亮青，亮青珠子畫在亮青線上等於沒畫——深色底是這個元素能被看見的唯一原因，也正是使用者反映的那個對比問題。礦砂珠因此從 `order.bright` 改成 `text.primary`（近白）：`order.bright` 與線色同色相，只讀得出「這裡有個洞」。
- **珠子大小跟著線寬走**（`clamp(w × 0.32, 1.4, 3)`）。固定大小的珠子會把 2px 的細線戳成虛線，把「線寬＝流量」這條 R-3 的地基蓋掉；跟著線寬走則是**強化**它。
- **速度 ∝ 流率**（固定間距 20px）。速度是第三個獨立通道，與線寬（飽和度）、顏色（狀態）不重複。
- **流率 < 0.05 就完全不畫**。順帶多出一個免費的可讀性訊號：待機的潮鳴那條支線在一片流動中是**靜止**的，「哪條線是死的」一眼可見。
- 只有一種資源在跑時走**線的正中央**；兩種都在跑才分兩排。多數導管屬前者，硬分兩排會讓珠子懸在細線外面像掉出來的東西。

**★ 這不是實體物品搬運。** 珠子由已解出的流率 ＋ 經過的模擬秒數推算，模擬層一無所知，不參與任何判定（`10_GDD.md` §3.1 鎖定：流量網路，非物品實體模擬）。一旦它變成有位置有狀態的實體，`O(物品數)` 的效能與確定性負債就回來了。

**升級刻度改為垂直短刻線**（原為圓點）：圓點會和流動珠混成同一個東西，而兩者語意相反（固定的線材等級 vs 變動的流量）。

**解算器回寫多一個 `conduit_net`**：每條導管的淨流率 `Vector2(礦砂, 能量)`，沿建造時的 a→b 為正。原本的 `conduit_flow` 只留了大小（線寬要的就是大小），珠子還需要方向與資源別。`hud_test` 加 3 項斷言鎖住「反過來蓋同一條線 → 淨流率恰好反號」——珠子往錯的方向跑，玩家會照著一張反過來的流向圖去診斷瓶頸。

**測試**：`hud_test` 42 → **45 項**；八支合計 **305 項**。`reduce_motion` 下的行為（珠子停止移動但仍畫）規格已寫進美術文件，實作與驗收排 **B1.6**（設定畫面 B1.4 才有）。

---

## [0.6.0] — 2026-07-28 — B0.6 HUD 與可讀性

M0 第六批，**R-3（流量網路可讀性）的正式驗收**。前五批把模擬做對了，這一批要證明那些對的東西**看得出來**。

**節點三態徽章**（`10_GDD.md` §3.1）：`缺料`＝橙色倒三角、`滿溢`＝青色正三角、**`正常` 不畫任何東西**。第三態的視覺編碼是「乾淨」——一屏 14 個節點全掛上「我很好」的圖示，等於把要找的那兩個異常埋進 14 個雜訊裡，那正好與 R-3 相反。

**兩態各自只掛在跟它有關的節點上**：`缺料` 只給**自己宣告過需求**的（發電機的燃料、交戰中的塔），`滿溢` 只給**自己產得出東西**的（採集器、發電機）。核心的礦砂需求（收剩下的）、儲槽的充能需求（有多少收多少）、中繼手上塞著的餘量，這三種在正常遊玩中會恆亮——照字面掛上去，例外標記就退化成背景雜訊。這兩條各有一組 `hud_test` 斷言在守。

**能量列改成一條圖形**（§6.2 硬性要求 1）：琥珀＝供給、右段橙色脈動＝短少的部分、細刻度＝需求落在哪裡。因此它**在 `TL_NAKED` 下要留著**——`TL_NAKED` 遮的是「數值標籤」，一條長度即資訊的橫條屬於線寬／顏色那一類，遮掉它等於把要驗的東西一起遮掉。

**提前召喚**（§3.4／§7.6）：按鈕常駐顯示當前倍率（`1 + 剩餘比例 × 0.5`，上限 1.5），**倍率在按下當刻算一次並鎖給那一波**——波次開始後倒數就停了，事後再算一律拿到 1.0，賭注會憑空消失。倍率乘在該波的礦砂掉落上。

**局末結算**：波次表跑完進 `won` 階段（模擬隨即停住，不再空轉排出空波次）。結算把研究數據**逐項攤開**——`10 × 波次` ＋ `2 × 產能積分` ＋ `提前召喚累計加成`——一個總分教不會玩家下一局該改什麼。§7.6 原本只給公式骨架，這批把三個詞的可執行定義補進 GDD（其中「總產出」＝**送達核心的**礦砂，與頂欄 `▲/秒` 同口徑）。

**階段色調**（§6.2 硬性要求 4）：準備期偏冷青、戰鬥期加暖橙。量到的差是背景由 `R19 G37 B52`（藍調 B−R ＝ +33）變成 `R40 G42 B47`（近中性 B−R ＝ +7）。

**兩個順手修掉的東西**：

1. **`TL_SHOT` 下模擬凍結**。舊寫法在截圖前的 3 秒延遲裡繼續跑 tick，於是**截圖落在第幾 tick 取決於這台機器那 3 秒跑了幾幀**——同一份佈局在不同機器上會拍出不同數字，截圖不能拿來做回歸比對。現在 `TL_DEMO_TICKS=N` 與 `TL_SIM=N` 是同一個局面（新鉤子 `TL_DEMO_TICKS`，預設 860）。
2. **示範佈局不再假裝開波**。舊的 `_demo_layout()` 直接呼叫 `start_wave()` 把準備期跳過去；B0.5 沒差別，但加上提前召喚之後那等於一次滿額的下注（倍率 1.5），截圖的掉落數字會和 `TL_SIM`（自然開波）對不起來。現在它推的是 tick，讓階段機自己跑。

**同時刪掉**：`_draw_engaged()` 的橙色缺口弧——它和 `缺料` 徽章編碼的是同一件事，留一個就好。

**測試**：新增 `tests/hud_test.gd` **42 項**；八支合計 **302 項**（flow 33／build 62／combat 77／tide 49／save 31／determinism 8／hud 42）。`sim/FlowNetwork.gd` 多回傳一個 `stuck`（迭代結束時推不出去的餘量），它一直都在迴圈裡，只是以前沒回傳。

---

## [0.5.0] — 2026-07-28 — B0.5 塔與取捨

M0 第五批，**核心取捨第一次成立**：同一份能量，餵塔還是餵生產線。

**四隻角色**（§7.4 原數值，`data/NodeDefs.gd`）：錨 50／交戰 4/秒、稜鏡 120／**20/秒**、潮鳴 90／9/秒（減速 −40%、破甲 −25%）、回收者 100／7/秒。**待機耗能 0**——塔蓋著不打時電網完全感覺不到它，約束因此是**峰值電力**不是平均電力。

**戰鬥解算**（`sim/Combat.gd`，純函式零 RNG）：護甲減法／屏障百分比、能量不足時**射速 = 基礎射速 × 滿足率**（線性、不停火）、全域擊殺回收 25% 礦砂、回收者射程內任何死亡回收價值 60% × **匯率 5** 為能量。**依節點類型的優先權面板**（7 列，含儲槽）。

**四條在實作時補齊的設計**（已回寫 `10_GDD.md`）：

1. **稜鏡的「直線」＝穿過自己的四條軸線中敵人最多的那一條。** 塔不替玩家選任意角度，擺位才能預測；限定四軸把稜鏡變成一道**對齊謎題**（與某段路徑同列就能一次貫穿整段）。
2. **回收的礦砂直接入帳，回收的能量必須走自己那條導管。** 這條不對稱是模型逼出來的：礦砂有全域帳戶（核心＝礦砂銀行），**能量沒有**。回收能量因此與儲槽同構——受自身 `cap` 限速、先供自己的交戰耗能、**緩衝上限 100**（沒有上限的話回收者就是一座沒造價、沒容量欄位、也沒被列進優先權面板的第二座儲槽）。
3. **多座潮鳴不疊加，取最強**；破甲吃護甲、不吃屏障（屏障的剋制方式是物理傷害）。
4. **塔的生命值 60**（原表沒有這一欄）。四隻同值，是**待調的佔位數**不是校準結果。

**★ 修掉一個看不見的缺陷：導管本來是有方向的。** 方向就是「玩家先點了哪個節點」——畫面上完全看不見，也無從選擇或修正。B0.5 才讓它現形：塔是末端消費者，「先點塔、再點幹線」是最自然的手勢，拉出來的線卻只能從塔往外送電，於是那座塔**永遠是 0 電**，而畫面上那條線看起來一切正常。**導管現在無方向**（一條管子兩頭都能送，`10_GDD.md` §7.2）。

連帶修掉解算器裡兩個只有在雙向邊上才會現形的錯誤（`30_TECH_DESIGN.md` §2.5）：

- **`reach` 是「對每個推送者各自成立」的量**，算的時候必須排除推送者自己。否則 A 會看到「經 B 下去還有需求」，而那份需求其實要繞回 A——資源在兩點間來回彈，一條 cap 28 的幹線報出 29.26 的流量。
- **舊寫法把「遇環回傳的 0」寫進全網共用的 memo**：只要某節點碰巧先從反方向被展開，它與上游全部節點就被永久釘成 0，整條產線一滴都送不出去。

**另外兩個會讓瓶頸圖說謊的顯示錯誤**：`conduit_flow` 把礦砂與能量**相加**（兩者各有獨立 cap），一條同時走礦與電的幹線因此報出超過 cap 的流量、被渲染成滿載 → 改為取最大值；開火線只留一個 tick，60Hz 下每 20 幀才閃一次 → 留 3 個 tick 並淡出。

**測試**：`combat_test.gd` 由 8 條待補寫成 **77 項斷言**；七支合計 **260 項全過**（flow 33／build 62／combat 77／tide 49／save 31／determinism 8／balance 2）。新增回歸項 RG-24～RG-29。

**收尾的精簡回合**（`/ponytail-review`，淨 −39 行）：`engaged()` 本來回傳射程內的敵人索引，但五個呼叫端全都只問「空不空」，改成回傳布林；回收者的供給本來先跟自己的交戰耗能相抵、事後再修正滿足率——解算器本來就會讓節點先吸收自己手上的供給，那 11 行是在重做它免費做的事（頂欄的能量供給／需求因此各多顯示 7，那才是塔真正的用量）；光環一個 tick 算兩次改成算一次共用；放置預覽的射程／射速／傷害／光環四段從單行提示列拿掉（射程在滑鼠底下已經畫成一個圈）；刪掉只剩一個呼叫端的 `is_tower()` 與 B0.4 起就沒人叫的 `_damage_tint()`。

**一個被浮點數吃掉的子彈**：`Vector2` 的分量是 32 位元浮點，把跨 tick 的射速累加器塞進去，每個 tick 截斷一次，100 個 tick 後誤差大到吞掉整整一發——**塔會因為容器型別少打一發**。改回傳 `Dictionary`，並加一個遠大於累積誤差、遠小於一發的常數扶正 `0.12` 這種在浮點裡除不盡的射速。

---

## [0.4.0] — 2026-07-27 — B0.4 波次與敵人

M0 第四批。**第一次會輸。** 敵潮沿路徑推進、打斷你的線、走到核心把它啃到歸零。

**walk-by**（`sim/Tide.gd`，純函式）：敵人**永不停步**，每 tick 傷害相鄰 1 格內的建築，然後繼續走。`advance()` 的簽章裡**根本沒有建築參數**——擋路這件事在型別層面就不可能發生，這比任何斷言都能防住「便宜中繼當肉盾」的迷宮塔防後門。**只有核心讓它們駐足**（路徑終點就是核心格）。

**設計上補齊的一條規則**（已回寫 `10_GDD.md` §3.5）：**橋的免疫範圍含引道（跨越點 ±1 格）**。只免疫橋面那一格的話這條規則等於不存在——敵人走在路徑格上、破壞半徑 1 格，而導管垂直穿過路徑必然佔用「橋 ＋ 兩側各一格」，兩側正好都在半徑內，**每一條過橋的線都會在引道被打斷**。這也正是 `20_ART_DIRECTION.md` §1.6 已經畫出來的樣子：橋面兩端本來就伸出格外。

**三種敵人**（§7.5 原數值）：漂蟲 60 血／1.0 格秒／8 傷、甲殼 180／0.6／14（護甲 8）、熾泳 90／1.6／10（屏障 40%）。護甲與屏障要到 B0.5 有塔開火才用得上，先進資料表。**淺灘的 5 波是手作的**，前兩波只出漂蟲（§7.7：難度只用玩家看得見的關卡參數）。

**時間流**：準備期 60 秒（關卡宣告，計時器在畫面上）、**快進 4× 只在準備期可按**（開打時按鈕變灰、`speed_mult` 強制歸 1，`10_GDD.md` B5）。快進是**多跑幾個 tick**，不是把 tick 拉長——固定時間步不能動，否則同一組操作在不同倍率下會跑出不同結果。失敗浮層的「立刻重來」是 1 次點擊（QA §4.4）。

**截圖當場抓到的兩個可讀性缺陷，當批修掉**：

1. **受損指示與滿充能的儲槽長得一樣**。兩者都是 12px 的琥珀／橙色圓弧，在那個尺寸上肉眼分不出來。改成**節點下方的血條**——靠顏色區分不夠，形狀不同才分得開。
2. **波次結束回到準備期時，核心還在掉血卻沒有解釋**。駐足在核心的敵人不隨波次結束而消失（§3.5），頂欄補上「殘敵 N」。

**測試**：新增 `tide_test.gd` **49 項斷言**；七支合計 **185 項**（flow 33／build 62／tide 49／save 31／determinism 8／balance 2；`combat_test` 待 B0.5）。

**兩個被測試釘住的數字**：一隻漂蟲打斷貼路徑的導管（7 格路徑打得到它 → 7 秒 × 8 = 56 > 40 血）卻打不死中繼（只有 3 格打得到 → 24 < 30 血）。**暴露量隨長度成長**——「貼路徑走的線短、便宜、危險」在數字上就是這個形狀。

---

## [0.3.0] — 2026-07-27 — B0.3 建造與地圖

M0 第三批。**第一次看得到東西在動**：一張 36×19 一屏可見的測試圖「淺灘」、四種可蓋的節點、可拉的導管、會隨流量變粗變亮的線。

**拍板的設計（使用者決定）：核心＝礦砂銀行。** 礦砂必須經導管送達核心才計入帳上。理由與後果寫在 `10_GDD.md` §7.3：

- 核心在敵人路徑終點＝最危險處 → **產線天生要往危險的方向拉**，「你的生產線就是你的防線」變成字面事實。
- 兩台採集器（12/秒）就塞爆基礎 `cap` 10 的幹線——比發電機更早教會讀瓶頸。
- **核心只收剩下的**：它的 `demand` ＝ `max(0, 全網供給 − 其他消費者需求)`，燃料永遠優先於入帳。於是「礦砂 ▲0/秒」是一句完整的診斷。

**另外兩條落地的設計**（都是既有權威文件的直接推論，不是新發明）：

| 決定 | 來源 |
|---|---|
| **導管只能走水平／垂直／45°**，其他角度要用中繼轉折 | `20_ART_DIRECTION.md` §0 已把「秩序側＝直線、90°/45° 轉折」寫成核心視覺命題。**限制走向不是簡化，是讓 15 礦砂的中繼有存在理由** |
| 導管 `cap` **每種資源各自計** | 混線在實際打法裡罕見（礦砂線與能量線本就不共用）。有意義的混線玩法出現時再改為共用額度 |

**新檔案**：`sim/Build.gd`（走向／佔格／造價／合法性，**純函式**）、`game/SessionState.gd`（局內狀態，無規則）、`game/BuildController.gd`（錢與落地＋預覽文案）、`game/BattleController.gd`（固定時間步 tick：**先礦砂、後能量**，發電機產出乘上自己的礦砂滿足率）、`screens/Battle.gd`、`data/NodeDefs.gd`、`data/Maps.gd`。

**`TL_SIM` 現在跑真的局**（不再是格式骨架），而且跑的是與 `TL_PANEL=battle` 截圖**同一份**示範佈局——截圖與數字不會各說各話。

**測試**：新增 `build_test.gd` **62 項斷言**；六支合計 **136 項**（flow 33／build 62／save 31／determinism 8／balance 2；`combat_test` 待 B0.5）。

---

## [0.2.0] — 2026-07-27 — B0.2 流量網路

M0 第二批。**全案的心臟演算法**：`scripts/sim/FlowNetwork.gd`，純函式、零副作用、零系統 RNG。仍然沒有玩法可看——這批交付的是一套被 33 項斷言鎖住的規則。

**解算器**（`30_TECH_DESIGN.md` §2.5）：一次只解一種資源。收集供需 → 依「下游還送得到多少需求 × 節點類型優先權」前向傳播（每條邊受 `cap` 截斷）→ 削減成 satisfaction → 回寫每條邊的 `flow` 供渲染算線寬。有環圖迭代上限 3 次。

**被斷言鎖住的四條設計承諾**：

| 承諾 | 斷言 |
|---|---|
| **無全域資源池**（RG-18） | 空儲槽的結果與**沒有儲槽完全相同**；拆掉儲槽後赤字立刻變成 `satisfaction = 40/63` |
| **儲槽受自身導管 cap 約束**（RG-19） | GDD §7.4 的 −23 缺口：cap 10 只放得出 10、**升到 cap 28 才放得出 23**。豁免這條就等於偷偷退回水池模型 |
| **優先權讀節點類型，不讀節點** | 節點自帶 `priority: 99` 欄位被無視；供給 12 對優先權 3/2/1 分成 6/4/2 |
| **採集器無輸入需求 → 網路永不死鎖** | 零連線的採集器仍產出；全網嚴重飢餓時其 satisfaction 恆為 1.0 |

**設計上補齊的一條語意**（已回寫 `30_TECH_DESIGN.md` §2.5）：**儲槽放電以全網赤字為上限**。原規格只寫 `min(充能, 導管 cap)`，照字面實作會讓 cap 28 的儲槽在 −23 缺口下放出 28——多的 5 流進網路無人可用，玩家看到的是「儲槽掉得比赤字還快」的無故漏電。**充能側則不封頂**：充能中的儲槽是普通消費者，搶不搶得贏熔爐是優先權滑桿要裁決的事，不該由解算器代勞。

**自檢抓到的一個真缺陷**：節點在同一次拜訪裡重新分配出邊時，用的是迭代開始時算的下游需求——同一份需求因此被服務兩次（環路測試中 `flow` 變成應有值的兩倍）。改為**推送後即時從 `reach` 扣掉已認領的需求**。這個 bug 在無環圖上完全看不出來，是環路測試把它逼出來的。

**測試**：`flow_test.gd` 33 項斷言全過；五支合計 **74 項**（flow 33／save 31／determinism 8／balance 2；`combat_test` 待 B0.5）。

**尚未有可跑的局**：地圖、節點放置與 `SessionState` 在 B0.3 才長出來，所以 `TL_SIM` 仍是格式骨架（註解已同步指向 B0.3，不再宣稱「模擬層於 B0.2 建立」）。

---

## [0.1.0] — 2026-07-27 — B0.1 地基

M0 第一批。**沒有任何玩法**，全部是後面 36 批都要踩在上面的基礎建設。

**Godot 專案**（`godot/`）：4.7、`gl_compatibility`、1280×720、stretch `canvas_items`/`expand`（手機預留條款 P1）。

**四層架構的骨架**（`30_TECH_DESIGN.md` §2.1）：`core/` 服務層 5 個 autoload、`render/` 美術 token、`ui/` 共用 helper、`screens/` 標題畫面。`sim/` 與 `game/` 留到 B0.2 有東西可放時才建。

**測試鉤子全套**（§4.1）——這是「能不能規模化驗證」的分水嶺，所以排在第一批：

| 鉤子 | 狀態 |
|---|---|
| `TL_SHOT` / `TL_SHOT_DELAY` | ✅ 等 N 秒 → 抓 viewport → 存 png → 退出 |
| `TL_MUTE` | ✅ 全域靜音；`TL_SHOT` 存在時自動套用（鐵律 2） |
| `TL_SEED` | ✅ 覆寫種子，`Rng` 為唯一擁有者 |
| `TL_SIM` | ✅ headless 跑完輸出 JSON 後退出（B0.1 為格式骨架，模擬層在 B0.2） |
| `TL_PANEL` | ✅ 已接線；目前只認得 `title`，未實作的面板警告但不當掉 |
| `TL_NAKED` | ✅ 隱藏數值標籤；真正的工作對象（HUD 數字）在 B0.6 出現 |

**鐵律 1 落地**：任何 `TL_*` 存在 → `SaveService.persist = false`。判定走純靜態的 `Env`，各 autoload 各自讀取、彼此不引用，避免 `_ready()` 順序變成隱性依賴。

**存檔**：`SAVE_VERSION = 1`、完整 schema 預設值、遞迴補預設（**玩家資料永遠優先，只補不覆蓋**）、遷移鏈骨架、原子寫入（寫 `.tmp` → 讀回驗證 → 換主檔）。

**美術 token 落地**：`Palette.gd` 是全專案唯一色值來源；`Motion.gd` 時長階與緩動；`Shapes.gd` 網格與**導管線寬公式**（`2 + 6 × flow/cap`，R-3 可讀性驗收的地基，已寫成可斷言的純函式）。

**CJK 字型**：`UiKit.theme()` 的 `SystemFont`（微軟正黑體 → Noto Sans TC → …）。截圖確認非豆腐字。

**測試**：五支全過，實斷言 41 項，19 條待補項全部印在輸出裡（沉默的空測試會讓人誤以為那塊已經被守住）。`save_test` 涵蓋 schema 補值、玩家資料優先、殘缺輸入、純函式性、深複製，以及**實際磁碟往返**（偵測到既有存檔即整段跳過，絕不覆蓋玩家進度）。`determinism_test` 釘住有種子產生器的可重現性與導管線寬公式。

**自檢發現並當場修掉**：畫面上的鉤子列原本連值一起印，`TL_SHOT` 的絕對路徑橫跨整個 1280px（小解析度會裁切，違反 P1）。改為畫面只列名稱、值只進 stdout。新增回歸項 RG-22、RG-23。

---

## [未發布] — 前期製作

### 2026-07-27（第三次修訂）— grilling 逐條拷問：開工前 15 條定案

六份文件全部收斂到可開工狀態。**這一輪不新增設計，只把互相矛盾、指向死值、與沒有執行者的東西處理掉。**

**修掉的矛盾與死值**

1. **待機耗能已歸零，但三處對策還指著它**——`§7.7` 新手係數 `塔待機耗能 ×0.6`（0×0.6=0，R-4 的對策從未生效）、`§7.8` 科技「能量效率 I/II/III」（12 個 M1 節點裡有 3 個在打折零）、`§4.1` 與 B0.5 DoD 的「待機與開火」措辭（v0.2 漏改）。科技改指**交戰耗能 −8%/級**；**新手係數整節刪除**。
2. **建造規則自相矛盾**——`§3.2` 說路徑格禁建、`§3.5` 說敵人攻擊「路徑上的導管」。新增**跨越點（橋）**：路徑上唯一可鋪導管處，節點仍禁；**橋是架高的 → 橋上導管不受攻擊**。
3. **敵人 AI 未定義**——改為 **walk-by**：永不停步、每 tick 傷害相鄰 1 格、只有核心讓其駐足。堵死「便宜中繼當肉盾」的迷宮塔防後門。
4. **「本作最重要的 UI 元件」有兩份規格**——`§3.1` 寫依節點、`§6.2` 畫依類型。定為**依節點類型**（5–8 條滑桿恆在同位置，儲槽佔一格）。
5. **能量模型有兩種**——`§6.2` 的 `340/520` 是存量、`§3.1` 的解算是流率。定為**流率 ＋ 儲槽在地緩衝，無全域水池**；儲槽充放電受自身導管 cap 約束。
6. **回收者的「價值 60% 轉能量」沒有匯率**——定為 **1 礦砂 = 5 能量**（沿用發電機轉換比）。照字面 1:1 的話回收者是淨耗電，「打破峰值約束的鑰匙」是假的。
7. **導管 cap 10 < 發電機輸出 20** 從未被說明——明文化為設計：第一台發電機就教你讀瓶頸，加粗幹線是取捨。
8. **升級材料只有 tycoon 一條路**——與「塔防不依賴 tycoon」矛盾（推得完戰役卻拿不到等級軸 → 上不了高難度層）。補上**塔防局末結算**這條路，B2.7 以斷言鎖住。
9. **版本編號自相矛盾**——原寫「M1 起 `1.x.y`、Gold 為 `1.0.0`」，四個里程碑後回不到 1.0.0。改為 `0.<全域批次序號>.<修補>`，B0.1=`0.1.0`、B4.6=`0.37.0`、Gold=`1.0.0`。

**沒有執行者的驗收**

10. **判官分工定案（`50_QA_PLAN.md` §0.1）**：**樂趣由使用者本人判，agent（Opus 5）只驗程式碼完整性與可讀性。**
11. **R-3「未受訓玩家 30 秒指出瓶頸」原本無人可跑**，且拿有數字的截圖問 agent 會**必過**（它讀得到「能量 40/63」）。新增 **`TL_NAKED=1`** 鉤子隱藏所有數值標籤，只留線寬／顏色／三態徽章——把「未受訓」翻譯成「無數值輔助」。
12. **新增 R-14**：M1 閘門的「陌生人可無指導遊玩 10 分鐘」與 QA §4.4 首次體驗，在拉到真人之前**不得標記為通過**。
13. **雙榜措辭誠實化**：第一版純離線，兩榜是本機的兩個數字、沒有對手。「對 P2W 的制衡」是**接線上榜後才兌現的承諾**，不是既成事實。

**其餘定案**：先做遊戲本體、變現之後再說（架構不砍）· tycoon 與等級軸照做，購買只賣時間 · M0 七批照原順序不重排 · 雙榜照做 · **戰役地圖硬性一屏可見**，無盡才可滑（小地圖與導引排 B2.1）· 局內 HUD 改為**全畫面地圖＋可收合浮層／抽屜**（三欄式塞不下 9 個 44px 建造項目）

**其他**：`20_ART_DIRECTION.md` 新增 §1.6 地圖元素視覺語言（跨越點的「架高」必須用畫的說清楚，它是橋上免疫規則的唯一解釋）· QA 新增 §4.6 路徑規則找碴清單、RG-15～21 七條回歸項、4 條設計承諾斷言 · 修正 `50_QA_PLAN.md` §4.3 錯字 · **Godot 4.7 console exe 已置於 `tools/godot/`**，工具鏈就緒

**下一批：B0.1 地基**（`VERSION = 0.1.0`）。阻塞項已清空。

### 2026-07-27（第二次修訂）— 五項設計方向調整

使用者修訂，五份文件同步更新：

1. **耗能模型改寫**：塔改為「**交戰時每秒持續耗電、待機耗能 0**」（原為待機耗能＋每發開火耗能）。
   核心約束因此從**平均電力**變成**峰值電力**——準備期充能、波次期放電，**儲槽 Silo 從緩衝配件升格為核心策略建築**（容量 200 → 300）。取捨的戲劇性提高，且提前召喚的下注張力增強。
2. **新增全域擊殺回收**：任何塔擊殺敵人回收其價值 **25%** 礦砂。
   因此**回收者重新定位為能量回收塔**：射程內任何敵人死亡（不限自己擊殺）即回收 **60% 為能量**注入電網——直接對抗新模型的峰值缺口，且擺放變成一道空間謎題。
3. **潮汐公司 tycoon 降級為附屬補給站**，不再是「可獨立遊玩的第二層」。產出改為含數值的升級材料，全部流向塔防。新增四條防膨脹約束（UI 深度上限兩個畫面為可稽核硬指標）。**風險 R-2 由「高」降為「低—中」。**
4. **商業模式改為免費遊玩 ＋ 內購（含 P2W 成分）**。新增 GDD §3.12 變現系統與憲法 B6「賣速度，不賣次數，不賣進度」。
   Windows 版**無廣告**（桌面無 SDK 生態），廣告保留至手機移植版。**新增手機移植的四條 UI 預留條款，從第一批就生效。**
5. **每日挑戰改為雙榜**：統一配置榜（零付費影響，免費玩家的競技場，對 P2W 的唯一制衡）＋自由配置榜（總戰力）。兩榜共用同一套模擬與計分。

另補：GDD §3.1 明確化「供不應求時按比例降速而非停機」的理由，並立下**「採集器無輸入需求 → 網路永不全域死鎖」**的架構不變量。
新增風險 R-11（F2P × 桌面平台結構性錯配，**頭號商業風險**）、R-12（P2W 數值膨脹）、R-13（手機預留條款被忽略）。
QA 新增 7 條被測試斷言鎖住的設計承諾、6 條回歸項。

### 2026-07-27 — G0 立項 ＋ 前期製作文件

- 完成 G0 立項：概念書 `docs/00_CONCEPT.md`，範圍拍板為 **L 級全案、先跑 M0 原型閘門**。
- 產出四大前期製作文件：
  - `docs/10_GDD.md` — 遊戲設計文件（核心循環、11 個系統、經濟公式、內容矩陣、UX 版面、數值附錄）
  - `docs/20_ART_DIRECTION.md` — 美術方向（design token、字型、動效、音訊、零素材程序繪製管線）
  - `docs/30_TECH_DESIGN.md` — 技術設計（Godot 4.7、四層架構、確定性規範、流量網路解算器、存檔、測試鉤子、效能預算）
  - `docs/40_PRODUCTION_PLAN.md` — 生產計畫（5 個里程碑、約 35 批、10 項風險登記、砍案清單）
- 產出 `docs/50_QA_PLAN.md` — QA 五層梯、測試清單、bug 分級、找碴清單、回歸清單。
- 建立 `CLAUDE.md`（文件權威順序、鎖定設計、技術慣例、跑與測指令）。
- 立下設計紅線：**R1 無永久損失／刪檔**、**R2 無體力值／等待閘門**。

**尚未建立 Godot 專案。** 下一批：**B0.1 地基**（專案骨架、測試鉤子、存檔骨架、美術 token）。
