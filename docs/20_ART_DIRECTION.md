# 《潮與線》美術方向

> **視覺／動效／音訊的唯一權威。** UI 一律引用本文件的 token，不得硬編碼新色值或新間距。
> 上位文件：`00_CONCEPT.md`、`10_GDD.md`。
> 狀態：**v0.3**（新增 **§7 美術強化計畫**——對 v0.25.1 實機逐張截圖稽核的結果）　|　最後更新：2026-08-06

---

## 0. 核心視覺命題

> **有機混沌（敵潮） vs 幾何秩序（你的線）。**

這不是裝飾性的風格選擇，而是**把遊戲主題直接畫出來**：玩家建造的一切都是**直線、正圓、銳角、對齊網格**；敵潮則是**曲線、不規則、脈動、不對齊**。玩家看著自己的秩序被混沌侵蝕，再把它重建回來——這正是核心循環的情緒。

這條命題支配以下所有決策。任何新增視覺元素都必須回答：**它屬於秩序還是混沌？**

| | 秩序（玩家） | 混沌（敵潮） |
|---|---|---|
| 形狀 | 正圓、正方、等邊多邊形 | 不規則凸包、水滴、鋸齒 |
| 線條 | 直線、90°/45° 轉折 | 貝茲曲線、抖動 |
| 對齊 | 嚴格對齊 32px 網格 | 完全不對齊 |
| 動態 | 恆定速率、線性或 ease-out | 呼吸式脈動、隨機微抖 |
| 色相 | 青／琥珀（冷靜、機械） | 品紅／紫（有機、危險） |

---

## 1. Design Tokens

### 1.1 配色

| Token | Hex | 用途 |
|---|---|---|
| `bg.deep` | `#0a1420` | 主背景（深潮） |
| `bg.panel` | `#10202e` | 面板底 |
| `bg.raised` | `#162c3d` | 浮起元件（按鈕、卡片） |
| `border.subtle` | `#1d3547` | 面板分隔線 |
| `border.strong` | `#2e5570` | 選中／聚焦邊框 |
| **`order.cyan`** | `#7fd4e8` | **主色**：玩家的節點、導管、UI 主體 |
| `order.bright` | `#a8f0ff` | 導管滿載、選中高亮 |
| `order.dim` | `#3c6e80` | 未啟用／飢餓狀態 |
| **`energy.amber`** | `#ffc857` | **能量**：能量條、發電機、耗能相關數字 |
| `alloy.steel` | `#b8c9d4` | 高階**建築**的金屬感（熔爐、稜鏡、碎浪） |
| **`alloy.violet`** | `#c8a0ff` | **合金這個資源本身**：流動珠與帳上數字（★ B1.1）|
| **`tide.magenta`** | `#ff4d6d` | **敵潮主色**：敵人本體 |
| `tide.deep` | `#7b2d5e` | 敵潮陰影、路徑帶、來襲方向指示、**護甲鎖邊** |
| `tide.bright` | `#ff9aab` | **混沌側的亮階**（★ B1.6.3）：高速敵人的亮核心。與 `order.bright` 對稱——秩序側一直有明度階，混沌側原本只有本體與陰影兩階 |
| `warn.orange` | `#ff8a3d` | 警示：能量不足、建築受損、波次倒數 <10s |
| `ok.green` | `#5fd39a` | 成功：交貨完成、解鎖、關卡通過 |
| `text.primary` | `#e8f1f5` | 主要文字 |
| `text.secondary` | `#8ba3b0` | 次要文字、單位、說明 |
| `text.disabled` | `#4a5f6b` | 停用文字 |

**配色紀律**：
- 全遊戲**只有敵人與危險狀態能用品紅／橙**。這保證玩家的眼睛永遠能在混亂的畫面裡瞬間找到威脅。
- 琥珀色**專屬於能量**。看到琥珀＝跟耗能有關。這是本作最重要的資訊通道（能量＝取捨的化身）。

### 1.2 字級階

| Token | px | 用途 |
|---|---|---|
| `text.xs` | 11 | 數值單位、tooltip 註腳 |
| `text.sm` | 13 | 次要標籤、面板說明、**頂欄附屬階**（`▲/秒`、交戰座數、下一波） |
| `text.base` | 16 | 內文、按鈕、**頂欄次階**（礦砂、合金＝「我買不買得起」） |
| `text.lg` | 22 | 面板標題、**頂欄主階**（能量供需、波次／倒數、核心＝「我現在會不會輸」） |
| `text.xl` | 32 | 畫面標題、局末星等 |
| `text.2xl` | 48 | 局末結算大數字、遊戲標題 |

> ★ **這張表在 B2.4.4 之前從來沒有被真的執行過**：全專案有 13 處階外字級
> （14／15／17／18／28／34／40），而 §6.3 的檢查清單裡就有這一條。
> 一條沒有人跑過的檢查等於不存在——**能寫成 grep 的檢查就該寫成 grep**：
> ```bash
> grep -rnE ', (12|14|15|17|18|19|20|21|23|24|25|26|28|30|34|36|40), Palette\.' godot/scripts/ | grep -v draw_arc
> ```

### 1.3 間距階

`4 / 8 / 12 / 16 / 24 / 32 / 48`（px）。**不得使用階外數值。**

### 1.3b 手機移植預留條款（從第一批就生效）

Windows 先行，但事後補觸控支援極痛，故以下四條在每批 UI 一致性檢查中稽核：

| # | 條款 |
|---|---|
| P1 | **版面全部可縮放**。以 16:9 為基準，但**不得硬編碼像素位置**——一律用錨點、容器與比例。 |
| P2 | **所有可點元素的觸控命中區 ≥ 44×44 px**（視覺可以更小，命中區不行）。 |
| P3 | **操作不得只靠 hover／右鍵／鍵盤快捷**。這些可以有，但每個功能都必須有一條純點擊／拖曳的途徑。 |
| P4 | **關鍵資訊不得只靠 tooltip 呈現**（觸控裝置沒有 hover 態）。 |

**P2 的直接後果（2026-07-27）**：局內 HUD 從三欄式改為**全畫面地圖 ＋ 浮層／抽屜**（`10_GDD.md` §6.2）。固定窄欄塞不下 9 個 44×44 px 的建造項目，硬塞就會犧牲 P2 或犧牲地圖面積。浮層還必須**可收合**，收合後地圖 100% 可見。

### 1.4 圓角與線寬

| Token | 值 | 用途 |
|---|---|---|
| `radius.none` | 0 | **遊戲內所有玩家建築**（秩序＝銳角） |
| `radius.ui` | 2 | UI 面板與按鈕（僅此一種） |
| `stroke.hair` | 1 | 分隔線、網格 |
| `stroke.base` | 2 | 節點外框、UI 邊框 |
| `stroke.emphasis` | 3 | 選中、聚焦 |

**導管線寬為動態值**：`width = 2 + 6 × (flow / 最大cap)`，範圍 2–8px。這是遊戲最重要的資訊視覺化——**線的粗細就是流量**。

> **★ 分母是全遊戲的最大 cap，不是這條線自己的**（B1.1.1 改）。原本的分母是自己的 cap，也就是飽和度——那讓**加粗一條線會讓它變細**（分母變大），而按鈕就叫「加粗」。使用者實看後的原話：「有時候升級的管道卻比沒升級的還小」「應該要是越大的流量管道越粗」。
>
> 順帶修掉的是**兩個通道在講同一件事**：線寬與顏色原本都由 `flow / cap` 驅動，等於重複一次飽和度，而「這條線實際搬了多少」沒有任何通道在講。現在**線寬＝絕對流量、顏色＝飽和度**，各講一件事。
>
> 代價：顏色因此必須改成**連續**的（原本是「滿了才亮」的兩段跳）——飽和度只剩這一個通道，兩段跳會讓九成滿和一成滿長得一樣。用 `sat²` 而不是線性，保住「第一台發電機讓那條線當場亮起來」那個 B0.3 的教學瞬間。

#### 1.4a 流動珠（★ B0.6.1 新增）

**問題**：線寬與顏色說的是「這條線有多滿」，**沒有一個元素在說「東西正在動」**。一張靜態的粗線和一張靜態的細線都不會動，玩家看到的是一張接線圖，不是一條產線。使用者實看 B0.6 的畫面時的原話：「看不到貨物流動的感覺」。

**做法**：導管上跑一列小珠子。

| 屬性 | 規則 | 為什麼 |
|---|---|---|
| **顏色** | 礦砂 `text.primary`（近白）／能量 `energy.amber`／合金 `alloy.violet` | 三種資源靠**色相**分開：無色相／暖／冷紫。沿用配色紀律 2（琥珀專屬能量）。同一條幹線同時走多種資源時，看得出**誰往核心走、誰往塔走** |
| **對比** | 每顆珠子先畫一圈 `bg.deep` 底，再畫亮色芯 | 導管本身就是亮青，亮青珠子畫在亮青線上等於沒畫。**深色底是這個元素能被看見的唯一原因** |
| **多列並行** | 礦砂 −、能量 ＋、**合金恆走中線**，沿線垂直偏移 ∓(線寬×0.45+1)px；只有一種資源在跑時全部走中線 | 疊在一起會互相蓋掉，也讀不出不同資源方向不同。細線上硬要分排會讓珠子懸在線外，看起來像掉出來的東西 |
| **速度** | ∝ 該資源的流率（單位/秒），固定間距 16px | 幹線 20/秒的珠子明顯跑得比支線 6/秒快。**速度是第三個獨立通道**，與線寬（飽和度）、顏色（狀態）不重複 |
| **方向** | 該資源的**淨流向**（解算器兩條有向邊之差） | 導管無方向（`10_GDD.md` §7.2），方向是每 tick 解出來的結果，不是玩家設定的屬性 |
| **停止** | 流率 < 0.05 就完全不畫 | 沒在流動的線必須是靜止的——這是「哪裡斷了」最快的讀法 |

> **★ 這不是實體物品搬運。** 珠子是**渲染層**由已解出的流率推出來的，模擬層一無所知（`10_GDD.md` §3.1 鎖定：流量網路，非物品實體模擬）。珠子的位置由 `tick_count` ＋ 幀內插值推算，**不是被搬運的物件**，不參與任何判定。這條界線不得越過——一旦珠子變成有位置有狀態的實體，`O(物品數)` 的效能與確定性負債就回來了。

> **`reduce_motion`**：開啟時珠子停止移動但**仍然畫**（位置固定在起點側），流量資訊靠線寬與顏色承載，不因關動效而丟資訊。設定畫面在 B1.4，實作與驗收排 **B1.6**。

**★ 為什麼合金珠不是 `alloy.steel`**（B1.1 定案）：鋼銀 `#b8c9d4` 的明度和導管的青 `#7fd4e8` 幾乎一樣，而珠子是畫在導管上的——鋼銀珠會直接融進線裡，重演 B0.6「看不到貨物流動」那個缺陷。**建築的顏色和它產出的資源的顏色本來就不是同一件事**：採集器是青色而礦砂珠是近白，熔爐是鋼銀而合金珠是紫，同一條慣例。

**升級刻度改為垂直短刻線**（原為圓點）：圓點會和流動珠混成同一個東西，而它們的語意相反（一個是固定的線材等級，一個是變動的流量）。**同一個形狀不得表達兩件會同時出現的事。**

### 1.5 網格

地圖網格 **32px**。所有玩家建築嚴格對齊。網格線用 `border.subtle` 於 1px，透明度 20%。

### 1.6 地圖元素的視覺語言（★ 新增）

| 元素 | 畫法 | 為什麼 |
|---|---|---|
| **敵人路徑** | `tide.deep` 的低透明度帶狀底色 ＋ 不對齊網格的抖動邊緣 | 它屬於**混沌**側。玩家一眼看出「這條帶子不是我的」 |
| **來襲方向**（★ B0.7） | 沿路徑每 4 格一個 `tide.magenta` 低透明度**箭羽**，指向行進方向 | 一條帶子只說「這裡是路」，**沒說潮從哪邊來**。空地圖上這是新玩家的第一個問題，而它決定所有擺位。順著箭羽走也就找到了核心——一個元素答兩題（`50_QA_PLAN.md` §4.4）。**靜態不動**：會動的東西在混沌側已經有敵人本體了，路面再動會搶掉真正該被注意的東西 |
| **跨越點（橋）** | 路徑帶之上的一段**架高結構**：`order.cyan` 的雙線橋面 ＋ 兩端 45° 引道 ＋ 橋下留可見的路徑帶陰影 | ①「架高」必須用畫的說清楚——它是**橋上導管不受攻擊**這條規則的唯一解釋（`10_GDD.md` §3.2）。玩家看到它跨在上面，就不會問「為什麼這段打不到」。②它屬於**秩序**側，是玩家可用的動線 |
| **礦點** | `order.dim` 的空心正圓，蓋上採集器後填實 | 秩序側，可用資源 |
| **核心** | 最大的幾何體，`order.bright` 描邊；受擊時整體閃 `warn.orange` | 唯一會讓敵人駐足的目標，必須最顯眼 |
| **潮鳴的場**（★ B1.8） | 交戰時，在**實際射程半徑**（5 格＝160px）畫一圈 `order.cyan` 低透明度脈動細環；同時被它影響的敵人**輪廓描一圈 `order.cyan`** | **它是全案唯一一座不開火的塔**（`rof = 0`），所以沒有開火線、沒有開火音——B1.7 之前它每秒吃 9 能量而畫面上一個回饋都沒有，玩家無從確認它在工作。這是本作最不能藏的一件事：「塔在交戰時每秒吃電」就是核心取捨本身。<br>**為什麼不是給它一個開火音**：它的動作是「場」不是「擊」，硬配一個打擊音是在說謊。<br>**為什麼標在敵人身上**：減速 −40%／破甲 −25% 作用在敵人身上，標在敵人身上因果才讀得出來——標在塔上只說得出「它開著」。<br>**形狀怎麼分得開**（§4.3b 那條規則）：場環半徑 160px vs 交戰環 15px，差一個數量級；敵人輪廓是**閉合折線**，與血量弧（`tide.deep` 圓弧）一個是線一個是弧 |

**浮層的硬性要求（★ B0.7.4）**：一律走 `UiKit.panel(opacity)`——近乎不透明的 `bg.panel` 底 ＋ `border.strong` 細邊 ＋ `radius.ui`，且**建構子就把 `mouse_filter` 設成 `IGNORE`**（浮層是資訊，不是障礙物）。兩條由使用者實玩逼出來的規則：

1. **不透明度不是風格選擇。** 預設的半透明面板讓導管從字後面穿過去，暗色的次要文字直接讀不到。要「看得見底下」的浮層（角色簡介）才降到 0.88，其餘一律 0.96。
2. **會隨內容改變尺寸的資訊面板 ＝ 讀不了的面板。** 列數與寬度必須固定，沒有值的列留在原位變暗，不得隱藏——隱藏會讓下面每一列往上跳。**位置固定本身就是可讀性**：玩家記得「淨值在第八行」，那一行就不該跑。

**硬性要求**：**跨越點必須在不縮放、不 hover 的狀態下一眼可辨**（`10_GDD.md` §3.2）。玩家不該靠試錯才知道哪裡能鋪管——那是浪費 45 秒準備期的操作成本（R-1）。

### 1.8 大圖導引：小地圖與邊緣箭頭（★ B2.1b）

只在**地圖大過框架**時出現（無盡）。戰役全部一屏可見，問題節點就在畫面上，多一個浮層只是多一塊遮擋。

| 元素 | 畫法 | 為什麼 |
|---|---|---|
| **小地圖底** | `bg.panel` 0.96 不透明 ＋ `border.strong` 細邊，貼框架**右下角**內側 | 浮層硬性要求（§1.6）。右下角是離建造欄（左）與能量列（上）最遠的角 |
| **路徑** | `tide.deep` 實線 | 與主畫面同一個編碼，不另發明 |
| **玩家節點** | `order.cyan` 1px 點 | 小地圖上讀的是**產線的形狀**，不是個別節點 |
| **問題標記** | `warn.orange` 方點，**比節點點大一倍** ＋ 脈動 | 橙＝危險通道（§1.1）。大一倍是因為它要在一堆青點裡被一眼挑出來 |
| **視野框** | `order.bright` 1px 空心矩形 | 「我在哪」——這是小地圖與「一張縮圖」的唯一差別 |
| **邊緣箭頭** | 框架內緣的 `warn.orange` 三角，指向畫面外的問題；最多 **3 個** | 打斷式通知。**上限 3 個**是反模式清單「資訊過載」的直接套用——十個箭頭等於沒有箭頭 |

**兩者都可點，點了把視野移過去。** 命中區 ≥ 44×44 px（P3 手機移植預留條款）。這也是大圖平移目前唯一的**純左鍵**動線——中鍵拖曳在觸控上不存在（R-16）。

**小地圖不畫敵人。** 敵人是路徑上的行進物，位置由路徑決定；把它們畫進去只會讓小地圖每一幀都在閃，而玩家從中得不到任何**決策**（他不能改變敵人的路線）。小地圖回答的是「我的產線長什麼樣、哪裡壞了」。

### 1.7 敵人輪廓與開火形態（★ B1.6.3）

> 起因：使用者實玩回報「**所有敵人都長一個樣，根本看不出差異**」「塔的攻擊可以有更多不同的特效嗎」。
> 查證屬實——三種敵人畫的是**同一個 9 邊形、同一個 `tide.magenta`**，只有半徑不同；五座塔的開火畫的是**同一條 `order.bright` 直線**。

#### ★ 統御規則：視覺由**既有數值**推導，不新增美術資料

每一個視覺特徵都掛在 `Enemies.DEFS`／`NodeDefs.DEFS` **已經存在**的機制欄位上，不新增 `shape` / `fx` 之類的美術欄位。三個理由，由重到輕：

1. **設計上不可能說謊**——沒有辦法做出一隻「有護甲但看起來不硬」的敵人。屬性就是外觀。
2. **M3 擴到 18 種敵人、24 隻角色時自動正確**（`10_GDD.md` §5 內容矩陣），不必逐一補美術欄位，也不會漏。
3. 少一張要跟著資料表一起維護的對照表。

#### 敵人：三種**明度結構**（全部仍屬**混沌**側——不規則、脈動、不對齊）

| 觸發條件（既有欄位） | 畫法 | 讀出來是什麼 | 現況命中 |
|---|---|---|---|
| 預設 | **9 邊柔性水滴**，wobble ±15%，`tide.magenta` 單一階 | 基準、軟 | 漂蟲 |
| `armor > 0` | **6 邊硬稜角**，wobble ±7%，加一圈 `tide.deep` 3px **同形鎖邊＝甲板** | 有殼、硬、**物理打不動** | 甲殼（armor 8） |
| `speed > 1.2` | **垂直行進方向壓扁**（×0.55）＋ 中心一顆 `tide.bright` **亮核心** | 快、薄 | 熾泳（speed 1.6） |

> **★ 這張表是實看 A/B 改過三次的結果，前兩版都被否決**（B1.6.3）。留著這段，因為它是「特效不能只在腦裡設計」的直接證據：

**① 拖尾被否決。** 第一版給高速敵人兩個遞減殘影。第 5 波的間距是 **0.6 格**，殘影落在後面那一隻身上——一列 11 隻分得開的敵人糊成一條連續的香腸。把「看不出差異」修成了「看不出有幾隻」。

**② 沿行進方向拉長也被否決。** 砍掉拖尾之後改拉長本體，A/B 顯示問題**完全一樣**：在單列縱隊裡，任何沿行進軸變長的東西都會碰到鄰居。

→ 結論寫成規則：**敵人的差異化只能用「不增加行進軸footprint」的手段**——壓扁（footprint 只會變小）、換頂點數、換明度。

**③ 輪廓在 fit 倍率下不夠用。** 敵人在 fit 倍率只有 **16–24px**。六邊形勉強讀得到，壓扁幾乎讀不到。**明度差活得下來**，所以三種各給一個明度結構：軟＝單一階、硬＝加暗鎖邊、快＝加亮核心。這是 `tide.bright` 這個 token 存在的唯一理由。

**為什麼甲板是「同形鎖邊」而不是外圈弧**：血量弧已經是 `tide.deep` 的圓弧、畫在 `r+4`（B0.6）。再加一圈外弧就是**同一個位置上的兩個訊息**，而 §4.3b 那條規則說「換顏色不夠，要換形狀」——這個專案已經在這裡踩過兩次（B0.6 儲槽充能弧撞血條、B1.6 受擊環撞交戰環）。鎖邊貼在輪廓上、亮核心在正中心，和任何弧都不會疊。

**為什麼亮核心不用橙**：橙是「建築受損／能量不足」的通道（§1.1）。熾泳的名字雖然是「熾」，但把橙放到敵人身上會讓玩家的警示通道多一個假訊號。亮階留在品紅色相內。

**barrier（能量抗性）刻意不另給視覺通道。** 三個結構已經解掉「看不出差異」；再疊第四個訊息在一顆 16px 的東西上會回到反模式清單的「資訊過載」。精確的抗性數字在**圖鑑**裡。

#### 塔的開火：四種形態（全部屬**秩序**側——直線、正圓、青系）

| 觸發條件（既有欄位） | 畫法 | 讀出來是什麼 | 現況命中 |
|---|---|---|---|
| 預設（`dmg_type = physical`） | **短促實心彈丸**沿線飛行（不是整條線亮起），`order.cyan` | 物理＝有質量的東西飛過去 | 錨 |
| `pierce` | **整條貫穿光束**一次亮到底，`order.bright`，寬度 3→1 衰減 | 能量＝瞬間、整條線上的都被打到 | 稜鏡 |
| `splash > 0` | 彈丸 ＋ 命中點一圈**擴張環**，半徑 ＝ 資料裡的 `splash` 格 | 濺射範圍**有多大**（原本玩家完全看不到） | 碎浪 |

| `reclaim > 0` | 細彈丸 ＋ 命中後一顆 `energy.amber` 珠**回流到塔** | 它把價值換成**能量**送回電網 | 回收者 |

**物理 vs 能量用形狀分，不用顏色分。** 琥珀專屬於能量這個**資源**（§1.1 配色紀律），拿它去畫「能量傷害」會污染本作最重要的資訊通道——玩家看到琥珀必須永遠是「跟耗能有關」。所以：**彈丸＝物理、光束＝能量**。回收者那顆珠是全域唯一的例外，而它是對的——那顆珠真的是能量，正在流回電網。

**★ 濺射環只掛在一發上。** 碎浪是「**一發**濺射打中 N 隻」，而模擬層為了記傷害會產生 N 筆命中記錄——照著畫就是 N 個同心圓，既吵又把機制講錯（實看抓到）。圓心是**主目標那一格**，不是每一隻。

**潮鳴不在此表**：它 `rof = 0`、不開火，回饋是 B1.8 的場環（§1.6）。

**★ 驗收這一節必須用 `TL_FOCUS` 近照，而且要挑 `ttl` 走到一半的那一格。** 開火特效只活 3 個 tick（0.3 秒），`TL_SHOT` 凍結在剛發射那一格時濺射環半徑是 0——**畫面上什麼都沒有，看起來就像沒做**。這是 RG-100（脈動元素要在波谷驗收）的同一類陷阱。

---

## 2. 氣質與參照

**一句話氣質**：*冷靜的工程製圖，正在被某種活的東西侵蝕。*

**參照座標**：

| 作品 | 我們取什麼 |
|---|---|
| **Mini Metro** | 線條的絕對克制；用最少的形狀傳達最多的拓樸資訊 |
| **Opus Magnum** | 機械運轉的節奏美感；每個零件都看得懂在做什麼 |
| **Dorfromantik** | 柔和但精確的色彩層次；不刺眼的長時間可讀性 |
| **Creeper World** | 有機威脅緩慢吞噬幾何秩序的視覺張力 |

### 反模式清單（明確「不是」我們的風格）

- ❌ **賽博龐克霓虹**：不用高飽和紫粉漸層、不用故障（glitch）特效、不用掃描線。
- ❌ **寫實科幻**：不用金屬材質貼圖、不用鏡頭光暈、不用體積光。
- ❌ **可愛化**：角色不擬人、不加表情、不做 Q 版。塔是機械，不是吉祥物。
- ❌ **資訊過載的 HUD**：不做同時顯示 20 個數字的儀表板。每個畫面最多 4 個「主要數字」。
- ❌ **賭博演出語彙**：招募畫面不做金光、不做十連特寫、不做「差一點」的假張力（`10_GDD.md` §3.9）。
- ❌ **粒子濫用**：爆炸不用 200 顆粒子。用 3–5 個幾何碎片＋一次縮放閃光。

---

## 3. 字型

| 用途 | 字型 | 說明 |
|---|---|---|
| 全部 UI 與遊戲內文字 | **`SystemFont`**（微軟正黑體 / Microsoft JhengHei，fallback：Noto Sans TC） | **硬性要求**：Godot 預設字型不含 CJK，直接使用會變豆腐字。 |
| 數值顯示 | 同上，但**啟用等寬數字**（tabular figures） | 資源數字跳動時不得左右位移。若引擎不支援，改用固定寬度容器 + 右對齊。 |

**禁止**：不得改回 Godot 預設字型；不得為了風格引入不含中文的英文字型作為主字型。

---

## 4. 動效原則

### 4.1 時長階

| Token | 時長 | 用途 |
|---|---|---|
| `dur.instant` | 0.12s | 按鈕回饋、hover、選中框 |
| `dur.base` | 0.24s | 面板開闔、數值補間、狀態切換 |
| `dur.slow` | 0.50s | 畫面轉場、局末結算逐項揭示 |
| `dur.ambient` | 2.0s+ | 敵潮脈動、背景呼吸（循環） |

### 4.2 緩動

- **玩家操作的回饋**：`ease-out-cubic`（快起慢收＝有回應感）
- **系統自動的變化**（資源數字、流量粗細）：`linear`（＝機械的誠實，不誇張）
- **敵潮的動態**：`ease-in-out-sine` 循環（＝有機的呼吸）

### 4.3 什麼動、什麼不動

| 一定要動 | 一定不動 |
|---|---|
| 導管粗細（隨流量即時變化） | 網格 |
| 能量條（含警示脈動） | 已放置節點的位置 |
| 敵人本體（脈動＋前進） | UI 面板容器 |
| 波次倒數最後 10 秒（數字脈動） | 文字（不做打字機效果） |
| 建造預覽（半透明幽靈） | 背景（不做視差） |

### 4.3b 實作狀態（★ B1.6）

**時長階與緩動的實作在 `scripts/render/Motion.gd`**（B1.6 才建立——它從第一批就列在 `CLAUDE.md` 的目錄樹裡，但一直不存在，於是每個動效都是散在畫面層的字面量）。全專案的脈動、效果壽命、碎片方向一律走它。

兩條紀律：

1. **一律由 tick 驅動，不用系統時間。** 渲染可以不確定，但別引入新的亂數源——`TL_SHOT` 凍結在第 N tick 時，同參數要拍出同一張圖。
2. **碎片與閃光是純渲染，不進 `state_hash()`。** 它們由模擬層在擊殺與清殘骸時生成（那兩個時刻只有模擬知道），但不參與任何判定。

已實作：碎片爆（擊殺／建築被毀）、正在被啃的四角括號、倒數最後 10 秒脈動、敵潮呼吸、能量條警示脈動。**未實作**：局末結算逐項揭示（`dur.slow`）、色彩層次校準。

**★ 一條從兩次錯誤裡長出來的規則**：**同一個位置上的兩個訊息，換顏色不夠，要換形狀。** B0.6 的血條與儲槽充能弧都是琥珀圓弧、B1.6 的受擊環與交戰環都是橙色圓環——兩次都是截圖當場才抓到。節點上要加任何新指示，先問「它和既有的那幾個形狀分得開嗎」。

### 4.4 可跳過原則

**所有動效都可跳過。** 設定裡提供「減少動態效果」開關（一併關閉脈動、閃光、轉場），這既是無障礙需求，也是高手快速重來的需求。局末結算的逐項揭示可按任意鍵直接跳到底。

---

## 5. 音訊方向

**氣質**：低頻環境嗡鳴 ＋ 機械節奏 ＋ 有機威脅音。像在一座深海工廠裡工作，而外面有東西在敲玻璃。

> ★ **B2.1f 修正（使用者拍板）：「敲玻璃」是氣質，不是一個要真的放進混音裡的音。**
> 戰鬥曲原本有一個金屬敲擊動機來扮演它，使用者回報「意義不明且會與其他東西誤會」。
> 那不是音色問題，是**通道問題**：這款遊戲的音訊是診斷通道（核心受擊是全場最醒目
> 的音、缺電有警報、生產嗡鳴的音量跟著流量走），而**瞬態一律被耳朵讀成「剛剛發生了
> 一件事」**。一個不對應任何事件的瞬態＝訓練玩家對假警報做反應。
> 「外面有東西」現在由**敵潮本身**與 walk-by 的破壞音表達——那些是真的有東西。

### 5.1 音軌

| 類別 | 內容 | 里程碑 |
|---|---|---|
| BGM 準備期 | 低頻 drone ＋ 稀疏的機械節拍，平靜 | M1 |
| BGM 戰鬥期 | ★ **沒有音樂**（B2.1f）。開打一次號令，然後音樂淡出到全靜 | M1 |
| SFX 開打號令 | 堆疊五度的長號令（A2→E3→A3），**起音慢**、音域壓在 900Hz 以下 | M1 |
| BGM 主選單 | 極簡、留白多 | M1 |
| BGM tycoon | 溫暖、低壓力、可長時間聆聽 | M2 |
| SFX 建造 | 節點放置（短促金屬扣合）、導管連接（上升掃頻） | M0 |
| SFX 生產 | 資源流動的低頻循環（音量隨總流量微調） | M1 |
| SFX 戰鬥 | 各角色開火音（4 種，可辨識）、敵人受擊、建築損毀 | M0 |
| SFX 警示 | 能量不足（低頻警報）、核心受擊（最醒目的一個音） | M0 |
| SFX UI | 點擊、確認、取消、解鎖 | M1 |

**★ BGM 分層設計已作廢（B2.1f）。** 原本是「準備期與戰鬥期用同一首曲子的不同層，只淡入淡出音軌層」。現在是：

| 階段 | 聽得到什麼 |
|---|---|
| 準備期 | `tl_bgm_battle_base`（drone ＋ 稀疏機械節拍） |
| 開打的那一刻 | `tl_sfx_wave_start` 號令一次，音樂同時開始淡出（1.2 秒） |
| 戰鬥期 | **沒有音樂**。生產嗡鳴（音量跟著流量）＋ 開火／受擊／警示 SFX |
| 回到準備期 | 音樂淡回來 |

**理由**：反差本身就是訊號（音樂停＝開打了），而且戰鬥期要讀的資訊全在 SFX 上——音樂只是在跟它們搶。原本擔心的「頻繁換曲會吵雜疲勞」在這個結構下更好解決：整局只有一種曲子，而且它只在你有空的時候響。

**號令的設計條件**（`audio_test` 逐條量）：
- **起音慢**（實測 411 ms 才到滿音量；打擊類全在 10 ms 內）。這是「不被讀成敲擊」在資料上唯一量得到的樣子。
- **音域壓在 900 Hz 以下**（低通 2000）。建造類音效是 880–2700 Hz 的非諧和金屬，撞在同一段就是上一版被誤會的成因。
- **音高往上走**（堆疊五度）。全庫沒有第二個會往上走的音，也沒有第二個超過一秒還在漲的音。

### 5.1b 實作狀態（★ B1.5）

**已交**（`assetgen/gen_audio.py` 程序生成，`godot/assets/audio/`）：

| 檔案 | 對應 §5.1 |
|---|---|
| `tl_bgm_battle_base` / `tl_bgm_menu` | 準備期／主選單，**同長度 16.0s、90 BPM、A 小調**（★ B2.1f 刪掉了第三首 `tl_bgm_battle_perc`，`audio_test` 斷言它不存在——留著一個沒人播的 wav 會一路被打包、被改音色、再被誤以為還在用） |
| `tl_sfx_wave_start` | ★ 開打號令（B2.1f） |
| `tl_sfx_build_place` / `build_wire` / `build_destroyed` | 建造（放置／連接／損毀） |
| `tl_sfx_prod_flow` | 生產的低頻循環（音量隨總流量 0.15–0.55 線性） |
| `tl_sfx_fire_{anchor,prism,knell,reclaimer,breaker}` | **每一座塔一個**（§5.1 寫 4 種，實際有 5 座塔） |
| `tl_sfx_enemy_hit` / `warn_power` / `core_hit` | 戰鬥與警示 |
| `tl_sfx_ui_click` / `ui_back` / `ui_unlock` | UI（點擊／取消＝ESC／解鎖） |

**尚未**：BGM tycoon（M2，隨 B2.5 進來）。

**實作條件**（`audio_test` 逐條守著）：兩首**同幀數**、**循環接點的跳幅不得大於鄰近取樣步幅的 4 倍**（「無縫」在資料上唯一量得到的樣子——耳朵最容易漏掉，量起來最便宜）、**號令到滿音量 ≥ 100 ms 而打擊類 ≤ 12 ms**（B2.1f）。

> ⚠ **新增 wav 時記得把 `.import` 的 `compress/mode` 改成 `0`。** Godot 4.4 起新匯入的
> WAV 預設是 `2`（QOA 失真壓縮）→ 格式變成 `FORMAT_QOA`、峰值會被推到削頂。
> `audio_test` 的 16-bit 與削頂兩條會當場抓到（B2.1f 實際踩到），但知道原因可以省十分鐘。

### 5.2 音量層級

| 匯流排 | 預設 |
|---|---|
| Master | 80% |
| BGM | 60% |
| SFX | 80% |

**★ B1.5 修正：沒有 UI 軌，UI 音走 SFX。** 原本列的第四軌會變成設定畫面的第四條滑桿，
而它買到的只是「點擊聲可以單獨調小」——那要玩家先意識到有這回事。
tycoon 的兩畫面上限是同一種紀律：UI 深度不是免費的。

### 5.3 全域靜音路徑（硬性要求）

- 設定面板提供 Master／BGM／SFX 獨立滑桿。
- **環境變數 `TL_MUTE=1` 或任何 `TL_SHOT` 存在時，自動全域靜音。**
- **絕不在沒有測試鉤子的情況下開啟遊戲視窗做自動化驗證**——使用者可能正在工作或遊玩。

---

## 6. 素材生產管線

### 6.1 全程序繪製，零外部圖檔

**決策**：所有遊戲內視覺元素以 Godot 的 `_draw()` API 與自訂 shader **程序繪製**，不使用任何外部 PNG／SVG 素材。

**理由**：
1. 極簡幾何風格的元素本來就是圓、線、多邊形——用繪圖 API 表達比用貼圖更自然。
2. 任意解析度下都銳利（向量本質），不需要多套解析度資產。
3. 導管粗細隨流量即時變化，本來就必須是程序繪製。
4. exe 體積可控（目標 < 80MB），且無授權疑慮。
5. 調色只需改一處 token，全遊戲同步。

**例外**：字型（SystemFont，系統提供）。

**★ B1.5：音訊也走同一條。** 音源不是外部素材，是 `assetgen/gen_audio.py`（純 Python
標準庫，零相依）算出來的——**那支腳本才是音源的原始碼，`.wav` 是它的輸出**。
要調音色改腳本再跑一次，不要去修 wav。
匯入一律 `compress/mode=0`（PCM）：Godot 預設的 QOA 是有損壓縮，壓過之後連
「這個檔案有幾個取樣」都問不到，循環接點更無從驗起。

### 6.2 命名與目錄慣例

```
godot/
  assets/
    audio/
      bgm/       tl_bgm_<場景>_<層>.wav      例：tl_bgm_battle_perc.wav
      sfx/       tl_sfx_<類別>_<動作>.wav    例：tl_sfx_build_place.wav
  scripts/
    render/      Palette.gd    ← 本文件 §1 的 token 實作，唯一色值來源
                 Shapes.gd     ← 共用繪圖函式（節點、導管、敵潮輪廓）
                 （時長與緩動常數暫時只活在本文件 §4——B0.1 建的 Motion.gd
                   到 B1.1 為止零呼叫者，已刪；B1.6 做動效時再建它用得到的部分）
```

**硬性要求**：`Palette.gd` 是全專案唯一的色值來源。任何 `Color(...)` 字面量出現在其他檔案，視為缺陷。

### 6.3 UI 一致性檢查（每批工作結束時）

- [ ] 沒有新的硬編碼色值（Grep `Color(` 只應命中 `Palette.gd`）
- [ ] 沒有階外間距（4/8/12/16/24/32/48 以外的 margin/padding）
- [ ] 沒有階外字級
- [ ] 新增的玩家側元素是銳角、對齊網格；新增的敵潮側元素是曲線、不對齊
- [ ] 新增動效使用本文件 §4 的時長階，且可被「減少動態效果」關閉（B1.6 起這批常數落回 `scripts/render/`）
- [ ] **手機預留條款 P1–P4 全過**（無硬編碼像素位置、觸控命中區 ≥ 44px、無 hover-only 操作、無 tooltip-only 資訊）
- [ ] **浮層可收合，收合後地圖 100% 可見**（`10_GDD.md` §6.2 要求 5）
- [ ] **★ `TL_NAKED=1` 檢定**：關掉所有數值標籤後，這一批新增的視覺元素**自己還說得出話嗎**？（線寬＝流量、顏色＝飽和度、三態徽章、跨越點可辨）——**這是本文件所有資訊視覺化的最終驗收方式**，見 `50_QA_PLAN.md` §0.1
- [ ] **★ 字級與間距真的落在階上嗎**（B2.4.2 新增）：`20_ART_DIRECTION.md` §1.2／§1.3 訂了字級階與間距階，而 code review 在**這一批自己新寫的 `Roster.gd` 裡就抓到十處階外值**（14/15/18/19/20/34…）。這條檢查項在清單上放了很多批，**從來沒有人真的跑過**。⚠ 舊畫面（`Tech.gd`／`Battle.gd`）也有一樣的問題，那是 B2.4.4 的事
- [ ] **★ 建造欄與底欄的碰撞檢查**（B2.4.2 新增）：建造欄的長度隨**可建造類型數**成長，而那個數字會隨名冊與內容矩陣一路長到 M3 的 24 隻。**每次加角色都要用 13 種以上的建造欄重拍一張**（`TL_PANEL=battle` 的測試圖恆為全表）。理由見 §7.1 的 A-1——這一條就是被漏掉才會出事的那一格

---

## 7. 美術強化計畫（★ B2.4.2 稽核，2026-08-06）

> **這一節是計畫，不是規格。** 條目落地時，把定案內容寫回上面對應的小節（§1.6、§3…），
> 並在這裡打勾。**上面各節仍然是唯一權威**。
>
> **稽核方法**：對 **v0.25.1 的實機 build** 逐張截圖判讀，不看程式碼推測畫面。
> 取樣：戰役第 5 關交戰中（`TL_DEMO_TICKS=600`）、玩家的第一眼（`TL_DEMO_TICKS=0`）、
> `TL_NAKED=1` 可讀性檢定、跨越點近照（`TL_FOCUS="10,4,3.5"`）、核心近照
> （`TL_FOCUS="32,13,3.5"`）、開火特效近照（`TL_FOCUS="30,15,3"`）、無盡全景。
>
> **★ 最重要的一句結論**：本次找到的問題**大多不是「本文件缺了設計」，而是
> 「實作沒有做到本文件已經寫下的設計」**。所以 §7 的多數條目不是新方向，是**還債**。
> 在提出任何新的視覺點子之前，先把 §1.6 已經白紙黑字寫著、但畫面上不存在的東西補完。

### 7.0 先看做對了什麼（不要在重構時弄丟）

| 元素 | 為什麼它是對的 |
|---|---|
| **流動珠** | 全案最成功的資訊視覺化。三種資源靠色相分得開（近白／琥珀／紫），深色底讓它們在亮青導管上看得見，速度∝流率。近照裡幹線上那一列白＋紫珠**一眼看得出這條線同時在搬兩種東西**。§1.4a 的每一條理由都在畫面上兌現了 |
| **導管的雙通道** | 線寬＝絕對流量、顏色＝飽和度。`TL_NAKED` 下仍然讀得出哪條粗哪條滿——B1.1.1 那次修正是對的 |
| **缺料倒三角＋三態徽章** | 橙色倒三角在一片青裡跳得出來，而且它指的是**具體缺哪一種**。這是「玩家看得懂自己為什麼輸」在畫面上的落點 |
| **潮鳴的場環** | 全案唯一不開火的塔，場環讓「它在吃電」變成看得見的事。半徑差一個數量級，和交戰環分得開 |

### 7.1 A 級：先修的四件（三件是還債，一件是我剛弄壞的）

#### A-1　★ 建造欄溢位，「藍圖」按鈕整顆掉出畫面（**功能缺陷，非美術**）

**證據**：`TL_PANEL=battle`（測試圖，建造欄＝全表 13 種）的三張截圖裡，
「加粗」「拆除」和底欄的「快進 4×」「提前召喚」**疊在一起**，而「藍圖」**完全不在畫面上**。
戰役第 5 關（10 種）不會發生。

**根因**：建造欄是「一個類型一顆鈕」的垂直串，長度∝可建造類型數。B2.4 加了三隻角色，
10 → 13，超出可用高度。**而這正是名冊系統的目標狀態**——全收集的玩家打無盡就是 13 種。

**為什麼測試沒抓到**：`TL_CLICKTEST` 的 `last_btn` 只驗**最後一顆建造鈕**的 y 座標，
沒有驗它下面那三顆模式鈕；而 `_press()` 是**用座標合成點擊**的，一顆被底欄蓋住、
甚至被推到畫面外的鈕**照樣按得到**，於是 `blueprint=true` 是一個假綠燈。
→ **驗收條件要改成「鈕的矩形與底欄矩形不相交」，不是「按得到」。**

**方向**（擇一，落地時定案）：① 建造欄改捲動容器（同科技樹 §RG-47 的解法）；
② 生產／防線分兩段並各自可收合；③ 類型多於 N 時改雙欄。
**②同時解掉 A-4**，是目前的建議。

- [x] ✅ **B2.4.2 修好**：建造清單裝進捲動容器（`BUILD_LIST_H`），模式鈕釘在它下面；順手補上生產／防線分段（＝B-4）。斷言改成幾何的——釘住的東西不得越過 `BAR_Y`、最後一顆建造鈕捲到底之後要整顆在視窗內。**紅得起來**（把捲動區撐到 700 → `last_btn=false`）

#### A-2　★★★ 路徑帶是一個完美的矩形——核心視覺命題在最大的元素上沒有兌現

**證據**：全部七張截圖。路徑帶是**軸對齊、邊緣筆直、轉角 90°、嚴格貼齊 32px 網格**的矩形。

**這不是新意見，是 §1.6 已經寫著的東西沒有做**：
> 敵人路徑｜`tide.deep` 的低透明度帶狀底色 **＋ 不對齊網格的抖動邊緣**｜它屬於**混沌**側。玩家一眼看出「這條帶子不是我的」

而 §0 是全文件的統御命題：**秩序＝直線／對齊網格／銳角；混沌＝曲線／不對齊／抖動**。
目前畫面上**最大的一塊混沌側元素，是整個畫面裡最幾何的東西**。§0 說「任何新增視覺元素
都必須回答：它屬於秩序還是混沌」——路徑帶現在回答的是「秩序」。

**為什麼這是 A 級而不是 polish**：這條帶子佔畫面 10–15%，是玩家全程盯著的東西，
而它是**遊戲主題本身**。修好它，一張截圖就說得出這款遊戲在講什麼；不修，
它永遠是一張接線圖上的一條紫色膠帶。

**方向**：沿帶子邊緣加**確定性**的低頻抖動（振幅 3–5px，週期 2–3 格，
由格座標推出——**零 RNG**，`TL_SHOT` 要拍得出同一張圖）；轉角改圓弧或磨掉尖角。
**帶子本身仍然靜止**（§1.6 已定：路面不動，會動的東西留給敵人本體）。

- [x] ✅ **B2.4.3 落地**：`Shapes.band_jitter()`，振幅約 ±4px（兩個不同頻率的正弦疊加）。**抖動掛在格點上不是格子上**——相鄰兩格共用的角落要算出同一個偏移，否則帶子裂成一排錯開的方塊。零 RNG。★ **這是刻意克制的第一版，等使用者看過再定案**
- [x] ✅ **B2.4.5 使用者拍板**：振幅維持 ±4px（「路線可以做抖動」），但**抖動只留給路徑帶，侵蝕暈改回貼齊格線**（見 C-1）
- [ ] ⚠ **同一條裡還有一半沒做**：§7.1 原文是「抖動 ＋ **轉角改圓弧或磨掉尖角**」，而 `_band_quad()` 只位移四個角，**每一個轉彎仍然是硬 90°**。振幅已拍板，圓角這一半仍未做（code review 抓到）

#### ~~A-3　路徑帶轉角有疊色瑕疵~~ → ❌ **誤判，已撤回**

初稿依據「第一眼」與「核心近照」兩張**縮到 1280 寬**的截圖，宣稱轉角處的
alpha 疊了兩次。**查證後不成立**：

- `Maps.path_of()` 每一格**只 append 一次**（轉折點由 `cells.append(to)` 收尾，
  下一段從 `from + step` 起跑），路徑陣列裡沒有重複格。
- `_draw_path()` 是一格一個 `Rect2`，格與格不重疊。
- 直接對轉角拍 4× 近照（`TL_FOCUS="30,5,4"`）：**轉角與直線段同一個值，沒有接縫。**

我當初讀到的深色是 `_draw_incoming()` 的品紅箭羽（alpha 0.35）在轉角附近的密集，
被縮圖糊成一片。

> ★ **記下這條，它比原本那個「缺陷」有價值**：
> **縮圖上讀到的瑕疵，在變成工作項目之前必須先拍一張近照。**
> 本文件 §1.7 已經有一條同源的規則（「敵人在 fit 倍率只有 16–24px，輪廓差在
> 這個尺寸會死掉」）——那條講的是**設計不能只在近照上驗**，這條講的是反面：
> **缺陷不能只在縮圖上判**。兩條要一起用。（`50_QA_PLAN.md` RG-141）

#### A-4　★★ 核心看起來不像核心

**證據**：核心近照（3× 放大）——核心是一顆青色方塊加一圈細框，
**尺寸與視覺重量和一個中繼站沒有差別**，而旁邊的採集器圓形還比它大。

**§1.6 已經寫著**：
> 核心｜**最大的幾何體**，`order.bright` 描邊；受擊時整體閃 `warn.orange`

沒有做到。而核心是**全遊戲唯一一個「歸零就結束」的物件**，也是 §7.9 說的
「產線天生要往危險的方向拉」那句話的空間錨點。它必須是畫面上第一個被看到的東西。

**方向**：真的做到「最大的幾何體」（≥ 1.5 格）＋ `order.bright` 描邊 ＋ 一圈**極慢**的
`dur.ambient` 呼吸（它是這張圖的心跳，不是警示）。⚠ 形狀要和儲槽的同心圓、
交戰環分得開——§4.3b 那條「同一個位置上的兩個訊息，換顏色不夠，要換形狀」。

- [x] ✅ **B2.4.3 落地**：30 → **42px**（1.3 格）＋ 3px `order.bright` 描邊 ＋ 內方塊走 `dur.ambient × 2` 的極慢呼吸（心跳，不是警示）。形狀仍是正方，和儲槽的同心圓／交戰環分得開。★ 第一版寫 42px（1.3 格）**低於本節自己訂的 ≥1.5 格**，code review 抓到後改成 48。同時補上 §1.6 另一句一直沒做的「**受擊時整體閃 `warn.orange`**」——核心掉血是這一局唯一不可逆的事，不該和一個中繼被啃長得一樣

### 7.2 B 級：跨越點與資訊層級

#### B-1　★★ 跨越點沒有「架高」的樣子

**證據**：跨越點近照（3.5×）——畫的是**兩條垂直青線＋中間稍亮的填色**，
讀起來像一道**門框**或閘門，不像一座橋。

**§1.6 寫的三件事其實都在程式碼裡**（初稿說「只做到第一件」，**那是錯的**——
`_draw_crossing()` 有 `BG_DEEP` 0.55 的落影，也有伸出格外 ±6px 的引道與兩端橫桿）。
真正的問題是**強度**，不是有無：

| §1.6 要求 | 實作 | 判定 |
|---|---|---|
| `order.cyan` 雙線橋面 | 兩條 3px 平行線 | ✅ |
| 兩端 **45°** 引道 | 直線**延長 6px** ＋ 一根橫桿 | ⚠ **不是 45°**，是延長線。而 45° 是秩序側的既有語彙（導管只走 90°/45°），用它才接得上 |
| 橋下**可見的**路徑帶陰影 | 有，但內縮 3px、alpha 0.55 | ⚠ 在 fit 倍率下幾乎看不出來 |

**為什麼重要**：§1.6 自己說了——「架高」是**「橋上導管不受攻擊」這條規則的唯一解釋**。
規則現在只活在說明浮層的文字裡，畫面沒有在教它。而 §1.6 還有一條硬性要求：
**跨越點必須在不縮放、不 hover 的狀態下一眼可辨**。

**方向**：補 45° 引道（秩序側的 45° 語彙，和導管同一套）＋ 橋面下方壓一道
`bg.deep` 的落影，讓橋面浮在帶子上。

- [x] ✅ **B2.4.3 落地**：落影改**滿格＋往側邊偏 4px**（有偏移才讀得出高度，等距暗邊只是描邊）；引道改**真的 45°**，橋頭往外張開接回地面。近照確認它現在讀起來是一座橋不是一道門框

#### B-2　★★ 頂欄有 11 個數字，全部同一個字級與權重

**證據**：交戰中的頂欄一行讀出來是——
`礦砂 1,059`／`▲9.1/秒`／`合金 125`／`▲1.9/秒`／`能量 60/10`／`儲槽 300/300`／
`交戰 0 座`／`擊殺 14`／`第 2 波`／`敵人 4`／`核心 1000/1000`。

**這直接違反本文件 §2 的反模式清單**：
> ❌ **資訊過載的 HUD**：不做同時顯示 20 個數字的儀表板。**每個畫面最多 4 個「主要數字」**。

**這一批做對的一件事要留著**：顏色分群是有效的（青＝礦砂、紫＝合金、琥珀＝能量、
品紅＝波次）。缺的是**字級的層級**——目前 11 個數字全是同一階。

**方向**：分三階，不刪任何資訊（資訊完備性是 R-3 的要求，不能為了乾淨砍掉）：
| 階 | 內容 | 依據 |
|---|---|---|
| 主（`text.lg`） | **能量供給／需求**、**核心**、**波次／倒數** | 這三個是「我現在會不會輸」的全部答案 |
| 次（`text.base`） | 礦砂、合金 | 「我買不買得起」 |
| 附屬（`text.sm`，貼在主數字旁） | `▲/秒`、儲槽、交戰座數、擊殺 | 它們是上面那幾個的**註腳**，不是獨立的數字 |

- [x] ✅ **B2.4.4 落地**：`Battle.TOP_CELLS` 一張表定義五欄，每欄 `[標籤 sm][數值 16 或 22][註腳 sm]`。三階已寫回 §1.2 的用途欄。
- [x] ⚠ **同批發現：三階字級排不下 11 個數字**（第一版實測**超出畫面 166px**，`bar_clear` 當場紅）。而 §2 反模式清單自己就寫著「每個畫面最多 4 個『主要數字』」——所以這一條的真正結論是「**頂欄數字太多**」，不只是「層級不夠」。從七欄縮成五欄，**搬家不是刪除**：
  - **儲槽存量** → 能量面板（它是「儲槽充能／放電」那兩列的分母，放在旁邊比放在頂欄更講得通）；地圖上每顆儲槽身上還有一圈琥珀弧，「哪一顆滿了」讀得到
  - **交戰座數** → 併進能量那一格的註腳（它**就是**能量需求的解釋）
  - **快進倍率** → 搬回底欄那顆「快進 4×」鈕自己身上（它原本同時出現在兩處，而那顆鈕**看不出開沒開**——因在底欄、果在頂欄）
  - **擊殺數** → ❌ **從局內 HUD 移除**。它是計分不是決策輸入，沒有任何一個當下的操作取決於它；局末結算裡有，回收者換到的能量在能量面板有專屬一列

#### B-3　~~★★ 等寬數字從來沒有實作過——頂欄每秒橫向抖十次~~ → ★ **半數誤判，已修正**

**初稿的證據是 grep**（`tabular|FontVariation|monospace` 零命中），由此推論
「礦砂每 tick 都在變 → 右邊六格每秒被推動十次」。**量過之後這句話是錯的。**

B2.4.4 先寫了 `_topbar_selftest()` 才動手，量兩件事：

| 情況 | 修之前 | 意義 |
|---|---|---|
| **同位數換數字**（1111 → 8888） | **不位移** ✅ | 微軟正黑體的數字**本來就是等寬的** |
| **跨位數**（1111 → 99） | 位移 ❌ | 這才是真的缺陷 |

於是：

- **「每秒橫向抖十次」不成立**——礦砂逐 tick 的變動絕大多數不跨位數。
- **原本開的處方（`FontVariation` 開 `tnum`）修的是一個不存在的問題**，而且它對真正的缺陷（跨位數）**無效**——`tnum` 只保證每個數字等寬，不保證總寬相同。
- 有效的只有 §3 自己寫的備案：**固定寬度容器 ＋ 右對齊**。

**教訓（登記為 RG-143）**：**用 grep 推出來的缺陷，連「症狀存在」都還沒證明。**
`grep` 證明的是「沒有用某個 API」，不是「有那個問題」——中間隔著一個
「不用那個 API 也可能沒問題」的假設，而這次那個假設是錯的。這與 A-3 是同一類，
但更危險：A-3 錯在證據（縮圖），這一條錯在**推論**，而且它還連帶開錯了處方。

- [x] ✅ **B2.4.4 落地**：每欄數值與註腳都是固定寬度＋右對齊；`TL_CLICKTEST/topbar` 兩種情況各一條斷言，兩條都紅得起來（拿掉 `custom_minimum_size` → 跨位數那條倒）

#### B-4　★ 建造欄沒有「生產／防線」的分界

**證據**：`採集器 發電機 熔爐 中繼 儲槽 錨 稜鏡 潮鳴 回收者 碎浪 長哨 霜礁 定潮`
一路排下來，13 顆一模一樣的鈕，中間沒有任何分界。

**而優先權面板為了同一件事**特地做了雙欄（`NodeDefs.PRIORITY_SPLIT`），
本文件也記了理由：「**兩欄不是排版，是這個面板的整句話**：左欄是生產線、右欄是防線」。

**建造欄是玩家做同一個決定的地方**（這 40 礦砂餵產線還是餵防線），卻沒有這個編碼。

**方向**：分兩段，中間一條 `border.subtle` ＋ 段標題。**A-1 的方向②同時解掉這一條。**

- [x] ✅ **B2.4.2 就順手做掉了**（勾選漏更新，B2.4.4 補記）：建造清單改捲動容器時一併加上「生產／防線」段標題

#### B-5　★ 底欄七顆鈕權重相同，其中一顆是不可逆的

**證據**：`快進 4×｜提前召喚 +50%｜優先權｜藍圖｜說明｜能量｜圖鑑`——
七顆同樣大小、同樣顏色的鈕。

其中**「提前召喚」會改變這一波的獎勵倍率且無法撤回**，而「說明」只是開一張浮層。
它們長得一模一樣。§2 反模式沒有明寫這一條，但 §1.6 浮層那兩條規則的精神是一致的：
**視覺重量要對應後果的重量**。

**方向**：三群——狀態切換（快進／提前召喚，帶 `energy.amber` 描邊）｜
浮層開關（優先權／藍圖／能量／圖鑑）｜說明。群間用間距分，不加線。

- [x] ✅ **B2.4.4 落地**：三群（群間 24、群內 8，皆在 §1.3 間距階上），不加線。
- [x] ⚠ **修正上面那條方向裡的一個錯誤**：`energy.amber` 描邊**違反 §1.1 配色紀律 2**
  （「琥珀色**專屬於能量**。看到琥珀＝跟耗能有關」），而快進與提前召喚都跟耗能無關。
  改成**只有「提前召喚」單獨用 `warn.orange` 文字色**：紀律 1 說橙／品紅專屬於
  「敵人與危險狀態」，而這顆鈕做的事**就是把敵潮叫過來**——它是玩家自己選的危險。
  同一條紀律，而且比琥珀是更強的編碼（全底欄唯一不可逆的那一顆，顏色說得出來）。
  **快進不上色**：它可逆，不該和不可逆的那顆共用重量。

### 7.2b　遊玩測試 P3／P2 回收到的缺陷（2026-08-06，v0.26.5）

`50_QA_PLAN.md` §8 的第一次實跑。**四條都不是 B2.4 這幾批引入的**——它們一直在，
只是從來沒有人坐在玩家的位置上看過。

#### P3-1　★★★ 發電機與錨是同一個形狀，只差大小與顏色

**證據**：`TL_CLICKTEST` 的 13 種合照，**fit 倍率**（32px 格，就是實際遊玩倍率）。

| | 形狀 | 尺寸 | 顏色 |
|---|---|---|---|
| **發電機** | 實心方塊 | 22px | `energy.amber` |
| **錨** | 實心方塊 | 16px | `order.cyan` |

§1.6 的硬性要求是**不靠顏色分辨**，而這兩個之間**只剩顏色與 6px 的尺寸差**。
更糟的是它們分屬**生產側與防線側**——正是「這 40 礦砂餵產線還是餵防線」那個
核心決定的兩端，也是玩家最需要一眼分開的兩類。

**方向**：改**錨**不改發電機（發電機的「琥珀實心方」和能量的關係更強，而錨是
八隻塔裡最基礎的一隻，改它波及最小）。候選：**厚邊空心方**（與碎浪的雙方框
仍要分得開）或**上寬下窄的梯形**（樁的意象）。**要在 fit 倍率驗收，不看近照**（RG-106）。

- [x] ✅ **B2.4.8 落地**：錨改成**上寬下窄的實心梯形**（打進地裡的樁）。fit 倍率合照確認與發電機的實心方分得開。

#### P3-2　★ 回收者與碎浪在 fit 倍率下都是「方框裡包東西」

內部形狀不同（實心圓 vs 實心方），但在 32px 上要盯著看。優先度低於 P3-1
（兩個都在防線側，混淆的代價比 P3-1 小）。

- [x] ✅ **B2.4.8 落地**：改的是**碎浪**——「實心方＋外框」換成**四角爆散星**。
  這樣不只解掉碰撞，還讓它**把自己的機制講出來**：碎浪是全遊戲唯一一隻範圍傷害的塔。
  方框那一族現在只剩回收者一個。
- [ ] ⚠ **換來一個新的鄰居要盯**：碎浪（四角爆散星，實心）與霜礁（六芒星，細線）
  都是合金銀的放射狀形狀。fit 合照上「實心塊 vs 細線」分得開，但這一對要列入
  P3 的常設觀察。**這是換位置不是解決**，如果實玩會混就再改碎浪（回收者已經讓位了）。

#### P2-1　★★★★ 缺料徽章說得出「誰在挨餓」，說不出「餓的是礦砂還是電」

**證據**：第 5 關 tick 900 的 `TL_NAKED` 圖上有三個橙色倒三角，讀得出「這三座在挨餓」。
而同一格的實際狀況是——**能量 60/51（供給充足），餓的是礦砂**。裸眼完全看不出來。

**為什麼這是這一批裡最重的一條**：全案的核心命題是**峰值電力**（`10_GDD.md` §3.1）。
「我現在缺的是電還是礦」是玩家每一分鐘都要回答的問題，而三態徽章——地圖上唯一
指出瓶頸的元素——**對這個問題完全沉默**。玩家只能回頭讀頂欄數字，
而那正是 R-3 要對策掉的東西。

**方向**：徽章分色（`energy.amber` ＝ 缺電、`order.cyan` ＝ 缺礦）**不夠**——
§1.6 說換顏色不夠要換形狀。可行的是**在倒三角裡放一個一格的內符**
（電＝直線閃電／礦＝小方塊），或**缺電用倒三角、缺礦用倒梯形**。

- [x] ✅ **B2.4.8 落地**：`SessionState` 的三態改四態，`STARVED`（缺礦砂，橙色倒三角）
  與 `STARVED_POWER`（缺電，琥珀閃電）分家。形狀與顏色**同時**不同。
  同一張第 5 關 tick 900 的裸眼圖現在讀得出「**碎浪缺電、熔爐缺礦**」——
  而修之前那三個徽章一模一樣。`flow_test`（斷電熔爐）與 `hud_test`（沒礦砂的發電機）
  兩條互為對照組，**單獨一條可以靠「永遠回傳同一個值」矇混過去**。

#### P2-2　~~★★ 階段色調實際上讀不到~~ → ❌ **誤判，已撤回**

初稿宣稱「兩張 `TL_NAKED` 並排看背景差異幾乎為零」，並提議刪掉階段色調。
**量過之後不成立**——同一片空地的像素值：

| 階段 | RGB |
|---|---|
| 準備期（`order.cyan` 0.03） | `(19, 37, 52)` 偏藍 |
| 波次期（`warn.orange` 0.10） | `(40, 42, 47)` 偏暖灰 |

R 差 **21**、B 差 5。色調**確實在工作**，而且是全畫面的整片位移（玩家看得到的是
**切換的那一瞬間**，不是兩張靜態圖的比對）。

**我當時是前後分開看兩張截圖，不是量。** 這是**第三次**同一個錯法
（A-3 錯在證據、B-3 錯在推論、P2-2 錯在**沒有並排也沒有取樣**）。
登記為 **RG-144**：**視覺差異的斷言一律取樣像素，不靠記憶比對兩張圖。**

### 7.3 C 級：氣氛與收尾（B1.6 尾巴的兩筆舊債）

#### C-1　★ 背景是一塊平的色，而本文件的氣質句子是「正在被某種活的東西侵蝕」

**證據**：全部截圖的背景都是均勻的 `bg.deep` ＋ 一層極淡的網格。**沒有深度、沒有環境**。

§2 的氣質是「*冷靜的工程製圖，正在被某種活的東西侵蝕*」，§5 的音訊氣質是
「像在一座深海工廠裡工作」。**音訊那一半做到了**（drone、生產嗡鳴隨流量、
開打號令），視覺這一半沒有對應物。四個參照裡的 **Creeper World**（有機威脅緩慢
吞噬幾何秩序）目前完全沒有在畫面上出現。

**方向（刻意保守，反模式清單擋掉大部分做法）**：❌ 不做視差（§4.3「一定不動」）、
❌ 不做霓虹漸層、❌ 不做粒子。可做的是**一層極低對比的深度**——例如網格在遠離
核心處更淡、或路徑帶周圍一圈極淡的 `tide.deep` 暈染（＝「侵蝕」的字面畫法，
而且它同時是有用的資訊：**敵人 walk-by 的傷害半徑就是那一圈**）。

> ★ **後者是這一節唯一一個「新設計」而非還債的點子，也是我認為最值得做的一個**：
> 它把一個純氣氛需求和一條玩家真的要知道的規則（「退開路徑 2 格就安全」）
> 合成同一個元素——**一個元素答兩題**，正是 §1.6 給來襲方向箭羽的同一條理由。

- [x] ✅ **B2.4.3 落地**（使用者拍板：做，而且要能讀出傷害半徑）：路徑外一格的 `tide.deep` 0.13 填色 ＋ **最外圈一條 0.5 的輪廓線**。★ **只有填色是不夠的**——淡淡一塊顏色說得出「這附近危險」，說不出「危險到哪裡為止」，而後者才是玩家要下的決定（塔要退開幾格）。`TL_NAKED` 下一眼看得出哪幾座建築在圈內。

> ⚠ **暈講的是「建築（節點）」，不是導管**（code review 抓到的一條真矛盾，
> 結論是**修措辭不是修畫面**）。兩條規則在同一片像素上都成立：
> **節點沒有任何免疫**（`BattleController.BLAST_CELLS` 九格，無例外）→ 暈說的是實話；
> 而**導管在橋與引道上免疫**（`Tide.immune_indices()`，`RAMP = 1`），那幾格正好落在暈裡。
> **不要在橋兩側把暈挖一個缺口去「修正」它**——那會讓一個蓋在缺口裡的中繼看起來安全，
> 而它會被啃。導管的那半邊由**橋的圖形**負責講（§1.6：「架高」是「橋上導管不受攻擊」
> 的唯一解釋），兩個元素各講各的那一半。

- [x] ✅ **B2.4.5 修正（使用者拍板：「侵蝕暈不要做抖動」）**：暈改走 `_cell_quad()`，**貼齊格線**；抖動只留給路徑帶。
  理由事後看很清楚，而我第一版沒想到——**這兩層要答的問題不同**：路徑帶講**氣氛**（潮＝混沌，§0 的視覺論題），
  侵蝕暈講**規則**（我的建築要退開幾格）。**一條抖動的輪廓線量不出格數**，而格子正是玩家放建築的單位。
  §0 說「任何新增視覺元素都必須回答：它屬於秩序還是混沌」——暈的答案是**秩序**，因為它是一條給玩家用的尺，
  不是潮的一部分。**教訓**：把一條規則畫成混沌側的樣子，等於用美術把它的可用性拿掉。

#### C-2　★ 擊殺碎片在實際遊玩倍率下幾乎看不到

**證據**：開火特效近照（3×）裡碎片是**幾顆小小的品紅三角**。
fit 倍率（≈1×）下它們只有那個尺寸的三分之一。

§2 反模式清單擋住了「200 顆粒子」，這是對的，**但 3–5 個看不見的碎片等於沒做**。

**方向**：不加數量（反模式），加**單片的尺寸與初速**，並讓它在生命前 30% 用
`order.bright`／`tide.bright` 起亮再落回本色——**明度差在小尺寸下活得下來**，
這正是 §1.7 的 `tide.bright` 當初被建立的理由，同一條經驗直接套用。

- [ ] 修好，並用 `TL_FOCUS` 在 `ttl` 走到一半那一格驗收（§1.7 末的陷阱）

#### C-3　色彩層次校準與局末結算逐項揭示（§4.3b 明列的「未實作」）

§4.3b 自己列著：**未實作**：局末結算逐項揭示（`dur.slow`）、色彩層次校準。
使用者 2026-08-01 表示「畫面的部分也先這樣，到時候再說」→ 這兩筆一直掛著。

- [ ] 色彩層次校準（參照 Dorfromantik：**不刺眼的長時間可讀性**。這是唯一一個
      需要真人長時間遊玩才判得出來的項目——**agent 判不了，要留給使用者**，§0.1）
- [ ] 局末結算逐項揭示（`dur.slow`，且照 §4.4 可按任意鍵跳到底）

### 7.4 建議的批次順序

| 批 | 內容 | 為什麼是這個順序 |
|---|---|---|
| **B2.4.2** | **A-1** | 它是功能缺陷，而且是我這一批弄壞的。不能過夜 |
| **B2.4.3** | **A-2 ＋ A-4 ＋ B-1 ＋ C-1** | 都在同一層（地圖底層繪製），一起改一次就好；而且它們合起來才是「核心視覺命題真的被畫出來了」這一件事。~~A-3~~ 已撤回（誤判） |
| **B2.4.4** | **B-2 ＋ B-3 ＋ ~~B-4~~ ＋ B-5 ＋ 階外字級清理** | ✅ 已完成。B-3 先做是對的，但**先做出來的是「量測」而不是「修法」**——量完才發現原本的處方在修一個不存在的問題（見 B-3）。B-4 在 B2.4.2 就順手做掉了 |
| **待拍板** | **C-1 ＋ C-2 ＋ C-3** | C-1 要使用者選方向；C-3 的色彩校準本來就只有使用者判得了 |

> **每一條落地時的共同驗收**：§6.3 全部勾完 ＋ `TL_NAKED` 檢定 ＋ **13 種建造欄的截圖**
> ＋ 對應的 `TL_FOCUS` 近照。**改完要親眼讀那張 png，並回答「這張圖證明了什麼」。**
