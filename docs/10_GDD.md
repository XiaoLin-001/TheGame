# 《潮與線 / Tide & Line》遊戲設計文件（GDD）

> **設計唯一權威。** 任何設計改動先改本文件，再改程式碼。程式碼與本文件衝突時，當批修正。
> 上位文件：`00_CONCEPT.md`（願景與紅線，本文件不得違反）。
> 狀態：**v0.3 前期製作定稿**（開工前設計已收斂，見 §8）　|　最後更新：2026-07-27

---

## 1. 願景陳述

> **你的生產線就是你的防線。**

玩家在一張有礦點與敵人路徑的地圖上，用**節點**與**導管**蓋出一套會自己運轉的生產網路；同一套網路同時餵養**塔（角色）**。塔不是蓋完就免費站著——**它持續消耗你的能量產出**。於是「防線再厚一點」與「產能再大一點」是同一條資源曲線的兩端，每一秒都在互相扣減。

**樂趣假設（G1 唯一驗收標準）**：
> 玩家會在波次倒數剩 40 秒時，明知有風險仍選擇再擴一條產線——並且在防線破掉之後想立刻重來一次。

**憲法級平衡哲學**（前期定死，生產期不重談）：

| # | 決策 | 內容 |
|---|---|---|
| B1 | 成長用**加法為主、乘法為輔** | 科技樹以「+2 吞吐」「+15% 效率」為主，禁止出現 ×2 以上的乘算堆疊，避免後期數值爆炸與平衡失控。 |
| B2 | **局外成長分兩軸，各有不同天花板** | **① 科技軸**（研究數據，只能靠遊玩取得）：全解鎖對戰鬥數值總增幅 ≤ **+35%**。<br>**② 等級軸**（tycoon 升級材料，可掛機／可加速取得）：塔與生產節點各 10 級，每級 +8%，滿級 **+80%**。<br>**硬性配套：等級軸的增幅必須被難度層同步吃掉**——難度層 N 的敵人強度縮放係數以「等級軸滿級」為基準校準。**等級軸買到的是「能挑戰更高難度層」，不是「碾壓現有內容」。**<br>局內只有臨時升級（隨局結束消失）。 |
| B3 | **競技分層：一榜純技術，一榜比戰力** | 每日挑戰跑**雙榜**，同種子、同地圖，只有起始配置不同：<br>**① 統一配置榜**——固定角色組、固定科技、固定等級；不論進度、抽卡或課金，所有玩家起始能力完全相同。<br>**② 自由配置榜**——玩家的完整戰力。<br>**統一配置榜永遠存在、永遠免費、永遠可爭第一。不得移除、不得收費、不得加入任何付費影響因子。**<br>**誠實註記**：第一版純離線（§3.10），兩榜實際上是本機最佳成績，沒有對手。**「對 P2W 的制衡」是接上線上榜之後才兌現的承諾**——在那之前它是架構保留，不是既成事實。不要在文案或對外說明中宣稱一個尚未存在的競技場。 |
| B6 | **賣速度，不賣次數，不賣進度** | F2P 變現只能**加快**既有路徑。所有內容與進度都必須能純靠遊玩取得（付費只是省時間）。<br>禁止：賣遊玩次數／體力（紅線 R2）、賣重試權、賣關卡解鎖鑰匙、**付費復活**（同時破壞 R1 與難度層／排行榜的意義）。 |
| B4 | **失敗只花時間** | 對應紅線 R1。任何失敗不扣除任何已獲得物。 |
| B5 | **不可暫停，但可跳過等待** | 戰鬥中固定 1× 速度。**準備期**可快進（跳過空等），戰鬥階段不可加速也不可減速。★ **B1.4.1：局內選單（ESC）也不暫停**——一顆可以隨時停下來慢慢想的鍵正是本條要擋掉的東西。選單面板上直接寫「時間仍在走」。 |

---

## 2. 核心循環

### 2.1 局內循環（一圈 60–120 秒）

```
        ┌──────────────────────────────────────────────────┐
        │                                                  │
        ▼                                                  │
  ①勘查 ──► ②拉線佈產 ──► ③資源流動 ──► ④【取捨】───┐      │
  讀地形     動詞：放節點    回饋：線變粗   擴產 or 築防  │      │
  找礦點     ＋連導管        數字上升                    │      │
  找路徑                                                 │      │
                                                         ▼      │
                              ⑦準備期修補 ◄── ⑥缺口暴露 ◄── ⑤波次來襲
                              45 秒可規劃      學習：哪條線      壓力：塔開火
                              可快進           不該那樣走        持續吃能量
                                     │                                 │
                                     └─────────────────────────────────┘
```

**每一步的時長與爽點**：

| 步驟 | 時長 | 爽點 |
|---|---|---|
| ① 勘查 | 15 秒（首波前） | 「這張圖的礦點分佈很刁鑽」的解謎感 |
| ② 拉線佈產 | 20–40 秒 | 線亮起、資源開始流動的即時回饋 |
| ③ 資源流動 | 持續 | **線的粗細會隨實際流量變化**——看得見自己的網路在呼吸。粗細在全圖可互相比較：同樣粗＝同樣流量，不管那條線升到幾級 |
| ④ 取捨 | 5–10 秒的決定 | **本作的心臟**。多一座塔＝少 8 能量/秒＝熔爐慢 20% |
| ⑤ 波次 | 20–40 秒 | 塔自動開火、能量條下探、線被打斷的緊張 |
| ⑥ 缺口暴露 | 即時 | 「早知道那條線該繞後面」的懊悔＝學習 |
| ⑦ 準備期 | 45 秒（可快進） | 修補、擴張、下注（提前召喚） |

### 2.2 外層循環（跨局）

```
打完一局 ──► 結算：研究數據＋稀有原料＋聲望
                │
                ├──► 科技樹（產能／防務／後勤三支）──► 下局起手更強、選擇更多
                ├──► 名冊（解鎖／抽卡新角色）────────► 下局玩法組合改變
                └──► 潮汐公司（tycoon，M2）─────────► 升級材料、招募券、藍圖槽、難度鑰匙
                                │
                                └──► 回到「再打一局」
```

### 2.3 長線留存的三根支柱

tycoon 已定位為**附屬補給站**而非獨立遊戲（§3.8），因此**留存不能靠「換個遊戲玩」**。三根支柱各自承擔不同的回訪動機：

| 支柱 | 回訪動機 | 節奏 |
|---|---|---|
| **成長曲線** | 「我的科技樹再推兩格就能解鎖那隻塔」 | 每次遊玩都在推進 |
| **每日種子挑戰** | 「今天的圖跟昨天不一樣，我想試試」 | 每天一次的新鮮理由 |
| **難度層精通** | 「上一層我差三波就過了」 | 卡關時的長期目標 |

**tycoon 的留存角色是「低壓力的短暫停留」**：打完一局進去收成、排下一批訂單、看著數字長，一兩分鐘就走。它降低關遊戲的動作成本，但它**不是**疲勞出口——真正的疲勞出口是**換一種塔防玩法**（戰役 ↔ 無盡 ↔ 每日挑戰，三種節奏完全不同）。

---

## 3. 系統清單

> **紀律**：每個系統都必須回答「服務核心循環的哪一步」。答不出來的進 backlog，不進 GDD。

### 3.1 流量網路（Flow Network）★核心
**服務循環步驟**：②③④　|　**里程碑**：M0

本作的自動化不是物件模擬（不搬運實體物品），而是**流量網路解算**。這是同時滿足效能、確定性、可讀性、極簡美術的唯一選擇。

**物件模型**：

| 概念 | 定義 |
|---|---|
| **節點 Node** | 地圖上的建築。有座標、生命值、輸入埠需求（demand）、輸出埠供給（supply）、內部處理速率。 |
| **導管 Line** | 連接兩節點的有向邊。有長度、吞吐上限（cap）、生命值、當前流量（flow）。 |
| **網路 Network** | 節點與導管構成的有向圖。每 tick 解算一次分配。 |

**解算規則**（純函式、零 RNG、確定性）：

1. 每 tick（0.1 秒）為一個解算週期。
2. 依資源種類分別解算（礦砂網、能量網、合金網互不干擾）。**解算順序固定為礦砂 → 能量 → 合金**：發電機的能量產出要乘自己的礦砂滿足率，熔爐的合金產出要乘自己那兩者中較低的一個。倒過來解會讓「上游缺料」延遲一個 tick 才傳到下游，玩家讀不出因果。
3. 每個消費節點聲明 `demand`，每個生產節點聲明 `supply`。
4. 沿圖做**容量受限的比例分配**：若某導管的請求總量 > `cap`，所有下游按**優先權加權**後等比例削減。
5. 消費節點拿到的量 < demand 時，以 `滿足率 = 實得/需求` 線性降低其產出或效能（**不直接停機**——避免全網崩潰的雪崩感）。
6. 解算完成後寫回每條導管的 `flow`，供視覺化使用。

**供不應求時的行為（重要設計決策）**：

當 `demand > supply` 時，本作**不停機，改為按比例降速**：`實際效能 = 標稱效能 × 滿足率`。

| 為什麼不停機 | 說明 |
|---|---|
| **停機會雪崩** | 能量不足 → 塔停火 → 敵人活更久 → 打壞更多導管 → 能量更少 → 全滅。玩家無法指出自己在哪一步輸的。降速是漸進的，玩家在 60% 效能下仍有搶救空間 |
| **降速可診斷** | 節點直接顯示「60% 飢餓」，玩家一眼知道缺口多大。停機只傳達「壞了」，不傳達「差多少」 |
| **與紅線一致** | R1「失敗只花時間」隱含「玩家要能看懂自己為什麼輸」。雪崩式崩潰違反這一點 |

> **真實工廠確實是批次運作**（湊不滿一批就停機）。本作刻意不模擬這一點——那是寫實感的代價，而我們要的是可讀性與可救性。

**★ 能量是流率，不是水池**（2026-07-27 定案）：

**沒有全域能量池。** 每 tick 的供給與需求就地結算，能量不會自動累積在某個看不見的帳戶裡。**唯一的儲存體是儲槽節點，它有座標、有導管、有射程外的距離。**

| | 決策 |
|---|---|
| 頂欄顯示 | `能量 40/63 ⚠`＝**本 tick 供給／需求**（滿足率 63%）。不是存量／容量。 |
| 儲槽顯示 | 另列 `儲槽 340/600`＝全部儲槽的充能總和／總容量。 |
| 儲槽解算語意 | 全網盈餘時它是**消費者**（充能，吃優先權權重）；赤字時它是**供給者**（放電）。充放電速率**受它自己那條導管的 `cap` 約束**——這是它與全域水池的根本差別。 |

**為什麼不做全域水池**：水池不需要管線送電，於是儲槽變成「+300 容量」的被動配件，蓋在地圖哪個角落都一樣。那會同時廢掉三件事——儲槽的「核心策略建築」定位、導管加粗的取捨（§7.2）、以及「你的生產線就是你的防線」這個空間論述。**儲槽放遠了、幹線沒加粗，它的電就趕不到稜鏡那裡**，這正是我們要的。

**★ 不變量：網路永不全域死鎖。**
**採集器沒有任何輸入需求**（只要蓋在礦點上就出礦）。因此網路永遠存在一個不依賴任何其他節點的源頭，結構上不可能出現「A 等 B、B 等 A」的全域凍結。
**任何新增的生產節點都不得破壞此不變量**——若未來要加入「需要能量才能採礦」的節點，必須同時保留至少一種零輸入的採集手段。

**優先權面板（本作最重要的 UI 元件）**：
玩家為**每一種節點類型**（錨／稜鏡／潮鳴／回收者／熔爐／儲槽／…）設定 1–5 的優先權。**當能量不足時，優先權決定誰先餓死：塔停火，還是熔爐停產。** 這把「痛苦的取捨」做成一個玩家隨時可以拉的滑桿——它不是選單設定，它是戰術動作。

**★ 為什麼是「依類型」而不是「依節點」**（2026-07-27 定案）：

1. **戰術動作必須是一個手勢。** 遊戲不可暫停。滑桿永遠在同一個位置＝一個手勢；在地圖上找到那顆節點再拖它＝兩三個手勢。依節點會把最重要的戰術動作變成操作負擔（R-1）。
2. **操作負擔必須恆定。** 依節點的話，蓋 20 座塔就有 20 條滑桿要管——每蓋一座建築就多一個要顧的東西，這是操作負擔的滑坡。依類型永遠是 5–8 條。
3. **依節點解的是耗能模型已經解掉的問題。** 待機耗能為 0，沒開火的塔本來就不吃電；「這座稜鏡比那座重要」只在兩座都在交戰時才存在，而兩座都在交戰就代表兩座都有敵人要打。單體差異已由每座塔各自的**目標選擇**（§3.3）承擔。
4. 面板恆為 5–8 條，44×44 px 觸控命中區放得下（`20_ART_DIRECTION.md` §1.3b P2）。

**★ 一列＝一個「角色的角色」，不是一個資料表的鍵**（B2.4 定案，§7.13 有表）：
上面第 2 點在 M3 的 24 隻角色底下會自己失效——一隻角色一條滑桿就是 28 條滑桿，而那個面板已經不是一個手勢，是一份表單。所以**新角色併進既有的那一列**（長哨・定潮 → 錨那一列＝單體物理；霜礁 → 潮鳴那一列＝光環），滑桿永遠是九條的子集。這不是放寬第 2 點，這是**兌現**它：優先權的單位一直都是「這一類東西要不要先餓死」，而不是「這個鍵」。
只影響 UI 與預設值——`FlowNetwork` 仍然逐 type 讀 `priorities[type]`。

**儲槽在優先權面板佔一格**：盈餘時它是可調的低優先權消費者（充能搶不搶得贏熔爐），赤字時它是供給者。這讓「這一波要不要留電」成為一個戰術決定。

**可讀性規範**（風險 3 的對策，硬性要求）：
- 導管**粗細 = 實際流量 ÷ 全遊戲最大 cap**（★ B1.1.1 改；原本分母是「這條線自己的 cap」＝飽和度）
- 導管**顏色**：正常（青）／飽和（亮青，滿載）／飢餓（暗青閃爍）
- 節點徽章：`缺礦砂` / `缺電` / `滿溢` / `正常`（★ **B2.4.8 從三態改為四態**）
  - **`正常` 不畫任何東西**（★ B0.6 定案）。徽章是**例外標記**：一屏 14 個節點全部掛上「我很好」的圖示，等於把要找的那兩個異常埋進 14 個雜訊裡——那正好與 R-3「一眼看出瓶頸」相反。第一態的視覺編碼就是「乾淨」。
  - `缺礦砂`＝**它要的礦砂沒到齊**：節點上方一個橙色**倒三角**（空的、往下）。
  - `缺電`＝**它要的能量沒到齊**：節點上方一個 `energy.amber` 的**閃電**。
    > ★ **為什麼要分家**（B2.4.8，遊玩測試 P2-1）：原本兩者共用同一個橙色倒三角，
    > 於是「地圖上唯一指出瓶頸的元素」**說得出誰在挨餓，說不出餓的是什麼**。
    > 而本作的核心命題就是**峰值電力**（§3.1）——「我現在缺的是電還是礦」是玩家
    > 每分鐘都要答的問題，把它留給頂欄數字，正好是 R-3 要對策掉的東西。
    > 形狀與顏色**同時**不同（§1.6 說換顏色不夠、要換形狀，這裡兩個都換）；
    > 琥珀是 `20_ART_DIRECTION.md` §1.1 配色紀律 2 的「琥珀專屬於能量」在幫忙。
  - **兩者都缺時取滿足率較低的那一個**；相等時算 `缺電`（峰值電力是本作的核心約束，
    而且兩邊都缺時先補電比較有機會一次解決）。程式碼一律用 `SessionState.is_starved()`
    問「有沒有在餓」，**不要寫 `== STARVED`**——那在分家的當下就會安靜地漏掉一半。
  - `滿溢`＝**它產得出來但送不掉**（本 tick 有推不出去的餘量）：節點上方一個青色**正三角**（滿的、往上）。這是「導管被打斷」在地圖上唯一的即時說法——B0.4 只有頂欄的 `▲0.0/秒`，玩家看得到結果卻找不到位置。
  - **兩態各自只掛在「跟它有關」的節點上**：`缺料` 只給**自己宣告過需求**的（熔爐、交戰中的塔、發電機的燃料），`滿溢` 只給**自己產得出東西**的（採集器、發電機）。核心的礦砂需求（收剩下的）與儲槽的充能需求（有多少收多少）是解算器合成的**機會性需求**，中繼手上塞著的餘量則是路由後果——照字面掛上去，這三種會在正常遊玩中恆亮，例外標記就退化成背景雜訊。
- **驗收標準（★ 執行方式已定案）**：以 **`TL_NAKED=1`** 產出**隱藏全部數值標籤**的截圖——只留線寬、顏色、三態徽章——交 Opus 5 指認瓶頸節點，答對才算過。
  *為什麼不是「找個未受訓玩家」*：本案沒有這個人（樂趣與首次體驗由使用者本人判，而他是設計者；agent 只驗程式碼與可讀性）。而拿**有數字**的截圖問 agent 一定會過——它答對的是「我讀得懂 `能量 40/63`」，不是「線寬與顏色在傳達資訊」。遮掉數字才驗得到真東西。詳見 `30_TECH_DESIGN.md` §4.1、`50_QA_PLAN.md` §0.1。

### 3.2 建造系統
**服務循環步驟**：②⑦　|　**里程碑**：M0

- **放置**：從建造面板選節點類型 → 地圖上點擊放置。網格對齊（32px）。
- **連線**：**從起始節點按住左鍵 → 拖到目標節點放開 → 生成導管**。可經中繼節點轉折。
  - **★ 拖曳是唯一的拉線方式，沒有「連線」模式**（B1.6.2 導入拖曳，B1.3.1 移除模式鈕）。按在既有節點上時，該次點擊延到放開才判定：放開在另一個節點上＝拉線，放開在原地＝該模式原本的動作。理由：真正的建造節奏是**蓋一個 → 接一條 → 蓋一個 → 接一條**（第 1 關的參考解就是 6 蓋 6 接交替），每一步都往返左欄等於在最頻繁的動作上加 10 次無決策的點擊。留一顆只能點兩次的模式鈕，等於在教一個比較慢的做法。
- **模式只有三個**：建造（選了哪一種建造鈕就是哪一種）、加粗、拆除。**平移不是模式**——按住滑鼠中鍵拖（B1.3.1，使用者指定）。模式的代價是「忘記切回來時點地圖沒反應」，而平移頻繁到會天天付這個代價。
- **建造鈕上只寫名字，不寫價**（B1.3.1，使用者指定）：價牌在提示列的第一行，滑鼠停在鈕上時圖鑑浮層也會寫一次。同一個數字印三遍只是把鈕撐長。
  - **★ 選好起點後，地圖上畫出八條方向導引線**（水平／垂直／四個 45°），並依合法性把預覽線染成綠／橙，提示列同步說明差在哪（「橫 4 直 3，要走 45° 兩邊得一樣長」）。**理由**：規則本身沒問題，難的是用肉眼在網格上找出正斜角——使用者實玩後回報「沒有辦法 45 度放置管道」。**一條規則如果只能靠試錯才知道自己有沒有踩中，它在體感上就等於壞掉。**
- **成本**：節點有固定造價；導管造價 = `長度(格) × 單位成本`。
- **即時預覽**：拖曳中顯示「這條線會帶來 +X/秒」與「總耗能將變為 Y/秒」。**這是把取捨資訊在決策前就攤開給玩家看**，不讓玩家事後才發現餓死了。
- **拆除**：返還 50% 成本。
- **禁止事項**：不可在敵人路徑格上建造**節點**（路徑是既定的，玩家不能封路——這不是迷宮塔防）。

#### ★ 跨越點（橋）— 路徑上唯一可鋪管的地方

地圖作者在敵人路徑上指定若干**跨越點**。規則：

| 項目 | 規則 |
|---|---|
| 可鋪 | **導管**可以鋪過跨越點 |
| 不可鋪 | **節點**（含中繼）一律不可——它們會擋路 |
| 安全性 | **橋是架高的 → 橋上的導管不受敵人攻擊**（§3.5） |
| 數量 | 由地圖作者決定，是**關卡設計的主要刻度尺**——一座橋的圖跟四座橋的圖是兩款遊戲 |

**設計意圖**：路徑把地圖切開，跨越點決定你的網路能怎麼縫回去。橋是安全動線，所以「該不該為了短路而貼著路徑走」這個取捨仍然存在（貼路的線會被打，§3.5），但玩家永遠有一條可規劃、不會被打斷的幹線選擇。這同時避免了「唯一通路被打斷 → 整側斷電」的雪崩，與 §3.1 的反雪崩原則一致。

**視覺**：跨越點在地圖上明示（見 `20_ART_DIRECTION.md` §1.6），玩家不需要試錯才知道哪裡能過。

### 3.3 塔（角色）系統 ★
**服務循環步驟**：④⑤　|　**里程碑**：M0

**塔即角色**：每個角色是一種可放置的防禦建築，**全自動開火**，射程內即攻擊，玩家不下達戰鬥指令。

**通用屬性**：

| 屬性 | 說明 |
|---|---|
| 造價 | 礦砂／合金 |
| **交戰耗能** | **射程內有目標時，每秒持續消耗能量**（與射速無關） |
| **待機耗能** | **預設 0**——射程內無敵人時幾乎不耗電。少數提供持續全域效果的特殊角色例外（未來設計時逐一標註） |
| 射程／射程形狀 | 圓形／扇形／直線 |
| 傷害類型 | 物理（被護甲減免）／能量（被屏障減免） |
| 目標選擇 | 最近／最前／最高血量／最低血量（玩家可設）。**M0 只實作「最前」（路徑進度最大者）並且沒有選單**——四選一是操作品質，不是核心取捨，排到後勤科技（§3.6）一起做 |
| 特殊機制 | 每個角色獨有 |

#### ★ 耗能模型：峰值約束，不是平均約束

塔**只在交戰時耗電**。這把核心取捨從「持續扣血」變成**尖峰衝擊**：

```
準備期（45 秒）        波次期（20–40 秒）
塔耗能 ≈ 0             全部塔同時開火 → 耗能暴增
生產線滿速運轉          生產線被塔搶電 → 產能驟降
儲槽充能 ↑↑            儲槽放電 ↓↓
```

**這條規則的三個後果**（全部是設計意圖，不是副作用）：

1. **儲槽 Silo 從「緩衝配件」升格為核心策略建築。** 你在準備期充能、波次期放電。儲槽容量直接決定你能養幾座塔。
2. **約束變成「峰值電力」而非「平均電力」。** 多蓋一座塔的代價不是持續的漏電，而是**波次期的電力缺口更大** → 你必須擴發電或擴儲槽 → 那些資源就不能拿去擴礦。取捨完好無損，只是換了發生的時刻，而且更戲劇性。
3. **提前召喚變得更兇。** 提前開波 = 提前讓工廠停擺、提前開始燒儲備電。下注的張力提高。

**能量不足時的行為**：射速按滿足率**線性下降**（滿足率 60% → 射速 60%），不會完全停火（理由見 §3.1 供不應求）。潮鳴的光環強度同樣按滿足率縮放。

#### ★ 全域規則：擊殺回收

**任何塔擊殺敵人，都回收該敵人價值 25% 的礦砂。** 這是全域規則，不是特定角色的能力。

設計意圖：讓防禦本身帶有經濟回饋，緩和「多蓋塔 = 純虧」的直覺，並讓高擊殺效率的佈局自然獲得經濟優勢。

> **★ 回收的礦砂直接入帳，不走導管**（B0.5 拍板）。它是塔在擊殺處撿到的殘骸，不是採出來要運回核心的礦——採集器的產出才需要接線入帳（§7.3）。
>
> **這條不對稱是模型逼出來的，不是便宜行事**：礦砂有一個全域帳戶（核心就是礦砂銀行），所以撿到的礦砂有地方可去；**能量沒有帳戶**（§3.1 無全域能量池），所以回收者回收的能量**只能從它自己那條導管注入**，見 §7.4。同一條規則落在兩種資源上，長相不同是因為兩種資源的儲存語意本來就不同。

#### ★ 礦砂↔能量匯率：**1 礦砂 = 5 能量**

敵人的「價值」（§7.5）以**礦砂**計。任何把價值換算成能量的地方一律用這個匯率，它不是新發明的數字——它就是發電機的轉換比（§7.3：4 礦砂/秒 → 20 能量/秒）。目前唯一的用處是回收者的能量回收。

**M0 四隻角色**（覆蓋四種戰術定位，足以驗證核心取捨）：

| 角色 | 定位 | 傷害型 | 特色 | 設計意圖 |
|---|---|---|---|---|
| **錨 Anchor** | 基礎 | 物理 | 短射程、高單體傷害、**交戰耗能低** | 便宜可靠的地基。教會玩家「開火才要錢」的成本概念。 |
| **稜鏡 Prism** | 爆發 | 能量 | 直線穿透雷射，可同時打穿一整列敵人、**交戰耗能極高** | 取捨的極端案例：一座稜鏡開火時 ≈ 五座錨。它讓儲槽容量成為硬需求。 |
| **潮鳴 Knell** | 控場 | 無傷害 | 範圍減速 −40%、破甲 −25%，交戰耗能中 | 證明「不造成傷害的建築也值得佔用電力」。光環僅在射程內有敵人時啟動（＝交戰狀態）。**★ 硬性要求（B1.8）：它必須有非開火的視覺回饋**——`rof = 0` 代表它永遠不產生開火線也不播開火音，而它每秒吃 9 能量。一座看不出自己在工作的塔，等於把「交戰時每秒吃電」這條全案的心臟藏起來。畫法見 `20_ART_DIRECTION.md` §1.6。 |
| **回收者 Reclaimer** | 經濟 | 物理 | 中等傷害，**射程內任何敵人死亡（不限自己擊殺）即回收其價值 60% 為「能量」注入電網**（匯率 1 礦砂 = 5 能量，見上） | **打破峰值約束的鑰匙**：唯一能在波次進行中補充電力的塔。因為算的是「射程內的死亡」而非「自己的擊殺」，它的擺放是一道空間謎題——要放在殺戮最密集處，而不是它自己射得最爽的地方。長波次與無盡模式的核心建築。 |

> **回收者的定位為何改變**：既然「擊殺回收礦砂」已成為全域規則（見上），原本的「回收礦砂 30%」失去獨特性。改為**回收能量**後，它直接對抗新耗能模型製造的峰值缺口——這使它成為長波次的關鍵解答，且與礦砂經濟不重疊。

**擴充角色**（M2 起，見 §5 內容矩陣）：目標 24 隻，其中 6 隻為抽卡專屬稀有角色。

### 3.4 波次與壓力系統
**服務循環步驟**：⑤⑦　|　**里程碑**：M0

- **波次表**：每關預先定義的敵人組成序列（戰役手作，無盡程序生成）。**確定性**：同種子必同波次。
- **準備期**：波與波之間 45 秒安全窗口（戰役第 1–5 關為 60 秒）。**時間與生產持續流動**，只是沒有敵人。可快進。
- **提前召喚**：準備期內任意時刻可按下，立即開波。
  - 獎勵倍率 = `1 + (剩餘秒數 / 準備期總長) × 0.5`（最高 +50%）
  - 獎勵形式：該波掉落的礦砂與研究數據按倍率增加
  - **設計意圖**：把取捨從「被動承受」變成「主動下注」。這是分數天花板的主要來源，也是高手與新手的分野。

### 3.5 敵人系統
**服務循環步驟**：⑤⑥　|　**里程碑**：M0

- **行為（★ walk-by，2026-07-27 定案）**：敵人沿預定路徑推進，**永不停步**。每 tick 對**相鄰 1 格內**的玩家建築造成其「對建築傷害」，然後繼續走。**只有核心會讓它們駐足**（停下來打到核心歸零）。
- **例外**：**跨越點（橋）上的導管不受攻擊**——橋是架高的（§3.2）。

> **★ 免疫範圍含引道**（B0.4 實作時補齊）：免疫的不只是跨越點那一格，而是**跨越點 ±1 格**——也就是導管上橋與下橋的兩段引道。
>
> **不這樣做的話這條規則等於不存在**：敵人走在路徑格上，破壞半徑 1 格。導管要垂直穿過路徑，就必然佔用「路徑格（橋）＋兩側各一格」，而那兩側正好都在破壞半徑內。只免疫橋面那一格的話，**每一條過橋的線都會在引道被打斷**，橋就從「又短又安全的路」變成純裝飾。
>
> 這也正是 `20_ART_DIRECTION.md` §1.6 已經畫出來的樣子——橋面兩端本來就伸出格外，引道是那個架高結構的一部分。
- **為什麼不做「停下來攻擊」**：一旦敵人會為建築駐足，玩家就會發現丟一排 15 礦砂的中繼是最便宜的拖時間道具——那等於把**迷宮塔防**偷裝回來（它在砍案清單上），而且波次長度變得不可預測，提前召喚的下注也算不清。walk-by 沒有索敵邏輯、沒有停頓、波次長度恆定。
- **關鍵設計**：**敵人會打斷你的線。** 這讓佈局有了空間策略——貼路徑走的線短、便宜、危險；退開一格就安全但長、貴；橋是唯一又短又安全的路，而橋有幾座是地圖作者決定的。沒有這一條，塔防就只是「保護一個點」。
- **塔的擺位**：塔的射程 4–9 格遠大於 1 格的破壞半徑，所以塔可以站在安全距離外開火。**願意把塔（或線）壓到路邊，換的是覆蓋率與長度，賭的是它撐不撐得住。**
- **屬性**：

| 屬性 | 效果 | 剋制方式 |
|---|---|---|
| 護甲 Armor | 物理傷害 −N | 能量傷害、潮鳴破甲 |
| 屏障 Barrier | 能量傷害 −N% | 物理傷害 |
| 迅捷 Swift | 免疫減速 | 高爆發單體 |

- **失敗條件**：核心生命歸零 → 本關失敗 → 可立即重來（**無任何損失**，紅線 R1）。

### 3.6 科技樹
**服務循環步驟**：外層　|　**里程碑**：M1

三支路線，用**研究數據**解鎖：

| 路線 | 內容 | 上限 |
|---|---|---|
| **產能** | 導管吞吐、採集速度、熔爐效率、儲槽容量 | 合計 +35%（B2） |
| **防務** | 塔傷害、射程、**交戰耗能效率**（最重要的一項） | 合計 +35%（B2） |
| **後勤** | 藍圖槽數量、優先權精度、建造返還率、預覽資訊深度 | 無數值上限（純便利性） |

**設計意圖**：後勤支不給數值，只給**操作品質**——這是對「不可暫停可能變手速」風險的長線解藥：玩得越久，操作負擔越低，思考空間越大。

### 3.7 藍圖庫
**服務循環步驟**：②⑦　|　**里程碑**：M2

- 框選一組節點與導管 → 存為藍圖。
- 下局在建造面板一鍵展開（自動計算成本，資源不足則顯示缺口）。
- 藍圖槽數量由後勤科技與 tycoon 供給。
- **設計意圖**：消除「每局前 10 分鐘重複勞動」，同時把玩家的精通結晶化——你的藍圖庫就是你的技術資產。

### 3.8 潮汐公司（tycoon）— 塔防的後勤補給站
**服務循環步驟**：外層供給　|　**里程碑**：M2

> **定位（2026-07-27 修訂）**：tycoon **不是**可獨立遊玩的第二個遊戲，而是塔防的**附屬資源供應系統**。
> 它的存在理由只有一個：**穩定供給塔防所需的資源與升級材料**。任何「讓 tycoon 本身變得好玩到可以取代塔防」的提案一律進 backlog。

**核心循環**（刻意保持單薄）：
```
接訂單（品項、數量、交期、報酬）
  → 排產線（複用流量網路引擎，無敵人、無時間壓力）
  → 生產累積（在線即時／離線掛機）
  → 交貨結算 → 資金與材料
  → 擴廠（產線位）／研發（新品項）
  → 接更大的訂單
```

**產出（全部流向塔防）**：

| 產出 | 用途 |
|---|---|
| 資金 Credit | 兌換以下各項 |
| 升級材料 | 塔的等級、生產節點的等級（**數值強化**） |
| 招募券 | 名冊招募 |
| 藍圖槽、難度鑰匙、外觀 | 橫向內容 |

**掛機規則**：離線時持續生產，受**倉儲上限**約束（滿了停產）。倉儲上限可升級。
**這不是體力閘門**（紅線 R2）：它不限制你能玩幾局塔防，它是「回來收成」的正向設計。塔防隨時可以玩，不需要任何 tycoon 產物。

**塔防 → tycoon 的回流**：塔防局掉落的稀有原料可加速高階訂單。tycoon 也能自行採購（較貴），因此**掛機路線與遊玩路線都走得通**。

**設計約束（防止它膨脹成第二個遊戲）**：
1. tycoon **沒有失敗狀態**、沒有時間壓力、沒有對手。它是水龍頭，不是挑戰。
2. tycoon 的 UI 深度上限：**兩個畫面**（訂單板＋產線編輯）。超過就是膨脹。
3. tycoon 的任何新機制都必須回答「這如何讓塔防更好玩」。答不出來就不做。
4. 市場價格波動、人事系統、股價、競爭對手、員工士氣 → **永久 backlog**。

**M2 第一版範圍**：只做「訂單 → 產線 → 交貨 → 擴廠」單一循環。

### 3.9 名冊與招募
**服務循環步驟**：外層　|　**里程碑**：M2

> **★ 成就系統的規格在 §7.15，不在本節**（B2.8 的 G2 勾稽補記）：它在這裡原本只是
> 「聲望券來源」的一個項目，而 B2.7 把它做成了一個有 20 條規則、一個畫面、
> 一個推導出來的公司等級的系統。§3 沒有給它自己的一節是文件的缺口，不是設計的。

- **確定性解鎖**（主要途徑，18 隻）：科技樹、戰役關卡獎勵、成就、商店直購。玩家能規劃「我再推兩關就能拿到那隻」。
- **招募（抽卡）**（6 隻稀有角色）：用聲望券抽「潮汐名冊」。
  - **不重複**：抽到的必定是尚未擁有的稀有角色。隨機的是**順序**，不是有沒有。
  - **有畢業**：UI 常駐顯示「還剩 N 隻未收集」。抽完即畢業，不再消耗券。
  - **不使用賭博語彙**：無十連演出、無稀有度金光、無「差一點就中」的假張力。
  - **不影響統一配置榜**：該榜使用固定角色組，抽卡結果完全不進入（B3）。自由配置榜才會反映。
- **聲望券來源**：戰役里程碑、成就、tycoon 產出、無盡分數門檻，**以及付費購買**。
- **B6 保障**：6 隻稀有角色**全部都能純靠遊玩取得**（券的產出速率保證在合理時間內可畢業），付費只是加快。不存在「只能課金拿到」的角色。

> **★ B2.4 落地**（數值與實作決定見 §7.13）：M2 的池是 **3 隻**（§5），確定性角色 5 隻，共 8 隻。
> 三件在實作時定案、原文未寫的事：
> ① **擁有清單與券數都是推導的，不是存的**——存進去的每一份平行事實都會漂。
> ② **確定性解鎖就是戰役的解鎖階梯本身**，不另寫一張會漂的對照表。
> ③ **名冊管無盡與自由配置榜，不管戰役**——戰役的建造欄是難度階梯（§7.9），
>    用名冊去交集它會讓「通關第 4 關卻蓋不出碎浪」變成可能。
> **⚠ 這一條同時是一個玩法變更**：B2.4 之前無盡是全建造欄，現在它吃名冊。
> 一份零進度的存檔進無盡只有錨——「名冊 → 下局玩法組合改變」（§1 服務循環圖）
> 要成立，招募到的東西就得有地方用得到。

### 3.10 排行榜與每日挑戰
**服務循環步驟**：長線引擎　|　**里程碑**：M2

- **每日挑戰**：全球同一固定種子（`seed = hash(日期)`）。**同一張圖跑兩榜**：

| 榜 | 起始配置 | 目的 |
|---|---|---|
| **統一配置榜** | 固定角色組、固定科技、固定等級、固定起始資源。**與玩家的任何進度與課金完全無關** | 純技術競爭。免費玩家爭第一的舞台。**對 P2W 的唯一制衡（B3）** |
| **自由配置榜** | 玩家的完整戰力（自選角色、自己的科技與等級） | 成長曲線的展示場；付費的兌現點 |

- **實作成本極低**：兩榜共用同一套地圖、波次、模擬與計分，差異只在 `SessionState` 的初始化參數。**不得為此拆成兩套邏輯。**
- **第一版純離線**：本機保存每日兩榜的最佳成績與歷史紀錄。**誠實註記**：離線狀態下兩榜是兩個本機數字，沒有排名、沒有對手。雙榜架構此時的價值是**接線上榜時零改動**，不是「已經在制衡 P2W」（B3）。
- **架構要求**：分數、局面、操作序列全程可序列化、可重現（見 `30_TECH_DESIGN.md` §2 確定性）。日後接線上榜**不需重寫核心**。
- **無盡模式**：程序生成地圖 + 無限波次，個人最佳紀錄（自由配置）。

### 3.11 難度層
**服務循環步驟**：長線引擎（深度精通）　|　**里程碑**：M3

通關戰役後解鎖，逐層疊加更嚴苛的規則（例：塔交戰耗能 +20%、敵人破壞導管速度加倍、準備期縮短為 30 秒）。共 12 層。成本極低（純數值與規則開關），是深度精通的主要載體。

**★ 難度層同時是等級軸的配重**（B2 ②）：每層的敵人強度縮放係數**以「等級軸滿級」為基準校準**，因此買到滿級不會碾壓既有內容，而是讓你有資格挑戰下一層。這是本作處理 P2W 數值膨脹的主要機制（`00_CONCEPT.md` 風險 6）。

> **★ B2.6 已交前 3 層，數值與解鎖階梯在 §7.16。** 三處與本節原文不同，先講：
> ① **只掛在無盡上**（戰役是難度階梯、每日兩榜要可比）；② 「準備期縮短為 30 秒」
> 依 §7.1 屬於難度層 4+，前 3 層不動它；③ 「敵人破壞導管速度加倍」落地為 ×1.50
> ——第 2 層同時要讓玩家還撐得住，加倍留給後面的層。

### 3.12 變現系統
**服務循環步驟**：外層（不進入核心循環）　|　**里程碑**：M4

**免費遊玩 ＋ 內購。Windows 版無廣告。**

| 商品 | 內容 | 性質 |
|---|---|---|
| tycoon 加速／立即收成 | 跳過生產等待、立即結算離線收益 | 賣時間 |
| 升級材料包 | 等級軸材料（B2 ②） | **P2W 成分**，由難度層縮放配重 |
| 聲望券 | 招募稀有角色 | 賣時間（全角色皆可遊玩取得） |
| 通行證／一次性禮包 | 賽季獎勵軌、支持者包 | 賣時間＋外觀 |
| 外觀 | 塔與導管的配色主題 | 純裝飾 |

**硬性約束（B6）**：
- ❌ 不賣遊玩次數／體力（紅線 R2）
- ❌ 不賣重試權、關卡解鎖鑰匙
- ❌ **不做付費復活**
- ❌ 統一配置榜不得受任何付費項目影響（B3）
- ✅ 所有內容都必須能純靠遊玩取得

**廣告**：Windows 版不做。手機移植版（若成案）以獎勵式廣告為主：玩家**主動**觀看換取 tycoon 加速或材料。不做插播、不做強制觀看。

**呈現紀律**：商店入口不常駐主畫面頂欄的顯眼位置、不做限時倒數的壓迫式促銷、不在戰鬥失敗後彈出購買提示（那是趁人之危，且與 R1「失敗只花時間」的精神衝突）。

---

## 4. 經濟與成長

### 4.1 資源表

| 資源 | 作用域 | 產出來源 | 消耗去向 | 里程碑 |
|---|---|---|---|---|
| **礦砂 Ore** | 局內 | 採集器、回收者擊殺回收 | 建造節點、導管、塔；發電機燃料 | M0 |
| **能量 Power** | 局內 | 發電機、回收者回收（§3.3） | **塔的交戰耗能**、熔爐運轉、儲槽充能 | M0 |
| **合金 Alloy** | 局內 | 熔爐（礦砂加工） | 高階節點與高階塔 | M1 |
| **研究數據 Data** | 跨局 | 局末結算（依產能積分與波次） | 科技樹 | M1 |
| **稀有原料 Flux** | 跨局 | 局內特殊事件、高難度關卡 | tycoon 高階訂單加速（可選） | M2 |
| **資金 Credit** | tycoon | 訂單交貨 | 擴廠、研發、兌換以下各項 | M2 |
| **升級材料 Component** | 跨局 | **① 塔防局末結算（依波次與難度層）　② tycoon 交貨　③ 付費購買** | **等級軸**：塔與生產節點升級（§1 B2 ②） | M2 |
| **聲望券 Token** | 跨局 | 戰役里程碑、成就、tycoon、無盡門檻 | 招募（抽卡） | M2 |

**★ 升級材料必須有三條路（2026-07-27 修正）**：原本只寫「tycoon 交貨」一條，那會讓不碰 tycoon 的玩家雖然推得完戰役、卻永遠拿不到等級軸，於是上不了高難度層——直接違反 `00_CONCEPT.md` §三「塔防不依賴 tycoon」與 B6「所有進度都必須能純靠遊玩取得」。**塔防局末結算必須是升級材料的一條完整路徑**（速率可以慢於 tycoon，但不得為零）。B2.7 以測試斷言鎖住：以「從未開啟 tycoon」的存檔可將等級軸推至滿級。

**防通膨機制**：
- 局內資源（礦砂／能量／合金）**每局歸零**，不跨局累積 → 結構上不可能通膨。
- 研究數據：科技樹總量有限（B2 上限 +35%），走滿即封頂。
- 資金：tycoon 的擴廠成本呈**指數上升**（×1.35/級），配合線性收入增長，形成自然的收斂曲線。
- 聲望券：抽卡池僅 6 隻且不重複，抽完畢業 → 券的需求有明確終點。

### 4.2 成長曲線公式

| 對象 | 公式 | 說明 |
|---|---|---|
| 科技樹節點成本 | `cost(n) = 50 × n^1.6` | n = 該支已解鎖數。前期便宜、後期需要多局累積。 |
| tycoon 擴廠成本 | `cost(L) = 500 × 1.35^L` | L = 當前廠等。 |
| tycoon 訂單報酬 | `reward(L) = 120 × L^1.25` | 線性偏下，配合指數成本形成收斂。 |
| 無盡波次強度 | `hp(w) = 100 × 1.11^w`，`count(w) = 4 + floor(w/3)` | 指數血量 + 線性數量。玩家產能需同步指數成長才撐得住 → 天然的技術檢定。 |
| 局末研究數據 | `data = 10 × 波次數 + 2 × 產能積分 + 提前召喚加成` | 產能積分 = 全局平均每秒總產出 / 10 |

### 4.3 局內臨時升級（★ B3.5 落地）

局內可用礦砂即時升級單座建築（+1 級，**最多 5 級**，B3.9 起），**隨局結束消失**（B2）。這給予局內的短期成長回饋，同時不污染跨局平衡。

> **這一節從 M0 就寫著，而做到 B3.4 為止都沒有實作**——`upgrade()` 只吃導管。使用者在 B3.4 問「我們會有角色升級機制嗎」時才發現這個缺口：局外有等級軸與科技樹，局內卻只有加粗導管一條路。

#### 數值（★ 唯一來源）

| 項目 | 值 | 為什麼 |
|---|---|---|
| 每級 | **耗能／輸入一律 +25%**；效果**只長被指定的那一項**（見下「五級各不相同」） | 見下 |
| 上限 | **5 級**（★ B3.9，使用者指定「升級會有共 5 級，且每一級都要有外觀上的改變」） | 三級走完只有三個決定點，而升級是本作**唯一**的局內成長軸 |
| 一級的價 | `造價 × (級數+1) ÷ 2`（半價、全價、一倍半、兩倍、兩倍半） | 五級全升 ＝ 造價的 **7.5 倍**換到耗能 ×2.25，**遠比多蓋幾座還貴**——買的仍是格子與導管，不是數值效率 |
| 存續 | 隨局結束消失 | `SessionState` 本來就不進存檔，所以這條是免費的 |
| 核心 | **升不得** | 它沒有產出也沒有耗能，一顆「5 級核心」只會讓玩家以為血量變多了 |

#### ★ 為什麼「效果」和「耗能」一起長

**只加效果的話，升級是嚴格更好的：能升就升，那不是決定。** 兩個同步 ×1.25 之後效率完全不變，買到的是**集中**：

- 同樣的火力用**更少的格子**——而路徑旁邊安全的格子是稀缺的（退開 2 格才打不到，§3.5）
- 同樣的火力用**更少的導管**（3 礦砂/格）

代價是那一條線要扛得動更大的量：**一座 3 級稜鏡要 35 能量/秒，而一條 cap 10 的線送不進去**。核心取捨（同一份能量餵塔還是餵產線）因此原封不動，而多了一個空間維度。

> **實測到的同一件事**：一座 3 級採集器產 10.5 礦砂/秒，而基礎導管 cap 是 10——**實際只送得到 10**。`combat_test` 兩條斷言把這件事釘住（cap 前 10.0、加粗整條線之後 10.5）。**升級把瓶頸推回導管上**，那正是這個機制想要的效果。

#### ★ 五級各不相同（B3.8 改寫、★ B3.9 擴到五級，皆為使用者指定）

> 使用者實玩回報兩件事：「**潮鳴跟雙嬌（霜礁）升級都沒用**」，以及「**為什麼沒有升級會增加攻擊距離的機制，應該是要每次升級有不同效果，這樣多元化的效果才會好玩**」。

**前者是缺陷。** 潮鳴與霜礁的 `dmg` 與 `rof` 都是 0——它們**整個效果**都在 `slow` 與 `armor_break` 上，而 B3.5 的實作只把級數乘進了 `dmg`／`ore_out`／`power_out`／`alloy_out`。於是升級對這兩隻的唯一作用是**多吃 25% 的電**：一筆嚴格更差的消費。碎浪的 `splash`、回收者的 `reclaim` 也同樣沒被乘過（那兩隻有傷害，所以升級不是全無作用，只是招牌能力不會長）。

**後者是設計改動。** 每一級加什麼寫在 `NodeDefs` 的 `steps` 裡，詞彙只有四個：

| 詞 | 意思 |
|---|---|
| `power` | 出力 ×1.25 累乘——傷害／減速／破甲／回收／產出／發電，**每種節點各自的那一項** |
| `range` | 射程 +1 格 |
| `rof` | 射速 ×1.25 累乘 |
| `splash` | 濺射半徑 +1 格 |

| 塔 | 1 級 | 2 級 | 3 級 | 4 級 | 5 級 | 滿級是什麼 |
|---|---|---|---|---|---|---|
| 錨 | 出力 | 射速 | 射程 | 出力 | 射速 | 一挺機槍：18→27 傷害、1.2→1.88 發/秒、射程 4→5 |
| 稜鏡 | 出力 | 射程 | 射速 | 出力 | 射程 | 那條貫穿線拉到 11 格、傷害 30→45 |
| 潮鳴 | 出力 | 射程 | 出力 | 射程 | 出力 | 控場塔沒有第二個維度：減速 0.4→0.7、破甲 0.25→0.44、範圍 5→7 |
| 回收者 | 出力 | 射程 | 出力 | 射速 | 射程 | 回收吃的是「射程內有誰死」，範圍就是它的產量（5→7、回收 0.6→0.9） |
| 碎浪 | 出力 | 濺射 | 射程 | 出力 | 濺射 | 它賣的就是濺射：半徑 2.5→4.5，一發蓋住整段隊列 |
| 長哨 | 出力 | 射速 | 射程 | 射速 | 出力 | 射速 0.35 是它的短板，兩級都補在那裡（→0.55、射程 13） |
| 霜礁 | 射程 | 出力 | 射程 | 射程 | 射程 | **唯一一條只長範圍的階梯**：3→7 格的減速場，減速 0.65→0.81 |
| 定潮 | 出力 | 射程 | 射速 | 出力 | 射速 | 電費最便宜的那一隻，升到滿是一座砲台 |

**沒有 `steps` 的節點**（採集器、發電機、熔爐、儲槽、中繼）走預設五個 `power` ＝ B3.5 的原行為，一個數字都不變——它們沒有射程也沒有射速，硬給第二個維度只會是假的。

**耗能那一半沒有動。** 「效果與耗能一起長」仍然成立，變的只是「長的是哪一項」。五級走完是一隻**質變過**的塔，而不是同一隻塔的 2.25 倍：錨滿級是 dps ×2.34 ＋ 射程 4→5，耗能 ×2.25——**多出來的那一點來自射速與出力複利，代價是 7.5 倍造價與一條扛得動 9 能量/秒的線**。

**★ 減速有硬上限 0.85**（`Combat.SLOW_MAX`）。**敵人永不停步**是鎖定設計（walk-by，§3.5），而 1.0 的減速就是釘住、超過 1.0 是倒著走。五級之後最強的組合是霜礁 0.65 × 一個 `power` 級 ＝ 0.81，仍在線下；而潮鳴三個 `power` ＝ 0.7。這條線守的是日後新增光環塔（或再加級數）時沒有人記得算那個乘積。

**★ 回收者只給兩個 `power`** 是刻意的：`reclaim` 0.6 三級就會變成 1.05——**回收超過敵人本身的價值**，那是一台印鈔機而不是一座塔。

**玩家在哪裡看得到這條階梯**：圖鑑浮層（滑鼠停在建造鈕上，**花錢之前**）逐級列出；檢視面板的升級鈕上寫著這一級買到的是什麼（`升級　射程 +1 格　120 礦砂`）。只有升級當下才看得到的話，「多元化」對玩家等於隨機。

#### 操作

**升級與拆除都在檢視面板上**（§6.2a）：點那個東西 → 它的面板打開 → 動詞就在旁邊，價與退款印在鈕上。

> **★ B3.7.1：「升級」與「拆除」兩顆模式鈕已刪除**（使用者指定）。B3.7 把兩個動詞搬到面板之後，模式鈕就變成同一件事的第二條路，而且是慢的那一條（切模式 → 點目標 → 切回建造，三步裡有兩步不是決定）。歷次刪掉模式鈕的理由完全一致：**一個模式鈕如果只是「同一件事的比較慢的做法」，它就該消失**（「連線」B1.6.2 被拖曳取代、「移動」B1.3.1 改中鍵拖）。底欄動作鈕只剩「藍圖」——它不是動詞，是**手上拿著的東西**，沒有別的地方表達得了。

#### ★ 每一級都看得出來（B3.9 立，★ B3.9.4 第三版，全部是使用者指定）

使用者說了三次，每一次都是看過上一版之後：

1. 「**升級會有共 5 級，且每一級都要有外觀上的改變**」→ B3.9：塔身放大 ＋ 頭上一排 4px 的級章。
2. 「**我希望的是升級會對於外型真的有變化，不只是變大而已**」→ B3.9.3：塔身周圍長出零件（托架／砲管／細環／尖刺）。
3. 「**我需要的是外型的變化，意思的是真實的變化，而不是外圍的變化，是形狀真正的變化**」→ 就是這一版。

**現在級數驅動的是輪廓自己的參數**，不是掛在它外面的東西：

| 買到 | 幾何參數怎麼動 |
|---|---|
| 出力 | 更厚重——樁多一層、齒輪多一齒、外框多一邊、配重多一塊、輻條更粗 |
| 射速 | 更多管口——樁頂多兩管、桅杆多一根橫桿、稜鏡裂成兩瓣、吊索多一條 |
| 射程 | 更高更長——整支拉高、多一圈場、多一條輻條、內圓長大 |
| 濺射 | 更尖——爆散星多一個尖角，而出力讓尖挖得更深 |

逐塔的規則寫在 `Battle._draw_node_body()` 的每一個 `match` 分支裡（一種節點一段，3–12 行），共用的多邊形產生器在 `Shapes`：`ngon()`／`star()`／`gear()`／`chamfer_square()` ——**四支純函式**撐起十三種節點的全部級數變形，而不是 65 份手寫座標。

三條規矩：

- **0 級一律走原本那一份形狀，一個像素都不變。** 既有的十三種輪廓是 B2.4.8 照遊玩測試調出來的；升級要做的是「這一座和旁邊那一座不一樣」，不是「這遊戲換了美術」。
- **`match` 的 default 分支留著**（`_no_glyph` 感測器）：漏掉一種的症狀是隱形不是報錯，這一版把每個分支都改過，那條哨兵比以前更需要。
- 體積仍然每級 ×1.05 累乘（滿級 ×1.276），但它不再是唯一的通道——B3.9 的級章與 B3.9.3 的外掛零件都已刪除，同一件事不講三次。

> ⚠ **`draw_set_transform()` 是取代不是疊加**（B3.9.2a，RG-171）。放大用的變換必須把 `_draw()` 那層地圖變換（原點＋縮放）乘進來，還原時也要還原成它而不是單位矩陣——否則升過級的塔會畫到別的地方去，而**那一幀後面畫的每一樣東西**都跟著少掉原點與縮放。算式在 `Shapes.level_xform()`，`hud_test` 釘住「塔心必須落在地圖變換原本會把它放的位置」。


---

## 5. 內容矩陣

> 生產期照表填。空格 = 尚未設計。

| 類別 | M0 | M1 | M2 | M3（Beta 完整） |
|---|---|---|---|---|
| 角色（塔） | 4 | 8 | 16（含 3 抽卡） | **24**（含 6 抽卡） |
| ↑ 實際 | 4 ✅ | 5 | **8**（含 3 抽卡 ✅） | — |
| 生產節點類型 | 5 | 8 | 11 | **13** |
| 敵人類型 | 3 | 6 | 12 | **18** |　　　★ 實作進度：**9**（B3.2 3→6、B3.2b 6→9）
| ↑ 實際 | 3 ✅ | **6 ✅**（B3.2） | — | — |
| 戰役關卡 | 1（測試圖） | 5 | 15 | **25** |
| ↑ 實際 | 1 ✅ | 5 ✅ | **10**（B3.3 第二幕五關） | — |
| 無盡地圖生成器 | — | 1 種佈局 | 3 種佈局 | **5 種佈局** |
| tycoon 訂單品項 | — | — | 8 | **20** |
| tycoon 廠房等級 | — | — | 10 | **25** |
| 科技樹節點 | — | 12 | 30 | **45** |
| 難度層 | — | — | 3 | **12** |
| 成就 | — | — | 20 | **60** |
| BGM 曲目 | — | 1 | 3 | **6** |
| 音效 | — | 12 | 30 | **50** |
| 商店商品（M4） | — | — | — | **12** |
| 外觀主題（M4） | — | — | — | **8** |

> **★ B2.4 只補到 8 隻，不是 16**（同一條理由的第二次套用）：B2.4 交的是**名冊與招募這個系統**，而系統要能動只需要一個**非空**的招募池——3 隻正是 M2 的抽卡目標數。剩下 8 隻確定性角色排在 B2.6（難度層要有東西可對抗）與 B3.1。**一次補齊 11 隻沒平衡過的角色，會讓 B2.6 的難度層校準建立在流沙上**——這句話 B1.1 已經寫過一次了。

> **★ M1 的角色／節點數是里程碑末的目標，不是單一批次的**（B1.1 記錄）：B1.1 交的是 **+1 生產節點（熔爐）＋ +1 角色（碎浪）**，因為它們是**合金這個機制自己需要的兩端**——一個產、一個花。其餘 3 隻角色與 1 種生產節點排在 B1.2（戰役五關要靠新機制區分關卡）與 B1.3（科技樹要有東西可解鎖）。**一次補齊 4 隻沒平衡過的角色，會讓 B1.2 的關卡難度校準建立在流沙上。**

---

## 6. UX 流程

### 6.1 畫面清單

| 畫面 | 職責（一句話） | 里程碑 |
|---|---|---|
| 標題 | 進入點；顯示版本；繼續／新遊戲 | M1 |
| 主選單 | 五個入口：戰役／無盡／每日挑戰／潮汐公司／名冊；側欄科技與設定<br>**★ B1.4 實作的是 M1 能到得了的那幾個**：戰役（附 `N／15 ★` 進度）／科技樹（附研究數據）／設定／測試圖／離開。無盡・每日・公司・名冊排 M2，**在做出來之前不放灰色的鈕**——一排點不動的入口是在展示還沒有的東西。<br>**★ B2.1a 補上「無盡」（附最高波次；沒有紀錄時寫「尚無紀錄」，不寫「0 波」——0 波看起來像一個很爛的成績，而事實是還沒玩過）**，共六顆。每日・公司・名冊仍不放 | M1 |
| 戰役地圖選擇 | 關卡節點圖，顯示星等與獎勵 | M1 |
| **局內 HUD** | **本作 90% 的時間都在這裡**——見 §6.2 | M0 |
| 局末結算 | 波次數、產能積分、獲得資源、新解鎖；一鍵重來／回主選單 | M0 |
| **局內選單** | ESC 或左上角「選單」鈕開關。繼續／重來（本局歸零）／設定／退出（放棄本局）。★ **不暫停**（B5）——面板上寫明「時間仍在走」。設定是同一個設定畫面疊上來，不另做局內版 | M1 |
| 科技樹 | 三支路線的解鎖面板 | M1 |
| 名冊 | 角色圖鑑、詳細數值、招募入口 | M2 |
| 潮汐公司 | 訂單板、產線編輯、廠房升級、離線收成 | M2 |
| 排行榜 | 每日挑戰歷史成績、無盡個人最佳 | M2 |
| 設定 | 音量（主／音樂／音效，**三軌；沒有 UI 軌**——UI 音走 SFX）、全螢幕、視窗大小、減少動態效果<br>**★ 沒有鍵位設定**（B1.4 定案）：這款遊戲一顆鍵都沒有綁（左鍵做事、中鍵移動視野、滾輪縮放），空的鍵位表只是把還不存在的東西畫出來。真的加鍵盤操作時再開。<br>語言（M3 在地化）與存檔管理（M2）順延。**改了當場生效、當場存檔，沒有「套用」鈕** | M1 |

### 6.2 局內 HUD 版面（1280×720 基準）

**★ 架構（2026-07-27 定案）：全畫面地圖 ＋ 浮層／抽屜 UI。** 不是三欄式——地圖是主體，面板浮在它上面。

```
┌────────────────────────────────────────────────────────────────┐
│ 礦砂 1,240 ▲12/s │ 能量 40/63 ⚠ │ 儲槽 340/600 │ 波次 7  0:38 │ ← 頂欄（浮層）
├────────────────────────────────────────────────────────────────┤
│ ┌────────┐                                        ┌──────────┐ │
│ │ 建造   │                                        │ 優先權    │ │
│ │ 抽屜   │          地圖 ／ 網路視圖（全畫面）        │ 浮層      │ │
│ │        │                                        │          │ │
│ │▪採集器 │      （節點 ● ／ 導管 ── ／ 敵人 ◆ ／橋 ╪）  │熔爐 ▓▓▓░░│ │
│ │▪發電機 │                                        │錨   ▓▓▓▓▓│ │
│ │▪熔爐   │    線粗＝流量、線亮＝快滿了               │稜鏡 ▓▓░░░│ │
│ │▪中繼   │       線的顏色＝飽和度                    │儲槽 ▓▓▓░░│ │
│ │▪儲槽   │                                        └──────────┘ │
│ │────   │                                        ┌──────────┐ │
│ │▪錨     │                                        │[提前召喚] │ │
│ │▪稜鏡   │                                        │ +32% 獎勵 │ │
│ │▪潮鳴   │                                        │[快進 >>] │ │
│ │▪回收者 │                                        │（僅準備期）│ │
│ └────────┘                                        └──────────┘ │
└────────────────────────────────────────────────────────────────┘
        ↑ 建造時即時預覽：「+8 礦砂/秒　總耗能 →412/秒 ⚠」
```

**地圖尺度（★ 定案）**：

| 模式 | 尺度 |
|---|---|
| **戰役關卡** | **硬性規定一屏可見**。★ **B1.2.2 起改由「固定框架 ＋ 進場自動 fit」保證**，不再靠「地圖必須小於某個格數」：畫面上有一塊恆定的地圖框架（`1152×604 px`，`screens/Battle.gd` 的 `FRAME`），進關卡時地圖自動縮放到剛好填滿它。理由不變——R-3 的驗收是「一眼看出瓶頸」，而 45 秒準備期不該花在平移上。 |
| **無盡（程序生成）** | 允許大過一屏、可滑動。**小地圖、畫面邊緣的瓶頸／受損指引箭頭、一鍵跳到問題點**隨之為必需品，一併排入 **B2.1**。 |
| 縮放 | 兩種模式都可縮放。★ **B1.2.2 已實作**：進場＝ fit（下限，比它更小只會把地圖縮進框架裡的一小塊）、上限 fit×3、滾輪或底欄 `−／＋` 皆可、**「全景」一鍵回 fit**（鈕上同時顯示當前倍率）。放大後以「移動」模式拖曳平移，平移夾在「地圖永遠蓋滿框架」之內。**縮放與平移只是視野，不影響模擬**——它們不進 `state_hash()`。 |

**★ 尺度的代價（B1.2.2 定案）**：框架固定之後，**地圖尺寸不再受畫面限制**，它變成純粹的關卡設計參數——但一格的像素大小 ＝ `32 × fit`，也就是**圖越大格越小**。而線寬（2–8px）、三態徽章、射程圈這套視覺編碼是**在 32px 的格子上校準與驗收的**（B0.6 的 `TL_NAKED` 讀圖）。

因此定一條**可讀性地板 24px**（`Shapes.MIN_READABLE_CELL`，32 的四分之三），由 `campaign_test` 逐關檢查。現行五關 fit 後是 `50.3 / 40.3 / 35.5 / 33.6 / 33.6 px`，全部在地板之上。

地板**不是硬性禁令，是一條要重新驗收才能跨過的線**：例如 40×40 的圖 fit 後只有 15.1px，做它之前得先拿那張圖跑一次 `TL_NAKED` 讀圖並把結論寫進 `50_QA_PLAN.md`。**進場那一眼不可讀，就不能用「玩家可以自己放大」當藉口**——那等於把 R-3 的驗收轉包給玩家。

**可收合浮層清單（B0.7.3 定案，底欄各一顆 toggle）**：

| 浮層 | 內容 | 預設 | 為什麼要有 |
|---|---|---|---|
| **操作說明** | 全部操作、三條建造規則、walk-by 安全距離、塔的耗能模型 | **空地圖時開，放下第一個節點自動收** | 玩家想回頭確認操作時要有地方可看，不能只靠情境提示逐步餵 |
| **能量收支** | 逐條列出這一局**真的存在**的每一項進出（發電機／儲槽放電／回收者存量　vs　交戰中的塔／儲槽充能）＋淨值＋「怎麼加電、怎麼省電」 | 關 | 頂欄的能量條講的是**結果**（30/37），它不說「這 37 是誰吃的」。這張表即時跟著跑，所以它同時是答案與教材：蓋一台發電機，`+` 那欄當場多 20——**因果在同一個畫面上發生** |
| **優先權** | 依節點類型的 1–5 滑桿（§3.1） | 關 | 不可暫停的戰術動作 |
| **角色簡介** | 滑鼠停在左欄建造鈕上即浮出，半透明，先講它在取捨裡的位置再列數值 | 開（僅 hover 時出現） | 八種節點的差異是玩法本體，逼玩家背下來不合理 |

> **所有浮層一律 `mouse_filter = IGNORE`。** 說明面板曾經預設開在玩家要放第一個節點的位置上，並且**把那一片的點擊整片吞掉**——教學把遊戲擋住了（B0.7.3 修）。浮層是資訊，不是障礙物。

**HUD 設計硬性要求**：
1. **能量列是全畫面最醒目的元件**——它是取捨的化身。顯示的是**本 tick 供給／需求**（滿足率），不是存量／容量（§3.1）；滿足率下探時整列變色並脈動。
   - **★ 能量列是一條圖形，不是一串數字**（B0.6 定案）：琥珀實心＝供給、右側橙色缺口＝短少的部分、細刻度＝需求位置。因此它**在 `TL_NAKED=1` 下要留著**——`TL_NAKED` 遮的是「數值標籤」，一條長度即資訊的橫條屬於線寬／顏色那一類，遮掉它等於把 R-3 要驗的東西一起遮掉。條上的數字才遮。
2. **建造預覽必須在決策前顯示總耗能變化**，不讓玩家事後才發現餓死。
3. **提前召喚按鈕常駐顯示當前獎勵倍率**，讓下注的誘惑一直在眼前。
4. 準備期與戰鬥期的**畫面色調不同**（準備期偏冷靜的青、戰鬥期加入警示暖色），讓玩家不必看計時器也知道自己在哪個階段。
5. **浮層不得遮住路徑與跨越點**。面板可收合；收合狀態下地圖 100% 可見。
6. **所有面板項目的觸控命中區 ≥ 44×44 px**（`20_ART_DIRECTION.md` §1.3b P2）。這是把面板改成浮層／抽屜的直接原因之一——固定窄欄塞不下 9 個 44px 的建造項目。

### 6.2a 檢視面板：選取即動詞（★ B3.6 開，B3.7 定案）

**點一個東西 → 它的面板打開 → 它能做的事就在面板上。** 這是局內唯一的「先選再動」流程，取代了「先去底欄按模式鈕 → 回來點目標 → 再按回建造」。

| 點什麼 | 選到什麼 | 面板寫什麼 | 面板上的動詞 |
|---|---|---|---|
| 既有節點（按下→原地放開） | 那一座建築 | 名稱／級數、生命、射程、傷害或產出（**都是升過的值**）、交戰與待機耗能、**此刻真的拿到多少電**、**供電來源幾座** | 升級（標出價）、拆除（標出退款） |
| 導管（游標壓在線上） | 那一條線 | 級數、生命、長度、**流量 / 上限（滿載時明講）**、加粗後的上限 | 加粗（礦砂＋合金）、拆除（退款） |
| 核心 | 核心 | 同節點 | **兩顆鈕都不出現**——它升不得也拆不得，一顆按下去保證失敗的鈕是在邀請玩家犯錯 |

**再點同一個東西＝取消選取。** 選取是開關，不是單向——沒有這條路的話面板一開就再也關不掉，而它蓋著畫面右半邊。蓋新東西、或選另一個，也一樣清掉舊的選取。

**★ 左欄的建造鈕也是開關**（B3.7.1，使用者指定）：按一下選、再按一下放下。`ButtonGroup` 預設永遠有一顆按著——那對模式是對的（總得在某個模式裡），對「手上拿著誰」不對，**玩家常常只是想看一下地圖**。手上什麼都沒拿的時候：

| 點什麼 | 會發生什麼 |
|---|---|
| 建築／導管 | 照常檢視它 |
| 空地 | **安靜地什麼都不做**。不報錯——「我只是想收起面板」和「我蓋錯地方了」不是同一件事，對前者丟一句 ✕ 是把正常操作講成失敗 |
| 游標所在的格 | 只畫淡框，**不畫落點預覽**——畫了就是承諾點下去會蓋東西，而這一刻不會 |

**★ 命中規則與它的代價（B3.7 定案）**

導管的中心線**正好穿過它經過的每一格的格心**，所以「點到線」與「點到那一格」在幾何上是同一件事。取捨如下：

- 命中帶是 **0.3 格**（`Battle.gd` 的 `WIRE_PICK`），不是 `conduit_near()` 的預設 0.5。0.5 等於整格都算在線上，那會讓**再也蓋不出「線從旁邊經過而不接它」的節點**——而那是流量網路裡一個真的戰術選擇（§3.2）。
- 代價：**要在有線經過的那一格蓋東西，得避開線往格子邊緣點**。提示列在游標壓上線的時候就講這句話，而且**落點預覽這時不畫**，改把那條線點亮——畫面必須先說出這一下點擊會做哪一件事。
- 手機移植風險：0.3 格在 fit 倍率下只有十幾個像素，低於 P3 的 44px 觸控門檻。移植時靠放大操作，已登記在 `40_PRODUCTION_PLAN.md` 的風險表。

**版面**：面板貼右側，讓開頂欄、能量收支浮層與**右下角的小地圖**。三者擠在同一個角落時**往左挪，不往上擠**——能量面板本身就有 358px 高，往上擠只是把一個重疊換成另一個重疊（實測 339–513 對 96–454）。

**★ 座標只能是「離散狀態」的函式，不能是內容的函式**（B3.9.1，使用者回報「點了升級之後，滑鼠會位移到一個很奇怪的位置」）。位移的不是游標，是面板：靠右對齊用的是**實際寬度**（字一長就往左滑）、避讓鄰居判的是**實際高度**（多一行就從右緣彈到 x=477）。於是升一級＝兩顆鈕跑掉，而停在原地的游標落到地圖上，或落到「拆除」上。兩條規矩：

- **寬高一律用常數算位置**（`INSPECT_W` 383、`INSPECT_MAX_H` 360），實際尺寸只拿來對齊底邊。位置因此只由「鄰居開著沒」決定——那是玩家自己按的開關，不是每 tick 都在變的數字。
- **底邊釘住**。兩顆動詞鈕在面板底列，底邊不動＝鈕不動；內容變多時往上長。頂邊釘住的話，多一行字就把鈕往下推。


### 6.3 跳轉圖

```
標題 ──► 主選單 ─┬─► 戰役地圖選擇 ──► 局內 HUD ──► 局末結算 ─┬─► 局內（重來）
                │                       │  ▲                  └─► 主選單
                │                       ▼  │ ESC
                │                    局內選單 ─► 設定（疊在局內，ESC 回選單）
                ├─► 無盡（直接進局）──► 局內 HUD ──► 局末結算
                ├─► 每日挑戰 ──► 局內 HUD ──► 局末結算 ──► 排行榜
                ├─► 潮汐公司 ──► （訂單板 ⇄ 產線編輯 ⇄ 廠房）
                ├─► 名冊 ──► 招募
                ├─► 科技樹
                └─► 設定
```

---

## 7. 數值附錄

> **實作照抄這裡，不要在程式碼裡即興發明數字。** 調平衡的流程是「改本節 → 改碼 → 校準驗證」。

### 7.1 時間常數

| 常數 | 值 | 說明 |
|---|---|---|
| `TICK` | 0.1 秒 | 流量網路解算週期 |
| `PREP_TIME` | 45 秒 | **預設值**。每張地圖可自行宣告覆寫——它是**關卡參數**，不是隱藏係數（計時器就在畫面上，玩家看得見，§7.7） |
| `PREP_TIME_TUTORIAL` | 60 秒 | 戰役前幾關的建議值（哪幾關由關卡設計決定，不再寫死「第 1–5 關」） |
| `PREP_TIME_HARD` | 30 秒 | 難度層 4+（玩家主動選擇、UI 明示的規則層） |
| `FIRST_WAVE_DELAY` | 60 秒 | 開局到第一波，同樣可由地圖覆寫 |
| `FAST_FORWARD_RATE` | 4× | 準備期快進倍率（戰鬥期不可用） |

### 7.2 導管

| 屬性 | 值 |
|---|---|
| 基礎吞吐 `cap` | 10 單位/秒 |
| 單位長度造價 | 3 礦砂/格 |
| 生命值 | 40 |
| 升級吞吐（局內，每級） | +6 單位/秒，上限 3 級（→ cap 28）。造價見下 |
| **升級造價**（B1.1 起） | **1 級：20 礦砂**（不變）／**2 級：40 礦砂 ＋ 20 合金**／**3 級：60 礦砂 ＋ 50 合金** |
| **走向** | **只能水平、垂直或正 45°**；其他角度要用**中繼轉折**（§7.3） |
| **方向性** | **沒有方向。** 一條導管是管子不是輸送帶，兩頭都能送 |
| **`cap` 的計法** | **每種資源各自計**。一條同時走礦砂與能量的線，兩者各有 10 的額度 |

> **★ 導管無方向**（B0.5 補上）。B0.3–B0.4 的實作只給一個方向，而那個方向是
> **玩家先點了哪個節點**——畫面上完全看不見，玩家也無從選擇或修正。
>
> B0.5 才讓它現形：塔是末端消費者，「先點塔、再點幹線」是最自然的手勢，
> 拉出來的線卻只能從塔往外送電，於是那座塔**永遠是 0 電**，而畫面上那條線
> 看起來一切正常。看不見、控制不了、又會靜靜毀掉整套佈局的東西不是規則，是缺陷。
>
> 這條其實本來就是既有規則的必然：儲槽要在同一條線上充電與放電（§3.1），
> 而 `Build.conduit_key()` 早就把 A→B 與 B→A 當成同一條線在擋重複。

**★ 走向限制的來源**：`20_ART_DIRECTION.md` §0 已經把「秩序側＝直線、90°/45° 轉折」寫成核心視覺命題。任意角度的導管會直接違反它，也會讓「中繼＝轉折器」這個 15 礦砂的便宜零件失去存在理由。**限制走向不是簡化，是讓中繼這個節點有意義。**

*（`cap` 每種資源各自計是 B0.3 的實作決定：混線在實際打法裡罕見——採集器線走礦砂、發電機線走能量，兩者本來就不共用。若日後出現有意義的混線玩法，再改為共用額度。）*

**★ 基礎 cap 10 小於發電機輸出 20，這是設計，不是漏洞**（2026-07-27 定案）：

你蓋的第一台發電機，有一半的電出不來——那條線會立刻滿載變色（§3.1 可讀性規範）。**遊戲用你蓋的第二個建築教你讀瓶頸**，正好是 R-3 要驗的能力。要送滿一台發電機得把幹線升 2 級（20 + 40 = 60 礦砂，剛好一台發電機的價錢）——**加粗幹線本身就是一個和「多蓋一座採集器」競爭的取捨**。

| 需要通過的量 | 基礎 cap 10 | 升 1 級 16 | 升 2 級 22 | 升 3 級 28 |
|---|---|---|---|---|
| 採集器輸出 6 | ✅ | ✅ | ✅ | ✅ |
| 發電機輸出 20 | ❌ 半速 | ❌ | ✅ | ✅ |
| 稜鏡需求 20 | ❌ | ❌ | ✅ | ✅ |
| 儲槽放電補 −23 缺口 | ❌ | ❌ | ❌ | ✅ |

*（科技樹「導管擴容 I/II/III」加的是**基礎** cap +2/+2/+2 → 16，與局內升級疊加，滿配 34。）*

**★ 2 級起要合金，1 級不要**（B1.1 定案）：加粗到 2 級是「送滿一台發電機」的門檻，也是上表唯一被 M0 教學實際用到的一級——**把它鎖在合金後面，等於把 B0.3 教會玩家讀瓶頸的那個動作鎖掉**。所以 1 級維持純礦砂，2/3 級才要合金。

它換來的是：**幹線的後半段升級與塔共用同一份合金**——熔爐每秒吃 8 礦砂 ＋ 10 能量（半台發電機）才產出 2 合金/秒，於是「加粗幹線 vs 蓋碎浪」變成核心取捨在第三種資源上的同一句話。合金不是新的進度條，是同一個取捨的第二個實例。

### 7.3 生產節點

| 節點 | 造價 | 輸入 | 輸出 | 生命值 | 備註 |
|---|---|---|---|---|---|
| 採集器 Extractor | 40 礦砂 | — | 6 礦砂/秒 | 60 | 需蓋在礦點上 |
| 發電機 Generator | 60 礦砂 | 4 礦砂/秒 | 20 能量/秒 | 50 | |
| 熔爐 Smelter | 80 礦砂 | 8 礦砂/秒 ＋ 10 能量/秒 | 2 合金/秒 | 70 | B1.1 起。**產出乘上「礦砂與能量兩個滿足率中較低的那一個」**——缺哪一樣都按比例降速，不停機（§3.1） |
| 中繼 Relay | 15 礦砂 | 任意 | 任意（轉發） | 30 | 分流／合流／轉折 |
| 儲槽 Silo ★ | 50 礦砂 | 任意 | 任意 | 80 | **緩衝 300 單位**。核心策略建築：準備期充能、波次期放電。**充放電速率受自己那條導管的 cap 約束**（§3.1）。*B0.3 起實作為**能量專用**——它的全部設計論述（準備期充能、波次期放電、峰值電力）都在能量上；要開放給礦砂／合金需要先有那個玩法上的理由* |
| 核心 Core | — | — | — | 1000 | 每關唯一，被摧毀＝失敗。**敵人抵達後會停下來打它**（唯一會讓敵人駐足的目標，§3.5）。**★ 同時是礦砂銀行**（見下） |

**★ 核心＝礦砂銀行**（2026-07-27 定案）：**礦砂必須經導管送達核心才計入帳上。** 沒接到核心的採集器等於沒在賺錢。

| | 理由 |
|---|---|
| 為什麼不讓採集器直接入帳 | 那會讓早期的導管只剩「餵發電機」一個用途，核心循環圖裡「②連導管 → ③數字上升」的第一個回饋就消失了（§2.1）。 |
| 它換來什麼 | ① 核心在敵人路徑終點＝最危險處，於是**產線天生要往危險的方向拉**，「你的生產線就是你的防線」變成字面事實。<br>② 兩台採集器（12/秒）就塞爆基礎 `cap` 10 的幹線——比發電機更早教會讀瓶頸（§7.2）。<br>③ 礦砂線被打斷＝收入中斷，是**看得懂**的失敗模式（R1 隱含「玩家要能看懂自己為什麼輸」）。 |
| 核心的解算語意 | **核心只收剩下的**：它本 tick 的 `demand` ＝ `max(0, 全網礦砂供給 − 其他消費者需求)`。發電機的燃料永遠優先於入帳。<br>因此「礦砂 ▲0/秒」是一句完整的診斷：你採到的剛好被發電機吃光。<br>*（與儲槽放電以赤字為上限同構——解算器保持通用，這條語意在遊戲層，見 `30_TECH_DESIGN.md` §2.5。）* |

**★ 核心也是合金銀行**（B1.1 定案）：**合金與礦砂同一條規則——要經導管送達核心才計入帳上。** 沒有第二套語意、沒有第二個入帳點。

| | 理由 |
|---|---|
| 為什麼不讓熔爐直接入帳 | 那會讓熔爐變成一台「蓋在安全角落就好」的機器，而它每秒吃 8 礦砂 ＋ 10 能量——**它是全圖最需要接進幹線的節點**。直接入帳等於把它從物流問題降級成一個按鈕。 |
| 它換來什麼 | ① 熔爐要同時接得到礦砂來源、電源、與核心，**三條線都要規劃**，這是 M1 唯一一個「三路匯流」的擺位問題。<br>② 一台熔爐的礦砂需求 8/秒就吃掉基礎 `cap` 10 的 80%——它是繼發電機之後第二個逼玩家加粗幹線的建築，而**加粗到 2 級又要合金**（§7.2），迴圈自己咬住自己。<br>③ 合金線被打斷＝加粗與碎浪同時停擺，仍然是**看得懂**的失敗模式。 |
| 核心的合金語意 | 合金**沒有其他消費者**（它是造價貨幣不是流量），所以核心的合金 `demand` ＝ 全網合金供給。這與礦砂的「只收剩下的」是同一個式子，只是減數為 0。 |

**地圖元素**（非建築，由地圖資料宣告）：

| 元素 | 說明 |
|---|---|
| 礦點 | 採集器唯一可蓋的位置 |
| 敵人路徑 | 既定不可變；**節點不可蓋在其上**，導管亦不可，除非該格是跨越點 |
| **跨越點（橋）** | 路徑上的指定格，**導管可鋪、節點不可**。橋是架高的 → **橋上導管不受敵人攻擊**（§3.2、§3.5）。每張圖的座數是關卡設計的主要刻度尺 |
| 核心位置 | 路徑終點。**同時是礦砂的匯入點**（見上） |
| **起始礦砂** | 由地圖宣告的**關卡參數**（不是隱藏係數，§7.7）。測試圖「淺灘」為 **900**（B0.5 由 300 調高——示範佈局要一口氣蓋出「產線＋電網＋四隻塔」才驗證得了核心取捨；真正吃緊的起始礦砂是 B0.7 手作圖與 B1.2 戰役五關的事） |

### 7.4 角色（塔）— M0 四隻

**耗能模型**：交戰耗能為「射程內有目標時每秒持續扣除」，與射速無關。待機耗能預設 0。

| 角色 | 造價 | 生命 | **交戰耗能** | 待機耗能 | 射程 | 射速 | 傷害 | 型 | 特殊 |
|---|---|---|---|---|---|---|---|---|---|
| **錨 Anchor** | 50 礦砂 | 60 | **4 /秒** | 0 | 4 格（圓） | 1.2 /秒 | 18 | 物理 | — |
| **稜鏡 Prism** | 120 礦砂 | 60 | **20 /秒** | 0 | 9 格（直線穿透） | 0.5 /秒 | 30（全穿透目標） | 能量 | 穿透整條直線 |
| **潮鳴 Knell** | 90 礦砂 | 60 | **9 /秒** | 0 | 5 格（圓光環） | — | 0 | — | 減速 −40%、護甲 −25%；光環僅在射程內有敵人時啟動 |
| **回收者 Reclaimer** | 100 礦砂 | 60 | **7 /秒** | 0 | 5 格（圓） | 0.9 /秒 | 14 | 物理 | **射程內任何敵人死亡（不限自己擊殺），回收其價值 60% 為能量注入電網** |
| **碎浪 Breaker** ★ | 140 礦砂 ＋ **60 合金** | 60 | **12 /秒** | 0 | 5 格（圓） | 0.6 /秒 | 22 | 物理 | **濺射 2.5 格**：對主目標周圍 2.5 格內的每一隻造同額傷害。B1.1 起 |

> **生命 60**（B0.5 補上）：原表沒有這一欄，但敵人是 walk-by、每 tick 傷害相鄰 1 格內的建築（§3.5），沒有生命值就無法回答「把塔壓到路邊撐不撐得住」。四隻同值 60（＝採集器級，高於中繼 30、低於儲槽 80）是**一個待調的佔位數**，不是校準過的結果——要分化先等 B1.3 的平衡批次。

**★ 碎浪為什麼是這些數字**（B1.1 定案）：它是**第一隻要合金的角色**，所以它必須解掉一個既有四隻解不掉的問題，而不是「錨但更強」。

| 設計問題 | 定案 |
|---|---|
| 它解什麼 | **密集隊列**。第 5 波是 8 隻漂蟲間隔 0.6 秒 ＋ 5 隻熾泳間隔 0.8 秒——那是全遊戲最擠的一段路，而四隻 M0 角色全是單體（稜鏡的穿透要求敵人剛好共軸，是**擺位謎題**不是**時機答案**）。 |
| 為什麼不強過稜鏡 | 單體 dps 22 × 0.6 ＝ **13.2/秒**，比錨（21.6）還低。**12 能量 ＝ 三座錨 ＝ 64.8/秒單體**，所以碎浪要打到 **5 隻**才追平——**濺射不是免費的傷害，是對敵人排隊方式的賭注**。 |
| 為什麼半徑是 2.5 | 主目標永遠是**最前**那一隻（與其他塔同一條規則，擺位才可預測），所以濺射圈只吃得到它**後面**那一段。半徑 2.5 ＝ 涵蓋領頭之後 2.5 格：第 5 波漂蟲間隔 0.6 秒 × 速度 1.0 ＝ 0.6 格/隻 → 一發 5 隻，剛好打平三座錨。間隔一鬆（第 1 波的 1.5 秒 ＝ 1.5 格/隻）就只吃得到 2 隻，當場虧。 |
| 為什麼是物理 | 甲殼護甲 8 是減法：碎浪一發 22 對甲殼只剩 14，**打不動裝甲隊**。錨／稜鏡／潮鳴仍是甲殼的答案，剋制表沒有被壓平。 |
| 12 能量/秒的位置 | 介於潮鳴 9 與稜鏡 20 之間。**一座碎浪 ＝ 三座錨的電**，而它只在敵人擠成一團時值這個價——峰值電力仍然是唯一的約束。 |

**★ 三條在 B0.5 實作時補齊的規則**（原表未寫，照字面實作會產生歧義）：

| 規則 | 定案 | 為什麼 |
|---|---|---|
| **稜鏡的「直線」是哪一條** | 穿過自己的**四條軸線**（水平／垂直／兩條 45°）中**敵人最多的那一條**；平手依固定順序取，零 RNG | 塔不能對玩家沒下的指令自己選任意角度——那會讓擺位無法預測。限定四軸則把稜鏡變成一道**對齊謎題**：把它擺在與某段路徑同列／同行，就能一次貫穿整段 |
| **多座潮鳴是否疊加** | **不疊加，取最強的那一座** | 疊加會讓控場塔變成堆量遊戲（蓋六座潮鳴＝敵人不會動），那和「峰值電力是唯一約束」的取捨互相打架 |
| **破甲吃護甲，不吃屏障** | 潮鳴的 −25% 只減護甲 | 屏障的剋制方式是物理傷害（§3.5 剋制表），不是破甲。讓潮鳴同時破兩種＝把剋制表壓平 |

**全域擊殺回收**：任何塔擊殺敵人 → 回收該敵人價值 **25%** 的礦砂。回收者的能量回收與此**並存**（同一次擊殺可同時產生礦砂與能量）。

**★ 回收者的能量回收算式**：`能量 = 敵人價值(礦砂) × 60% × 5`（匯率見 §3.3）

| 敵人 | 價值 | 回收能量 |
|---|---|---|
| 漂蟲 | 12 礦砂 | **36** |
| 熾泳 | 22 礦砂 | **66** |
| 甲殼 | 30 礦砂 | **90** |

*校準基準*：回收者一波耗 7 × 30s ＝ 210 能量。射程內死 10 隻（均價 20 礦砂）＝ 回收 600 能量，淨 +390 ≈ 0.65 台發電機——不吃礦砂燃料，但要靠擊殺密度。**條件式強勢，形狀健康**。若實測回收者變成必蓋或廢物，先動的是 60% 這個數字，不是匯率。

> **★ 回收的能量是一個緩衝，經回收者自己那條導管以 `cap` 限速注入**（B0.5 拍板）。它**先供自己的交戰耗能**，剩下的才推進電網。
>
> **為什麼不能瞬間到帳**：能量沒有全域帳戶，而「唯一的跨 tick 儲存體是儲槽，且充放電受它自己那條導管的 `cap` 約束」是鎖定的設計。一次擊殺回收 36–90 能量瞬間注入全網，等於在回收者身上偷開了第二個水池。**與儲槽同構處理**是唯一不破壞模型的做法：回收者的線因此成為一個真正的瓶頸，加粗它要和「多蓋一座採集器」競爭——這正是本作要的形狀。
>
> **對上面校準基準的影響（已知、待 B1.3 實測）**：基礎 `cap` 10 的線，一波 30 秒最多只推得出 **300** 能量，低於基準假設的 600。回收者因此是「**擊殺密度 × 導管粗細**」的雙重條件，比原本更難用、也更有得選。這個交互作用留到 B1.3 平衡批次用 headless 模擬實測，**先不動 60%**。
>
> **緩衝上限 100 能量，滿了溢流丟棄。** 沒有上限的話回收者就是**第二座儲槽**——一座沒有容量欄位、沒有造價、也沒被列進優先權面板的儲槽，正是「無全域能量池」要擋掉的東西。100 剛好裝得下單次最大的一筆回收（甲殼 90），所以一發大的永遠不會溢流，一連串的才會。推不出去的電本來就沒人用得到，丟棄不損失任何玩家做得到的事。

**能量不足時**：`實際射速 = 基礎射速 × 滿足率`，滿足率 = 實得能量/需求能量。潮鳴的光環強度同樣按滿足率縮放。

**峰值電力參考算式**（設計數值時的校準基準）：

> **前提：本表假設幹線導管已升到足夠 cap**（§7.2）。基礎 cap 10 送不出 20/秒的發電機輸出，也放不出 23/秒的儲槽缺口——**加粗幹線的礦砂成本要算進下表的每一列**。

| 情境 | 波次期峰值耗能 | 發電配置 | 缺口 | 儲槽支撐時間 | 幹線需求 |
|---|---|---|---|---|---|
| 早期：4 錨 | 16 /秒 | 1 發電機（20） | 盈餘 +4 | — | cap 16（升 1 級） |
| 中期：6 錨＋1 潮鳴＋1 熔爐 | 43 /秒 | 2 發電機（40） | −3 | 300 ÷ 3 = 100 秒 ✅ | cap 22（升 2 級） |
| 加一座稜鏡 | 63 /秒 | 2 發電機（40） | −23 | 300 ÷ 23 ≈ 13 秒 ⚠️ 波次撐不完 | 儲槽線需 cap 28 才放得出 23 |
| 加一座稜鏡＋1 儲槽＋1 發電機 | 63 /秒 | 3 發電機（60） | −3 | 600 ÷ 3 = 200 秒 ✅ | cap 22 |

**這張表就是核心取捨的數學形狀**：多一座稜鏡，你得補一台發電機＋一座儲槽（＝110 礦砂＋持續 4 礦砂/秒的燃料）＋把幹線再加粗，那些資源本來可以拿去多蓋一座採集器。

### 7.5 敵人（M0 三種 ＋ ★ B3.2 三種 ＋ ★ B3.2b 三種）

| 敵人 | 生命 | 速度 | 護甲 | 屏障 | 對建築傷害 | 價值 | 新規則 |
|---|---|---|---|---|---|---|---|
| 漂蟲 Drifter | 60 | 1.0 格/秒 | 0 | 0 | 8 /秒 | 12 礦砂 | — |
| 甲殼 Carapace | 180 | 0.6 格/秒 | 8 | 0 | 14 /秒 | 30 礦砂 | 護甲 |
| 熾泳 Ember | 90 | 1.6 格/秒 | 0 | 40% | 10 /秒 | 22 礦砂 | 屏障 |
| **潛涌 Surge** | 70 | **1.9** 格/秒 | 0 | 0 | 9 /秒 | 20 礦砂 | **迅捷：免疫減速** |
| **苔群 Bloom** | 24 | 1.1 格/秒 | 0 | 0 | 3 /秒 | 5 礦砂 | **群體：連佔 3 個出場位** |
| **癒殼 Mender** | 150 | 0.7 格/秒 | 0 | 0 | 12 /秒 | 34 礦砂 | **再生：每秒回最大血 4%** |
| **鏽潮 Rustsurge** | 110 | 0.9 格/秒 | 0 | 0 | 10 /秒 | 26 礦砂 | **蝕線：對導管 ×3、對節點 ×0.5** |
| **殼衛 Bulwark** | 130 | 0.6 格/秒 | 4 | 0 | 11 /秒 | 32 礦砂 | **庇護：相鄰 1 格的同伴護甲 +6** |
| **汲潮 Drainer** | 100 | 1.0 格/秒 | 0 | 0 | 7 /秒 | 28 礦砂 | **汲取：3 格內的塔交戰耗能 ×1.5** |

#### ★ B3.2 的三隻：一隻一條新規則，而且三條都由現成的塔回答

**不補到 §5 的 12 隻。** 理由是本節底下 §5 已經記過兩次的同一條：
**一次補齊一堆沒平衡過的內容，會讓後面的校準建立在流沙上。** 3 → 6 是 M1 那一欄。

| 新規則 | 它擋掉哪一個「沒有代價的答案」 | 剋制它的 |
|---|---|---|
| **迅捷**（免疫減速） | 「全押減速光環」。§3.5 的屬性表從 M0 就寫著這條屬性，而 B3.2 之前**沒有任何一隻敵人有它**——潮鳴／霜礁對全場有效，等於一個沒有反例的答案 | 高爆發單體（長哨、定潮） |
| **群體**（連佔 3 個出場位） | 「只蓋單體」。一次抽中 ≈ 一份壓力（24×3 = 72 血，和漂蟲的 60 同級），改的是壓力的**形狀**不是總量 | 濺射（碎浪）、貫穿（稜鏡） |
| **再生**（每秒回最大血 4%） | 「電不夠就慢一點而已」。塔缺電時射速線性下降，**降到某一點之後 dps 追不上再生，它永遠不會死**——峰值電力（§3.1）從此有一個看得見的門檻 | 補電、或高爆發 |

> **再生寫成比例不是絕對值**：它要跟著無盡曲線與難度層一起長，否則第 30 波的它等於沒有這條規則。
>
> **三隻只進無盡與每日的出場池**（§7.10），戰役五關一格都不動——那五關的參考解是
> `campaign_test` 431 條斷言的地基。

> **視覺**（`20_ART_DIRECTION.md` §1.7「輪廓由既有的機制欄位推導」）：
> 苔群靠半徑（小一號）、潛涌靠**空心菱形核心 ＋ 半徑 6**（熾泳是實心圓 ＋ 8——
> 兩隻都過得了迅捷的速度門檻，共用一個亮核心的話「抓不抓得住」在畫面上是看不見的）、
> 癒殼靠**輪廓內一圈會呼吸的亮線**。另外，潛涌站在光環裡**沒有那圈青色描邊**，
> 那就是「抓不住它」——這一條不必新增任何東西，既有的編碼已經在說了。

#### ★ B3.2b 的三隻：同一條紀律，而且**第一版的第三隻被規格階段否決了**

原訂第三隻是「破壞半徑 2 格」，要擋的是「離路徑一格就安全」。它撞到鎖定設計：
`Tide.immune_indices()` 的橋免疫範圍是**跨越點 ±1 格**，而那個 ±1 正是照
「破壞半徑 1」推導出來的（導管垂直穿過路徑必然佔用「橋 ＋ 兩側各一格」）。
半徑 2 的敵人會打到引道，等於把 §3.5 那條「橋上導管不受攻擊＝**玩家可規劃的
安全動線**」悄悄挖空——而症狀是「橋好像沒用了」，不是任何一條斷言會紅的東西。
**血量與速度可以調，`BLAST` 是幾何，動它就是動鎖定設計。**

| 新規則 | 它擋掉哪一個「沒有代價的答案」 | 剋制它的 |
|---|---|---|
| **蝕線**（對導管 ×3、對節點 ×0.5） | 「導管 3 礦砂一格，斷了下個準備期再接」。前六隻對節點與導管一視同仁，於是**導管的耐久從來不是一個要規劃的東西** | **走橋**。橋免疫從第 3 關就存在，但它到此為止只是一條**限制**（導管只能從這裡過）——這一隻讓它第一次變成**答案** |
| **庇護**（相鄰 1 格的同伴護甲 +6） | 「一群薄血的東西＝濺射的免費點心」。苔群（B3.2）教了「壓力的形狀」，而它的解一直是**同一發濺射**，不必問打的是誰 | **潮鳴的破甲**（`armor_break` 0.25，升級可到 0.44）。這個欄位從 M0 就在表上，而全場只有甲殼一隻吃得到——和「迅捷」之前的處境一模一樣 |
| **汲取**（3 格內的塔交戰耗能 ×1.5） | 「峰值電力算得準」。玩家在準備期就能把整條防線的峰值加總出來，而那個數字**在戰鬥期永遠成立** | **儲槽**（準備期充能、波次期放電）＋ **優先權滑桿**（決定誰先餓死）。這兩個系統從 B0.2／B0.5 就在，而它們一直是「蓋得夠多就用不到」的保險 |

> **三條規則各自對應玩家的一個動詞**：蝕線 → **走線**、庇護 → **塔的組成**、
> 汲取 → **儲槽與優先權**。三條都指向「擺位」的話，三隻其實是同一隻。
>
> **殼衛自己也有 4 點護甲**：它不是一個只會發光環的空殼——一隻站在自己光環裡的
> 敵人是 10 點護甲，那正好是「先打它還是先打旁邊」在畫面上看得出差別的量。
>
> **汲取乘的是交戰耗能，不是待機耗能**（待機早就是 0，乘上去是空操作——
> 科技「能量效率」在 v0.3 定案第 ⑬ 條踩過同一個坑）。
>
> **三隻只進無盡與每日的出場池**（§7.10），戰役十關一格都不動——那十關的參考解
> 是 `campaign_test` 934 條斷言的地基。第三幕要用它們的時候，那是另一批的事。

> **視覺**（`20_ART_DIRECTION.md` §1.7「輪廓由既有的機制欄位推導」）：
> 鏽潮＝**輪廓外一圈斷續的短刺**（它咬的是線，不是塊）；
> 殼衛＝**六邊形的甲板輪廓 ＋ 它罩到的同伴腳下一圈暗色底**（光環要在被罩的那一隻
> 身上看得見，不是只在發光環的那一隻身上——潮鳴的青色描邊就是這個做法）；
> 汲潮＝**空心環 ＋ 向內收的三根短線**（琥珀色，因為它吃的是電；§1.1 配色紀律 2
> 說琥珀專屬能量，而這是全案第一隻碰能量的敵人）。

### 7.6 波次與獎勵

| 項目 | 公式／值 |
|---|---|
| 無盡波次血量 | `hp(w) = base × 1.11^w` |
| 無盡波次數量 | `count(w) = 4 + floor(w/3)` |
| 提前召喚獎勵倍率 | `1 + (剩餘秒數 / PREP_TIME) × 0.5`，上限 1.5 |
| 局末研究數據 | `10 × 波次數 + 2 × 產能積分 + 提前召喚累計加成` |
| 產能積分 | `全局平均每秒總產出 / 10` |

**★ 上表三個數字的可執行定義（B0.6 補，原文只給了公式骨架）**：

| 詞 | 定義 | 為什麼是這樣 |
|---|---|---|
| **提前召喚累計加成** | `Σ 每波 10 × (該波倍率 − 1)`，每波最多 +5 | 「獎勵形式：該波掉落的礦砂與研究數據按倍率增加」（§3.4）。研究數據裡屬於「該波」的項就是 `10 × 波次數` 的那 10 點，倍率直接乘在它上面；攤開來就是這一行。不另發明係數。 |
| **該波倍率** | 按下提前召喚**當下**算一次，鎖給那一波 | 波次進行中準備期倒數已經停了，事後再算會拿到 0。鎖住的另一個好處：玩家按下去就知道自己賭到多少。 |
| **總產出** | **送達核心的礦砂累計**（不是採出來的量） | 與 `▲/秒` 同一個口徑（§7.3 核心＝礦砂銀行）。採了卻運不回去的礦砂不算產能——那正是 `滿溢` 徽章要指出來的事。 |
| **平均每秒** | `累計 ÷ (tick 數 × 0.1)`，含準備期 | 準備期也在生產，把它排除等於獎勵「一直快進」。 |

### 7.7 新手難度：靠關卡設計，不靠隱藏係數（★ 2026-07-27 改寫）

**原本的「新手係數」（塔待機耗能 ×0.6、敵人對建築傷害 ×0.7）已刪除。** 兩個理由：

1. **待機耗能已經是 0**（§7.4），×0.6 是空操作——R-4 的對策根本沒生效過。
2. **更根本的**：本作的核心就是「學會電力數學」。在教學期偷偷把物理改掉、第 6 關再無預警換回來，等於前五關教了玩家一個錯的模型。隱藏係數在這款遊戲特別有害。

**取而代之的硬性設計要求（寫入 B1.2 的 DoD）**：

> **戰役第 1–2 關必須以「新手可通關」為驗收條件**，且**只用關卡參數達成**，不得用隱藏乘數。

可用的關卡參數（全部是玩家看得見、學得到的東西）：

| 參數 | 說明 |
|---|---|
| 敵人數量與組成 | 前兩關只出漂蟲，數量少 |
| 波次數 | 前兩關波數短 |
| 礦點密度與距離 | 礦點多、離核心近 → 起手產能寬裕 |
| 核心距離 | 路徑短或長，決定塔的覆蓋壓力 |
| 跨越點座數 | 給得多＝網路好縫；給得少＝空間謎題 |
| 準備期 | **每關各自宣告**（§7.1 的 `PREP_TIME_TUTORIAL = 60s` 保留，但它是關卡參數不是隱藏係數——計時器就在畫面上） |

**難度層 1+ 的嚴苛係數不受影響**——那是玩家主動選擇、UI 明示的規則層（§3.11），不是偷偷改的。

### 7.8 科技樹（M1 首批 12 節點）

| 支線 | 節點 | 效果 | 成本（研究數據） |
|---|---|---|---|
| 產能 | 導管擴容 I / II / III | 基礎 cap +2 / +2 / +2 | 50 / 152 / 289 |
| 產能 | 採集精煉 I / II | 採集器 +1 礦砂/秒（每級） | 50 / 152 |
| 防務 | 能量效率 I / II / III | **塔交戰耗能 −8% / −8% / −8%**（連乘，滿級 −22.1%） | 50 / 152 / 289 |
| 防務 | 校準 I / II | 塔傷害 +6%（每級） | 50 / 152 |
| 後勤 | 藍圖槽 I / II | 藍圖槽 +1（每級） | 50 / 152 |

**成本＝每一串之內的階數**：I 50、II 152、III 289（`data/Tech.gd` 的 `TIER_COST`）。

> **★ B1.3 修正**：這裡原本寫「成本套用 `50 × n^1.6`，n 為該支已解鎖數＋1」，但那句話**複述不出上面這張表**——
> 依它算，「採集精煉 I」會是該支的第 4 個節點、要 460 而不是表上的 50；就算改讀成「同串的階數」，
> `50 × 2^1.6 = 151.57`（要進位才得 152）而 `50 × 3^1.6 = 289.98`（要捨去才得 289），**沒有任何一種取整方式同時得到 152 與 289**。
> §7 的數值表是數值權威，所以實作直接列表。日後往一串裡加第 4 階時再定它的價，不要回頭去救那個公式。

**★ 上限的量法（B1.3 定案）**：B2 的「≤ +35%」量的是**戰鬥數值**——`(傷害增幅) + (交戰耗能降幅)` = `12.36% + 22.13% = 34.49%`，
兩者**相加**而不是相乘（它們作用在不同的東西上，相乘沒有意義，而相加是比較嚴的那個算法）。
產能支不計入：導管基礎 cap 10 → 16 是 §7.2 已明文的設計，它是經濟數值不是戰鬥數值。
斷言在 `tests/tech_test.gd`，往防務支加節點時它會先變紅。

### 7.9 戰役關卡（★ B1.2 定案第一幕；★ B3.3 加第二幕）

#### 難度階梯＝**建造欄的解鎖順序**

戰役的每一關解鎖一種新的建造選項，**那個新選項就是那一關要教的機制**。這不是把內容鎖起來拖時間（紅線 R2），而是 §7.7 那張「可用關卡參數」表的直接延伸：**第 1 關給玩家四顆按鈕，不是十顆。**十顆按鈕對一個還不知道「電是流率」的人來說不是自由，是雜訊。

| 關 | 地圖 | 新解鎖 | 要教的那件事 | 尺寸 | 橋 | 波 | 準備期 |
|---|---|---|---|---|---|---|---|
| 1 | **潮口** | 採集器・發電機・中繼・錨 | 礦砂 → 能量 → 塔，一條線走完 | 20×12 | **0** | 3 | 60s |
| 2 | **雙灣** | ＋儲槽・稜鏡 | **峰值電力**：稜鏡交戰 20/秒＝一整台發電機，而一條導管只給得起 10 | 26×15 | 1 | 4 | 60s |
| 3 | **窄橋** | ＋潮鳴・回收者 | **一座橋就是一條幹線**：北岸的礦共用它，cap 就是全關瓶頸 | 28×17 | 1 | 5 | 45s |
| 4 | **熔渣灣** | ＋熔爐 | **第三資源**：合金 → 幹線加粗到 2 級 cap 22 → 一條線扛得動整組防線 | 30×18 | 2 | 5 | 45s |
| 5 | **潮汐之喉** | ＋碎浪 | 全解鎖綜合考：礦在對岸、三座橋、五波混編 | 34×18 | 3 | 5 | 45s |

#### ★ 第二幕（第 6–10 關，B3.3）：階梯換一根軸

第 5 關**已經全解鎖**——建造欄發完了，第一幕的階梯到此為止。第二幕的階梯是**來的是什麼**：

| 關 | 地圖 | 新規則 | 要教的那件事 | 尺寸 | 橋 | 波 | 準備期 |
|---|---|---|---|---|---|---|---|
| 6 | **苔灘** | 苔群（一次來一團） | 貫穿與濺射終於比單體划算——同樣的電費，一發吃掉整段 | 30×16 | 2 | 5 | 45s |
| 7 | **逆流** | 潛涌（免疫減速） | **光環有一個洞**。潮鳴從第 3 關就在，而它到此為止對每一隻都有效 | 32×17 | 2 | 5 | 45s |
| 8 | **殼場** | 癒殼（每秒回最大血 4%） | **dps 有一個地板**。前七關的「電不夠」只是慢一點，這一關是打不死 | 34×17 | 3 | 5 | 45s |
| 9 | **亂潮** | 三條規則**兩兩同時** | 一份電要分給三種答案——優先權滑桿存在的理由 | 36×18 | 3 | 5 | 45s |
| 10 | **深喉** | 六種全上 | 畢業考。最後一波六種同時出場（全案唯一一次） | 38×19 | 4 | 6 | 45s |

第二幕的建造欄一律沿用第 5 關的全解鎖（`Campaign.ACT2_BUILD`）。三隻新敵人**只在第二幕與無盡出現**，第一幕五關的波次表一格未動——那五關的參考解是 `campaign_test` 全部斷言的地基。

**第 10 關是全案唯一一份同時用上熔爐 → 合金 → 碎浪、稜鏡菱形與潮鳴的佈局**，也是唯一一關的參考解**把四座橋併成兩條獨立幹線**而不是一條。理由是量出來的（見下）。

> **為什麼停在 10 而不是補到 §5 的 15**：同 B3.2 的理由（也是 §5 在 B1.1／B2.4 記過兩次的那一條）——**一次補齊一堆沒平衡過的內容，會讓後面的校準建立在流沙上**。每一關都要有一份實跑通關的參考解，而那是這幾批最貴的東西。

#### ★ 第 10 關量出來的兩件事（B3.3b）

**① 熔爐是一個比它看起來大得多的消費者。** 它吃 **8 礦砂/秒**，而且 **待機也吃 10 能量/秒**（§7.3）。把它和三台發電機一起掛在同一條 1 級幹線（cap 16）下游，進核心的礦砂**幾乎是 0**（實測產能積分 0.05）。而想加粗到 2 級要合金，合金正是那座熔爐要煉的東西——**先有雞還是先有蛋**。

解法不是加粗，是**兩條獨立幹線**：地圖本來就有四座橋，併成兩組就把頻寬翻倍，一塊合金都不用。這是「一座橋就是一條幹線」（第 3 關）在畢業考上的反面用法。

**② 存著的合金不是產能。** 第一版收尾時帳上 1310 塊合金，而產能積分只有 0.23——熔爐吃掉 8 礦砂/秒去煉一堆沒有用途的東西。把合金花在幹線上（2 級，cap 22）之後是 **0.43**。倉庫不是產線。

起始礦砂（也是關卡參數，§7.7）：`300 / 430 / 540 / 720 / 1240 / 1100 / 1200 / 1400 / 1500 / 1800`。它隨圖變大而增加是因為**導管本身就變貴了**（3 礦砂/格，第 5 關的南緣幹線一條就 90），不是難度在放水——真正的壓力來自波次組成、準備期與橋的座數。

**★ 第 1–2 關的「新手可通關」只用關卡參數達成**（§7.7、B1.2 DoD）：波數短、只出漂蟲、礦點多且離核心近、起始礦砂寬裕、準備期 60 秒、**第 1 關零座橋**。沒有任何隱藏乘數。

**為什麼第 1 關不給稜鏡**：稜鏡交戰吃 20 能量/秒，而導管基礎 cap 是 10、升到 1 級是 16——**在拿到合金（第 4 關）之前，一條線餵不飽一座稜鏡**。第 1 關就給它，等於讓新手的第一座塔永遠掛著「缺料」徽章。錨（4/秒）在基礎 cap 底下綽綽有餘，核心循環才學得乾淨。

**第 2 關的正解是「菱形」**：發電機 →（左中繼／右中繼）→ 稜鏡，兩條 cap 10 的線並聯成 20。這是 §7.2「cap 10 < 發電機 20」那一課換到消費端再演一次，而且它給玩家的是一個**可以複製的手法**——第 3、4、5 關的稜鏡都用同一個形狀。

#### 星等（3 顆）

| 星 | 判準 | 為什麼是這個 |
|---|---|---|
| ★ | **通關**（波次表跑完且核心存活） | 撐住 |
| ★★ | **核心無損**（`core_hp` 等於滿值） | 敵人只在核心駐足——核心掉血等於「有東西走到底了」，玩家看得見因果 |
| ★★★ | **產能積分 ≥ 該關宣告門檻** | 守得住之後才問「你的生產線有多好」。用的是 §7.6 已定義的產能積分，不另發明分數 |

三顆星就是三個學習階段：撐住 → 守好 → 生產。**門檻寫在關卡資料表裡、局末結算逐條列出「拿到沒有、差多少」**，不是隱藏值。三條是**累進**的：沒通關 0 顆，核心破了封頂 1 顆——讓「產能爆表但核心快沒了」拿三星，等於把第一課教反。

**門檻是實測校準的**（B1.2，`tests/campaign_test.gd` 每次回歸重跑）：五關的參考解量到的產能積分是 `0.80 / 0.48 / 0.38 / 0.29 / 0.19`，門檻取它的 1.3–2 倍：

| 關 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| ★★★ 產能積分門檻 | 1.00 | 0.70 | 0.60 | 0.50 | 0.40 |

**門檻隨關卡遞減不是筆誤**：後面的圖更長（導管更多）、消費者更多（熔爐與第三台發電機都吃礦砂），而積分的分母含整局時間。三星的意思始終是同一句：**你的產線得比「剛好夠打」那一版好一半以上。**

#### ★ 導管不得和既有導管疊在同一排格上（B1.6.1）

使用者實看後回報「有破圖、有重疊」。根因是**兩條共線導管可以疊在同一排像素上**：畫面上是一條線、實際是兩條，兩排流動珠交錯成一團，而 `conduit_near()` 的命中判定在它們之間只能任選一條（加粗會加到你沒指的那一條）。

規則：**共用 2 格以上不准；共用 1 格照樣可以**（那是交叉或在同一個節點分岔，本來就該允許）。

這條規則刻意**不是**「不准穿過節點」——從一個節點旁邊經過而不接它，在流量網路裡是一個真的戰術選擇（接上去就得和它共用那條線的 cap）。被禁掉的只有「看起來是一條線、其實是兩條」這件事。

#### ★ 不要把塔擺在幹線的接點上（B1.6.1，關卡設計規則）

`sim/FlowNetwork` 的節點語意是**先吃滿自己的需求才轉發**。所以一座塔如果同時是幹線上的中繼，它會**餓死它下游的一切**——第 5 關的參考解一度把稜鏡當接點，結果核心旁邊那座錨滿足率 0.00，而頂欄顯示供給 60（問題從來不在發電量上）。

要讓一個大消費者拿到兩條進線，就得由**兩個中繼**（不吃電）從幹線分岔進去。第 5 關的核心因此上移一格，把 y=16 讓出來給這個菱形。

#### 兩條在 B1.2 校準時才現形的關卡設計規則

這兩條不是新設計，是既有規則的必然推論——但沒寫下來之前，五關裡有四關踩到：

1. **每一關至少要有一座塔搆得到核心。** 敵人 walk-by 永不停步，**只有核心會讓它們駐足**（§3.5）——所以任何漏到底的敵人會待在核心上啃到其中一方死。射程搆不到核心的防線，一次漏怪就是必輸，而且畫面上完全看不出為什麼。
2. **稜鏡必須和某一段路徑同排或同列。** 它的貫穿是**沿著一條軸線**（`sim/Combat.pierce_indices` 取八個方向裡命中最多的那一條），不是「射程內全打」。擺在路徑斜前方的稜鏡只打得到一兩格，看起來卻和擺對位置的一模一樣。這就是 §7.4 說的對齊謎題，它比原本寫的更強硬。

#### 獎勵

**研究數據 = `該關基礎獎勵 × 星數`**，以**歷史最高星數**結算、**只補發差額**——回頭補星拿得到，重複刷同一星數拿不到第二次。

| 關 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 基礎獎勵 | 30 | 40 | 55 | 70 | 90 |

滿星全通＝ `(30+40+55+70+90) × 3 = 855` 研究數據，足以解掉 §7.8 十二節點裡的前六個。**戰役是科技樹的主要資金來源**——B1.3 的成本表就是照這個數量級開的。

#### 解鎖規則

第 N 關通關（≥1 星）即解鎖第 N+1 關。**沒有星數門檻**——星是給自己看的紀錄，不是閘門（紅線 R2「無等待閘門」的同一條精神：卡關只能卡在技術上，不能卡在補星上）。

### 7.10 無盡模式（★ B2.1a 定案）

#### 生成器輸出的是**和手作地圖一模一樣的一份資料**

`sim/MapGen.gd` 回傳的 dict 與 `data/Maps.gd` 的手作圖同構（`size`／`core`／`waypoints`／`crossings`／`ore`／`start_ore`／`prep_time`）。**渲染、建造、命中判定、示範佈局、截圖鉤子全部零改動**——生成器只是換一個地方生產同一種資料。這也讓「生成圖能不能玩」這件事可以直接沿用 `campaign_test` 那套斷言。

#### 佈局 1「潮汐迴廊 Tideway」

敵潮自左緣進入，正交蛇行折向右側核心。**這是五關手作圖的共同骨架**（全部是正交折線），不是新的空間語彙——B2.1a 要驗的是生成器的確定性與可通關性，不是新玩法。

| 參數 | 值 | 理由 |
|---|---|---|
| 尺寸 | **`64×40`**（★ B2.1b 由 34×18 放大） | 見下方「大過一屏」 |
| 折段數 | **`4–6`**（seeded） | 少於 4 段在 64 格寬會變成幾乎筆直的長廊、塔隨便擺都覆蓋得到；多於 6 段會擠成鋸齒，而且每多一段就多一條直走要走 |
| 縱向走法 | **單調掃掠**（★ B2.1c 改） | 見下方「掃掠與礦脈」。橫走的列由**分層抽樣**產生：`[EDGE, H-1-EDGE]` 切成 `折段數` 條等寬帶、一帶一列，**頭尾兩帶只取外側半邊** |
| 跨越點 | **`4–6`** 座 | 第 3 關教過「一座橋就是一條幹線」；路徑長到約 90 格之後 2 座橋等於整張圖只有兩個過河點 |
| 礦點 | **`20–28`** 個（★ B2.1d 改） | 總數跟著面積算（34×18 是 6–8）。**放大地圖而不動這個數字，等於悄悄把難度調高**（§7.7）。上限留了補洞的餘裕 |
| 礦點分佈 | **沿地圖面積分層**：整張圖切成 `rows×cols` 個區塊，**一塊恰好一顆**（★ B2.1d） | 「平均」是**構造出來的**，不是撒完再檢查。區塊網格取自一張表（5×4／7×3／6×4／8×3／5×5），**礦點數 ＝ 區塊數**——由 `n` 反推區塊數會多出一兩塊、多的那幾塊被跳過，畫面上就是「這一區沒有礦」 |
| 礦點離路徑 | **≥ `2` 格，Chebyshev（八方）距離** | 下限是 **walk-by 真的打不到**（§3.5）。⚠ **距離的種類要和 `Tide.in_blast()` 一致**——那支是 Chebyshev ≤ 1，用四方距離量的話**斜對角貼著路徑轉角**的礦點會通過檢查、蓋下去卻每 tick 挨打（實測 300 張圖有 167 顆）。**上限已移除**——原本的 `7` 正是「只長在路邊」的成因 |
| 平均分配的規格 | 整張圖切 **`4×3` 個大區，每一區都要有礦** | 生成器負責保證（放置後補洞），`endless_test` 獨立再驗一次 |
| 起始礦砂 | **`1900`** | 第 5 關（34×18）是 1240，而這張圖的導管長度約 1.9 倍（3 礦砂/格） |
| 準備期 | `45s` | §7.1 標準值。無盡不是教學，不給 60s |

#### ★ 十一條生成不變量（`endless_test.gd` 逐條斷言）

前四條是**格式正確**，第五條是**這張圖玩得動**，**6–9 是這張圖值不值得玩**（★ B2.1c 新增）——
那四條全部來自實玩回饋，而不是設計時想得到的：格式正確、可通關、且完全不好玩的圖，生成器一天擲得出幾千張。

1. **同種子逐格相同**——`size`／`core`／`waypoints`／`crossings`／`ore` 全欄位深度比對。
2. **路徑不自交**——展開後的路徑格無重複。自交的路徑會讓 walk-by 的傷害重複結算在同一批建築上。
3. **每座跨越點落在路徑格上**——橋不在路徑上就不是橋（§3.2）。
4. **礦點不在路徑格上、不與核心重疊**——路徑格不可蓋節點（§3.2），生成在那裡的礦點等於不存在。
5. **★ 至少 7 個礦點不必過橋就到得了核心**（B2.1a 是 3，跟著總數一起提高——`3/26` 的保證比 `3/8` 弱得多）——這一條是「生成圖必然可通關」的實質保證。橋是可規劃的安全動線，但**把所有礦砂都放在對岸**會讓開局的第一條產線非過橋不可；生成器一旦擲出這種圖，玩家的失敗原因跟他的決策無關。手作圖能靠設計者的眼睛避開，生成器只能靠斷言。
6. **★ 路徑縱向跨幅 ≥ 18 列**（B2.1c）——見下方「掃掠與礦脈」。
7. **★ 死列 ≤ 6 列**（離任何路徑列超過 6 列的列）——塔的射程與產線的合理長度都在這個尺度上，所以死列＝**畫面上有、玩法上沒有**的空間。
8. **★ 每個礦點離路徑 2–7 格**——下限是 walk-by 打不到，上限是「值不值得拉一條導管穿過空地」。
9. **★ 整張圖切 `4×3` 個大區，每一區都要有礦**（★ B2.1d 取代原本的「孤點 ≤20%」）——「平均分配」的可量版本。**前兩輪的指標都只看路徑附近**，所以「全擠在路邊」在它們眼裡是滿分。
10. **★ 沿路徑最大空檔 ≤ 40 格**（B2.1d 由 24 放寬）——礦點改成鋪滿全圖之後，「路線旁邊一定有礦」不再是承諾，這一條退居為粗略的理智檢查。原本的（連續幾格路徑旁邊沒有任何礦）——「只在某些地方有，其他地方都是空的」的可量版本。
11. **★ 半徑 4 內 ≤ 8 顆礦**——「一坨擠在一起」的可量版本。面積分層之後守的是**相鄰區塊的邊界**：兩顆各自貼著共同邊界一樣會擠在一起。

#### 無限波次

沿用 §7.6 的兩條公式，**不另發明第三條曲線**：

| 項目 | 值 | 說明 |
|---|---|---|
| 血量倍率 | `1.11^(w-1)` | 乘在敵人自己的 `hp` 上（§7.5）。`w` 自 1 起 → 第 1 波是原始強度 |
| 隻數 | `4 + floor(w/3)` | §4.2 原文 |
| 出場間隔 | **固定 `1.0s`** | 壓力曲線已經由血量與隻數表達完了。再給間隔一條曲線，玩家就分不出「這波難是因為血厚還是因為密」 |
| 敵種池 | `w≥1` 漂蟲｜`≥3` ＋甲殼｜`≥6` ＋熾泳｜**`≥9` ＋苔群｜`≥12` ＋潛涌｜`≥15` ＋癒殼** | 與戰役同一條登場順序（§7.9）：護甲先教、能量抗性後教。**每三波才多一種**——新規則要有一段只有它自己的時間，玩家才對得起來「這一波難是因為多了什麼」 |

**組成由 seeded `Rng` 從當前池中抽**，同種子同波次必得同一張出場表（§2.4 確定性）。

#### ★ 大過一屏（B2.1b）

**可讀性地板優先於「看得到全貌」。** `Shapes.MIN_READABLE_CELL = 24px` 是視覺編碼校準與驗收的基準（RG-59）；一格低於它，「一眼看出瓶頸」（R-3）就不再是驗過的事。所以無盡的縮放下限不是 fit，而是**地板**：

| | 戰役（一屏可見） | 無盡（大圖） |
|---|---|---|
| 縮放下限 | `fit`（＝地板以上） | **`max(fit, 24/32)`** ＝ 地板 |
| 「全景」鈕 | 回到 fit ＝ 全圖 | 回到**地板**，仍看不到全圖 |
| 全圖在哪裡看 | 就是主畫面 | **小地圖**（下方） |

`64×40` 的來由：框架 1152×604 在地板倍率下看得到 **48×25 格** → 64×40 看得到 **75%×62% ＝ 面積的 46%**。

**第一版做的是 56×32，實看之後改掉**：那個尺寸看得到 86%×79%，**小地圖上的視野框幾乎和外框重疊**——「我在哪」讀不出來，兩個導引通道的價值一起被稀釋。一個只是技術上大過一屏的地圖，不值得為它做導引。

**為什麼高的比寬的多加**：路徑長度 ≈ 寬度 ＋ 各段直走，而直走跨距有上限（下表）。**加高幾乎不影響一波的時間，加寬是一比一地加。**

**誠實記一個代價**：路徑變長（實測 min 90／p50 95／max 98 格，★ B2.1c 的單調掃掠把離散度從 73–119 收成 90–98——**一波的秒數不再憑種子浮動**）→ **一波的時間也跟著變長**（約 1.9 倍）。這是刻意接受的：無盡的價值在「產線能長多大」，而大圖給的正是建造空間；行走時間變長同時也讓塔的覆蓋窗口變長（偏易），由 `1.11^w` 的血量曲線自然吃掉。

#### ★ 掃掠與礦脈（B2.1c，來自實玩回饋）

使用者的原話是兩句：「**東西都集中在路線上半區，下半區很空**」「**採集器的分布顯然是隨機分布，缺乏合理規劃**」。300 個種子的實測證實兩句都成立，而且比看起來嚴重：

| | B2.1b | B2.1c |
|---|---|---|
| 路徑縱向跨幅（全高 40） | 19.3 列（最差 **8**） | **33.1** 列（最差 28） |
| 死列（離路徑 >6 列） | **26%** | **0%** |
| 礦點離路徑 >8 格 | **50%** | **0%** |
| 孤點（最近的另一顆礦 >4 格） | **72%** | 12% |
| 路徑長度（＝一波的秒數） | 95.9 | **95.1** |

**① 跨越全高的代價只付一次。** 舊版每段橫走獨立抽一列，走出來的是一條**在窄帶裡折返**的路徑：花掉 34 格的直走，只換到 19.3 列的跨幅——折返的部分是白花的。改成**單調掃掠**（由上而下或由下而上，不折返）之後，直走總長恆等於 `y[末] − y[首]`，**跨幅拿滿而路徑長度不變**。

這一點非做不可的理由是 `Tide.advance()` 是 `progress += speed * TICK`：**路徑格數就是一波的秒數**。用「把路徑拉長來填滿地圖」解這個問題，等於直接把每一波變慢——那是拿一個缺陷換另一個。

**② 礦點平均鋪滿整張圖。** 這一條改了**三次**才對，三次都是實玩回饋，而且每一次的失敗都很有代表性：

| 版本 | 作法 | 使用者的回報 | 為什麼失敗 |
|---|---|---|---|
| B2.1b | 全圖均勻**隨機**撒點 | 「顯然是隨機分布，缺乏合理規劃」 | 均勻隨機**必然結塊**——那是隨機的性質，不是 bug |
| B2.1c ① | **隨機**挑錨點再長「礦脈」 | 「一坨擠在一起，其他地方都是空的」 | 把隨機從點搬到錨點，結塊照舊 |
| B2.1c ② | 沿**路徑索引**分層 | 「只生在道路旁邊，其他區域都沒有」 | 沿路線均勻了，但 `OFF_MAX=7` 把礦鎖在路邊的帶子裡 |
| **B2.1d** | 沿**地圖面積**分層，一區塊一顆 | — | 「均勻」是構造出來的；上限移除，全圖都有礦 |

**三次共同的教訓：均勻要靠分層取樣，不能靠隨機**——而且**分層的座標系要選對**。前兩次選錯了座標系（一次是沒分層，一次是分在路徑索引上），指標也跟著只看路徑附近，於是「全擠在路邊」在指標眼裡是滿分（RG-118 的同一類錯誤）。

**③ 死區面積 45% 因此一併解掉。** B2.1c 記過「路徑是由角到角的單調階梯，對角兩個角落永遠離路徑很遠，離路徑 >10 格的格子佔 45%」。礦點鋪滿全圖之後，那些地方不再是空的——它們是**安全但要拉長線**的取捨，而那正是本作要的決策。

**④ 這一批沒有動任何難度參數。** 路徑長度、起始礦砂、跨越點數、波次公式全部不變。

#### ★ 小地圖不常駐（B2.1d.1，使用者指定）

小地圖蓋在地圖右下角。**常駐等於那一塊永遠點不到**——使用者實玩回報「有些地方會點不到」。

| | 規則 |
|---|---|
| 出現 | **縮放或平移時**（含小地圖自己的點擊與拖曳、邊緣箭頭的一鍵跳、全景鈕、進場） |
| 消失 | 視野靜止 **3 秒**後，最後 0.5 秒淡出 |
| 隱藏時 | **不攔截點擊**——這才是這條需求的重點，不是視覺上的乾淨 |

⚠ `TL_SHOT` 下**不倒數**：截圖落在第幾秒取決於這台機器跑了幾幀，會倒數的話同一組參數在不同機器上會拍出「有／沒有小地圖」兩種圖（和模擬凍結同一條理由）。

#### ★ 導引：兩個通道，缺一不可（B2.1b）

大圖模式的 R-3 驗收是「**瓶頸節點即使不在畫面內也能在 30 秒內被找到**」。兩個通道分工明確，不是互為備援：

| 通道 | 回答的問題 | 為什麼不能只留一個 |
|---|---|---|
| **小地圖** | 「**全局**現在長什麼樣？我在哪？」 | 邊緣箭頭只說「那個方向有事」，說不出「離多遠、有幾個、我的產線整體結構」 |
| **邊緣箭頭** | 「**現在**有東西壞了，在畫面外哪一邊？」 | 小地圖是常駐的靜態資訊；玩家在忙的時候不會盯著它。箭頭是**打斷式**的 |

**兩者都可點，點了就把視野移過去**（一鍵跳到問題點）。這同時是手機移植的預留（P3：操作不得只靠 hover／右鍵／鍵盤——大圖的平移目前只有中鍵，R-16；小地圖與箭頭是**純左鍵**的替代動線）。

**「問題」的定義不新發明**，就是既有的兩件事：
- **瓶頸** ＝ 節點三態徽章的 `缺料`／`滿溢`（`s.rates["node_state"]`）
- **受損** ＝ 正在被 walk-by 啃的格（`Tide.BLAST` 的同一份判定）

**一屏可見的地圖不顯示這兩個東西。** 戰役關卡全部一屏可見，問題節點就在畫面上——多一個浮層只是多一塊遮擋（§6.2「浮層收合後地圖 100% 可見」的同一條紀律）。

#### 個人最佳

記錄**撐過的波次數**與**產能積分**（§7.6 已定義，不另發明分數）。存 `SaveService`，以最高波次為主排序。**無盡是自由配置**（§3.10）——它用玩家的完整戰力，不是統一配置。

---

### 7.11 每日挑戰雙榜（★ B2.2 定案）

**這一批不新增任何玩法。** 每日挑戰＝**無盡模式跑在一個固定的日種子上**，跑兩次、兩種起始配置。地圖、波次、模擬、計分全部沿用 §7.10 那一套（§3.10 明文：「不得為此拆成兩套邏輯」）。

| 參數 | 值 | 理由 |
|---|---|---|
| 日種子 | **`YYYYMMDD`**（例：`20260804`） | **刻意不做雜湊混合**。① 除錯與回報時看得懂——種子會出現在截圖與錯誤回報裡；② 混合買不到東西，`Rng.stream()` 底下是 PCG32，相鄰種子本來就長出完全不同的序列（`daily_test` 直接斷言「連續 14 天的圖互不重複」）。<br>⚠ **不得改用 `String.hash()`**：那是引擎實作細節，換一版 Godot 就可能換一組數字，而「同一天全球同一張圖」是跨版本的承諾 |
| 日期鍵 | **`YYYY-MM-DD`** | 字典序＝時間序，歷史清單直接排 |
| 地圖 | `MapGen.generate(日種子)`，即 §7.10 的佈局 1 | 同一張圖跑兩榜 |
| 波次 | §7.10 的無限波次公式 | 同上 |
| 統一配置榜的起始科技 | **空陣列＝完全沒有科技** | `SessionState.setup()` 的第三個參數是**所有局外成長進入這一局的唯一入口**，堵住它就堵住全部（憲法 B3） |
| 統一配置榜的可建造 | ✅ **`Daily.UNIFORM_BUILD`：明列的十種**（B2.4 還清） | 五種生產節點 ＋ 五隻確定性角色。**招募池的三隻不在上面**（憲法 B3：抽到什麼都不改變這張榜），而且日後加角色**不動這一行**——要動它是一個賽季級的決定，該有版本註記，不是加一筆資料的副作用。`roster_test._uniform_board_is_pinned()` 守著（紅得起來）。詳見 §7.13 |
| 自由配置榜 | 玩家的完整科技 | §3.10 |
| 成績 | **波次 ＋ 產能積分，兩欄各自取最大值** | 與無盡同一條理由（§7.10）：撐得久與產線好是兩件事，綁成一組會讓一局「波次 +1、積分砍半」洗掉玩家真正的最佳產線紀錄 |
| 歷史保留 | **30 筆**（兩榜 × 15 天） | 再多也沒有人往下捲，而存檔的空間要留給玩家的進度 |

**跨日的處理在 `SaveService.apply_daily()`，不在畫面層**：畫面層有兩個入口（兩張榜）而換日只該發生一次，放在那裡就會出現「先點哪一榜決定昨天的紀錄有沒有被存進歷史」。
**跨日時今天的兩榜一起歸零**——只歸零玩的那一榜的話，昨天的另一榜會混進今天，而它是在另一張圖上打出來的。

**離線註記是規格的一部分**（§3.10）：畫面上必須寫出「兩張榜都是你自己的本機紀錄，還沒有對手」。`TL_CLICKTEST/daily` 的 `honest` 斷言守著它——不要在文案中宣稱一個尚未存在的競技場。

### 7.12 藍圖庫（★ B2.3 定案）

| 參數 | 值 | 理由 |
|---|---|---|
| 免費槽數 | **2** | 槽數 0 等於「這個功能要先買才存在」，而藍圖庫要消除的重複勞動（§3.7）**第一局就開始了** |
| 科技加成 | 後勤「藍圖槽 I／II」各 **+1**（§7.8） | 上限 4 |
| 座標格式 | **`[dx, dy]` 整數陣列**，原點是框選矩形左上角 | 藍圖要進 `user://save.json`，而 `Vector2i` 過不了 JSON（會變成讀不回來的字串）。存成 JSON 原生型別 → **存檔往返不需要任何序列化程式碼**，少一層轉換就少一個「存進去和讀出來不一樣」的機會 |
| 展開成本 | 節點造價 ＋ 導管造價的總和，**與放在哪裡無關** | 導管造價只看兩端的相對位移（Chebyshev × 每格單價），而位移是藍圖存下來的東西 |
| 展開語意 | **全有全無** | 藍圖是一個單位。半套展開＝花掉資源換到一個接不起來的殘骸，而拆掉只退 75%——那是玩家沒有辦法補救的損失。所以整份先驗過，一格不合就一格都不放 |
| 缺口提示 | **具體數字**（「礦砂差 25」）＋ **具體格子**（「有 2 格蓋不下（(21,9)…）」） | §3.7「資源不足則顯示缺口」。先講資源、再講格子：資源不足是等一下就能解決的，位置不對要他移動滑鼠——兩件事的下一步不一樣 |
| 落點預覽 | **逐格上色**（綠＝放得下、橘＝這一格擋住了） | 整份一個顏色只能說「不行」，指不出是哪一格，而那正是玩家的下一步要知道的事 |

**兩條收錄規則**（框選時）：
- **核心不收**——它蓋不出來，收進去只會讓每次展開都固定失敗一格。
- **導管要兩端都在框內才收**——只有一端在框內的，展開時另一端接不到任何節點。那不是藍圖的一部分，是它和外界的接線，而接線是玩家在新地圖上要自己決定的事。

**節點與導管排序後才存**，所以同一份佈局不論當初蓋的順序如何，框出來的藍圖逐欄相同。

**槽數吃的是玩家的科技，不是這一局的 `s.mods`**：藍圖庫是跨局資產，而每日挑戰的統一配置榜會把 `s.mods` 壓成中性值（憲法 B3）——用 `s.mods` 的話，打統一配置榜時玩家的槽會憑空少兩個。

### 7.13 名冊與招募（★ B2.4 定案）

**這一批不新增任何戰鬥規則。** 三隻招募角色全部只用既有的欄位（`range`／`rof`／`dmg`／`slow`），`sim/Combat.gd` 一行都沒改。理由是 B6：**招募不該是新機制的載體**——「抽到才玩得到的機制」與「所有東西都能純靠遊玩取得」直接衝突。

#### 招募池的三隻（§5 的 M2 目標「含 3 抽卡」）

| 角色 | 造價 | 生命 | **交戰耗能** | 射程 | 射速 | 傷害 | 特殊 | 它解什麼 |
|---|---|---|---|---|---|---|---|---|
| **長哨 Longcall** | 180 礦砂 | 60 | **8 /秒** | **12 格** | 0.35 /秒 | 52 物理 | — | **覆蓋範圍**。無盡大圖上一座蓋掉一個象限，也是唯一夠得到漏網之魚的塔 |
| **霜礁 Frostreef** | 200 礦砂 ＋ **40 合金** | 60 | **16 /秒** | 3 格 | — | 0 | 減速 **−65%**，無破甲 | **隘口**。潮鳴的極端版：減速更兇、範圍剩一半、電費 1.8 倍 |
| **定潮 Ballast** | 240 礦砂 ＋ **100 合金** | **90** | **3 /秒** | 4 格 | 1.0 /秒 | 24 物理 | — | **電網已經拉到極限之後還能加的火力**：唯一一隻用礦砂與合金買電費折扣的塔 |

**每瓦 dps 的位置**（校準基準，§7.4 的同一把尺）：

| 角色 | 每瓦 dps | 對照 |
|---|---|---|
| 定潮 | **8.0** | 最高。代價是造價 4.8 倍 ＋ 100 合金（＝一整條熔爐產線：8 礦砂/秒 ＋ 待機 10 能量/秒） |
| 錨 | 5.4 | 基準 |
| 長哨 | **2.28** | 買的是射程 12，不是輸出 |
| 稜鏡 | 0.75（單體） | 買的是貫穿 |

> **定潮為什麼不是「錨但更強」**：它把取捨從**峰值電力**挪到**經濟**，而不是取消取捨。合金要熔爐，熔爐要礦砂又要待機電力，那條產線本身就在跟「多蓋一座採集器」競爭。它是給「電網已經滿了、礦砂還有剩」的局面用的——那個局面在戰役五關不存在（所以它不進戰役），在無盡的後段才會出現。

> **霜礁與潮鳴會疊嗎**：`Combat.auras()` 對減速與破甲**各自取最大**，所以霜礁（減速 0.65）＋潮鳴（破甲 0.25）是一個真的組合技。這不違反「多座潮鳴不疊加」（§7.4）——不疊加的是同一種效果，而兩座都要吃電，峰值電力仍然是唯一的約束。

#### 擁有與解鎖

| 項目 | 定案 | 理由 |
|---|---|---|
| **擁有清單怎麼存** | **不存。推導。** ＝「戰役開到第幾關」＋「招募過哪幾隻」 | 平行存一份 `owned` 會有兩個事實講同一件事，而它們遲早會漂——漂掉的那天玩家看到「第 3 關通了但潮鳴還鎖著」，兩邊各自都「對」。這是 sv2 砍掉 `campaign.cleared` 的同一條理由 |
| **確定性解鎖途徑** | **就是戰役的解鎖階梯本身**（§7.9） | 開得到那一關，就等於擁有那一關給你的那幾顆鈕。不另寫一張「哪一關送哪一隻」的表——那是 `data/Campaign.gd` 的第二份副本 |
| 五隻確定性角色 | 錨（開局）／稜鏡（通過第 1 關）／潮鳴・回收者（第 2 關）／碎浪（第 4 關） | 同上，直接讀 `L1..L5_BUILD` |
| **聲望券的數量** | **也是推導的**：賺到的 − 已招募數 | 券只有一種用途，而招募結果本來就要存。存一份計數器會漂成「我有 2 張券但只招募過 1 次」 |
| 券的兩條途徑 | 戰役**每 `(關卡數×3)÷招募池大小` 顆星 1 張**（★ B3.3 起推導，八關時 ＝ 每 8 顆星）；無盡**每 10 波 1 張** | 都是里程碑不是掉落——玩家看得到「再 2 顆星就有券」。**匯率推導而不寫死**：「戰役滿星 ＝ 剛好畢業」這個對齊要在關卡數與招募池都會長的情況下仍然成立，而寫死的 5 只在五關的時候對——第二幕一加到八關，24÷5 ＝ 4 券就多出一張永遠用不掉的 |
| ★★★★ **B6 的證明** | **戰役滿星 15 顆 ÷ 5 ＝ 3 張 ＝ 剛好抽乾招募池** | §3.9「6 隻稀有角色全部都能純靠遊玩取得」在這裡是一條斷言：`roster_test._free_to_play_can_graduate()` 用一份**只推戰役、從未碰過無盡、一毛錢都沒花**的存檔把名冊收滿 |
| 抽卡 | 從「尚未擁有的稀有角色」裡等機率取一個 | §3.9：**隨機的是順序，不是有沒有**。所以資料表沒有權重欄位，也不該有 |
| 畢業 | 抽完不再消耗券；UI 常駐顯示「還剩 N 隻」 | 「還剩 2 隻」是一個看得到終點的進度條；「稀有度 3%」是一個沒有終點的賭局 |
| 已由別的途徑取得的稀有角色 | **`_pool_owned()` 問的是 `owned()` 不是 `recruited`** | 日後成就／商店送出一隻時（§3.9），「還剩幾隻」要當場少一格，而抽卡不能把它再抽一次 |
| 付費買券（§3.12） | **M4**，會加 `roster.bought` 一個鍵 | 那時 `tokens()` 加一項，推導的部分不變 |

#### 名冊管得到哪幾個模式

| 模式 | 建造欄來自 | 為什麼 |
|---|---|---|
| **戰役** | **關卡自己宣告**（§7.9），**不吃名冊** | 那是難度階梯與教學順序。用名冊去交集它會讓「通關第 4 關卻蓋不出碎浪」變成可能，而那一關的參考解需要它 |
| **無盡・每日自由配置榜** | **玩家的名冊** | §1 的服務循環圖：「名冊 → 下局玩法組合改變」。招募到的東西如果哪裡都用不到，名冊就只是一本圖鑑 |
| **★ 每日統一配置榜** | **`Daily.UNIFORM_BUILD`：一份明列的十種** | §7.11 記的債（B2.2 留下的「空陣列＝全部」）。**憲法 B3**：抽到什麼都不會改變這張榜；而且日後每加一隻角色都不會悄悄改變它，昨天的成績跟今天的仍然可比 |
| 測試圖・沙盤 | 全表（不吃名冊） | 開發用的圖，要驗的是節點本身 |

#### ★ 優先權面板：一列＝一個「角色的角色」，不是一個資料表的鍵

§3.1 鎖死「5–8 條滑桿恆在同一位置」，理由是 **R-1：不可暫停的戰術動作必須是一個手勢，操作負擔不得隨建築數量成長**。一隻角色一條滑桿的話，M3 的 24 隻角色就是 **28 條滑桿**——那個面板已經不是一個手勢，是一份表單。

所以 B2.4 起：**新角色併進既有的那一列**（`NodeDefs.PRIORITY_GROUP`），滑桿永遠是 `PRIORITY_ROWS` 那九條的子集。

| 併入的列 | 成員 | 這一列的意思 |
|---|---|---|
| 錨 | 錨・**長哨・定潮** | 單體物理 |
| 潮鳴 | 潮鳴・**霜礁** | 光環 |

**只影響 UI 與預設值**：`FlowNetwork` 仍然逐 type 讀 `priorities[type]`，模擬層不知道有合併這回事。

### 7.14 潮汐公司（★ B2.5 定案）

> §3.8 的實作規格。**這一節做的每一個決定都在回答同一個問題：怎麼讓它不要長成
> 第二個遊戲**（`00_CONCEPT.md` §三 約束 2、風險 R-2）。

#### 三項與 §3.8 原文不同的地方（先講，理由在下面）

| §3.8 原文 | B2.5 實作 | 為什麼 |
|---|---|---|
| 「排產線（**複用流量網路引擎**）」 | **產線位指派**：把 N 個產線位指給已接的訂單 | 空間式產線編輯器＝第二個 `Battle` 畫面，而且它**重複塔防的核心動詞**（拉線佈局）。§3.8 自己說「刻意保持單薄」、留存角色是「一兩分鐘就走」——一個五分鐘的佈局編輯器直接違反那句話。指派仍然是**真的取捨**（產線位稀缺、訂單報酬不同），而且和優先權滑桿是同一類決定，主題上對得起來 |
| 訂單有「**交期**」 | **沒有交期** | 約束 1 明文：「tycoon **沒有失敗狀態**、沒有時間壓力」。交期就是時間壓力，而錯過交期就是失敗狀態。兩者不能並存，**約束是上位的** |
| 「倉儲上限可升級」 | **一個產線位一次只做一張訂單，做完就停** | 「倉儲上限」不另外開一個數字：**產線位數本身就是倉儲上限**。一個機制答兩題，而且離線收益天然被夾住——離線 30 天的上限就是「每個產線位各完成一張」（RG-10） |

#### 數值

| 項目 | 值 | 依據 |
|---|---|---|
| 廠等 L | **1–8**（M2；M3 擴到 25） | §5 內容矩陣 |
| 產線位 | `slots(L) = 1 + floor((L−1) / 3)` → **1,1,1,2,2,2,3,3** | ★ 見下方「一個看起來無害的乘數推翻了收斂」 |
| 擴廠成本 | `cost(L) = 500 × 1.35^L` | §4.2 原文 |
| 訂單報酬 | `reward(t) = 120 × t^1.25` 資金 | §4.2 原文，`t` ＝ 訂單階 |
| 訂單工作量 | `work(t) = 60 × t` 秒 | 第 1 階一分鐘、第 8 階八分鐘。**掛機一晚必定全滿**，所以真正的限制是產線位不是時間 |
| 訂單品項 | **8 種**（階 1–8，各由廠等解鎖） | §5 內容矩陣「M2：8」 |
| 升級材料 | `component(t) = t` 個 | 等級軸的貨幣（B2 ②）。**兌換介面在 B2.7**，這一批只累積與顯示 |
| 招募券 | 第 4 階起每張訂單 **1 張** | 名冊的第三條路（§7.13 的兩條是戰役星數與無盡波次） |
| 生產速率 | **1 工作單位／秒／產線位** | 唯一的速率常數。「賣速度」（B6）日後就是加它 |

#### ★ 一個看起來無害的乘數推翻了收斂（B2.5 落地時量到）

§4.2 給的兩條公式沒問題：**成本 `500 × 1.35^L` 是指數的、報酬 `120 × t^1.25` 是
線性偏下的**，直覺上必然收斂。出問題的是**這一節自己補的第三個參數：產線位**。

產線位是收入的**乘數**，所以它決定收入曲線的階數：

```
收入率 ≈ slots(L) × reward(L) ÷ work(L) = slots(L) × 2 × L^0.25
```

第一版寫 `slots = 1 + floor(L/2)`（每兩級加一個）→ `slots ≈ L/2` → 收入 ≈ `L^1.25`。
在 M2 的八級範圍內，**指數還來不及贏過那個乘數**：

| | 存到下一次擴廠要多久 |
|---|---|
| 每兩級加一個產線位 | 廠等 1 → **338 秒**；廠等 7 → **314 秒**（❌ 越擴越快） |
| 每三級加一個產線位 | 廠等 1 → **338 秒**；廠等 7 → **419 秒**（✅ 收斂） |

**教訓**：驗收斂不能驗公式長什麼樣，要驗**玩家感覺得到的量**——「我還要存多久」。
`tycoon_test._shape_and_curves()` 的第一版比的是「成本倍率 vs 報酬倍率」，
那兩個不可比（一個是每級、一個是每張訂單，中間差了產線位與工作量），
量出來 8.2× vs 13.5×，看起來像會通膨，其實兩個數字都不是要問的東西。

#### 離線結算

```
elapsed = max(0, now - last_seen)        ← 時鐘倒退不獎不罰（只更新 last_seen）
每個「已指派且未完成」的產線位：done += elapsed × RATE，夾在 work 上限
```

**沒有補償、沒有離線加成、沒有上限時數**——上限是結構性的（做完就停），不是一個
需要調的數字。

#### 兩個畫面（硬指標，每批 QA 必查）

| # | 畫面 | 內容 |
|---|---|---|
| ① | **訂單板** | 資金／材料／券 ＋ 可接訂單清單 ＋ **收成**（完成的訂單）＋ **擴廠**一顆鈕 |
| ② | **產線編輯** | 產線位 × 已接訂單的指派 |

**擴廠是訂單板上的一顆鈕，不是第三個畫面。** 研發（新品項）不做——訂單由廠等
解鎖，一個機制答兩題。

#### 鎖住 B6 的斷言

`tycoon_test._campaign_needs_no_tycoon()`：**以一份 `tycoon` 全預設（從沒開過）的
存檔跑完戰役五關的參考解**。塔防不依賴 tycoon 是憲法，不是善意。

---

### 7.15 進度整合：等級軸、成就、公司等級（★ B2.7 定案）

> §1 B2 ②、§4.1「升級材料必須有三條路」、§3.9 的成就在這裡一次落地。
> B2.5 交出來的升級材料到這一批才有花的地方。

#### 等級軸（§1 B2 ②）

兩軸各 10 級，**每級 +8%，加法**，滿級 **×1.80**：

| 軸 | 作用 | 局內的樣子 |
|---|---|---|
| **塔** `tower` | `damage_mult` 再乘 `1 + 0.08L` | 所有塔的傷害 |
| **生產** `line` | `produce_mult ＝ 1 + 0.08L`，乘在 `ore_out`／`power_out`／`alloy_out` | 所有生產節點的產出 |

**兩軸各自 +80%，不是加起來 +80%**（B2 原文「塔與生產節點各 10 級」）。
科技軸的 +35% 上限與這裡是**兩把各自獨立的尺**——`Tech.combat_gain()` 只量科技，
`Levels.gain()` 只量等級軸，兩支測試各自斷言自己那一條。混成一個數字的話，
日後任一邊調數值都會讓另一邊的上限無聲地位移。

| 買第 n 級 | 材料 |
|---|---|
| n | **12 × n** |
| 單軸 1→10 合計 | **660** |
| **兩軸滿級合計** | **1320** |

#### 升級材料的三條路（§4.1 的落地）

| # | 來源 | 數量 |
|---|---|---|
| ① | **塔防局末結算** | **撐過的波數**，戰役與無盡（含每日）都算 |
| ② | 潮汐公司訂單 | `component_of(tier) = tier`（B2.5 已有） |
| ③ | **成就** | 一次性，見下表；不碰 tycoon 也拿得到 **860** |

★ ① **只數波數，不加星數加成**。加了的話，「重刷第 1 關（3 波 3 星）」會比
「打完第 5 關（5 波）」划算——一條讓最短的局付最多錢的曲線是退化的。
波數是唯一同時對戰役與無盡成立、而且**撐得久必定拿得多**的量。

★ 難度層的加成（§3.11）排 B2.6。這一批的公式**刻意不留難度項**——
留一個恆為 1 的乘數，等於在 B2.6 之前就宣告了它的形狀。

#### 成就（20 條，§3.9）

**成就是推導的，不是存出來的**：每一條都是「存檔上某個量 ≥ 門檻」，
所以沒有 `claimed` 清單、不可能重複發放、也不可能漏發（同 §7.13 的券）。

| # | 成就 | 條件 | 材料 | 券 |
|---|---|---|---|---|
| 1 | 初次通關 | 通關 1 關 | 20 | |
| 2 | 五關全通 | 通關 5 關 | 60 | |
| 3 | 滿星 | 15 ★ | 100 | |
| 4 | 無盡十波 | 最高 10 波 | 20 | |
| 5 | 無盡廿五波 | 最高 25 波 | 60 | |
| 6 | 無盡五十波 | 最高 50 波 | 150 | |
| 7 | 產線成形 | 最佳產能 20 | 30 | |
| 8 | 產線精練 | 最佳產能 50 | 80 | |
| 9 | 第一項科技 | 解鎖 1 個 | 15 | |
| 10 | 半棵樹 | 解鎖 6 個 | 50 | |
| 11 | 整棵樹 | 解鎖 12 個 | 120 | 1 |
| 12 | 廠等三 | 廠等 3 | 30 | |
| 13 | 廠等六 | 廠等 6 | 80 | |
| 14 | 大宗交貨 | tycoon 累計 3 券 | 40 | |
| 15 | 第一次招募 | 招募 1 隻 | 20 | |
| 16 | 名冊全滿 | 招募池全到手 | 100 | |
| 17 | 第一張藍圖 | 存下 1 張 | 15 | |
| 18 | 出海一日 | 打過 1 次每日 | 20 | |
| 19 | 塔軸滿級 | 塔 10 級 | 150 | 1 |
| 20 | 生產軸滿級 | 生產 10 級 | 150 | 1 |
| | **合計** | | **1310** | **3** |

第 19／20 條的 300 材料是**滿級之後才拿得到的**——它是收尾的獎狀，不是預算的一部分。
**滿級前拿得到的是 1010**，其中 **150 綁在 tycoon（12／13／14）**，
所以不碰 tycoon 拿得到 **860**。

##### ★ 券只發給「沒有別的來源在數同一件事」的成就

B2.7 第一版在「五關全通」「滿星」「無盡五十波」上各發一張券，`roster_test`
當場變紅：**§7.13 那條刻意的對齊（戰役滿星 15 ★ ÷ 5 ＝ 3 券 ＝ 剛好抽乾招募池，
也就是 B6 的證明）變成 5 券**。`Roster.earned()` 已經在把星數與波數換成券了，
成就對著同一個量再發一次就是**同一份進度領兩次**。

「名冊全滿」也不發券——那是招募池已經空了才拿得到的東西，一張用不掉的券。
於是只剩三條（整棵樹、兩軸滿級），它們量的都是別處沒在數的進度。
`progress_test` 用一條**通則**看著它（發券的成就不得使用 `earned()` 已在數的
metric），不是釘住那四個 id：第 21 條成就加進來時它會自己攔。

#### 鎖住 B6 的斷言

`progress_test._free_to_play_can_max_levels()`：以一份 **`tycoon` 全預設**的存檔
只靠「成就 ＋ 局末波數」湊滿 1320。**實跑印出來的是 860 ＋ 460 波**
（≈ 10 局打到無盡 50 波，或 21 輪戰役全清）——**慢是可以的，不通就是違憲**
（B6：所有進度都能純靠遊玩取得）。配一條反向對照：零進度的存檔**一級都升不了**，
否則上面那條可能只是因為到處白送。

#### 統一配置榜吃不到等級軸（憲法 B3）

等級軸是**可以課金加速的那一軸**，所以它比科技軸更該被 `Daily.meta_for()`
這道閘堵住。`SessionState.setup()` 的 `meta` 參數（`sim/Loadout.gd`）是「這一局
所有局外成長的唯一入口」——`progress_test` 拿滿級滿科技與零進度兩份存檔在統一榜上
開局，斷言 `mods` 一模一樣，並**逐一列舉 `Loadout.KEYS`**，所以新增一軸不會漏。
（B2.7.1 改寫：原本是一軸一道閘，而靠列舉守住的憲法漏掉一道不會有任何東西報錯。）

#### 公司等級（共享進度條）

**一個推導出來的數字，沒有自己的欄位、也不發任何獎勵。** 它存在的唯一理由是
「兩層的進度看得到彙總」；一旦掛上獎勵，它就變成第四套經濟。

```
點數 = ★×10 + 無盡最高波×2 + 已解鎖科技×8 + (廠等−1)×12 + 成就數×15 + 等級軸總級數×6
公司等級 = 1 + 點數 / 120（向下取整）
```

塔防那幾項與 tycoon 那一項都在式子裡 → **兩層都推得動它**（B2.7 的完工定義）。

---

### 7.16 難度層（★ B2.6 定案）

**只掛在無盡模式上。** 戰役是難度階梯與教學順序（§7.9），每日兩榜要可比（§3.10）
——兩者恆為第 0 層。M2 交前 3 層，§5 內容矩陣的 M3 目標是 12 層。

#### 三條規則、四層

| 層 | 名稱 | 敵人血量 | 敵人破壞建築 | 塔交戰耗能 | 疊了幾條 |
|---|---|---|---|---|---|
| 0 | 標準 | ×1.00 | ×1.00 | ×1.00 | 0 |
| 1 | 漲潮 | **×1.25** | ×1.00 | ×1.00 | 1 |
| 2 | 暴潮 | ×1.50 | **×1.50** | ×1.00 | 2 |
| 3 | 深潮 | ×1.80 | ×1.50 | **×1.20** | 3 |

**每層只多疊一條新規則**（粗體那一格）。血量是校準軸、每層都動；另外兩條各自
在第 2、第 3 層才登場。理由與 §7.10 不給第三條波次曲線相同：一層同時多三條的話，
玩家唯一的心得會是「這層難」，而難度層的全部價值是說得出**難在哪**。

三條打的是三件不同的事——塔夠不夠（血量）、線撐不撐得住（破壞）、電夠不夠（耗能）。

> **準備期不在這裡**：§7.1 的 `PREP_TIME_HARD`（30 秒）明文屬於難度層 4+。

#### ★ 第 3 層的 ×1.80 就是等級軸滿級

憲法 B2 ② 的硬性配套：「等級軸的增幅必須被難度層同步吃掉——難度層 N 的敵人強度
縮放係數以『等級軸滿級』為基準校準」。等級軸滿級是傷害 ×1.80（§7.15），
所以第 3 層的敵人血量就是 ×1.80。**這不是一個調出來好看的數字，是那一軸的鏡像**，
`difficulty_test._calibrated_to_the_level_axis()` 逐位元比對這兩個數——
任一邊被調動，另一邊當場變紅。

實測（第 5 關參考解，同一份佈局跑三次）：

| 配置 | 結果 | 核心 | 建築失血 |
|---|---|---|---|
| 第 0 層／零進度 | 通關 | 956/1000 | 44 |
| 第 3 層／等級軸滿級 | 通關 | 877/1000 | 123 |
| 第 3 層／零進度 | **核心已毀**（第 1940 tick） | −1/1000 | 1001 |

**這三行就是 B2 ② 那句話的證據**：沒有等級軸過不了第 3 層，有了也只是過得驚險
（建築失血仍是第 0 層的 2.8 倍）。買到的是「打得動第 3 層」，不是「碾平第 0 層」。

#### 解鎖階梯（推導，不存）

| 層 | 條件 |
|---|---|
| 0 | 永遠開著 |
| 1 | **戰役全通**（每一關至少 1 星） |
| N | 在第 N−1 層撐過 **10 波** |

10 波是「這一層我站穩了」而不是「我試過一次」——無盡的血量曲線 `1.11^(w-1)`
在第 10 波是 ×2.56，那時能不能站住已經是產線的問題。

**階梯跳不過去**：第 2 層的紀錄開不了第 3 層。而「解鎖到第幾層」沒有存檔欄位，
它由戰役星數與各層紀錄推導（sv2 砍 `campaign.cleared`、B2.4 不存 `owned`、
B2.5 不存產線佔用、B2.7 不存材料餘額的同一條）。

#### 紀錄逐層各一筆（sv3）

`endless.best` 的鍵是難度層，值是 `{wave, output}`（兩欄仍各自取最大值，§7.10）。

同一個「12 波」在標準層與深潮層不是同一件事，共用一格會讓其中一個永遠洗不掉
另一個。**也不用分數係數把高層的成績折算成低層的**——那是一個沒有人能驗證的匯率。

舊存檔（sv2）的那一筆一律搬進第 0 層：它是在還沒有難度層的世界裡打出來的，
而第 0 層就是那個世界的規則。

**★ 局外的里程碑取「不分層的最好那一筆」**（`Difficulty.best_any()`，B3.3 補）。
聲望券與五條無盡成就問的是「你在無盡撐過幾波」，而那個問題不該逼玩家回第 0 層
再刷一次——第 3 層的 20 波嚴格難於第 0 層的 20 波。**逐層的紀錄只給局末面板用**
（它要說得出「這一層的紀錄」）。

> ⚠ **這一段是補一個活的缺陷**（B3.3、RG-167）。sv3 改結構時，`Roster.earned()`、
> `Achievements.progress()` 與名冊畫面**三處都還在讀 `endless.best_wave`**。
> 於是自 B2.6 起：無盡一張券都沒發過、五條無盡成就永遠達成不了、名冊上那句
> 「還差 N 波」恆為滿值。三處的測試全綠，因為**測試自己在存檔裡塞那個舊鍵**。
> 教訓寫進 `50_QA_PLAN.md`：**斷言要走玩家走的那條寫入路徑**（`apply_endless()`），
> 自己捏出來的存檔形狀驗不到「形狀改了」。

#### 統一配置榜恆為第 0 層（憲法 B3）

難度層是 `Loadout` 的第三軸，也就是第三個有機會洩進統一榜的東西。它進了
`Loadout.KEYS`，所以 B2.7.1 那道**單一**閘（`Daily.meta_for()`）自動涵蓋它，
而 `progress_test` 逐鍵列舉的那條斷言不必改一個字就守住了它。

---

## 8. 變更紀錄

| 日期 | 版本 | 變更 |
|---|---|---|
| 2026-07-27 | v0.1 | 前期製作草案建立。依 `00_CONCEPT.md` 拍板結論撰寫。 |
| 2026-07-27 | v0.3 | **grilling 逐條拷問後的 15 條定案**（開工前最後一次設計收斂）：<br>① §1 B3 措辭誠實化——離線版兩榜沒有對手，「對 P2W 的制衡」是接線上榜後才兌現的承諾。<br>② §3.1 **優先權改為依「節點類型」**（5–8 條滑桿恆在同位置）；儲槽在面板佔一格。<br>③ §3.1 **能量是流率不是水池**——無全域能量池，唯一儲存體是儲槽節點，充放電受自己那條導管 cap 約束。<br>④ §3.2 新增**跨越點（橋）**：路徑上唯一可鋪導管處，節點仍不可；**橋是架高的 → 橋上導管不受攻擊**。<br>⑤ §3.3 定義**礦砂↔能量匯率 1:5**（沿用發電機轉換比），回收者回收算式因此落地。<br>⑥ §3.5 敵人改為 **walk-by：永不停步、每 tick 傷害相鄰 1 格、只有核心讓它們駐足**——堵死「中繼當肉盾」的迷宮塔防後門。<br>⑦ §4.1 **升級材料補上「塔防局末結算」這條路**（原本只有 tycoon，與「塔防不依賴 tycoon」矛盾）；能量去向改為「交戰耗能」。<br>⑧ §6.2 HUD 改為**全畫面地圖＋浮層／抽屜**；**戰役地圖硬性一屏可見**，無盡才可滑（小地圖與導引推 B2.1）；頂欄能量改顯供給／需求。<br>⑨ §7.2 明文化**基礎 cap 10 < 發電機輸出 20 是設計**：第一台發電機就教你讀瓶頸，加粗幹線是一個取捨。<br>⑩ §7.3 新增地圖元素表（含跨越點）。<br>⑪ §7.4 補回收者回收數值表與校準基準；峰值表補「幹線需求」欄與前提註記。<br>⑫ **§7.7 新手係數整節刪除**，改為「戰役第 1–2 關以新手可通關為硬性設計要求，只用關卡參數達成」——隱藏乘數在一款教你算電力的遊戲裡是有害的。<br>⑬ §7.8 能量效率科技改指**交戰耗能** −8%/級（原本指著已歸零的待機耗能）。<br>⑭ §3.6 防務支敘述同步為「交戰耗能效率」。 |
| 2026-07-27 | v0.2 | **使用者修訂五項**：<br>① **耗能模型改為「交戰時每秒耗電、待機 0」** → 約束從平均電力變成峰值電力，儲槽升格為核心策略建築（§3.3、§7.4）。<br>② **新增全域擊殺回收 25% 礦砂**；回收者因此重新定位為**能量回收塔**（射程內任何死亡回收 60% 能量），§3.3。<br>③ **tycoon 降級為塔防的附屬補給站**，不再是可獨立遊玩的第二層；產出改為含數值的升級材料（§3.8、§1 B2）。<br>④ **商業模式改為免費遊玩＋內購（含 P2W）**，新增 §3.12 變現系統與憲法 B6；Windows 版無廣告，廣告保留至手機移植版。<br>⑤ **每日挑戰改為雙榜**（統一配置榜＋自由配置榜），B3 改寫（§3.10）。<br>另補：§3.1 明確化「供不應求按比例降速」與「網路永不死鎖」不變量。 |
| 2026-08-02 | v0.4 | **§7.10 無盡模式定案**（B2.1a）：<br>① **生成器輸出的 dict 與手作地圖同構**——渲染／建造／命中判定全部零改動，生成器只是換一個地方生產同一種資料。<br>② 佈局 1「潮汐迴廊」參數表（34×18 一屏、3–5 折段、2–3 座橋、6–8 礦點、起始礦砂 900、準備期 45s）。<br>③ **五條生成不變量**，第 5 條（至少 3 個礦點不必過橋）是「生成圖必然可通關」的實質保證——手作圖靠設計者的眼睛，生成器只能靠斷言。<br>④ 無限波次沿用 §7.6 兩條公式，**出場間隔固定 1.0 秒**：不給第三條曲線，否則玩家分不出「這波難是因為血厚還是因為密」。<br>⑤ 個人最佳記波次與產能積分，**兩欄各自取最大值**。<br>⑥ §6.1 主選單補上「無盡」入口（六顆）。 |
