# 《潮與線》QA 計畫

> 測試策略、bug 登記、回歸清單。
> **QA 帽的立場：你的工作是證明遊戲有問題，不是證明它沒問題。**
> 狀態：**v0.3**（新增 §8 遊玩測試腳本、RG-142）　|　最後更新：2026-08-06

---

## 0. 鐵則：證據或閉嘴

任何「完成／能動／修好了」的聲明**必附證據**：測試輸出、截圖路徑、實跑紀錄。
「應該可以」「理論上沒問題」不是證據。回報時必須引用證據的具體位置（哪個測試、哪張圖、哪行輸出）。

---

## 0.1 誰判什麼（★ 2026-07-27 定案，全案分工）

| 判官 | 負責 | **明確不負責** |
|---|---|---|
| **agent（Opus 5）** | **程式碼完整性與可讀性**：L1–L3、五支測試、`TL_NAKED` 的視覺編碼檢定、首次體驗的**資訊完備性**（該給的資訊有沒有給、術語會不會勸退、失敗後知不知道原因） | **好不好玩。** agent 不會想再玩一次，也不會困惑——它讀得懂就是讀得懂 |
| **使用者本人** | **樂趣判定**：G1 的樂趣假設、手感、節奏、「還想不想再來一次」 | **首次體驗的真實感受。** 設計者永遠不可能有第一次 |
| **（目前缺席）真人陌生人** | M1 閘門的「無指導 10 分鐘」、真正的可讀性盲測 | — |

**兩條由此而來的硬性規則**：

1. **可讀性驗收一律用 `TL_NAKED=1`。** 拿有數值標籤的截圖問 agent「瓶頸在哪」，它讀數字就答得出來——那是**假綠燈**，驗到的是「我看得懂文字」，不是 R-3 要的「線寬與顏色在傳達資訊」。
2. **凡是判準寫「陌生人」「未受訓玩家」的項目，在拉到真人之前不得標記為通過。** 目前受影響：G2 前的 M1 閘門、§4.4 首次體驗的主觀部分。登記為 **R-14**（`40_PRODUCTION_PLAN.md` §3）。

---

## 1. 自檢五層梯

**每批工作至少跑 L1–L3；里程碑閘門跑全五層。**

| 層 | 檢什麼 | 本專案怎麼跑 |
|---|---|---|
| **L1 編譯／靜態** | 跑得起來、無腳本錯誤 | `<godot> --headless --path godot --import` 後啟動一次，開機 log 必須乾淨（零 ERROR、零 WARNING） |
| **L2 邏輯測試** | 數值、演算法、存檔遷移、**確定性** | `<godot> --headless --path godot --script res://tests/<name>.gd`，五支全跑＝回歸 |
| **L3 視覺驗證** | 版面、裁切、字型、動效、可讀性 | `TL_SHOT=<png> TL_PANEL=<畫面> TL_SEED=<n>` 產圖，**親眼讀圖**逐項核對。**可讀性項目另加跑一張 `TL_NAKED=1`**（見 §0.1） |
| **L4 冒煙遊玩** | 完整流程：新開局 → 核心循環 → 存檔 → 讀檔 | `TL_SIM=<ticks>` 腳本化跑局；或靜音實玩。**前 60 秒的首次體驗是重點** |
| **L5 找碴審查** | 邊界與惡意 | 見 §4 找碴清單 |

**L3 的常見自欺**：截圖產出來了但沒真的看內容。
**每張驗證截圖都要回答：這張圖證明了什麼？**（元素齊？字沒變豆腐？沒裁切？狀態對？線寬有隨流量變化？）

---

## 2. 自動化測試清單

| 測試檔 | 涵蓋 | 建立於 |
|---|---|---|
| `tests/flow_test.gd` | 流量網路：單線滿載、容量削減比例、三方競爭的優先權加權、環路 3 次迭代收斂、飢餓時 satisfaction 計算、多資源網互不干擾 | B0.2 |
| `tests/build_test.gd` | 建造：走向與佔格幾何、導管造價／cap／升級、路徑格禁節點、**導管只能走跨越點**、拆除返還、**核心＝礦砂銀行**、**基礎 cap 10 < 發電機輸出 20**、放置前預覽 | B0.3 |
| `tests/tide_test.gd` | 敵潮：破壞半徑、**橋（含引道）免疫**、永不停步、walk-by 打斷產線、退 2 格無損、只有核心讓其駐足、核心歸零＝失敗、波次表確定性、準備期與快進 | B0.4 |
| `tests/combat_test.gd` | 傷害計算、護甲減法／屏障百分比減免、能量不足時射速線性縮放、潮鳴光環按滿足率縮放、回收者回收比例、**交戰耗能／待機 0**、**匯率 1:5**、稜鏡四軸穿透、優先權裁決 | B0.5 ✅ 77 項 |
| `tests/hud_test.gd` | HUD 背後的量：節點三態（含「哪些節點永遠不掛徽章」）、提前召喚倍率與鎖定時機、掉落倍率、產能積分口徑、研究數據公式、通關的終止條件 | B0.6 ✅ 42 項 |
| `tests/save_test.gd` | 存檔往返、缺欄位安全預設、各版本遷移分支鏈式套用、寫檔原子性、時鐘倒退不獎不罰 | B0.1（骨架）／B1.4（完整） |
| `tests/determinism_test.gd` | ★ 同 `(seed, ops)` 兩次跑出相同狀態雜湊；**700+500 tick ＝ 1200 tick**（跨 tick 累加器不含幀率相依）；反向對照（少一條導管／差一個 tick 必得不同雜湊） | B0.1（骨架）／**B0.7 ✅ 15 項** |
| `tests/difficulty_test.gd` | 難度層：第 3 層的敵人血量 ＝ 等級軸滿級（憲法 B2 ② 逐位元）、每層只多疊一條規則、倍率真的進得了模擬、解鎖階梯跳不過去、逐層紀錄、sv3 遷移、統一榜恆為第 0 層 | B2.6 ✅ 65 項 |
| `tests/m2_test.gd` | **M2 驗收（G2）**：內容矩陣實測與不得倒退、**做了但到不了的內容 0 項**、玩過一輪的存檔冪等且無孤兒鍵、**紅線 R1／R2** | B2.8 ✅ 365 項 |
| **`TL_SIM`** | 平衡調校的主力：跑標準關卡 N 個 tick，輸出經濟曲線 JSON 供人工核對。（B0.1 的 `tests/balance_probe.gd` 空殼已於 B1.1 刪除） | 可用 |

**GDScript 測試地雷**：`--script` 模式**不載入 autoload**。`tests/*.gd` 不得 `preload` 引用 autoload 的腳本 → 反向強制 `scripts/sim/` 必須是自足的純函式（正是 `30_TECH_DESIGN.md` §2.4 要的）。

### 被測試斷言「鎖住」的設計承諾

以下承諾不靠自律，靠測試強制：

| 承諾 | 測試斷言 |
|---|---|
| 科技軸增幅 ≤ +35%（GDD B2 ①） | 科技樹全解鎖後的戰鬥係數上限檢查 |
| 等級軸增幅 ≤ +80%（GDD B2 ②） | 塔與生產節點全滿級後的戰鬥係數上限檢查 |
| **統一配置榜零付費影響**（GDD B3） | 以三組截然不同的存檔（零進度／滿科技／滿等級滿課金）載入每日挑戰的統一配置榜，斷言起始 `SessionState` 完全相同 |
| **雙榜共用同一套邏輯** | 斷言兩榜的地圖、波次表、計分函式為同一份，差異僅在初始化參數 |
| **所有內容純遊玩可取得**（GDD B6） | 斷言每個可購買品項都存在至少一條非付費取得路徑 |
| **無付費復活**（GDD B6） | 斷言失敗流程中不存在任何消費入口 |
| **不賣次數**（紅線 R2） | ✅ B2.8：`m2_test` 掃存檔 schema 的**每一個欄位名**比對禁字（stamina／lives／retries／plays_today…）。禁字清單而不是允許清單——後者每加一個功能就要改一次 |
| 失敗不扣任何東西（紅線 R1） | ✅ B2.8：`m2_test._red_lines_still_hold()` 走真正的局末結算路徑（0 星 0 波），斷言星、無盡紀錄、材料、等級、研究數據一項都不減 |
| **塔防不依賴 tycoon** | 以「tycoon 從未開啟」的存檔斷言戰役全關卡皆可通過，**且等級軸可推至滿級**（升級材料的塔防路徑必須真的通，GDD §4.1） |
| **能量無全域池**（GDD §3.1） | 斷言拆除所有儲槽後，赤字 tick 的滿足率立即下降——不存在任何隱形緩衝 |
| **儲槽不豁免導管上限** | 斷言 cap 10 的導管上，儲槽單 tick 放電量 ≤ 1.0 單位（10/秒 × 0.1s） |
| **優先權讀類型不讀節點** | 斷言同類型的兩個節點在同一次解算中拿到相同的權重 |
| 有測試鉤子時不寫真存檔 | ✅ B2.8：`TL_CLICKTEST/title` 的 `persist_off`（**在 B2.8 之前這一格是空的**——只有一行 `print`） |
| 招募不重複 | 斷言連抽至畢業，每次結果皆為未擁有角色 |
| 網路不死鎖（GDD §3.1 不變量） | 斷言任意隨機拓樸下，採集器的產出恆 > 0 |

---

## 3. Bug 分級與處置

| 嚴重度 | 定義 | 處置 |
|---|---|---|
| **阻斷 Blocker** | 進不了遊戲、卡死、壞存檔、資料遺失 | **立刻修，修好前不准 bump 版本** |
| **嚴重 Critical** | 核心循環壞掉、數值嚴重錯誤、必現崩潰 | 當批修 |
| **主要 Major** | 功能錯但可繞過、明顯視覺破版 | 當批修或排下一批（必須登記） |
| **次要 Minor** | 小瑕疵、極端條件才觸發、打磨項 | 登記，里程碑前清算 |

**修好的 bug 必須把重現步驟變成回歸測項**（自動化測試或 §5 清單的一行）。**回歸清單只增不減。**

### Bug 登記簿

| 編號 | 嚴重度 | 症狀 | 重現步驟 | 狀態 |
|---|---|---|---|---|
| — | — | *（尚無。B0.1 開工後開始登記）* | — | — |

---

## 4. 找碴清單（L5）

換上 QA 帽，帶著「我要弄壞它」的目標。**每個里程碑閘門必跑一遍。**

### 4.1 數值邊界
- [ ] 資源為 0 時建造、拆除、升級
- [ ] 資源溢位（跑到極大值）時的顯示與運算
- [ ] 導管長度 0（同格連線）、極長導管
- [ ] 優先權全設為同值／全設為最低
- [ ] 塔全部蓋滿導致能量嚴重不足（滿足率趨近 0）
- [ ] 網路完全斷開（孤島節點）
- [ ] 環路：A→B→A、以及多層巢狀環

### 4.2 流程中斷
- [ ] 建造拖曳到一半按 ESC／點空白／切畫面
- [ ] 波次來襲瞬間拆除關鍵節點
- [ ] 提前召喚後立刻再按（連點防護）
- [ ] 準備期快進中被波次打斷
- [ ] 局內直接關遊戲再開（不得留下髒狀態）

### 4.3 存檔與時間
- [ ] 刪除存檔後啟動
- [ ] 存檔檔案損壞／被截斷／非 JSON
- [ ] 舊版本存檔遷移（每個 `sv` 都要有樣本檔）
- [ ] tycoon：系統時鐘往前調（不得爆量獎勵）
- [ ] tycoon：系統時鐘往後調（不得懲罰、不得負數）
- [ ] 離線 30 天後回來（倉儲上限必須生效）

### 4.4 首次體驗（前 60 秒，最高優先）

**B0.7 逐條過了一遍**（證據：`b07_first.png`＝空地圖的第一眼，`TL_DEMO_TICKS=0`）。

- [x] 新玩家開局後 15 秒內知道要做什麼嗎？→ **★ B0.7 這一條我打了勾，而使用者拿到 build 的第一句話是「你沒教我要怎麼操作，怎麼玩」。勾是錯的，B0.7.1 補修**：提示列改成**兩行**（上行永遠是下一步、下行是當下動作的細節，預覽不再蓋掉指引），每一句都講出**按哪裡、點哪裡**；新增**「操作說明」抽屜**（空地圖時預設開啟，放下第一個節點後就不再擋路）；路徑上的**來襲方向箭羽**回答「潮從哪來」與「核心在哪」
- [x] 第一次蓋塔時，「它會吃能量」這件事有被明確傳達嗎？→ 放置預覽：`交戰時 −N 能量/秒（待機 0）` ＋ `總能量需求 X → Y /秒（交戰時）`
- [x] 第一次能量不足時，玩家看得出來是什麼原因嗎？→ 能量條的橙色缺口段（脈動）＋ 該塔的 `缺料` 徽章 ＋ 頂欄「交戰 N 座」
- [x] 第一次失敗後，玩家知道自己做錯什麼嗎？→ 局末結算逐項攤開撐過幾波／產能積分／提前召喚加成。⚠ **它講的是「你的產能長什麼樣」，不是「你為什麼被打穿」**——真正的死因歸因（哪一波、哪一段防線破了）未做，登記為 B1.2 戰役框架的項目
- [x] 第一次失敗後，重來按鈕在幾次點擊之內？（必須 ≤ 1）→ **1 次**（結算面板上的「立刻重來」）

> **★ 這五條的判官是我（Opus 5），不是真人。** 能證明的是「該給的資訊有給、給在看得到的地方、而且會隨狀態更新」；**不能**證明「陌生人真的看得懂」——那是 R-14，在拉到真人之前不得標記通過（§0.1）。
>
> **★★ 2026-07-28：這條但書當天就被兌現了，而且方向比預期的更糟。**
>
> 我在 B0.7 把上面五條全打了勾。使用者拿到 build，第一句回饋是**「你沒教我要怎麼操作，怎麼玩」**——連第一條都沒過。
>
> **失效的原因值得記下來，因為它是這一類自檢的通病**：我逐條檢查的是「**資訊在不在**」，而第一條問的其實是「**玩家會不會操作**」。提示列當時寫的是「先在礦點上蓋一台採集器」——資訊確實在，目標也確實明確，**但畫面上從頭到尾沒有任何一個地方說過「蓋」＝左欄選一種、再左鍵點地圖**。對已經知道怎麼玩的人（我）那句話是完整的；對新玩家它少了唯一重要的那一半。
>
> **可稽核的教訓，往後每批照用**：
> 1. **首次體驗的每一句提示都必須包含一個動詞化的操作**（「左欄點 X → 左鍵點 Y」），只講目標不講動作的一律不算過。
> 2. **自檢問「這句話對一個沒讀過程式碼的人夠不夠」，不是問「這個資訊有沒有被顯示」。** 我讀得懂畫面是因為我知道輸入怎麼接的——那正是這條驗收要排除的東西，和 §4.5 用 `TL_NAKED` 遮數字是同一個道理。
> 3. **操作方式本身要有一個常駐可查的地方**，不能只靠情境提示逐步餵——玩家想回頭確認時沒有地方可看。

### 4.5 可讀性（R-3 對策的驗收）
- [x] **★ `TL_NAKED=1` 產出的無數值截圖，Opus 5 能指認出瓶頸節點**（M0 硬性驗收項；答錯＝線寬／顏色沒在傳達，要改視覺編碼，不是換個問法）→ **B0.6 讀圖紀錄見下**
- [x] 導管粗細變化在 1280×720 下肉眼可辨（cap 10 → 28 的四個檔位要看得出差別）
- [x] **能量列（供給/需求）在滿足率下探時的警示足夠醒目**（B0.6 起是一條**圖形**橫條：琥珀＝供給、右段橙色脈動＝短少）
- [x] **儲槽充能列與能量列不會被誤讀成同一件事**（能量是橫條、儲槽是節點上的圓弧，形狀不同）
- [x] 敵潮（品紅）與玩家元素（青／琥珀）在混亂畫面中可瞬間區分
- [x] **跨越點（橋）在地圖上一眼可辨**，玩家不需要試錯才知道哪裡能過
- [ ] `reduce_motion` 開啟後仍能取得全部關鍵資訊 ← **B1.6**（設定畫面在 B1.4 才有）

> **★ B0.6 的 `TL_NAKED` 讀圖紀錄（2026-07-28，判官 Opus 5）**
>
> 圖：`C:/tmp/b06_naked.png`（`TL_NAKED=1 TL_DEMO_TICKS=900 TL_SEED=42`）。畫面上**沒有任何數字**——建造鈕只剩名稱、頂欄整列不畫、提示列不畫。
>
> **問題**：哪個節點塞住了？
> **回答**：右上角那一座（稜鏡 (33,4)）。三個獨立的訊號指同一個地方——① 它上方有一個**橙色倒三角**，全圖唯一；② 它被**琥珀環**圈住＝正在吃電；③ 頂端的**能量條**右段有一截橙色，代表全網供不應求。合起來讀是「電不夠，而付不出錢的是它」。
> **對照**：另外三座塔都沒有倒三角——回收者 (28,6) 有琥珀環（在吃電、吃得飽）、潮鳴 (28,12) 與錨 (32,12) 連環都沒有（待機，耗能 0）。
> **答案核對**：`TL_SIM=900` 的 `badges` 只有 `稜鏡(33,4): 缺料`，`towers.稜鏡.sat = 0.84`。**答對。**
>
> **兩個誠實的但書**：
> 1. **這不是盲測**——編碼是我設計的。這條驗收能證明的是「編碼有承載資訊且彼此不衝突」，不能證明「陌生人也讀得出來」。後者是 R-14，在拉到真人之前不得標記通過（§0.1）。
> 2. **稜鏡自己的幾何體也是三角形**，和 `缺料` 徽章同一個形狀家族。目前靠顏色（合金銀 vs 橙）、朝向（正 vs 倒）、位置（環內 vs 環外上方）三重區分，1× 下仍分得開；但**再有第二座塔用三角形就會出事**，列為 B1.6 美術打磨的追蹤項。

### 4.6 建造合法性與路徑規則（B0.3／B0.4 新增）
- [ ] 在路徑格放節點被擋下，且**說明原因**（不是靜默失敗）
- [ ] 導管跨越非跨越點的路徑格被擋下，且說明原因
- [ ] 導管過跨越點可成功，且**該段全程不受敵人攻擊**
- [ ] 貼路徑 1 格內的導管／節點會被行進中的敵人打壞
- [ ] 退開 2 格的建築全程無損
- [ ] **丟一排中繼在路徑旁擋不住任何一隻敵人**（walk-by 不停步，防迷宮塔防後門）

---

## 5. 回歸清單（只增不減）

> 每個里程碑閘門手動跑一遍；能自動化的優先轉成 `tests/`。

| # | 項目 | 加入於 |
|---|---|---|
| RG-1 | headless 啟動 log 乾淨（零 ERROR／WARNING） | B0.1 |
| RG-2 | `TL_SHOT` 產圖並自動退出，`err=0` | B0.1 |
| RG-3 | 有 `TL_*` 鉤子時不寫真存檔 | B0.1 |
| RG-4 | **十七支**測試全過 ＋ **九個畫面的 `TL_CLICKTEST` 全 exit 0** | B0.2 起逐步（B0.5：七支 260 項；B2.4.6：十五支 1290 項，另加 `TL_CLICKTEST/glyph` 的 13 種節點圖形／圖鑑覆蓋；**B2.5：十六支，加 `tycoon_test` 64 項**；**B2.7：十七支，加 `progress_test` 131 項與 `TL_PANEL=achievements`**） |
| RG-11 | **UI 字串裡沒有 Markdown 星號**（RG-147，四次了）。一行 grep：<br>`grep -rnE '\*\*' godot/scripts/screens/*.gd \| grep -E 'label\(\|\.text'` | B2.5 |
| RG-5 | 新開局 → 蓋出第一條產線 → 撐過 3 波 → 失敗 → 重來 | B0.7 |
| RG-6 | 存檔 → 關閉 → 讀檔 → 狀態完全一致 | B1.4 |
| RG-7 | 舊版存檔遷移至最新 sv | B1.4 |
| RG-8 | 壓力情境（500 節點／2000 導管／200 敵人）維持 60 FPS | ~~B1.7~~ ~~B2.1~~ **B2.1e 🟡 一半過**：模擬 **30,200 → 31.0 ms/tick**（974×，`30_TECH_DESIGN.md` §5.3），已不是阻斷（佔 `TICK=100ms` 的三成）；**渲染仍是 23 FPS，這一批沒碰**，RG-8 維持不通過直到渲染那一半處理掉 |
| RG-9 | 每日挑戰**統一配置榜**在三種不同進度／課金的存檔下起始配置完全相同 | B2.2 ✅ `daily_test._uniform_ignores_progress()`（全新／科技半開／科技全開三份存檔，`state_hash()` ＋ `mods` 兩層都比）。紅得起來（讓統一榜吃玩家科技 → 兩條倒） |
| RG-10 | tycoon 離線 30 天回來受倉儲上限約束 | B2.5 |
| RG-11 | tycoon 的 UI 仍只有兩個畫面（防膨脹硬指標） | B2.5 |
| RG-12 | 以「從未開啟 tycoon」的存檔可通過戰役全關卡 | B2.5 |
| RG-13 | 所有 UI 可點元素的觸控命中區 ≥ 44×44 px（手機移植預留） | B0.6 ✅ 全部互動元件一律經 `UiKit.touchable()`（`TOUCH_MIN = 44`）建立 |
| RG-14 | 等級軸滿級在對應難度層下仍有挑戰性（`TL_SIM` 批次驗證） | B2.6 |
| RG-15 | **`TL_NAKED=1` 的無數值截圖仍可指認瓶頸節點** | B0.6 ✅ 見 §4.6 的讀圖紀錄 |
| RG-16 | **橋上導管全程無損；貼路 1 格內的建築會被打壞；退 2 格無損** | B0.4 ✅ 已自動化於 `tide_test.gd`（建造側的「導管只能走跨越點」在 `build_test.gd`） |
| RG-17 | **敵人不因任何建築停步**（中繼牆擋不住） | B0.4 ✅ 已自動化於 `tide_test.gd`（8 個中繼貼路徑，20 秒仍走滿 20 格） |
| RG-18 | **無全域能量池**：拆掉所有儲槽後，赤字立即反映為滿足率下降，沒有隱形緩衝 | B0.2 ✅ 已自動化於 `flow_test.gd` |
| RG-19 | **儲槽充放電受自身導管 cap 約束**（cap 10 的線放不出 23/秒） | B0.2 ✅ 已自動化於 `flow_test.gd` |
| RG-20 | **浮層面板收合後地圖 100% 可見**；戰役地圖一屏可見不需捲動 | B0.6 ✅ 建造欄／能量條／底欄都在地圖矩形之外，唯一會壓到地圖的優先權抽屜預設收合 |
| RG-30 | **`正常` 節點不掛徽章**；核心、儲槽、中繼在正常遊玩中永遠不會被標記 | B0.6 ✅ 已自動化於 `hud_test.gd` |
| RG-31 | **提前召喚倍率在按下當刻鎖給那一波**，事後不因倒數歸零而消失 | B0.6 ✅ 已自動化於 `hud_test.gd` |
| RG-32 | **產能積分只算送達核心的礦砂**（採得出來但運不回去的不算） | B0.6 ✅ 已自動化於 `hud_test.gd` |
| RG-33 | **波次表跑完＝通關且模擬停住**；最後一波的殘敵還在啃核心時不算通關 | B0.6 ✅ 已自動化於 `hud_test.gd` |
| RG-39 | ★ **浮層不得吃掉地圖的點擊**：所有可收合浮層一律 `mouse_filter = IGNORE` | B0.7.3 ✅ 說明面板曾預設開在玩家要放第一個節點的位置上並整片吞掉點擊；B0.7.4 起由 `UiKit.panel()` 建構子保證 |
| RG-41 | ★ **資訊面板的列數與尺寸恆定**：沒有值的列變暗留在原位，不得隱藏（隱藏會讓下面整片往上跳，表就讀不了） | B0.7.4 ✅ 能量收支面板固定 13 列 |
| RG-40 | **45° 導管可經由真實輸入拉出來**（先點起點、再點正斜角的終點） | B0.7.3 ✅ 已納入 `TL_CLICKTEST` |
| RG-37 | ★ **地圖點得動**：合成滑鼠事件點一格會蓋出節點；點路徑格被擋下且說明原因 | B0.7.2 ✅ `TL_CLICKTEST=1`（**必須開真視窗**，headless 不做 GUI 命中測試） |
| RG-38 | **凡是只有真人會走的路徑（輸入／焦點／拖曳），都要有走完整管線的檢查** | B0.7.2 起的通則——截圖證明「畫得對」、`TL_SIM` 證明「算得對」，兩者都不證明「按得動」 |
| RG-35 | **流動珠的方向 ＝ 淨流向**：同一條線用相反順序蓋出來，`conduit_net` 恰好反號（珠子往錯方向跑＝一張反過來的流向圖） | B0.6.1 ✅ 已自動化於 `hud_test.gd` |
| RG-36 | **流率 0 的導管上完全不畫珠子**（待機的支線在一片流動中必須是靜止的） | B0.6.1 ✅ `BEAD_MIN_RATE` 門檻；截圖可見潮鳴支線無珠 |
| RG-34 | **`TL_SHOT` 下模擬凍結**：同一組 `(TL_DEMO_TICKS, TL_SEED)` 在任何機器上拍出同一張圖 | B0.6 ✅ `Battle._process()` 在 `Hooks.shot_path` 非空時不推 tick |
| RG-24 | **塔待機耗能為 0**：射程內無敵人時，全網能量需求不含任何塔 | B0.5 ✅ 已自動化於 `combat_test.gd` |
| RG-25 | **能量不足時射速線性下降且永不停火**（滿足率 10% 仍打得出子彈） | B0.5 ✅ 已自動化於 `combat_test.gd` |
| RG-26 | **礦砂↔能量匯率恆為 1:5**，且等於發電機的轉換比 | B0.5 ✅ 已自動化於 `combat_test.gd` |
| RG-27 | **回收者算的是「射程內的死亡」不是「自己的擊殺」**，且回收速率受自身導管 cap 約束、緩衝有上限 | B0.5 ✅ 已自動化於 `combat_test.gd` |
| RG-28 | **拉動類型優先權會改變誰先餓死**；面板含儲槽與熔爐，**列數 ≤ 12** 且左欄全是生產節點、右欄全是塔 | B0.5 ✅／B1.1 修訂（原判準「5–8 列」在角色名冊長大後失效，真正被鎖的是「依類型不依單一節點」，見 R-15）已自動化於 `combat_test.gd` |
| RG-42 | ★ **合金與礦砂同一條入帳規則**：熔爐產的合金要送達核心才進帳；送不掉就掛 `滿溢` 且帳上不動 | B1.1 ✅ 已自動化於 `flow_test.gd` |
| RG-43 | ★ **熔爐的產出乘「礦砂與能量兩個滿足率中較低的那一個」**，不是相乘（50% 電 ＋ 50% 礦 ≠ 25% 產出） | B1.1 ✅ 已自動化於 `flow_test.gd` |
| RG-44 | ★ **三種資源網互不干擾**：同一局面下礦砂入帳 0 而合金入帳 2/秒；一條導管的 `cap` 每種資源各自計 | B1.1 ✅ 已自動化於 `flow_test.gd` |
| RG-45 | ★ **導管加粗 1 級不要合金**（B0.3 教玩家讀瓶頸的那一步不得被鎖在熔爐後面）；2/3 級要，且拆除時合金按 75% 一起返還 | B1.1 ✅ 已自動化於 `build_test.gd` |
| RG-46 | ★ **碎浪的單體 dps 必須低於錨**——濺射是對敵人排隊方式的賭注，不是免費傷害。它變成「錨但更強」的那一天這條要先紅 | B1.1 ✅ 已自動化於 `combat_test.gd` |
| RG-47 | ★ **建造欄最後一顆鈕仍在畫面內且點得到**（名冊每長一隻就逼近底欄一次；鈕被擠出畫面時 `_draw()` 完全不會抱怨） | B1.1 ✅ `TL_CLICKTEST` |
| RG-48 | ★ **能量收支面板的每一列都對得上一個真實去處**：熔爐的耗能不得被記進「儲槽充能」（B1.1 之前的算式 `demand − tower` 會） | B1.1 ✅ 新增獨立的「熔爐」列 |
| RG-49 | ★ **每種資源的流動珠都要有人看過**：新資源的珠子色相必須與既有的分得開，且**不得與導管的青同明度**（鋼銀會直接融進線裡） | B1.1 ✅ `TL_PANEL=sandbox` ＋ `TL_NAKED`；沙盤佈局與 `flow_test` 共用同一份 |
| RG-50 | ★★ **五關的參考解每一關都真的通得了**（零建造失敗、`phase = won`），且**第 1–2 關核心無損**。「這一關過得了」不能是一句宣稱 | B1.2 ✅ 已自動化於 `campaign_test.gd` |
| RG-51 | ★ **關卡難度只由玩家看得見的參數表達**：地圖字典裡不得出現白名單（尺寸／核心／轉折點／礦點／跨越點／起始礦砂／準備期／波次）以外的鍵。任何人偷加一個係數，這條先紅（§7.7、R-4） | B1.2 ✅ 已自動化於 `campaign_test.gd` |
| RG-52 | ★ **解鎖是規則不是畫面**：第 1 關直接呼叫建造層蓋稜鏡必須被 `locked` 擋下（藍圖展開 B2.3 與重播都會繞過 UI）。解鎖階梯**只增不減**，第 5 關等於全解鎖 | B1.2 ✅ 已自動化於 `campaign_test.gd` |
| RG-53 | ★ **星等累進**：核心破了封頂 1 顆，產能再高也補不回來。**獎勵以歷史最高星數只補差額**，重刷同星數不再給，失敗不扣星也不抹通關紀錄（R1） | B1.2 ✅ 已自動化於 `campaign_test.gd` |
| RG-54 | ★ **兩條線一起餵一個大消費者要真的送得到 100%**：同一根導管的兩個方向共用一份 cap，反向邊上不得有任何流量（來回彈是缺陷指紋） | B1.2 ✅ 已自動化於 `flow_test.gd` |
| RG-55 | ★ **提示列與說明抽屜不得提到這一關沒解鎖的東西**，橋的座數要讀地圖。叫玩家去點一顆不存在的鈕，比不給提示更糟——他會以為是自己看漏了 | B1.2 ✅ 讀圖複驗（`b12_l1_first.png`）；**尚未自動化**，B1.4 補 |
| RG-56 | ★ **每一關至少要有一座塔搆得到核心**（敵人只在核心駐足，搆不到＝一次漏怪必輸）；**稜鏡必須和某一段路徑同排或同列**（貫穿沿軸線，不是射程內全打） | B1.2 ✅ 由 RG-50 的實跑間接守住；B1.7 ✅ 已補直接斷言（`campaign_test._tower_geometry`） |
| RG-57 | ★★ **短線與斜線都點得到**：一格長的 45°（兩個對角相鄰的節點之間）沒有任何「中間格」，格子命中判定必然落空。判定一律用**點到線段的距離**，且畫面層要傳格為單位的**浮點**座標——格解析度分不出同一個節點上的哪一條線 | B1.2.1 ✅ 已自動化於 `build_test.gd` ＋ `TL_CLICKTEST` 的 `diag_upgrade` |
| RG-58 | ★★ **地圖框架恆定 ＋ 進場自動 fit**：進關卡時倍率＝ fit（至少一軸剛好填滿框架、兩軸都不溢出），縮放下限＝ fit、上限＝ fit×3，「全景」回得去，平移夾在「地圖永遠蓋滿框架」之內。**放大之後命中判定仍要對得上那一格**——縮放最容易靜靜壞掉的地方是繪圖與滑鼠各算一次變換 | B1.2.2 ✅ 已自動化於 `campaign_test.gd`（fit 幾何）＋ `TL_CLICKTEST` 的 `view`（含 `hit_after_zoom`） |
| RG-59 | ★★ **fit 之後一格不得低於可讀性地板 24px**（`Shapes.MIN_READABLE_CELL`）。視覺編碼是在 32px 上校準與驗收的（B0.6 `TL_NAKED`）；**進場那一眼不可讀，不能用「玩家可以自己放大」當藉口**——那是把 R-3 的驗收轉包給玩家。要跨過地板（例：40×40 → 15.1px）必須先拿那張圖重跑 `TL_NAKED` 讀圖並在此登記結論 | B1.2.2 ✅ 已自動化於 `campaign_test.gd`（現行五關 50.3／40.3／35.5／33.6／33.6 px） |
| RG-60 | ★ **建造欄與動作鈕全部留在畫面內**：第 5 關是 10 顆建造鈕，加上 4 顆動作鈕（含「移動」）單欄會撞到底欄 → 動作鈕改兩欄兩列。鈕被擠出畫面時 `_draw()` 完全不會抱怨（RG-47 的同一個教訓）。頂欄也不得長到撞上右上角的縮放鈕 | B1.2.2 ✅ `TL_CLICKTEST`（`last_btn` ＋ `view` 的 `bar_clear`） |
| RG-61 | ★★ **測試鉤子在兩個方向上都是空操作**：有任何 `TL_*` 時**不寫也不讀**真存檔。只擋寫入的話，跑出來的畫面會跟著這台機器上玩家的進度變——`TL_SHOT` 的「同參數在任何機器上拍出同一張圖」在關卡選擇畫面上就是假的，而進度相關的斷言會在玩過的機器上無故變紅 | B1.2.2 ✅ `TL_CLICKTEST=1 TL_PANEL=campaign`（在一份已通關四關的真存檔上仍斷言「第 2 關鎖著」） |
| RG-62 | ★★ **碎片爆是純渲染**：開與不開（`reduce_motion`）局狀態雜湊必須完全相同。哪天有人把碎片改成「會打到人的東西」，確定性會靜靜地壞掉 | B1.6 ✅ 已自動化於 `hud_test.gd` |
| RG-63 | ★★ **「所有動效都可跳過」（§4.4）真的關得掉**：`reduce_motion` 時效果壽命為 0（碎片不生成）、所有脈動回靜止值。B1.6 之前這是個**沒有任何人讀的死設定** | B1.6 ✅ 已自動化於 `hud_test.gd` |
| RG-64 | ★★ **同一個位置上的兩個訊息，換顏色不夠，要換形狀**。已經踩過兩次：B0.6 血條 vs 儲槽充能弧（都是琥珀圓弧）、B1.6 受擊 vs 交戰（都是橙色圓環）。新增任何節點上的指示前先問「它和既有的那幾個形狀分得開嗎」 | B1.6 ✅ 讀圖複驗（`b16_threat2.png`）；**判準是視覺，無法自動化** |
| RG-65 | ★ **動效一律由 tick 驅動，不用系統時間**：同一個 tick 必得同一個相位，否則 `TL_SHOT` 的「同參數同一張圖」對任何有脈動的畫面都不成立 | B1.6 ✅ 已自動化於 `hud_test.gd` |
| RG-66 | ★★ **導管不得和既有導管疊在同一排格上**（共用 2 格以上）；**共用 1 格（交叉／同節點分岔）必須照樣可以**。疊在一起的兩條線在畫面上是一條，而命中判定只能任選一條 | B1.6.1 ✅ 已自動化於 `build_test.gd` |
| RG-67 | ★★ **流動珠一律走管子中線**，多種資源沿長度錯開相位而不是各佔一排。垂直偏移沒有夾在管子半寬內時珠子會掛在管外（使用者回報「有破圖」）；描邊一律**螢幕固定粗細**，否則細線上光暈佔直徑近半、讀起來是洞 | B1.6.1 ✅ 讀圖複驗（`c3_sandbox.png`／`d1_l5.png`）；**判準是視覺，無法自動化** |
| RG-68 | ★ **加粗刻度必須明確伸出管緣、且不被節點自己的圖形蓋住**。畫成只比管子寬一點的近白青線＝看起來像管子破了一個洞；貼節點太近＝被畫在它底下看不見（兩版都是截圖當場抓到） | B1.6.1 ✅ 讀圖複驗（`c5_ticks.png`） |
| RG-69 | ★★ **不要把塔擺在幹線的接點上**（關卡設計）：節點先吃滿自己才轉發，所以塔當接點會餓死它下游的一切，而頂欄的供給看起來完全正常。大消費者要兩條進線就用**兩個中繼**分岔 | B1.6.1 ✅ 由 `campaign_test` 的實跑間接守住（第 5 關參考解） |
| RG-70 | ★★ **拖曳拉線在建造模式下就成立**（不必先切「連線」），且**兩次點擊那條舊路徑不得因此壞掉**。這條只有真的送出「按下 → 移動 → 放開」三顆事件才驗得到——`_click_at()` 那種原地按放驗不到「放開在別的格子」 | B1.6.2 ✅ `TL_CLICKTEST`（`drag_wire`，全程停在建造模式） |
| RG-71 | ★ **兩個可同時開啟的浮層不得互相蓋掉**（圖鑑 vs 能量收支）。不設寬度的 `Label` 會長到最長那一行那麼寬——文案一改就靜靜地蓋過去，畫面上沒有任何東西會抱怨 | B1.6.2 ✅ `TL_CLICKTEST`（`codex` 現在同時斷言不重疊，比照 `prio`） |
| RG-72 | ★★ **買了的科技必須在跑過的 tick 上量得到差異**，不是只斷言 `mods()` 的回傳值。一個買了沒生效的科技在畫面上完全正常——它只是讓玩家的研究數據消失 | B1.3 ✅ `tech_test`（cap／採集／交戰耗能／傷害各自跑模擬取差） |
| RG-73 | ★★ **沒買任何科技的一局，數字必須和 B1.3 之前逐項相同**。`mods` 的中性預設只要有一個寫錯，五關參考解與平衡基準線會整組漂掉而沒人立刻發現 | B1.3 ✅ `tech_test._no_tech_changes_nothing` ＋ `campaign_test` 五關實跑 ＋ `TL_SIM` 基準線 |
| RG-74 | ★ **科技效果要同步反映在建造預覽上**（交戰耗能、採集器產出、加粗上限的文字）。玩家在最重要的那條資訊上讀到未打折的舊值，等於預覽在說謊 | B1.3 ✅ 讀圖複驗；`BuildController.preview_place` 與提示列都改吃 `s.mods` |
| RG-75 | ★★ **會長大的清單一律裝在捲動容器裡**，不要靠「把卡片壓矮一點」。科技樹 M1 是 12 個節點、M3 是 45 個——第一版沒有捲動，最長那一支的第 5 張卡直接被推到畫面外，而畫面上看起來只是「這一欄比較短」 | B1.3 ✅ `TL_CLICKTEST=1 TL_PANEL=tech`（`last_reachable`：捲到底後最後一顆鈕整顆在畫面內） |
| RG-76 | ★★ **縮放要以 anchor 為圓心**：以框架正中放大時畫面中心不得跑掉；以滑鼠位置放大時游標底下那一格必須還是同一格。B1.2.2 的版本按一次「＋」就把地圖甩到左上角、pan 當場撞上夾限再也拖不動，**而當時的斷言全是綠的**——「倍率有變大」與「pan 夾得住」都成立，錯的是「往哪邊放大」 | B1.3.1 ✅ `TL_CLICKTEST` 的 `view`（`centered` ＋ `anchored`） |
| RG-77 | ★ **平移只剩滑鼠中鍵這一條路**（B1.3.1 拿掉「移動」模式鈕），所以它必須有一條真的走過去的自檢。判斷用自己記的 `_panning` 而不是讀 `mm.button_mask`——那個位元是 Input 單例從實體滑鼠狀態填的，合成事件填進去的值不保證留得住 | B1.3.1 ✅ `TL_CLICKTEST`（`mid_pan`）。⚠ 觸控沒有中鍵，見風險 R-16 |
| RG-78 | ★★ **同一個事實不得存兩份**。`campaign.cleared` 與 `campaign.stars` 講的是同一件事，漂掉的那一天玩家會看到「這一關通關了但下一關還鎖著」，而兩個欄位各自都「對」 | B1.4 ✅ sv2 已移除 `cleared`；`campaign_test` 斷言它不再出現 |
| RG-79 | ★★ **存檔遷移必須冪等**，而且**不得假設舊資料自洽**（真實存檔會有中途版本、當機、手改過的檔）。最常見的事故是「開遊戲兩次跑了兩次遷移」 | B1.4 ✅ `save_test._migration_sv1_to_sv2`（四條：升級／補不自洽的資料／沒有 sv 的檔／冪等） |
| RG-80 | ★★ **設定要真的接到外面那個東西**，不是只寫進字典。一個沒接到匯流排／`Motion.reduce`／視窗的設定畫面，看起來和正常的完全一樣（與 RG-72「買了沒生效的科技」同一類） | B1.4 ✅ `TL_CLICKTEST=1 TL_PANEL=settings`（`buses`／`reduce_on`／`reduce_off`／`resize`） |
| RG-81 | ★★ **靜音時 `AudioBus.apply()` 必須是空操作**（鐵律 2）。否則「有鉤子就一定不出聲」會變成「取決於這台機器的玩家把音量存成多少」 | B1.4 ✅ `TL_CLICKTEST/settings` 的 `mute_wins` |
| RG-82 | ★ **設定的套用只能有一支**（`GameState.apply_settings()`）。散在各畫面 `_ready()` 裡讀設定的話，新加一個畫面就多一個會忘記讀的地方，而忘記的那個畫面看起來完全正常 | B1.4 ✅ `Battle` 原本那一行已刪；**判準是程式碼結構，無法自動化** |
| RG-83 | ★★ **循環素材的接點要量，而且門檻要自己校準**。拿固定門檻比「接點跳幅」會把 330Hz 鋪底音自己正常的取樣步幅誤判成爆音；真正會啪一聲的是**接點跳幅遠大於鄰近跳幅** | B1.5 ✅ `audio_test._loops_are_seamless`（接點 ≤ 鄰近 128 取樣平均步幅 × 4） |
| RG-84 | ★★ **BGM 兩層必須同幀數**。差一個取樣，跑一分鐘就差 60 個，節拍會慢慢錯開而沒有人說得出哪裡怪 | B1.5 ✅ `audio_test._bgm_layers_align` |
| RG-85 | ★★ **每一座塔都要有自己的開火音，清單從資料表推導不手抄**。手抄的那一份會在加第六座塔的那一天悄悄過關 | B1.5 ✅ `audio_test._paths()` 走 `NodeDefs.BUILDABLE` 的 `tower` 旗標 |
| RG-86 | ★★ **音效一律從畫面層推導，`scripts/sim/` 維持零副作用**。在模擬層塞一行 `play()` 就等於讓每日挑戰的重播會出聲、讓 headless 批跑變成一場噪音 | B1.5 ✅ `Battle._audio_tick()` 比對上一幀；**判準是程式碼結構**（`grep AudioBus scripts/sim/` 應為空） |
| RG-87 | ★ **會重複觸發的音要節流**。核心每 tick 都在掉血，照實播就是每秒十次爆炸；響個不停的警報等於沒有警報 | B1.5 ✅ 核心受擊 0.8s／能量不足 4s；**聽感由使用者判**（R-14 同類） |
| RG-88 | ★★ **選單／浮層開著時，底下的地圖不得吃到滑鼠**。遮罩畫得出來不代表擋得住（B0.7.2 的 `mouse_filter` 同一課） | B1.4.1 ✅ `TL_CLICKTEST` 的 `scrim_blocks` ＋ `cell_buildable`（同一格關掉選單後蓋得起來，證明是遮罩擋的） |
| RG-89 | ★★ **兩個節點都接了 ESC 時，誰先拿到要量不要推論**。局內畫面是關卡選擇的子節點，順序若反了「戰鬥中按 ESC」就會變成被踢回關卡選擇 | B1.4.1 ✅ `TL_CLICKTEST/campaign` 的 `esc_to_battle` |
| RG-90 | ★ **`AudioStreamWAV.data` 是屬性，每讀一次複製一整份 PackedByteArray**。寫在取樣迴圈裡的那一版看起來像當掉了（35GB 複製） | B1.5 ✅ `audio_test` 一律先取進區域變數 |
| RG-91 | ★★★ **用差值偵測的東西，要確定自己是在「變化的那一刻」被呼叫的**。開火音靠「`ttl` 還是滿的」判定「剛開火」，但 ① 畫面 60fps／tick 10Hz → 同一發播六次；② 局結束後 `step()` 直接 return，`ttl` 不再遞減 → **每一幀重播一次，聽起來像卡住了**（使用者回報） | B1.5.1 ✅ `_audio_tick()` 只在 `tick_count` 前進時判定；`TL_CLICKTEST` 的 `over_silent`，**拿掉守衛會當場變 false（驗過）** |
| RG-92 | ★★ **局結束時要收掉凍住的純渲染狀態**（開火線、碎片）。模擬停了，但那些清單是靠 `ttl` 遞減自己消失的 | B1.5.1 ✅ `over_cleared` |
| RG-93 | ★★★ **測試的觸發條件要真的觸發得了**。第一版的局末測試用中繼當開火線起點，而 `fire_relay` 沒有這個檔案 → `play()` 直接 return → 不管修沒修都會過。**一個永遠會過的測試比沒有測試更糟** | B1.5.1 ✅ 改成先蓋一座真的錨，`tower_built` 納入判定 |
| RG-94 | ★★★ **只做「結果不變」的最佳化，並且要證明結果真的沒變**。B1.7 把每次拜訪重算的常數提到迴圈外（−32%~46%），驗收條件不是「比較快」而是**逐位元相同**：416 項戰役斷言 ＋ `TL_SIM` 的每一欄。⚠ `PackedFloat32Array` 會降精度 → 換了一組數字 | B1.7 ✅ `campaign_test` 416 項、`TL_SIM=5600` 逐欄未動 |
| RG-95 | ★★ **效能測試要量「產品真正的負載」，不是只量壓力情境**。只看 RG-8 會得到「全面不通過」，只看壓力情境的平均值會漏掉「第 5 關已經超出目標」——五關逐關量才知道**這一版能不能出** | B1.7 ✅ `perf_test._campaign_levels` 逐關斷言 |
| RG-96 | ★★★ **每一個 `TL_*` 都要進 `Env.HOOKS`**，否則它單獨使用時**會寫進玩家的真實存檔**（鐵律 1）。`TL_LEVEL`／`TL_FOCUS` 漏了兩個批次沒被發現，因為實務上都跟 `TL_PANEL` 一起用——**「實務上不會單獨用」不是保護** | B1.7 ✅ 已補齊三個 |
| RG-98 | ★★★ **斷言「可達性」，不要斷言「存在性」**。`audio_test._paths()` 從 `tower` 旗標推開火音清單，於是它**要求一個永遠播不到的 wav 存在**（潮鳴 `rof = 0` → `_fire()` 跳過 → 永遠不產生開火線 → `_audio_tick()` 永遠選不到 `fire_knell`）。那個檔案一路過關、一路被打包，還在 B1.5.1 被「改良」過一次音色——**改的是一個沒有人聽得到的音**。一個只檢查資產在不在的測試，永遠抓不到「這個資產從來沒被播過」 | B1.8 ✅ 篩選改 `tower and rof > 0`；wav 與產生器一併刪除 |
| RG-99 | ★★★ **從讀程式碼得到的結論，要用一個會紅的迴圈驗過才能講給使用者聽**。「潮鳴不開火所以沒聲音」我連續回報兩次都只是讀 `rof = 0` 推出來的，沒有跑過任何東西。實際建迴圈之後才發現第一次跑是**假紅**（潮鳴 0 交戰 tick，敵人根本還沒走到）——要跑到 5600 tick、拿「錨 135 交戰 tick → 13 次開火音」當對照組，那個紅燈才證明得了原因是 `rof` | B1.8 ✅ 探針實跑；`fire_knell` 0 次 vs `fire_reclaimer` 171 次 |
| RG-100 | ★★ **會脈動的視覺元素要在「波谷」那一格驗收**。`TL_SHOT` 凍結在單一 tick，而脈動相位由 tick 決定——潮鳴的場第一版 alpha 落在 0.23–0.50，驗收截圖剛好落在波谷，讀起來是灰的不是青的。**調下限不是調振幅**：谷底讀得出來，整條曲線就都讀得出來 | B1.8 ✅ `pulse01(..., 0.72) × 0.92`，同一 tick 1556 重拍 |
| RG-101 | ★★★ **不開火的塔也要有「我在工作」的回饋**。潮鳴每秒吃 9 能量、是全案唯一 `rof = 0` 的塔，而 B1.7 之前畫面上零回饋——等於把「塔在交戰時每秒吃電」這條全案的心臟藏起來。**回饋要標在效果作用的對象上**（減速作用在敵人身上 → 標敵人），標在塔上只說得出「它開著」 | B1.8 ✅ 交戰時畫射程場環 ＋ 被影響的敵人描青色輪廓；`TL_CLICKTEST` 的 `knell_field` |
| RG-97 | ★★ **輸掉的時候要說得出為什麼**（M1 驗收句子）。局末面板原本只有成績（撐過幾波、產能積分），那是結果不是原因；玩家要的是下一句「所以我下次該改什麼」 | B1.7 ✅ 死因歸因一行（第幾波開始漏／缺電還是火力不足）；`TL_CLICKTEST` 的 `why`／`why_fits`。**「陌生人看不看得懂那一句」仍是 R-14** |
| RG-29 | **導管無方向**：先點哪一端都不影響任何資源的流向 | B0.5 ✅ 已自動化（`build_test` 的礦線 ＋ `combat_test` 的塔全部由末端往幹線拉） |
| RG-21 | **以「從未開啟 tycoon」的存檔可將等級軸推至滿級**（升級材料三條路都通） | B2.7 |
| RG-22 | **畫面上的除錯／狀態文字不得包含檔案路徑等不定長字串**（會橫跨全螢幕，小解析度下裁切，違反 P1）。值一律只進 stdout | B0.1 |
| RG-23 | **自動化測試不得留下 `user://save.json`**（測試若偵測到既有存檔必須整段跳過，絕不覆蓋玩家進度） | B0.1 |
| RG-108 | ★★★ **大圖模式：問題節點在畫面外時，畫面上要有指得到又點得到的東西**（R-3 在大圖的對應驗收）。兩個通道缺一不可——小地圖答「全局長什麼樣、我在哪」，邊緣箭頭答「現在有東西壞了、在哪一邊」。**兩者都必須可點跳轉**：那是大圖平移唯一的純左鍵動線（中鍵在觸控上不存在，R-16） | B2.1b ✅ `TL_CLICKTEST/endless` 11 條；拿掉攔截 → `jump`／`mini_pan`／`mini_safe` 三條同時紅 |
| RG-109 | ★★ **「大過一屏」要大到讓導引有價值，不是技術上成立就好**。56×32 看得到 86%×79%，小地圖上的視野框幾乎和外框重疊——「我在哪」讀不出來。改 64×40（看得到 46% 面積）才成立 | B2.1b ✅ 斷言 `view` 兩軸都在 30%–100% 之間 |
| RG-110 | ★★★ **改地圖尺寸就必須重算所有跟面積走的參數**（礦點數、跨越點數、起始礦砂、可達性下限）。放大地圖而不動它們＝**悄悄把難度調高**，而 §7.7 說難度只能用玩家看得見的參數表達——那也包括「不要用改尺寸偷改」 | B2.1b ✅ 四個參數全部重算並寫進 `10_GDD.md` §7.10 |
| RG-112 | ★★★ **生成器要有「值不值得玩」的不變量，不只是「格式對不對」**。B2.1a/b 的 5 條不變量全部是格式與可通關——**格式正確、可通關、且完全不好玩的圖，生成器一天擲得出幾千張**。無盡的兩個缺陷（死區、撒點礦）都通過了原本那 5 條 | B2.1c ✅ 補上 4 條（跨幅、死列、礦點離路徑、孤點），全部掃 300 種子 |
| RG-113 | ★★★ **填滿地圖不可以靠拉長路徑**。`Tide.advance()` 是 `progress += speed*TICK`，**路徑格數就是一波的秒數**——「路徑走遍全圖」的直覺解法會直接把每一波變慢，等於拿一個缺陷換另一個。正解是**單調掃掠**：跨越全高的代價只付一次，折返才是白花的 | B2.1c ✅ 跨幅 19.3→33.1 而路徑長度 95.9→**95.1** |
| RG-114 | ★★ **「隨機」不等於「有規劃」**。均勻撒點在數學上無可挑剔，玩起來是 20 幾個各自獨立的瑣碎決定。手作圖的規劃長什麼樣，看它自己的註解就知道——「北岸三顆，共用一座橋」＝**共線、共用一條幹線、合起來是一個決定** | B2.1c ✅ 礦點改成脈；孤點 72%→12% |
| RG-115 | ★★ **門檻值要從實測往回退一步訂，不要照實測填**。照實測填等於把「現在剛好長這樣」寫成規格，之後任何一點變動都會假紅 | B2.1c ✅ `SPAN_MIN=18`（實測最差 28）、`DEAD_MAX=6`（實測最差 **0**）、`LONELY_MAX=0.2`（實測 12%） |
| RG-125 | ★★★ **兩個地方比同一件事，距離的「種類」要一致**。`Tide.in_blast()` 是 Chebyshev ≤1，`MapGen.OFF_MIN` 卻用四方 BFS ≥2 → **斜對角貼著路徑轉角**的礦點通過檢查、蓋下去每 tick 挨打（300 張圖 167 顆）。**規則用哪種距離，檢查就要用哪種** | B2.1d.1 ✅ 距離場改八方 BFS；測試改用 Chebyshev 獨立量；紅得起來（改回四方 → 167 顆） |
| RG-126 | ★★ **常駐的 UI 浮層＝那一塊永遠點不到**。小地圖攔截點擊是對的（RG-111），但**常駐**攔截就變成把地圖右下角整塊鎖住 | B2.1d.1 ✅ 縮放／平移時出現、靜止 3 秒淡出；**隱藏時不攔截**（`mini_gone` 斷言，紅得起來） |
| RG-121 | ★★★ **量對維度（第二次踩到）**。「平均分配」的前兩版指標（沿路徑的空檔、半徑內的擁擠度）**都只看路徑附近**——礦點全擠在路邊時它們是滿分。指標的座標系要跟著設計的座標系走 | B2.1d ✅ 改量「整張圖 4×3 大區，每一區都要有礦」；紅得起來（鎖回路邊 → 1028/3600 個大區空著） |
| RG-122 | ★★★ **純渲染欄位沒有任何斷言看過，就會靜靜地壞掉一整批**。`s.shots.append()` 被誤縮排進 `if` 裡，四座塔的開火形態**整整一批沒畫出來過**，而 12 支測試全綠——`shots` 不進 `state_hash()`，沒人驗它 | B2.1d ✅ `hud_test` 補「非濺射塔開火後 `shots` 不得為空」 |
| RG-123 | ★★ **「推到底貼不貼邊」要用數字驗，不要用縮放過的截圖判**。視野框與小地圖外框之間 5px 的內縮，在 2.5 倍的截圖裁切上看起來像 17px | B2.1d ✅ `flush` 斷言：夾到底後視野的世界矩形對齊地圖四角（夾限本來就是對的，錯的是外框畫在 `grow(5)` 上） |
| RG-124 | ★★ **合成輸入要能表達「按住不放」**。`_click_at()` 是按下即放開，用它驗不到拖曳——第一版的 `drag` 斷言恆為 false，因為送移動事件時早就放開了 | B2.1d ✅ 用既有的 `_press_at(at, pressed)` 拆成按下／移動／放開三步 |
| RG-135 | ★★★★ **半套展開會花掉資源換到一個接不起來的殘骸**，而拆掉只退 75%——那是玩家沒辦法補救的損失。藍圖是一個單位，不是一疊各自獨立的指令 | B2.3 ✅ `BuildController.blueprint_place()` 全有全無；`blueprint_test._all_or_nothing()`。紅得起來（拿掉檢查閘門 → 錢扣了 75、只放了 2 格，七條同時倒） |
| RG-136 | ★★★ **測試佈局自己也要有前置斷言**。`blueprint_test` 第一版把節點蓋在靜水沙盤的核心格與界外，`place()` 靜靜失敗 → 藍圖少一個節點 → 十幾條斷言全紅，而紅的樣子看起來像是藍圖模組壞了 | B2.3 ✅ `_built(t)` 改走 `apply_ops()` 並斷言 `fails.is_empty()`；座標常數化並註明沙盤是 20×16、核心在 (10,10) |
| RG-137 | ★★★ **「數量對得上」在數量為 1 時是恆真的**。clicktest 的 `bp_has_nodes` 比對「藍圖節點數 ＝ 框到的格數」，而框到 1 個時它必然成立——既測不到導管也測不到多格落點。**截圖上只看到一格預覽才發現** | B2.3 ✅ 補前置條件「框到的格數 ≥ 2 且導管非空」 |
| RG-138 | ★★ **新浮層要和既有浮層一起驗不相疊**。藍圖抽屜第一版放 (128,150)，被圖鑑整個蓋住（文字從後面透出來）。畫面上已經有三張可同時開啟的浮層，位置是**擠出來的**不是挑出來的 | B2.3 ✅ 移到 (560,400)；`bp_fits` 斷言它夾在優先權右緣與能量左緣之間且不超出底邊 |
| RG-139 | ★★★★ **「按得到」不等於「看得到」——用座標合成點擊的自檢，對一顆被蓋住、甚至被推出畫面的鈕照樣是綠的**。B2.4 把可建造類型從 10 加到 13，建造欄的垂直串長出畫面：「加粗」「拆除」和底欄疊在一起，**「藍圖」整顆掉到畫面外**——而 `TL_CLICKTEST` 的 `blueprint=true` 全程沒紅過，因為 `_press()` 是往 `global_position + size*0.5` 送事件的。`last_btn` 那條也沒抓到，它只驗**最後一顆建造鈕**，不驗它下面那三顆模式鈕。⚠ 這正是名冊系統的目標狀態（全收集＝13 種），而戰役第 5 關只有 10 種所以永遠不會現形 | B2.4.2 ✅ 建造清單裝進捲動容器、模式鈕釘在其下。判準改成幾何的，且**分兩種東西問**——釘住的東西（模式鈕、捲動容器）不得越過 `BAR_Y`；最後一顆建造鈕**捲到底之後**要整顆在視窗內（捲出去的清單項目本來就在視窗外，那是合法的）。**紅得起來**（把捲動區撐到 700 → `last_btn=false`）。§6.3 已加「13 種建造欄的碰撞檢查」 |
| RG-140 | ★★★ **文件寫了、程式沒做，而沒有任何一條檢查在對照兩者**。v0.25.1 逐張截圖稽核發現 `20_ART_DIRECTION.md` §1.6 明文要求的三件事從來沒有實作：路徑帶的**抖動邊緣**（現在是完美的軸對齊矩形——**全畫面最大的混沌側元素，是最幾何的東西**，直接牴觸 §0 統御命題）、跨越點的**45° 引道與橋下落影**（於是「架高」＝「橋上導管不受攻擊」的唯一解釋沒有被畫出來）、核心的**「最大的幾何體」**（現在和一個中繼一樣大）。§3 的**等寬數字**同樣零實作（`grep tabular\|FontVariation` 零命中），頂欄七個變寬 Label 裝在 HBox 裡，每 tick 橫向推一次 | B2.4.3 ✅ 路徑帶抖動邊緣、核心 48px（1.5 格）＋ 受擊整體閃橙、跨越點 45° 引道＋偏移落影。**B-3 等寬數字仍未做**（排 B2.4.4）。★ 教訓：**`TL_NAKED` 檢定問的是「這個元素說不說得出話」，沒有問「文件要求的元素在不在」**。§6.3 已加「本批動到的小節，逐條對照文件與畫面」 |
| RG-141 | ★★★ **縮圖上讀到的瑕疵，在變成工作項目之前必須先拍一張近照**。美術稽核初稿依據兩張縮到 1280 寬的截圖，宣稱路徑帶轉角的 alpha 疊了兩次、還「看得到接縫」，並排進了修復批次。查證後**完全不成立**：`Maps.path_of()` 每格只 append 一次、`_draw_path()` 一格一個不重疊的 `Rect2`、4× 近照下轉角與直線段同一個值。我讀到的深色是 `_draw_incoming()` 的品紅箭羽被縮圖糊成一片 | ✅ 撤回，改寫進 `20_ART_DIRECTION.md` §7.1。★ 這條與 §1.7／RG-106 是同一條規則的兩面：**設計不能只在近照上驗**（RG-106）、**缺陷不能只在縮圖上判**（本條）。兩條要一起用 |
| RG-163 | ★★ **一條在它該紅的情境下也是綠的斷言**。B2.9 新寫的「底欄與提示文字不得相交」把提示的矩形寫成 **1px 高**（y 666–667），而底欄從 y 668 起——兩個矩形**永遠**不相交。是把提示故意推到底欄正中間去測它才發現。★ RG-158 的同一課（先確認尺會動，再拿它量東西），這次連新寫的斷言都中招 | B2.9 ✅ 改用提示真正的高度；兩個方向都跑過：正常 PASS、故意疊上去 FAIL |
| RG-162 | ★★★ **寫死的座標記的是「當時」的版面**。局內提示文字的 `x` 是寫死的 520，而 B2.9 給按鈕套上內距之後底欄那七顆鈕加起來變寬，提示整段被壓在「說明」那顆鈕上面（也違反手機預留條款 P1「不硬編碼像素位置」）。★ §7.1 A-1 的結論一字未改地適用：「驗收條件要改成『鈕的矩形與底欄矩形不相交』，不是『按得到』」——**被壓在字底下的鈕照樣按得到**，所以既有的每一條斷言都是綠的 | B2.9 ✅ 提示的 x 跟著底欄的實際右緣走（`bar.resized`）；補上矩形不相交 ＋ 提示不出視窗兩條斷言。順手把按鈕內距從 12 降到 8（**按鈕的內距是一個全域尺寸，在最擠的那個版面上才看得出代價**） |
| RG-161 | ★★★ **全遊戲 41 顆按鈕從來沒有讀過美術 token**。`20_ART_DIRECTION.md` §1.1 的 `bg.raised` 那一列寫的就是「浮起元件（**按鈕**、卡片）」、§1.4 的 `radius.ui` 寫的是「UI 面板與**按鈕**」——兩條 token 訂了三個月，`add_theme_stylebox_override` 在按鈕上是 **0 次**。★ 為什麼漏這麼久：每批的 UI 一致性檢查問的是「有沒有新的硬編碼色值」（`Grep Color(`），而**沒有套色值的東西不會出現在那個 grep 裡**——清單抓得到「用錯顏色」，抓不到「根本沒上色」 | B2.9 ✅ `UiKit.theme()` 一組 Button 樣式（五態 stylebox ＋ 五個字色），設在主題上所以 41 顆一次蓋完。停用態**底色退回面板色**（看起來是凹下去的，不是一顆比較暗的凸起），聚焦態用 `stroke.emphasis` |
| RG-160 | ★★★ **一條永遠「待補」的測試等於沒有這條測試**。磁碟往返（原子寫入 → 讀回 → 改名）的規則是「偵測到既有存檔就整段跳過，免得碰壞使用者的進度」——立意對，結果是它**在任何玩過遊戲的機器上都沒被執行過**，也就是我的機器與使用者的機器。它從 B0.1 起一直印著「待補 —」，而那行字每一批都被讀過、每一批都被當成背景 | B2.8 ✅ `SaveService.path` 改成實例欄位（預設仍是玩家那一個），測試寫**自己的檔名**：真實存檔一個位元組都沒被碰到，而原子寫入三段真的跑得到。順手補上「半寫壞的檔要退回預設值」（`save_test` 33 → 83 項） |
| RG-159 | ★★★ **`_fill_defaults()` 只補缺的鍵，不修型別錯的鍵**。一個 `"campaign": 5` 的存檔（舊版寫的、手改壞的、寫到一半的）會讓整個局外層噴 **14 條型別錯誤**——成就、名冊、材料餘額、難度層解鎖全部中招，而每一個讀取端都寫著 `d.get("campaign", {}) as Dictionary`，那個 cast 對一個整數只會得到 null | B2.8 ✅ 遷移**之前**多一步 `_repair_containers()`：只修「容器 vs 非容器」不合的鍵。★ **不是嚴格比對型別**——JSON 沒有整數，`"data": 12` 讀回來是 `12.0`，嚴格比對會把玩家的研究數據當壞資料清掉（`save_test` 有反向對照）。修一處，14 條錯誤歸零 |
| RG-158 | ★★ **對照組必須自己也會動**。B2.6 的校準實測本來只有兩腳（第 0 層／零進度 vs 第 3 層／滿級），而那兩腳的差距在「等級軸根本沒生效」時**一樣是綠的**——量到的只會是難度層。★ 這是 B2.7「量錯欄位」（`delivered_total` 的比值 2,640,000）的同一個形狀：**先確認尺會動，再拿它量東西** | B2.6 ✅ 補第三腳（第 3 層／零進度）。三腳一跑就看見真相：**沒有等級軸過不了第 3 層**（第 1940 tick 核心已毀），而那正是憲法 B2 ② 想要的形狀，比原本想斷言的「損失比較多」強得多 |
| RG-157 | ★★ **測試把「最新存檔版本」寫死成 2**。`save_test` 兩條斷言 `t.eq(up["sv"], 2, ...)`，於是 B2.6 加 sv3 時它們變紅——但紅的不是程式碼，是斷言：那兩條要問的是「遷移鏈跑得完」，不是「最新版恰好是第 2 版」 | B2.6 ✅ 改對 `Save.SAVE_VERSION`。★ 同一族的第四次（RG-142／149／151 都是「靠列舉／寫死守住一個會長大的東西」） |
| RG-156 | ★★ **`rules` 的長度就是「這一層疊了幾條」，所以第 0 層不能塞一句佔位的話**。第一版在標準層寫了 `["沒有額外規則"]`，於是「第 N 層有 N 條規則」這條斷言在第 0 層當場失效，而它是「每層只多疊一條」的唯一守衛 | B2.6 ✅ 資料表留空陣列，那句話由畫面層補（空白的卡片說不出它是刻意空的） |
| RG-155 | ★★★★ **把開發代號寫進玩家看得到的字串，而且它會過期**。兩條：「升級材料 %d（塔的等級・**B2.7 開放兑換**）」（B2.7 已經上了，這句話變成假的）、「藍圖槽 +1。（**藍圖庫本體排 M2**，先把槽位存起來。）」（藍圖庫 B2.3 就做完了）。★ 批次編號在字串裡是**一個沒有任何東西看著的到期日**——功能上線那一批不會有人想到回頭改它 | B2.7.3 ✅ 兩條重寫成不帶代號的說法；`20_ART_DIRECTION.md` §3.5 禁令 ④ 寫成規則 |
| RG-154 | ★★★ **程式碼註解的語氣會漏進 UI 文案**。全案 505 條玩家字串裡有 27 條用「——」接一句把前半句換句話說的尾巴（「擺位就是它的謎題」「買的是覆蓋範圍不是輸出」），另有三處在**替自己的設計辩護**（「它只是一個彙總，不另外發獎勵」）。註解是寫給自己的（為什麼這樣做），UI 是寫給玩家的（這是什麼、我現在能做什麼） | B2.7.3 ✅ 破折號 27 → 8（剩下的全是「錯誤＋補救步驟」，那是 UX 文案該有的形狀）；圖鑑 8 條全改寫。規則寫進 `20_ART_DIRECTION.md` §3.5，並附一支可重跑的計數腳本 |
| RG-153 | ★★★ **把一支函式搬進共用 module 會改變它的生命週期假設**。五份合成點擊各自寫在畫面裡時，`await get_tree()` 問的是**畫面**（活著）；搬進 `UiKit.click(button)` 之後問的是**那顆鈕**，而這一顆很可能就是觸發 `_build()` 的那一顆——送完事件它已經被釋放。四個畫面當場 `Cannot call method '"'"'get_tree'"'"' on a previously freed instance`。★ **去重不是純文字操作**：五份逐字相同的程式碼，包起來之後的執行環境不同 | B2.7.2 ✅ 先把 `tree` 拿起來再送事件。★ **這個回歸是 clicktest 自己拒絕執行才暴露的**——十七支 headless 測試全綠（它們不開畫面） |
| RG-152 | ★★ **直接丟進 `VBox` 的鈕會被拉成整欄寬**。B2.5 的 `TycoonLines` 踩過一次（截圖抓到），B2.7.2 的 `/codebase-design` 審視又在 `DailyScreen` 找到一個當時還活著的（「返回標題」735px）。八顆返回鈕裡七顆包了 HBox、一顆沒包——**要記得的規矩會被忘記** | B2.7.2 ✅ `UiKit.back_row()`，全部八個畫面改走它；包 HBox 從一條規矩變成一支函式。同批 `UiKit.screen()` 收掉六份 margin 樣板（24 是 `20_ART_DIRECTION.md` §1.3 的間距階，六份各自寫死等於之後要改六個地方） |
| RG-151 | ★★★★ **憲法級的閘不得靠列舉守住**。B3（統一配置榜與玩家的任何進度與課金完全無關）原本是一軸一支 `Daily.*_for()`，而 `SessionState.setup()` 也是一軸一個位置參數（B1.3 三個 → B2.7 四個 → B2.6 會是五個）。**漏掉一道閘不會有任何東西報錯**，而後果是一條可課金加速的軸洩進「對 P2W 的唯一制衡」。★ 與 RG-142（以 type 為鍵的表）、RG-149（位置斷言只列舉鈕）同一族的**第三次**，只是這一次踩到的是憲法。這條是 `/codebase-design` 審視抽出來的，**不是缺陷報告**——形狀尚未失效前就修 | B2.7.1 ✅ 新增 `sim/Loadout.gd`：局外成長收成一個字典，`setup()` 固定三個參數、`Daily.meta_for()` 一道閘、斷言**逐鍵列舉 `Loadout.KEYS`**。兩個方向都驗：改成只擋 `tech` → `daily_test` 1 條、`progress_test` 4 條變紅。★ 漏掉「把新軸加進 `KEYS`」的後果是它進不了局（吵的），不是洩進統一榜（安靜的） |
| RG-150 | ★★★ **同一份進度不得在兩個系統各換一次獎勵**。B2.7 的成就在「五關全通」「滿星」「無盡五十波」上各發一張聲望券，而 `Roster.earned()` 早就在把**戰役星數**與**無盡波數**換成券了——同一份進度領兩次。症狀是 §7.13 那條刻意的對齊（戰役滿星 15★ ÷ 5 ＝ 3 券 ＝ 剛好抽乾招募池，也就是 B6 的證明）悄悄變成 5 券，`roster_test` 四條當場變紅。★ 修法不是改那四個 id，是寫成**通則**：發券的成就不得使用 `earned()` 已在數的 metric——第 21 條成就加進來時它會自己攔 | B2.7 ✅ 只剩三條發券（整棵樹、兩軸滿級）；`progress_test._achievement_table_is_consistent()` 用 metric 黑名單斷言，並額外釘住「名冊全滿不發券」（畢業之後券沒有用途） |
| RG-149 | ★★★★ **「所有元素都在畫面內」的斷言只列舉了鈕，於是版面第二次溢出還是綠的**。B2.5 的 RG-139 修法走 `_menu_buttons`；B2.7 加一行「公司等級」之後版本號被切在 720 之外，`on_screen=true` 照樣印出來。**沒有被列舉的東西不會被守住**——RG-142（以 type 為鍵的表）的同一個形狀，換到版面上 | B2.7 ✅ 改成問容器：`_menu_col.get_children()` 逐一量，並印出第一個被切到的節點名。把間隔調回 20 驗證會紅（`切到 @Button@30@690`）。版面同批改為「公司等級併進標語同一行、拿掉純留白的間隔物、間隔 12 → 8」 |
| RG-148 | ★★ **`UiKit.label()` 預設不換行**，長句直接切在畫面右緣。B2.7 的「局外成長」說明段落當場踩到（截圖抓到；`TL_CLICKTEST` 一個字都沒說，同 RG-147 的第二半）。凡是超過一張卡寬的說明文字，一律自己開 `AUTOWRAP_WORD_SMART` | B2.7 ✅ 該段落補上 autowrap ＋ `SIZE_EXPAND_FILL` |
| RG-147 | ★★ **`Label` 不解析 Markdown**——`**強調**` 會原樣印出兩排星號。B0.7.1／B0.7.3／B1.1／**B2.5** 各犯過一次，**四次了**。共同點是「寫文件的手在寫 UI 字串」。強調一律用「」 | B2.5 ✅ 檢查寫成一行 grep（RG-11），並掃過全部畫面：只有 `Tech.gd` 有，而它已經把星號 replace 掉。★ 四次都是**截圖抓到的**，`TL_CLICKTEST` 一次都沒說話——它驗的是鈕按不按得到 |
| RG-146 | ★★★★ **「指數成本必定收斂」是直覺不是證明；一個線性乘數就能推翻它**。tycoon 的成本 `500 × 1.35^L` 與報酬 `120 × t^1.25` 都照 §4.2，直覺上必然收斂——但**產線位是收入的乘數**，「每兩級加一個」讓收入變成 `L^1.25`，在 M2 的八級內指數還來不及贏過它：存到下一次擴廠的時間是 338 秒 → **314 秒**（越擴越快）。★ 更值得記的是**斷言的第一版也錯了**：它比「成本倍率 vs 報酬倍率」，而那兩個不可比（一個每級、一個每張訂單，中間差了產線位與工作量），量出來 8.2× vs 13.5×，兩個數字都不是要問的東西 | B2.5 ✅ 改成每三級加一個（338 → 419 秒）；斷言改驗**玩家感覺得到的量**（`_expand_seconds()`），紅得起來 |
| RG-145 | ★★★ **地圖上唯一指出瓶頸的元素，必須說得出瓶頸是什麼**。三態徽章的 `缺料` 把「缺礦砂」與「缺電」畫成同一個橙色倒三角——於是它說得出**誰**在挨餓，說不出**餓的是什麼**。而本作的核心命題就是峰值電力，「我現在缺的是電還是礦」是玩家每分鐘都要答的問題，把它留給頂欄數字正好是 R-3 要對策掉的東西。遊玩測試 P2-1 抓到 | B2.4.8 ✅ 三態改四態（`STARVED` 缺礦砂／`STARVED_POWER` 缺電，橙倒三角 vs 琥珀閃電，形狀與顏色同時不同）；`flow_test`（斷電熔爐）與 `hud_test`（沒礦砂的發電機）**互為對照組**——單獨一條可以靠「永遠回傳同一個值」矇混過去。凡是問「有沒有在餓」一律走 `SessionState.is_starved()` |
| RG-144 | ★★★★ **視覺差異的斷言一律取樣像素，不靠記憶比對兩張圖**。遊玩測試 P2-2 宣稱「階段色調讀不到，兩張 `TL_NAKED` 並排幾乎無差異」並提議把它刪掉。取樣同一片空地之後：準備期 `(19,37,52)`、波次期 `(40,42,47)`，**R 差 21**——色調一直在工作。錯誤來源是**前後分開看兩張圖**（相隔數分鐘），而人眼對絕對色的記憶極差。★ 這是同一族的**第三次**：RG-141（A-3）錯在證據是縮圖、RG-143（B-3）錯在用 grep 推論、這一條錯在**沒有並排也沒有取樣**。共同形狀是「**用了一個比實際結論弱的觀察，然後照結論開了處方**」，而三次的處方都會動到已經正確的東西 | B2.4.8 ✅ P2-2 撤回。**遊玩測試的產出一律要再驗一次才進 backlog**——首跑四條裡有一條是誤判 |
| RG-143 | ★★★★ **用 grep 推出來的缺陷，連「症狀存在」都還沒證明**。美術稽核的 B-3 靠 `grep tabular\|FontVariation\|monospace` 零命中，推論「頂欄每秒橫向抖十次」。量過之後：**同位數換數字本來就不位移**（微軟正黑體的數字已經是等寬的），會位移的只有**跨位數**那一刻，一局發生個位數次。更糟的是它連帶開錯處方——`tnum` 修的是不存在的那一半，對真正的那一半**無效**。`grep` 證明的是「沒有用某個 API」，不是「有那個問題」，中間隔著一個「不用那個 API 也可能沒問題」的假設。★ 與 RG-141 是同一族但更危險：RG-141 錯在證據，這一條錯在**推論** | B2.4.4 ✅ 先寫 `_topbar_selftest()` 量兩種情況再動手；修法改用 §3 自己寫的備案（固定寬度＋右對齊），兩條斷言都紅得起來 |
| RG-142 | ★★★★★ **1290 條斷言，沒有一條看畫面**。B2.4 新增三隻招募塔，資料表／戰鬥／耗電／存檔／招募／音效全部正確，`_draw_nodes()` 的 `match` 卻沒有它們的 case——**GDScript 的 `match` 沒對到就靜靜地什麼都不做**，症狀是那三座塔在地圖上完全隱形（蓋得下去、會開火、會吃電、會被啃，就是看不見）。當批十五支測試全綠，**使用者用眼睛五秒就抓到了**。同一個錯法在 `_codex_lines()` 的字典（`.get(type, "")`）上原封不動又發生一次。**通則：凡是以 type 為鍵、而「查不到」等於「什麼都不做」的表，都必須有一條把該表的鍵集合對照權威清單的斷言** | B2.4.6 ✅ `match` 補 default 分支記錄漏網型別；`TL_CLICKTEST/glyph` 把 13 種全塞進 `s.nodes` 逼 `_draw()` 跑過每個分支，圖鑑說明一起驗。紅得起來（拿掉三個 case → 點名 `ballast,frostreef,longcall`）。同批掃過其餘每一張 type 表（`DEFAULT_PRIORITY`／`PRIORITY_ROWS`＋`GROUP`／音效／`UNIFORM_BUILD`／戰役解鎖）確認無其他破口 |
| RG-133 | ★★★★ **`state_hash()` 不涵蓋 `mods`**，所以「起始局面完全相同」只用雜湊驗會是**假綠燈**。`mods` 是科技的查表結果、不是權威狀態，刻意不進雜湊（§2.4）——而科技正是憲法 B3 要擋的那個東西。紅燈證明時只有 `mods` 那兩條倒、雜湊那兩條全綠 | B2.2 ✅ 雜湊與 `mods` 兩層都斷言；紅燈證明留在測試註解裡 |
| RG-134 | ★★★ **UI 自檢不可以用「第幾顆鈕」定位**。B2.2 在主選單插進「每日挑戰」，索引整個往後挪一格 → `TL_CLICKTEST/title` 去按了科技樹以外的東西（`tech=false`）。**選單順序是會動的**，而測試要驗的是「這顆鈕通到那個畫面」，不是「它排第三」 | B2.2 ✅ 改成依按鈕文字找鈕（`_menu_button(prefix)`），順帶把新入口也納入同一條往返 |
| RG-131 | ★★★★ **音訊是診斷通道，所以音樂裡不可以有前景瞬態**。戰鬥曲的「敲玻璃」動機是一顆金屬敲擊，和 `build_place`（880/1319/1976Hz）、`build_wire` 的扣（1180/1770Hz）同一個音色家族同一個音域，而且是整首唯一的瞬態——耳朵一律把瞬態讀成「剛剛發生了一件事」，然後去找那件事。**它不對應任何事件＝訓練假警報** | B2.1f ✅ 戰鬥期改成完全沒有音樂＋一次開打號令；`audio_test` 量「到滿音量的時間」（號令 ≥100ms、打擊類 ≤12ms），紅得起來（拿舊敲擊當號令 → 5ms，兩條同時倒） |
| RG-132 | ★★★ **跟實時掛鉤的東西不能用「等 N 幀」去等**。`TL_CLICKTEST` 的音樂淡出斷言等 120 幀，而那個視窗沒有垂直同步——120 幀可能只有 0.2 秒，淡出要 1.2 秒，`wave_hush` 恆為 false | B2.1f ✅ 改成等牆鐘時間（上限 2 秒、淡完就跳出）。同一段還踩到「局面的 `spawn_queue` 是空的 → 下一個 tick 就被打回 prep」，phase 要每幀釘住 |
| RG-127 | ★★★★ **量測台自己也要有驗收條件**。`perf_test` 的無盡曲線（`30_TECH_DESIGN.md` §5.2）佈局裡**一顆採集器都沒有** → 全網供給恆為 0 → 解算器的傳播迴圈一次都沒跑過。量到的是建索引的成本，卻被當成「解算器在大圖上的成本」寫進技術文件，**低估約 50 倍**（219 節點：27 ms vs 真值 1,449 ms）。「效能沒問題」和「什麼都沒跑」在輸出上長得一模一樣 | B2.1e ✅ 量測台改蓋採集器，並補斷言 `供給 > 0`（三個規模各一條） |
| RG-128 | ★★★★ **一個迭代只算一次的估計量，必須有辦法在它過期時就地修正**。可送達需求從「每次推送重算 DFS」換成「每迭代算一次」之後，中繼拿的是上一刻的快照：**發電機已經吃飽了，上游還以為它餓著**，於是把三分之二的餘量原路推回去。淺灘示範佈局掉 55% 產出（`delivered_total` 4412.8 → 2008.4） | B2.1e ✅ 節點吸收需求時沿祖先鏈 `O(深度)` 扣減；`flow_test._satisfied_nodes_stop_attracting()`，紅得起來（拿掉扣減 → 核心滿足率 1.0 → 0.43、2.28 單位彈回去） |
| RG-129 | ★★★ **回歸測試的佈局要能真的走到那條路徑**。RG-128 的第一版斷言把採集器擺在 id 1（＝生成森林的根），餘量往樹**下**走，根本不會經過那個吃飽的節點——**拿掉修法仍然全綠**。改成「採集器在離核心最遠端、中間隔兩個中繼、吃飽的節點優先權高於核心」才紅得起來 | B2.1e ✅ 兩次紅燈證明（一次假綠、一次真紅）都記在測試的註解裡 |
| RG-130 | ★★ **斷言要看它想證明的性質，不要看某一格的快照**。`hud_test` 的「儲槽已充飽」寫在**通關那一格**上——那一刻塔正在放電打最後幾隻敵人。B2.1e 讓通關晚了 45 個 tick，同一個局面就變紅了（儲槽第 299 tick 就充飽過，末值 283.2 是放電中的快照） | B2.1e ✅ 改在充飽的那一格驗；中繼徽章兩個局面都驗（和 RG-120 同一種毛病） |
| RG-120 | ★★★ **確定性的閘門不可以有「某些種子才過」的行為**。`TL_CLICKTEST/endless` 固定點小地圖左上角驗平移——前一步的箭頭跳轉若剛好把視野帶到左上（`_pan` 已夾在 0,0），再點左上當然不會動：**產品沒問題，測試紅掉**（`TL_SEED=42` 實際踩到）。改點「離目前視野最遠的那個角」 | B2.1c ✅ 五個種子（預設／42／7／1337／999）全 PASS |
| RG-116 | ★★★ **均勻分佈要靠分層，不能靠隨機**。均勻隨機取樣**必然**結塊——「一坨擠在一起、其他地方都是空的」是隨機的性質，不是 bug。程序生成裡凡是要「散得開」的東西（礦點、跨越點、敵人出生點），一律切段取樣 | B2.1c ✅ 礦脈改沿路徑索引分層；空檔 16.8→5.8 |
| RG-117 | ★★ **沿索引分層不等於空間上散得開**。路徑轉角處，相隔十幾個索引的兩段在地圖上可能只差 5 格 → 兩條脈疊成一坨。索引空間的均勻要再加一道**地圖座標**的隔離 | B2.1c ✅ `_anchor()` 對已放置的礦點做 `VEIN_APART` 距離檢查 |
| RG-118 | ★★★ **量錯維度的指標會給假綠燈**。「死列 0%」對**由角到角的對角路徑**必然是綠的——它碰得到每一列，卻碰不到對角的兩個角落。實測**死區面積 45%**，而列的指標完全看不到 | B2.1c ⚠ 已量到，**未解**：三條路寫進 `10_GDD.md` §7.10 待決策 |
| RG-119 | ★★ **分層取樣要「段內掃描」而不是「抽一格就放棄」**。抽到的位置放不下時直接跳過整段 → 那一段變成一個洞，而分層的全部意義就是不要有洞 | B2.1c ✅ 段內依序掃過所有候選；掃描次數由段寬決定，與種子無關（確定性） |
| RG-111 | ★★ **UI 浮層要在建造層之前攔截點擊**。小地圖蓋在地圖上面，不先攔的話點它會在**它底下那一格**蓋出東西——而玩家看不出為什麼，因為節點生在畫面外 | B2.1b ✅ `_guide_click()` 先行；`mini_safe` 用哨兵字串驗（看「節點數沒變」的第一版是假斷言，紅不起來） |
| RG-102 | ★★★ **程序生成的每一張圖都要玩得動**。手作圖有設計者的眼睛把關「礦砂別全在對岸」，生成器每天會擲出幾千張沒有人看過的圖。硬指標：**至少 3 個礦點不必過橋就到得了核心**（`10_GDD.md` §7.10 不變量 5），且**構造性達成**（先塞滿核心那一側的配額）而非撒完再檢查 | B2.1a ✅ `endless_test` 掃 300 個種子 |
| RG-103 | ★★ **生成器的幾何用截圖判不出來**。說明浮層要放下第一個節點才收、格子只有 32px——B2.1a 的兩個真缺陷（**轉角上的橋是廢的**、**路徑貼到 y=1 讓「退開 2 格」做不到**）都是把 `Maps.path_of()` 印成 ASCII 才看到的。**新增／修改生成器一律先讀 ASCII 再讀截圖** | B2.1a ✅ 兩條都已改成構造性規則並各配斷言（拿掉轉角排除 → 300 張圖 108 座廢橋） |
| RG-105 | ★★★ **敵人的差異化只能用「不增加行進軸 footprint」的手段**（壓扁、換頂點數、換明度），不得用拖尾或沿行進方向拉長。波次最密時間距是 **0.6 格**，任何往身後或往前延伸的東西都會壓到鄰居——一列分得開的敵人會糊成一條連續香腸，**把「看不出差異」修成「看不出有幾隻」**。新增／修改敵人外觀一律跑 fit 倍率的改前／改後 A/B | B1.6.3 ✅ 拖尾與拉長各被否決一次，實看紀錄在 `20_ART_DIRECTION.md` §1.7 |
| RG-106 | ★★ **fit 倍率下敵人只有 16–24px，輪廓差在這個尺寸會死掉**。六邊形勉強讀得到、壓扁幾乎讀不到——**明度結構才活得下來**。任何「靠形狀分辨」的設計都必須在 fit 倍率驗收，不得只看放大的近照 | B1.6.3 ✅ 三種敵人各有一個明度結構；新增 `tide.bright` token |
| RG-107 | ★★ **短命特效要挑 `ttl` 走到一半的那一格驗收**。開火特效只活 3 tick（0.3s），`TL_SHOT` 凍結在剛發射那一格時濺射環半徑是 0——**畫面上什麼都沒有，看起來就像沒做**（RG-100 脈動元素的同一類陷阱） | B1.6.3 ✅ 驗收 tick 記在 §1.7 |
| RG-104 | ★★★ **斷言要問「畫面真的用了嗎」，不是「欄位被設上了嗎」**。B2.1a 第一版 clicktest 印 `endless=true` 而遊戲開的是淺灘——`Main._enter()` 先 `add_child()`（`_ready()` 當場跑完 setup）才 `setup.call()`，欄位是在畫面已經用錯地圖之後才被設上的。**凡是「呼叫端要在 `add_child()` 之前指派」的欄位，斷言一律看下游狀態** | B2.1a ✅ `_enter()` 改為 setup 先行；斷言改看局面地圖的 `endless` 旗標 |

---

## 6. 里程碑驗收清單

### G1 原型
- [ ] L1–L4 全過；核心循環從頭到尾可玩
- [ ] 可讀性驗收通過（30 秒指出瓶頸）
- [ ] 樂趣假設判定已寫並交使用者
- [ ] **使用者實玩後確認：「明知有風險仍選擇再擴一條產線，且破防後想立刻重來」**

### G2 Alpha（功能完整）★ 2026-08-10 通過（B2.8）

- [x] GDD §3 系統清單逐項對照：每個系統可玩、有測試鉤子 → 下表
- [x] 存檔：新開局／存／讀／舊版遷移 全過 → `save_test` 83 項（**磁碟往返這次真的跑了**，RG-160）
- [x] §2 的「設計承諾」斷言全部生效 → 補上兩條缺口（見下）
- [x] 零阻斷、零嚴重 bug；Major 以下已登記 → 本批修掉 RG-159（Major），其餘見登記簿

#### §3 系統逐項勾稽

| § | 系統 | 里程碑 | 實作 | 測試 | 畫面鉤子 |
|---|---|---|---|---|---|
| 3.1 | 流量網路 ★核心 | M0 | `sim/FlowNetwork.gd` | `flow_test` 50 | `TL_SIM`／`TL_NAKED` |
| 3.2 | 建造系統 | M0 | `sim/Build.gd` ＋ `game/BuildController.gd` | `build_test` 79 | `TL_CLICKTEST`（battle） |
| 3.3 | 塔（角色） | M0 | `data/NodeDefs.gd` ＋ `sim/Combat.gd` | `combat_test` 95 | `TL_FOCUS` 特效近照 |
| 3.4 | 波次與壓力 | M0 | `sim/Tide.gd`／`Score.gd`／`game/BattleController.gd` | `tide_test` 47・`hud_test` 73 | `TL_DEMO_TICKS` |
| 3.5 | 敵人 | M0 | `data/Enemies.gd` | `combat_test`・`tide_test` | 同上 |
| 3.6 | 科技樹 | M1 | `data/Tech.gd` ＋ `screens/Tech.gd` | `tech_test` 56 | `TL_PANEL=tech` |
| 3.7 | 藍圖庫 | M2 | `sim/Blueprint.gd` | `blueprint_test` 51 | `TL_CLICKTEST`（battle 的藍圖鈕） |
| 3.8 | 潮汐公司 | M2 | `meta/TycoonSim.gd` ＋ 兩個畫面 | `tycoon_test` 64 | `TL_PANEL=tycoon` |
| 3.9 | 名冊與招募 | M2 | `data/Roster.gd` ＋ `screens/Roster.gd` | `roster_test` 83 | `TL_PANEL=roster` |
| 3.9† | 成就與公司等級 | M2 | `data/Achievements.gd` ＋ `screens/Achievements.gd` | `progress_test` 136 | `TL_PANEL=achievements` |
| 3.10 | 排行榜與每日挑戰 | M2 | `sim/Daily.gd` ＋ `screens/DailyScreen.gd` | `daily_test` 42 | `TL_PANEL=daily` |
| 3.11 | 難度層（前 3 層） | M2 交 | `data/Difficulty.gd` ＋ `screens/Tiers.gd` | `difficulty_test` 65 | `TL_PANEL=tiers` |
| 3.12 | 變現系統 | **M4** | — | — | — |

† **成就在 §3 沒有自己的一節**——它在 §3.9 只是「聲望券來源」的一個項目，而 B2.7 把它做成了一個有 20 條規則、一個畫面、一個推導出來的公司等級的系統。規格寫在 §7.15。**這是 G2 勾稽找到的文件缺口**，已在 §3.9 補一行指路。

#### §2 設計承諾的兩個缺口（本批補上）

| 承諾 | 之前 | B2.8 |
|---|---|---|
| **有測試鉤子時不寫真存檔**（鐵律 1） | ⚠ **一條斷言都沒有**，只有一行 `print`。全案最重要的安全規則靠自律 | ✅ `TL_CLICKTEST/title` 的 `persist_off`——那支自檢每次都跑在鉤子底下，是這條規則天生就該在的地方。已證明它紅得起來（把斷言反過來寫 → FAIL） |
| **紅線 R1／R2** | ⚠ 沒有測試。變現排 M4，「還沒有東西可以違反它們」 | ✅ `m2_test._red_lines_still_hold()`：R2 掃 schema 全部欄位名的禁字（stamina／lives／retries／plays_today…）、R1 走真正的局末結算路徑（0 星 0 波）斷言星、紀錄、材料、等級、研究數據一項都不減。**等到有東西違反再寫，就是等到玩家先看到它** |

#### 內容矩陣：M2 欄有 6 列沒填滿（**不是 G2 的判準**）

`m2_test` 每次跑都印出實測值：

| | 角色 | 生產節點 | 敵人 | 戰役 | 科技 | 無盡佈局 | 難度層 | 成就 | 訂單 | 廠等 |
|---|---|---|---|---|---|---|---|---|---|---|
| M2 目標 | 16 | 11 | 12 | 15 | 30 | 3 | 3 | 20 | 8 | 10 |
| 實際 | **8** | **5** | **3** | **5** | **12** | **1** | 3 ✔ | 20 ✔ | 9 ✔ | **8** |

**內容矩陣是 G3 的判準，不是 G2 的**（本節上方的清單：G2 問系統、G3 問內容）。而 §2 生產計畫裡填內容的批次（B3.1–B3.6）**直接跳到 M3 的數字**，所以 §5 的 M2 欄是一個沒有任何批次交付的中間目標。**這是排程的事實，不是缺陷**——記在這裡是為了不讓它靜靜地變成「以為填過了」。

「做了但到不了的內容」則是 **0 項**（`m2_test._everything_is_reachable()`：每一種可蓋節點、每一隻敵人、每一隻角色、每一條成就的計量都有路走到）。

#### 已知未過、帶進 M3 的

| 項目 | 狀態 |
|---|---|
| **RG-8 渲染** | 610 節點的無盡大圖單 tick **19.5 ms**（超出 8 ms 上限）。模擬那一半 B2.1e 已修（974×），**渲染那一半沒碰**。屬 G3 的效能判準，排 M3 |
| **R-14 陌生人驗收** | 🟡 降級留存（使用者拍板不找真人）。B3.6 教學批重新評估 |
| **R-15／R-16** | ⚪ 已登記待觸發（鏡頭 module／`Ledger` 拆分） |

### G3 Beta（內容完整）
- [ ] GDD §5 內容矩陣填滿；全內容可到達（沒有做了但進不去的內容）
- [ ] 回歸清單全跑一輪
- [ ] 效能達 `30_TECH_DESIGN.md` §5 預算；長時間遊玩無退化
- [ ] 零阻斷；Critical 清零；Major 清算（修掉或使用者同意帶病出貨）

### G4 Gold
- [ ] 發布檢查表全勾（見 `game-studio` skill 的 release reference）

---

## 7. 整合 QA（多線合流時）

除 L1–L3 外，加驗交界處：
- [ ] 共用狀態（兩線都讀寫的資料）一致性
- [ ] 訊號／事件接線（A 線的事件 B 線聽得到）
- [ ] 存檔欄位無撞名、遷移順序正確
- [ ] UI 疊放與焦點（各線新增的面板互不遮擋、不搶焦點）

---

## 8. 遊玩測試腳本（★ B2.4.6 新增）

### 8.0 為什麼要有這一節

**RG-142**：B2.4 交付時十五支測試 1290 條斷言全綠，而三隻新塔在地圖上是**隱形的**。
使用者開起來五秒就看到了。原因不是斷言寫得不好，是**斷言驗的是數值與流量，
沒有一條坐在玩家的位置上**。

自動化擋得住「算錯」，擋不住「沒畫」「看不懂」「不好玩」「進不去」。
這一節就是後面那四類的捕蟲網。

> **這一節不是「玩一玩看看」。** 每一場都有**一個問題**、**一條停止規則**，
> 以及**事先寫好的缺陷判準**——判準要在開始玩之前就寫下來，否則玩完只會得到
> 「感覺還行」，那不是測試結果。

---

### 8.1 三條紀律

1. **先寫判準再玩。** 每一場的「缺陷判準」是預測：看到就是缺陷，沒看到就是通過。
   玩到一半才發明的標準不算數（那是在替結果找理由）。
2. **一場只問一個問題。** 想同時驗五件事的那一場，五件都會驗得很淺。
3. **能自動化的不要放進來。** 只要一條判準寫得出程式碼，它就該變成斷言，
   然後從這張表上刪掉。**這一節應該隨時間變短，不是變長。**

---

### 8.2 缺陷回報格式（一行就好）

```
[場次] 我做了什麼 → 我預期什麼 → 實際發生什麼 ｜ 種子/關卡/tick ｜ 截圖
```

例：`[P3] 蓋了長哨 → 應該看到一座塔 → 那一格是空的 ｜ 無盡 seed 42 ｜ shot.png`

三個欄位缺一不可：**沒有「我預期什麼」的回報無法判斷是缺陷還是設計**。

---

### 8.3 場次表

| 場次 | 問題 | 時間 | 判官 |
|---|---|---|---|
| **P0** | 一個沒看過這遊戲的人，十分鐘後在做什麼？ | 10 分 | **陌生人**（R-14 未解，目前不得標記通過） |
| **P1** | 戰役五關把該教的教會了嗎？ | 40 分 | 使用者 |
| **P2** | 「峰值電力」這個核心命題，玩家讀得出來嗎？ | 15 分 | 使用者 ＋ agent（`TL_NAKED`） |
| **P3** | 每一種節點在戰場上都看得見、認得出、值得蓋嗎？ | 15 分 | 使用者 |
| **P4** | 程序生成的圖每一張都玩得動嗎？ | 20 分 | 使用者 |
| **P5** | 每日挑戰與雙排行榜的承諾兌現了嗎？ | 15 分 | 使用者 |
| **P6** | 外層系統（名冊／科技／藍圖）進得去、看得懂、關得掉嗎？ | 20 分 | 使用者 |
| **P7** | 中離、讀檔、改設定之後，遊戲還是同一局嗎？ | 10 分 | 使用者 |
| **P8** | 不看數字，畫面自己說得出局勢嗎？ | 10 分 | agent（`TL_NAKED`） |
| **P9** | 聲音是診斷通道還是噪音？ | 10 分 | 使用者 |
| **P10** | 故意亂搞會壞在哪裡？ | 20 分 | 使用者 |

---

#### P0　冷啟動十分鐘（最高優先，目前無法通過）

**前置**：全新存檔（`TL_*` 一律不下，或先備份 `user://`）。**不給任何說明。**
**做**：開遊戲，計時十分鐘，中途不提示。
**缺陷判準**：

- [ ] 十分鐘後**還沒蓋出第一條完整產線**（採集器→核心）
- [ ] 曾經**卡在某個畫面不知道怎麼回去**超過 30 秒
- [ ] 出現過「我不知道現在該做什麼」的停頓超過 60 秒
- [ ] 說得出「我輸了」但**說不出為什麼輸**
- [ ] 把塔當成生產建築蓋，或反過來

> ⚠ **這一場設計者自己跑沒有意義**（§0.1）。在拉到真人之前，本場一律標記
> 「未驗證」，不得因為 agent 或使用者跑過就打勾。

---

#### P1　戰役五關（教學曲線）

**前置**：全新存檔，從第 1 關依序打到第 5 關，**每關都要真的打贏才進下一關**。
**做**：一關一關打。每關結束立刻寫下「這關逼我學會了什麼」。
**缺陷判準**：

- [ ] 有任何一關**寫不出「逼我學會了什麼」**（＝那一關沒有教學功能，是填充）
- [ ] 有任何一關**用上一關的打法照抄就過**（＝難度沒有上升）
- [ ] 有任何一關**第一次就過**且毫無驚險（太簡單）或**試五次以上還過不了**（太難）
- [ ] 某一關解鎖的新節點，**打完那一關還是沒用過一次**
- [ ] 參考解（`campaign_test` 跑的那一份）明顯不是人類會想到的擺法

**記錄**：每關的嘗試次數、通關時剩餘核心血量、用了哪些節點。

---

#### P2　電力數學（核心命題）

**前置**：第 3 關或無盡，蓋到「導管變色」為止。
**做**：故意把自己推進**供不應求**，然後只用畫面上的資訊把它救回來。
**缺陷判準**：

- [ ] **不看頂欄數字**時，說不出「現在是缺電還是缺礦」
- [ ] 看得出缺電，但**說不出是哪一條導管卡住**
- [ ] 拉優先權滑桿之後，**畫面上沒有任何東西當場改變**
- [ ] 儲槽蓋下去之後，**看不出它有沒有在充電**
- [ ] 加粗幹線之後，**看不出差別**

> ★ 這一場要跑兩次：一次正常玩，一次 `TL_NAKED=1` 拍圖交給 agent 判（§0.1 規則 1）。

---

#### P3　節點可見性與辨識度（RG-142 的常設防線）

**前置**：`TL_CLICKTEST=1 TL_SHOT=... TL_FOCUS="23,10,2"` 拍那一排 13 種節點的合照
（自檢會把每一種各放一個在 `y=14` 那一列，見 `Battle._glyph_selftest()`）。
再開一局無盡，**把手上有的每一種都真的蓋一個**。

**缺陷判準**：

- [ ] 合照上有任何一格是**空的**（＝RG-142 復發，自檢應該先紅）
- [ ] 兩種節點在 **fit 倍率**下**必須靠顏色才分得開**（§1.6 明文禁止）
- [ ] 蓋下去之後，**圖鑑那一行說明是空白的**
- [ ] 塔開火時**沒有聲音**，或三隻招募塔的火力聲和既有的分不出來
- [ ] 有任何一種**蓋了之後想不出它存在的理由**（＝設計問題，不是 bug，另記）

---

#### P4　無盡（程序生成）

**前置**：至少五個不同種子，各玩到第 10 波或陣亡。
**缺陷判準**：

- [ ] 有任何一張圖**開場找不到能蓋的礦點**
- [ ] 有任何一張圖**必須過橋才拿得到礦砂**（違反 §7.10 不變量 5，`endless_test` 應該先紅）
- [ ] 路徑貼著地圖邊緣，導致「退開 2 格」做不到（RG-103）
- [ ] 出現**通不到任何地方的橋**（RG-103）
- [ ] 小地圖上看得到的東西，**捲過去卻不在那裡**
- [ ] 波數上去之後**掉幀**到操作變黏（RG-8 的渲染那一半仍未通過，預期會中）

**記錄**：種子、死在第幾波、最後的節點數。

---

#### P5　每日挑戰與雙排行榜

**缺陷判準**：

- [ ] 同一天重開遊戲，**種子變了**
- [ ] **統一配置榜**的起始配置受到科技樹／名冊／課金影響（憲法 B3 破防，`daily_test` 應該先紅）
- [ ] 統一配置榜的建造欄裡出現**招募來的三隻**
- [ ] 離線後回來，**沒有任何註記**說明錯過了哪幾天
- [ ] 兩個榜在畫面上**分不出來是兩個榜**

---

#### P6　外層系統（名冊／科技樹／藍圖）

**缺陷判準**：

- [ ] 從主選單**找不到**其中任何一個入口（B2.4 的原始缺陷就是這一條：「我沒有看到抽卡畫面」）
- [ ] 有券卻**看不出來自己有券**
- [ ] 招募到的角色**沒有出現在建造欄**
- [ ] 科技樹買了東西，**局內感覺不到差別**
- [ ] 藍圖存了之後**放不回去**，或放回去的形狀和存的時候不一樣
- [ ] 任何一個畫面**按 ESC 回不去**
- [ ] tycoon（B2.5 之後）**超過兩個畫面**（防膨脹硬指標，每批必查）

---

#### P7　中離與存檔

**做**：局內按 ESC → 開設定 → 改音量／動效／解析度 → 回到局內 → 存檔 → 關遊戲 → 重開 → 讀檔。
**缺陷判準**：

- [ ] 開選單時**時間停了**（設計是不停，面板上也寫著）
- [ ] 改完設定回到局內，**局面變了**（敵人位置、資源、tick 數）
- [ ] 讀檔後**有任何一個數字對不上**
- [ ] 改解析度之後**有東西跑到畫面外**
- [ ] 全螢幕切回視窗，**版面沒有跟著縮放**（手機移植的前提，`20_ART_DIRECTION` §1.2）

---

#### P8　裸眼可讀性（agent 判）

**做**：`TL_NAKED=1` 拍四張圖（準備期／交戰中／缺電／快輸了），**不給數值標籤**，
逐張回答三個問題：現在是哪個階段？瓶頸在哪？我下一步該做什麼？

**缺陷判準**：

- [ ] 任何一張**答不出階段**
- [ ] 任何一張**答不出瓶頸**
- [ ] 侵蝕暈的邊界**數不出格數**（B2.4.5 之後應該數得出來）
- [ ] 三態徽章與交戰環在同一顆節點上**糊在一起**

> ⚠ **本場有一個已知的擺不出來的狀態**（2026-08-06 首跑發現）：**缺電**與
> **快輸了**這兩張圖**沒有鉤子擺得出來**——`TL_DEMO_TICKS` 跑的是參考解，而參考解
> 是調校過會贏的。目前只驗得到「準備期」「交戰中」兩張。
> **這是 §8 自己的缺口，不是通過。** 補法有二：① 給 `TL_*` 加一個「照著腳本
> 打壞自己」的鉤子；② 用無盡跑到陣亡前那一格。在補上之前，P8 一律標記
> **「半場」**，不得整場打勾。

---

#### P9　音訊

**缺陷判準**：

- [ ] 出現**不對應任何事件**的前景瞬態（RG-131：耳朵會去找那件事）
- [ ] 準備期與交戰期**聽不出切換**
- [ ] 同時十座塔開火時**糊成一片**或爆音
- [ ] 靜音之後**還有聲音漏出來**

---

#### P10　惡意玩法（找碴）

**做**：故意做每一件不該做的事。

- 在準備期最後 0.5 秒瘋狂點擊建造／拆除
- 拖曳導管拖到畫面外、拖到自己身上、拖到敵人身上
- 把所有優先權拉到同一格，再全部拉回
- 蓋滿整張圖直到沒錢，然後全部拆掉
- 連按 ESC、快進、提前召喚各二十次
- 讀一個手動改壞的存檔（改 `SAVE_VERSION` 成很大的數字）

**缺陷判準**：

- [ ] 任何一項導致**閃退、卡死、或畫面白掉**
- [ ] 資源出現**負數**
- [ ] 拆掉建築後**電力數字沒跟著變**
- [ ] 壞存檔導致**無法進入遊戲**（應該回退到全新存檔並說明）

---

### 8.4 什麼不該進這一節

下列全部已有自動化閘門，**不要在遊玩測試裡重複驗**（浪費的是最貴的資源：人的注意力）：

| 已自動化 | 由誰擋 |
|---|---|
| 數值、流量、傷害、匯率 | `flow`／`combat`／`hud` 等十五支測試 |
| 確定性（同種子同結果） | `determinism_test` |
| 存檔往返與遷移 | `save_test` |
| 按鈕點得到、扣得到款、捲得到底 | 八個畫面的 `TL_CLICKTEST` |
| **每一種節點畫得出圖形、圖鑑有說明** | `TL_CLICKTEST/glyph`（RG-142） |
| 生成圖的不變量（礦點可達、無廢橋） | `endless_test` 300 種子 |
| 音檔存在、格式正確、峰值合理 | `audio_test` |

**反過來也成立**：這一節裡任何一條判準，只要哪天寫得出程式碼，就把它搬到上表去，
然後從 §8.3 刪掉。**這張表變長代表自動化沒跟上。**

---

### 8.5 什麼時候跑

| 時機 | 跑哪幾場 |
|---|---|
| 每一批交付後 | **P3**（可見性）＋ 該批直接動到的那一場 |
| 每個里程碑閘門 | 全部十場 |
| 動到渲染層 | P3 ＋ P8 |
| 動到程序生成 | P4 |
| 動到存檔結構 | P7 ＋ P10 最後一項 |
| 準備上架 | 全部十場，**P0 必須拉到真人**（R-14） |
