# 《潮與線》生產計畫

> 製作人的儀表板。里程碑、批次 backlog、風險登記簿、砍案清單。
> 上位文件：`00_CONCEPT.md`、`10_GDD.md`。
> 狀態：**v0.2**（依 `10_GDD.md` v0.3 收斂 M0 各批 DoD；新增 R-14）　|　最後更新：2026-07-27

---

## 1. 里程碑

| 里程碑 | 定義 | 出關標準（閘門） | 狀態 |
|---|---|---|---|
| **M0 原型** | 核心循環可玩，佔位美術，**只做塔防局內** | **G1**：使用者實玩後確認樂趣假設成立——「明知有風險仍選擇再擴一條產線，且破防後想立刻重來」 | ⬜ 未開始 |
| **M1 垂直切片** | 一條完整體驗路徑打磨到最終品質 | 陌生人可無指導遊玩 10 分鐘並理解自己為什麼贏／輸　**⚠ 目前無執行者（R-14）——在拉到真人之前不得標記通過** | ⬜ |
| **M2 Alpha** | 功能完整（內容可缺） | **G2**：`10_GDD.md` §3 系統清單全部實作 | ⬜ |
| **M3 Beta** | 內容完整 | **G3**：`10_GDD.md` §5 內容矩陣填滿 ＋ 零阻斷級 bug ＋ 回歸全過 | ⬜ |
| **M4 Gold** | 出貨 | **G4**：發布檢查表全勾 | ⬜ |
| **營運** | 長線 | `60_LIVEOPS_PLAN.md` 就緒（M3 前產出） | ⬜ |

### 里程碑決策點

- **G1 未過**：不進 M1。優先調整順序為 ①準備期長度 ②塔耗能係數 ③優先權面板的操作成本 ④（若仍不成立）重新檢視「塔吃能量」這條核心機制。
- **M1 結束時複審 tycoon 的膨脹狀況**：稽核指標是「UI 是否仍只有兩個畫面」。超標即砍回去。
- ~~M3 前拍板商業模式~~ → **已於 2026-07-27 拍板**：免費遊玩＋內購，Windows 版無廣告，廣告保留至手機移植版。詳見 `10_GDD.md` §3.12。
- **M3 平衡校準必須把「等級軸滿級 × 各難度層」跑過一輪**（`TL_SIM`），這是 P2W 不碾壓內容的唯一保證（R-12）。

---

## 2. 批次 backlog

> **一批 = 一個可出貨單位**：版本號 bump ＋ CHANGELOG ＋ commit ＋ **可玩 build**。
> 新點子一律先進本節末尾或砍案清單，**不插隊進當前批次**。

### M0 原型（7 批）— 唯一目標：驗證樂趣假設

| 批次 | 內容 | 完工定義（DoD） | 依賴 |
|---|---|---|---|
| **B0.1** 地基 | Godot 專案建立、autoload 註冊、`Palette.gd`／`Motion.gd`／`Shapes.gd`、**測試鉤子全套**（`TL_SHOT`／`TL_SHOT_DELAY`／`TL_PANEL`／`TL_MUTE`／`TL_SEED`／`TL_SIM`／**`TL_NAKED`**）、`SaveService` 骨架與 `SAVE_VERSION`、空的 `tests/` 五支測試檔 | 空專案能開起來顯示標題＋版本號（`0.1.0`）；`TL_SHOT` 能截圖並自動退出；`TL_MUTE` 生效；有鉤子時不寫真存檔；五支測試檔可執行（即使斷言為空） | — |
| **B0.2** 流量網路 ★ | `sim/FlowNetwork.gd` 純函式解算器：供需收集、容量削減、**依「節點類型」的優先權加權分配**、**儲槽充放電語意**（盈餘為消費者／赤字為供給者，速率受自身導管 cap 約束）、環路 3 次迭代、flow 回寫 | `flow_test.gd` 全過，涵蓋：單線滿載、容量削減比例、三方競爭的優先權分配、環路收斂、飢餓時的 satisfaction 計算、**儲槽充放電受 cap 約束**、**無全域資源池**（斷開儲槽即無緩衝） | B0.1 |
| **B0.3** 建造與地圖 | 32px 網格、地圖資料結構（礦點／敵人路徑／**跨越點**／核心）、放置節點、拉導管（含中繼轉折）、**建造合法性檢查**（路徑格禁節點；導管僅可過跨越點）、拆除返還、**即時預覽（含總耗能變化）**、**導管吞吐局內升級** | 能在測試圖上蓋出「採集器→發電機」並看到能量開始流動；**第一台發電機的輸出線立刻滿載變色**（cap 10 < 20，見 GDD §7.2）；升級幹線後流量提升；預覽在放置前顯示 `+X/秒` 與 `總耗能 →Y/秒`；試圖在路徑格放節點會被擋下並說明原因 | B0.2 |
| **B0.4** 波次與敵人 | 波次表資料、敵人沿路徑推進、**walk-by 破壞（永不停步、每 tick 傷害相鄰 1 格、橋上導管免疫）**、三種敵人（漂蟲／甲殼／熾泳）、核心生命與失敗判定（**唯一會讓敵人駐足的目標**）、準備期與快進 | 貼路徑的導管會被行進中的敵人打斷造成產能中斷；**橋上的導管全程無損**；敵人不因任何建築停步（丟一排中繼擋不住任何一隻）；核心歸零觸發失敗畫面；準備期可快進 4× | B0.3 |
| **B0.5** 塔與取捨 ★ ✅ | 四隻角色（錨／稜鏡／潮鳴／回收者）、**交戰耗能（待機 0）**、能量不足時射速線性下降、**全域擊殺回收 25% 礦砂**、**回收者能量回收（價值 ×60% ×5）**、**依類型的優先權面板（含儲槽一格）** | 蓋滿塔會讓熔爐/採集速度可見地下降；拉動優先權滑桿能改變誰先餓死；回收者在擊殺密集處能明顯回補電力；`combat_test.gd` 全過（含匯率斷言） | B0.4 |
| **B0.6** HUD 與可讀性 | 局內 HUD（`10_GDD.md` §6.2 **全畫面地圖＋浮層／抽屜**）、**頂欄能量顯示供給/需求＋儲槽另列**、導管粗細／顏色隨流量、節點三態徽章、能量列警示脈動、**提前召喚（含獎勵倍率顯示）**、局末結算、**`TL_NAKED` 生效** | **可讀性驗收（改良）**：以 `TL_NAKED=1` 產出**無任何數值標籤**的截圖，交 Opus 5 指認瓶頸節點——答對才算過（見 `30_TECH_DESIGN.md` §4.1）；浮層收合後地圖 100% 可見；所有可點元素命中區 ≥ 44px；提前召喚按鈕常駐顯示倍率；結算顯示波次／產能積分／獲得 | B0.5 |
| **B0.7** M0 打磨與 G1 | 一張手作測試地圖（**一屏可見**、含 2–3 個跨越點）、`determinism_test.gd` 通過、截圖驗證、**交付可玩 build 給使用者實玩** | 使用者實玩 20 分鐘以上並回覆樂趣假設是否成立 → **G1 閘門**（判準見下方「G1 的判官」） | B0.6 |

> **★ G1 的判官（2026-07-27 定案）**
> **樂趣由使用者本人判，agent 不判。** agent（Opus 5；Fable 已不再使用）只負責**程式碼完整性與可讀性**——L1–L3、測試、`TL_NAKED` 的視覺編碼檢定。
> 這條分工的兩個直接後果，已分別落地：① B0.6 的 R-3 驗收改用 `TL_NAKED`（否則 agent 讀數字必過，發假綠燈）；② **M1 閘門「陌生人可無指導遊玩 10 分鐘」目前沒有執行者**——見 R-14。

### M1 垂直切片（7 批）

| 批次 | 內容 | DoD |
|---|---|---|
| **B1.1** 第三資源 | 合金、熔爐、高階節點與塔的合金造價 | 三種資源網獨立解算；`flow_test` 擴充通過 |
| **B1.2** 戰役框架 | 5 關手作地圖（**全部一屏可見**）、關卡選擇畫面、星等評分、關卡獎勵 | 5 關可依序遊玩並解鎖；每關有明確的新機制或新地形挑戰；**★ 第 1–2 關以「新手可通關」為硬性驗收，且只用關卡參數達成（敵人數量／礦點密度／核心距離／跨越點座數／該關宣告的準備期）——不得使用任何隱藏乘數**（`10_GDD.md` §7.7） |
| **B1.3** 科技樹 | 12 節點（`10_GDD.md` §7.8）、研究數據結算與消耗 | 局末產出研究數據；科技效果實際生效；**能量效率三節點作用於「交戰耗能」**（不是已歸零的待機耗能）；B2 上限（+35%）以測試斷言鎖住 |
| **B1.4** 外框完整化 | 標題、主選單、設定畫面（音量／解析度／減少動態效果／鍵位）、存檔完整化與遷移分支 | `save_test.gd` 涵蓋 sv1→sv2 遷移；設定變更即時生效並存檔 |
| **B1.5** 音訊 | BGM 分層（準備期／戰鬥期無縫切層）、主選單曲、12 個 SFX | 靜音路徑生效；切層無爆音；音量滑桿分軌可用 |
| **B1.6** 美術打磨 | 動效時長階套用、爆炸的克制表現、色彩層次校準、`reduce_motion` 全覆蓋 | `20_ART_DIRECTION.md` §6.3 一致性檢查全過（無硬編碼色值／階外間距／階外字級） |
| **B1.7** M1 驗收 | 壓力情境效能測試、回歸、交付可玩 build、**R-14 決斷**（本批要拍板：找不找真人） | **陌生人可無指導玩 10 分鐘並說得出自己為什麼輸**。⚠ 目前無執行者——Opus 5 可代跑的只有「資訊完備性」（該給的有沒有給、術語會不會勸退、失敗後知不知道原因），**「無指導可玩」這件事在拉到真人之前不得標記通過**（R-14） |

### M2 Alpha（8 批）

| 批次 | 內容 | DoD |
|---|---|---|
| **B2.1** 無盡模式 | 程序地圖生成（1→3 種佈局，**可大過一屏**）、**小地圖＋畫面邊緣瓶頸／受損指引箭頭＋一鍵跳到問題點**、無限波次曲線、個人最佳紀錄 | 同種子必生成相同地圖；波次曲線符合 `10_GDD.md` §4.2；**在可滑動的大圖上，瓶頸節點即使不在畫面內也能在 30 秒內被找到**（R-3 在大圖模式的對應驗收） |
| **B2.2** 每日挑戰雙榜 | 固定日種子；**統一配置榜**＋**自由配置榜**（同地圖、同波次，只有初始化參數不同）；本機成績歷史、離線榜 UI | 同日同種子；**統一配置榜的起始 `SessionState` 在三組不同進度／課金狀態的存檔下完全相同**（以測試斷言鎖住）；兩榜共用同一套模擬與計分，未分岔成兩套邏輯 |
| **B2.3** 藍圖庫 | 框選存藍圖、一鍵展開、成本預估與缺口提示、藍圖槽數量 | 存取往返正確；展開時資源不足有明確提示而非靜默失敗 |
| **B2.4** 名冊與招募 | 角色圖鑑、確定性解鎖途徑、招募（不重複、有畢業、無賭博語彙） | 抽卡必得未擁有角色；UI 常駐顯示剩餘數；無金光／無十連演出 |
| **B2.5** 潮汐公司 | tycoon 第一版單一循環：訂單→產線→交貨→擴廠；離線結算與倉儲上限；產出升級材料與招募券 | **UI 只有兩個畫面**（訂單板＋產線編輯，硬指標）；離線結算受倉儲上限約束；時鐘倒退不獎不罰；**塔防在完全不碰 tycoon 的情況下仍能推完戰役**（以測試斷言鎖住 B6） |
| **B2.6** 難度層 | 前 3 層規則與解鎖；敵人強度以「等級軸滿級」為基準校準 | 每層的規則差異在 UI 上明示；分數與難度層綁定；滿級等級軸在對應難度層下仍有挑戰性（`TL_SIM` 驗證） |
| **B2.7** 進度整合 | 公司等級（共享進度條）、20 個成就、材料與券的兌換介面、**塔防局末的升級材料產出** | 兩層都能推公司等級；等級軸增幅不超過 B2 ② 的 +80% 上限（以測試斷言鎖住）；**★ 以「從未開啟 tycoon」的存檔可將等級軸推至滿級**（以測試斷言鎖住——`10_GDD.md` §4.1 的三條路必須真的都通） |
| **B2.8** M2 驗收 | 整合 QA、效能壓測、回歸 | **G2**：GDD §3 系統清單逐項勾稽全部實作 |

### M3 Beta（9 批）

| 批次 | 內容 |
|---|---|
| **B3.1** 角色擴充至 24（含 6 抽卡專屬） |
| **B3.2** 敵人擴充至 18 |
| **B3.3** 戰役擴充至 25 關 |
| **B3.4** tycoon 內容填滿（20 訂單品項、25 廠等） |
| **B3.5** 科技樹擴至 45 節點；難度層補齊 12 層 |
| **B3.6** 成就 60；教學與引導打磨 |
| **B3.7** 平衡校準（以 `TL_SIM` 批次跑經濟曲線，逐項對照 GDD §7 修正） |
| **B3.8** 回歸全過、零阻斷 bug、多解析度驗證 |
| **B3.9** 產出 `60_LIVEOPS_PLAN.md` 長線路線圖 |

### M4 Gold（4 批）

| 批次 | 內容 |
|---|---|
| **B4.1** 變現系統（GDD §3.12）：商店介面、商品清單、購買流程、entitlement 存檔與還原 |
| **B4.2** 通行證／賽季獎勵軌；外觀系統 |
| **B4.3** 變現的 B6 稽核：所有內容純遊玩可取得的時程實測、統一配置榜零付費影響、無付費復活 |
| **B4.4** 發布檢查表：鍵位重綁、多解析度、崩潰回報、選項完整性、無障礙 |
| **B4.5** 匯出、安裝包、版本檢查 |
| **B4.6** G4 Gold 驗收 |

### 手機移植（M5，暫不排程）

視 Windows 版表現決定是否成案。**架構預留不代表承諾移植。** 若成案，主要工作為：觸控操作重設計、直式／橫式版面、廣告 SDK 接入、帳號與雲端存檔、上架流程。初估 +50~70% 於當時的專案規模。

**總計約 37 批**（不含 M5）。

---

## 3. 風險登記簿

| # | 風險 | 影響 | 對策 | 狀態 |
|---|---|---|---|---|
| **R-1** | 純即時不可暫停 × 拉線佈局 → 滑向手速遊戲，違背樂趣核心 | **高**：核心體驗錯位 | 波間準備期（45s／新手 60s）＋藍圖一鍵展開＋建造預覽＋後勤科技降低操作負擔。**M0 第二觀測項** | 🟡 監控中 |
| **R-2** | ~~tycoon 是實質第二個遊戲~~ **已降級**（2026-07-27 重新定位為附屬補給站） | 低—中 | 剩下的是**紀律風險**（附屬系統漸進膨脹）。硬指標：`00_CONCEPT.md` §三 約束 2「tycoon UI 深度上限兩個畫面」，**每批 QA 必查** | 🟢 已解除主要部分 |
| **R-11** ★ | **F2P × 桌面平台結構性錯配**：免費＋P2W 的收入模型成立於手機，Windows 的 F2P 獲客與變現效率遠低，廣告在桌面幾乎不存在 | **高**（商業，非玩法） | Windows 首發＝免費＋內購無廣告；**從第一批就以手機移植為前提設計 UI 與操作**（四條預留條款）；手機版視 Windows 表現再決定 | 🔴 頭號商業風險 |
| **R-12** ★ | **P2W 數值膨脹碾壓關卡設計** | 高 | 等級軸與科技軸分離、各有上限；**難度層敵人強度以「等級軸滿級」為基準校準**；統一配置榜完全排除等級軸。M3 用 `TL_SIM` 批次驗證 | 🟡 監控中 |
| **R-13** ★ | 手機移植的預留條款在生產期被忽略（硬編碼像素、hover-only 操作），到移植時全部要重做 | 中 | 四條條款寫入 `20_ART_DIRECTION.md` 與每批 UI 一致性檢查；**觸控命中區 ≥ 44px 列為可稽核項** | 🟡 監控中 |
| **R-3** | 流量網路可讀性不足 → 最佳化樂趣不成立 | **高**：核心機制失效 | 線寬＝流量、顏色＝飽和度、節點三態。**M0 硬性驗收改良**：以 `TL_NAKED=1` 產出無數值標籤的截圖交 Opus 5 指認瓶頸——原本的「未受訓玩家 30 秒」沒有執行者，而拿有數字的截圖問 agent 會必過（假綠燈）。大圖模式的對應驗收排 B2.1 | 🟡 監控中 |
| **R-4** | 「塔吃能量」造成新手死亡螺旋 | 中 | **對策已改**：原「新手係數」失效（待機耗能已為 0，×0.6 是空操作）且有害（教學期偷改物理 → 教錯模型）。改為**戰役第 1–2 關以「新手可通關」為硬性設計要求，只用關卡參數達成**（B1.2 DoD）。能量不足時射速線性下降而非停火不變 | 🟡 監控中（由 🟢 降級：對策從「一個數字」變成「關卡設計工作」） |
| **R-5** | 抽卡的演出與體感傷害調性 | 低 | 不重複、有畢業、常駐顯示剩餘數；無十連特寫／無稀有度金光／無假張力。付費入口存在但演出克制 | 🟢 已有對策 |
| **R-6** | 三種長線動力（成長／競技／精通）互相稀釋，三頭平行做不深 | 中 | 主軸明確為**成長曲線**；競技（每日挑戰）與精通（難度層）設計為它的自然副產品，共用同一套模擬與分數系統 | 🟢 已有對策 |
| **R-7** | 環路圖的流量解算不收斂或抖動 | 中 | 固定 3 次迭代上限、依 id 排序遍歷保證浮點順序一致；`determinism_test` 每批必跑 | 🟢 已有對策 |
| **R-8** | 大量節點／敵人的渲染效能 | 中 | 模擬 10Hz、渲染 60Hz 插值；導管批繪；敵人不用 Node2D 改資料陣列批繪。每里程碑跑壓力情境 | 🟢 已有對策 |
| **R-9** | 內容量（25 關手作地圖）超出單人產能 | 中 | 地圖以資料表驅動、建立關卡編輯鉤子；若 M3 進度落後，戰役縮至 18 關並補足無盡佈局 | 🟡 監控中 |
| **R-10** | 使用者實玩節奏成為瓶頸（每批都要玩測） | 低—中 | 每批交付前先跑自檢與截圖驗證，把使用者的時間留給「好不好玩」而非「跑不跑得起來」 | 🟢 已有對策 |
| **R-14** ★ | **「陌生人」驗收沒有執行者**。M1 閘門寫「陌生人可無指導遊玩 10 分鐘並理解自己為什麼贏／輸」、QA §4.4「首次體驗前 60 秒」——但樂趣與首次體驗由使用者本人判，而**使用者是設計者，永遠不可能有第一次體驗**；agent 依分工只驗程式碼與可讀性 | 中 | ① M0 內的可讀性部分已由 `TL_NAKED` 接手（R-3）；② **首次體驗（資訊是否完備、術語是否勸退、失敗後知不知道原因）交 Opus 5 以冷上下文跑**——它測得出「資訊有沒有給」，測不出「感不感到困惑」；③ **「陌生人」這個判準在拉到真人之前，不得當作已通過**。M1 閘門前重新評估是否找 1–3 個真人 | 🟡 監控中 |

---

## 4. 砍案清單

> 被砍或延後的功能與理由。**留著，避免同一個點子反覆插隊。**

| 功能 | 決定 | 理由 |
|---|---|---|
| 每日簽到獎勵 | 不做 | 若長線留存需靠簽到撐，代表核心循環失敗。通行證若需每日活躍，以「錯過不懲罰、可補做」的任務形式取代。 |
| 廣告變現（Windows 版） | **不做** | 桌面無可用 SDK 生態、PC 玩家容忍度極低。**保留至手機移植版**，屆時做獎勵式廣告（玩家主動觀看） |
| 付費復活 | **永久不做** | 同時破壞 R1（失敗只花時間 → 變成失敗要花錢）與難度層／排行榜的意義（GDD B6） |
| 體力值／每日局數上限 | **永久不做** | 紅線 R2。F2P 變現改走「賣速度不賣次數」 |
| tycoon 的市場價格波動／人事／股價／競爭對手／員工士氣 | **永久 backlog** | tycoon 已定位為附屬補給站，不是第二個遊戲（`00_CONCEPT.md` §三 約束 4） |
| 線上排行榜、帳號系統 | 延後至 M4 後 | 第一版純離線；架構已預留（分數與局面可序列化、可重現） |
| 即時 PvP／非同步對戰 | 不做 | 與核心體驗（單人規劃取捨）無關，且需要完整後端 |
| 場上英雄單位（可操作角色） | 不做 | 使用者已明確定義「塔＝角色、全自動開火」；可操作英雄會把重心拉向手感 |
| 迷宮式塔防（玩家可封路改變敵人路徑） | 不做 | 路徑固定是設計選擇——本作的空間策略在「導管怎麼走」，不在「敵人怎麼繞」 |
| 實體物品搬運（Factorio 式輸送帶） | 不做 | 已選定流量網路模型（`30_TECH_DESIGN.md` §2.5），效能、確定性、可讀性、美術四方面皆優 |
| Web／手機版 | 不做 | 交付形式已定為 Windows exe |
| 多語系 | 延後至 M4 | 繁體中文優先；文字集中於資料表，日後抽出成本可控 |

---

## 5. 當前狀態

| 項目 | 值 |
|---|---|
| 目前里程碑 | **M0 原型**進行中 |
| 目前版本 | **`0.5.0`**（B0.5 塔與取捨已完成） |
| 工具鏈 | ✅ Godot 4.7 console exe 已置於 `tools/godot/`（gitignore）· ✅ GitHub 遠端已接（**公開** repo） |
| 已完成批次 | ✅ **B0.1 地基** · ✅ **B0.2 流量網路** · ✅ **B0.3 建造與地圖** · ✅ **B0.4 波次與敵人**（皆 2026-07-27）· ✅ **B0.5 塔與取捨**（2026-07-28） |
| 下一批 | **B0.6 HUD 與可讀性**（R-3 的正式驗收） |
| 阻塞項 | **無** |

### B0.5 完工證據（2026-07-28）

`TL_SIM=900`（60 秒準備期 ＋ 第 1 波打了 30 秒）與 `TL_SIM=590`（準備期尾聲）的對照，一組就把 DoD 講完：

| DoD 項目 | 證據 |
|---|---|
| 四隻角色 | `data/NodeDefs.gd` 用 §7.4 原數值；示範佈局四隻全上場，`towers` 欄逐座列出滿足率 |
| **交戰耗能、待機 0** | ★ 準備期（`TL_SIM=590`）：四座塔都在，`power_demand: **0.0**`、`engaged: 0`。波次中（900）：`engaged: 2`、`power_demand: 30.0`。`combat_test` 另有直接斷言（RG-24） |
| **蓋滿塔會讓產線可見地下降** | ★ 波次中 `power_supply: 30.0` vs `power_demand: 37.0`，儲槽由 300 掉到 **292.1** 正在放電；稜鏡滿足率 **0.84**＝它的射速只剩八成四 |
| **射速隨滿足率線性下降、不停火** | `combat_test`：滿足率 50% → 10 秒正好 6 發（滿電 12 發）；**10% 仍打得出 1 發**（RG-25） |
| **全域擊殺回收 25% 礦砂** | `salvage_ore: 6.0`＝兩隻漂蟲 × 12 × 25%。`combat_test` 另證**直接入帳、不必接線運回核心** |
| **回收者能量回收（價值 ×60% ×5）** | ★ `reclaimed_power: 72.0`＝兩隻漂蟲 × 36。`combat_test` 證明**擊殺者是錨、回收者一發都沒打**（RG-27），且死在射程外回收 0 |
| **匯率 1:5** | `combat_test`：三種敵人逐一對表（36／66／90），並斷言匯率＝發電機的 4 礦砂→20 能量轉換比（RG-26） |
| **依類型的優先權面板（含儲槽）** | 面板 7 列恆定順序、含儲槽格；`combat_test` 證明稜鏡 3→5、錨 3→1 之後**滿足率真的互換**（RG-28） |
| `combat_test.gd` 全過 | **77 項全過**；七支合計 **260 項**（flow 33／build 62／combat 77／tide 49／save 31／determinism 8／balance 2） |

> **DoD 措辭與現況的落差（誠實記錄）**：這一條原文寫的是「蓋滿塔會讓**熔爐**/採集速度可見地下降」，但**熔爐是 M1（B1.1 第三資源）才進來的**，而採集器沒有能量需求。B0.5 能證明的是同一件事的另一半：塔搶的電讓**儲槽停止充能並開始放電**、讓**塔自己的射速掉到八成四**，而養活那些電要多蓋發電機、多燒 4 礦砂/秒的燃料——**入帳的礦砂就是這樣被塔吃掉的**。等 B1.1 有了熔爐，這條 DoD 才有字面上的證據，屆時回頭補測。

**截圖讀圖結論**：`b05_battle_lean.png` — 頂欄「能量 **30/37**｜儲槽 **222**/300｜**交戰 2 座**　**擊殺 2**」，稜鏡的雷射**沿著路徑那一列**橫貫過去打中敵人（§7.4 的對齊謎題在畫面上就是這個樣子）。`b05_naked_lean.png`（`TL_NAKED=1`，遮掉全部數值）**仍可判讀**：琥珀環＝這座塔正在吃電、環外多一段橙弧＝它餵不飽（稜鏡有、回收者沒有）、無環＝待機中（潮鳴與錨）。四隻塔的幾何體彼此可分辨。

*（R-3 的正式驗收在 B0.6，此處只確認 B0.5 新增的視覺編碼沒有製造新的不可讀項。）*

**本批修掉的四個缺陷**（全部由示範佈局／截圖當場抓到，詳見 `CHANGELOG.md`）：導管本來有方向（看不見也控制不了，害三座塔永遠 0 電）、解算器的 `reach` 與 memo 在雙向邊上會來回彈與被永久釘成 0、`conduit_flow` 把兩種資源相加導致線被畫成假滿載、開火線只留一個 tick 等於沒畫。

### B0.4 完工證據（2026-07-27）

`TL_SIM=1200`（120 秒＝準備期 60 ＋ 第 1 波打完回到準備期）的輸出，一份就把 DoD 講完：

| DoD 項目 | 證據 |
|---|---|
| 波次表資料 | `tide_test`：淺灘 5 波、第 1 波 4 隻全是漂蟲、第 5 波 17 隻；**同一波兩次產生完全相同的出場表**（零 RNG） |
| 敵人沿路徑推進 | `phase: prep, wave: 1, enemies: 4`——第 1 波跑完，4 隻全部抵達核心 |
| **貼路徑的導管被打斷造成產能中斷** | ★ `ore_in_per_sec: 0.0`、導管 6 → **4**、節點 8 → **7**。礦砂線最後兩段與中繼 (33,13) 被打掉，**核心與礦砂網已斷開** |
| **橋上導管全程無損** | ★ `(28,9)→(33,9)`（過跨越點 (30,9) 那條）仍在 `conduit_flow` 清單裡，完好 |
| **敵人不因任何建築停步** | ★ `tide_test`：8 個中繼貼路徑排成一排，漂蟲 20 秒仍走滿 20 格——**一秒也沒擋住** |
| 核心生命與失敗判定 | `core_hp: 896.8`（4 隻駐足啃食中）；`tide_test` 一路跑到 `phase == "lost"`，且失敗後模擬停住不再空轉 |
| 三種敵人 | 資料表 §7.5 原數值；護甲／屏障已進表，B0.5 有塔開火才生效 |
| 準備期與快進 | `tide_test`：59.9 秒仍在準備期、60.0 秒自動開打、**開打時 `speed_mult` 強制歸 1**；戰鬥中再按提前召喚無作用（連點防護） |
| 退 2 格無損（RG-16） | `tide_test`：退開 2 格的導管與節點在 400 tick 後**血量一點沒掉** |

**截圖讀圖結論**：`b04_broken2.png` — 頂欄「礦砂 271 ▲**0.0**/秒｜準備期 52.4s　下一波 2　**殘敵 4**｜核心 **897**/1000」。地圖上礦砂線的末段連同中繼一起消失，過橋那條完好無損；核心方框內擠著 4 隻品紅敵人，下方一條橙色血條。`b04_wave.png` — 波次進行中，**快進 4× 按鈕變灰**（B5）。

**本批自檢發現並當場修掉的兩個可讀性缺陷**：① 受損指示（橙色圓弧）與滿充能的儲槽（琥珀圓弧）在 12px 上肉眼分不出來 → 改為節點下方的**血條**，靠形狀而非顏色區分。② 波次結束回到準備期時核心仍在掉血卻無解釋 → 頂欄補「殘敵 N」。

### B0.3 完工證據（2026-07-27）

| DoD 項目 | 證據 |
|---|---|
| 32px 網格、地圖資料結構 | 測試圖「淺灘」36×19 格＝1152×608，**一屏可見**；礦點 6、跨越點 3、核心於路徑終點 (34,14) |
| 放置節點、拉導管（含中繼轉折） | `TL_SIM=300` 的 `build_failures: []`——13 步示範佈局全部合法落地 |
| 建造合法性檢查 | `build_test.gd`：路徑格禁節點、**跨越點也禁節點**、導管跨非跨越點被擋、走向不合法被擋、礦點只留採集器 |
| 拆除返還 | 採集器 40 → 返還 30（75%）；拆節點會一併移除接在它上面的導管（斷線留著只會騙人） |
| 即時預覽（含總耗能變化） | `preview_place` 斷言：`＋6 礦砂/秒`、`總能量供給 0 → 20 /秒`、`總能量需求 0 → 10 /秒`、擋下時**說明原因**、錢不夠也講清楚 |
| 導管吞吐局內升級 | cap 10 → 16 → 22 → 28；第 4 次被擋；造價 20/40/60 |
| **蓋出「採集器→發電機」看到能量流動** | `TL_SIM=300`：`(16,8)→(16,11)` flow 4.0（燃料）、`(16,11)→(20,15)` flow **10.0** |
| **第一台發電機的輸出線立刻滿載變色** | ★ 同上：輸出 20、基礎 cap 10 → flow 恆為 10，**一半的電出不來**；截圖 `b03_battle2.png` 中該線最粗最亮（`ORDER_BRIGHT`） |
| 升級幹線後流量提升 | `build_test`：升 1 級 → 16、升 2 級（cap 22）→ 送滿 20，瓶頸解除 |
| 核心＝礦砂銀行（本批拍板） | `build_test`：沒接到核心時入帳 **0**；接上後 6/秒，10 個 tick 正好入帳 6。`TL_SIM=300` 的 `ore 185` ＝ 300 − 295 建造 ＋ 30 秒 × 6 |

**截圖讀圖結論**：`b03_battle2.png` — 頂欄「礦砂 20 ▲6.0/秒｜能量 20/10｜儲槽 25/300」與 `TL_SIM` 數字一致；三座跨越點的架高橋面一眼可辨（第一版太細，本批當場加粗到 3px 並補橋頭橫桿）；路徑帶（品紅紫）與玩家線（青／琥珀）分屬混沌／秩序兩側。`b03_naked.png` — `TL_NAKED=1` 下頂欄、提示列、建造造價全數隱藏，**僅憑線寬仍可指認瓶頸**（發電機→儲槽最粗且最亮）。正式的 RG-15 驗收在 B0.6。

### B0.2 完工證據（2026-07-27）

| DoD 項目 | 證據 |
|---|---|
| 單線滿載 | `supply 20 / cap 10 → flow 10`、`satisfaction 0.5`。**頂欄供給仍是 20**——供給不等於送達，這是 B0.6 頂欄的語意基準 |
| 容量削減 | 幹線 cap 10 後方兩座同型塔各得 5，satisfaction 同步降到 0.5 |
| 三方競爭的優先權分配 | 供給 12、三型各需求 10、優先權 3/2/1 → **6 / 4 / 2**。節點自帶的 `priority: 99` 欄位被無視（斷言鎖住「只認節點類型」） |
| 環路收斂 | 4 節點環＋外掛需求 5：需求完整滿足、環內不空轉、**兩次解算結果逐元素相同** |
| 飢餓 satisfaction | `4/10 → 0.4`，且斷言 `> 0`（不停機，GDD §3.1「停機會雪崩」） |
| 儲槽充放電受 cap 約束（RG-19） | GDD §7.4 的 −23 缺口情境：**cap 10 的線只放得出 10**（不是 300、也不是 23）；**升到 cap 28 才放得出 23** 並補平缺口。充能側同樣被 cap 10 限制在 10/秒 |
| 無全域資源池（RG-18） | 拆掉儲槽 → `satisfaction = 40/63` 立即反映赤字；**空儲槽的結果與沒有儲槽完全相同**（無隱形緩衝） |
| 不變量：採集器無輸入需求 | 零連線的採集器仍產出；全網嚴重飢餓時其 satisfaction 恆為 1.0，供給恆 > 0（結構上不可能全域死鎖） |
| `flow_test.gd` 全過 | **33 項斷言全過**；五支測試全綠（flow 33／save 31／determinism 8／balance 2，combat 待 B0.5） |

**本批補上的設計語意**（已回寫 `30_TECH_DESIGN.md` §2.5）：**儲槽放電以全網赤字為上限**。原規格只寫「放電速率 = min(充能, 導管 cap)」，照字面實作會讓 cap 28 的儲槽在 23 缺口下放出 28——多的 5 流進網路無人可用，玩家看到的是「儲槽掉得比赤字還快」的無故漏電。充能側則**不封頂**：那是優先權滑桿要裁決的事，不該由解算器代勞。

### B0.1 完工證據（2026-07-27）

| DoD 項目 | 證據 |
|---|---|
| 空專案能開起來顯示標題＋版本號 `0.1.0` | 截圖 `b01_title2.png`：標題／英文名／標語／`v0.1.0 ・ B0.1 地基` 全部正確渲染，CJK 非豆腐字 |
| `TL_SHOT` 能截圖並自動退出 | `[TL_SHOT] size=1280x720 err=0`，`EXIT=0` |
| `TL_MUTE` 生效 | `[TL] audio muted`（`TL_SHOT` 存在時自動靜音，鐵律 2） |
| 有鉤子時不寫真存檔 | `[TL] persist=false（有鉤子時不寫真存檔）`；跑完 `user://` 無 `save.json` |
| 五支測試檔可執行 | 五支全過，`FAILED_SUITES=0`；實斷言 41 項（save 31／determinism 8／balance 2），待補項 19 條全部印出 |
| `TL_SIM` / `TL_SEED` / `TL_PANEL` / `TL_NAKED` | 各自實跑驗證：SIM 輸出可解析 JSON 後退出；SEED 覆寫生效；PANEL 未實作時警告但不當掉；NAKED 確實隱藏版本列與鉤子列 |

**本批自檢發現並當場修掉的缺陷**：畫面上的鉤子列原本連值一起印，`TL_SHOT` 的絕對路徑橫跨整個 1280px（小解析度會裁切，違反 P1）。改為畫面只列鉤子名稱、值只進 stdout。已立 RG-22。

### 5.1 開工前的設計定案（2026-07-27 grilling）

15 條決策已寫入各文件，此處僅列索引：

| # | 定案 | 落在哪 |
|---|---|---|
| 1 | 先做遊戲本體，變現之後再說（架構不砍） | 全案 |
| 2 | tycoon／等級軸照做；購買只賣時間，慢慢玩可集齊 | GDD §3.8、§3.12 |
| 3 | **G1 判官＝使用者本人**；agent（Opus 5）只驗程式碼完整性與可讀性 | §2 B0.7 註、R-14 |
| 4 | M0 七批照原順序，不重排 | §2 |
| 5 | 刪新手係數，改「第 1–2 關以新手可通關」為關卡設計要求 | GDD §7.7、B1.2 DoD |
| 6 | 能量效率科技 → **交戰耗能** −8%/級 | GDD §7.8、B1.3 DoD |
| 7 | R-3 驗收 → **`TL_NAKED=1`** 遮數字後交 Opus 5 指認瓶頸 | 30_TECH §4.1、B0.6 DoD |
| 8 | 路徑禁建 ＋ **跨越點（橋）**；橋架高 → 橋上導管免疫 | GDD §3.2、§3.5、§7.3 |
| 9 | 敵人 **walk-by**：永不停步、傷害相鄰 1 格、只有核心讓其駐足 | GDD §3.5、B0.4 DoD |
| 10 | 雙榜照做，但措辭誠實化（離線版無對手） | GDD §1 B3、§3.10 |
| 11 | **戰役一屏可見**，無盡才可滑；HUD 改全畫面地圖＋浮層 | GDD §6.2、B2.1 DoD |
| 12 | 優先權**依節點類型**（5–8 條滑桿） | GDD §3.1、30_TECH §2.5 |
| 13 | 礦砂↔能量匯率 **1:5**；回收者回收算式落地 | GDD §3.3、§7.4 |
| 14 | 導管基礎 cap 維持 10，靠局內升級加粗（第一台發電機就教你讀瓶頸） | GDD §7.2 |
| 15 | 能量＝**流率 ＋ 儲槽在地緩衝**，無全域水池 | GDD §3.1、30_TECH §2.5 |
