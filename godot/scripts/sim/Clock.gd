extends RefCounted
## 固定時間步（`30_TECH_DESIGN.md` §2.4）。**這是全案唯一一份。**
##
## B1.9 之前 `TICK := 0.1` 在四個檔案裡各寫一份（BattleController／Motion／
## Combat／Tide），每一份還各配一段解釋自己為什麼要再宣告一次的註解。
## 那些理由都是真的——`sim/` 不該為了一個常數去依賴 `game/`、`render/` 也不該
## ——但正確的結論是**把它放到大家都在下游的地方**，不是抄四份。
##
## 抄四份的代價不是行數，是「有一天有人只改了其中三份」。而這個數字改了之後
## 每一個既有存檔、每一份重播、每一張榜單都會對不上——它是確定性的地基本身。
##
## 不宣告 `class_name`，一律以路徑 preload：`--script` 模式（測試）不保證
## global class 可用，而模擬層必須自足（`50_QA_PLAN.md` §2）。

## 模擬每一步的長度（秒）。**模擬不使用 `delta`**；渲染 60Hz 以插值呈現。
const TICK := 0.1
